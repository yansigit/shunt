---
phase: 06-capability-aware-fallback
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [README.md, README.ko.md, README.ja.md, README.zh-CN.md, docs/upstreams-failover.md, site/src/content/docs/guides/providers.md, site/src/content/docs/ko/guides/providers.md, site/src/content/docs/ja/guides/providers.md, site/src/content/docs/zh-cn/guides/providers.md, .planning/phases/06-capability-aware-fallback/06-VALIDATION.md]
autonomous: true
requirements: [CAP-01]
estimate: {tasks: 2, confidence: high}
must_haves:
  truths:
    - "Operators know which required features exclude each fallback class and that the primary never changes."
    - "All focused and repository-wide gates pass without native ingress, ordinary routing, or wiki regressions."
  artifacts:
    - {path: docs/upstreams-failover.md, provides: "capability-aware chain contract"}
    - {path: .planning/phases/06-capability-aware-fallback/06-VERIFICATION.md, provides: "CAP-01 evidence"}
---

<objective>Document, review, and verify capability-aware fallback.</objective>

<context>@AGENTS.md @.planning/phases/06-capability-aware-fallback/06-CONTEXT.md @.planning/phases/06-capability-aware-fallback/06-VALIDATION.md @docs/upstreams-failover.md</context>

<tasks>
<task type="auto">
  <name>Task 1: Document the adapter matrix and primary-preservation rule</name>
  <files>README.md, README.ko.md, README.ja.md, README.zh-CN.md, docs/upstreams-failover.md, site/src/content/docs/guides/providers.md, site/src/content/docs/ko/guides/providers.md, site/src/content/docs/ja/guides/providers.md, site/src/content/docs/zh-cn/guides/providers.md</files>
  <action>Describe requirement detection, the compact adapter matrix, the unchanged primary, pre-network exclusion, 1M fallback suppression, warnings/metrics, and lack of public config. Update all maintained locales; never edit wiki.</action>
  <verify><automated>git diff --check && test -z "$(git status --short -- wiki/)"</automated><fails_when>behavior is undocumented, locales drift, wiki changes, or diff checks fail</fails_when></verify>
  <done>Every maintained surface states the same fallback contract.</done>
</task>
<task type="auto">
  <name>Task 2: Review, secure, and verify the phase</name>
  <files>.planning/phases/06-capability-aware-fallback/06-VALIDATION.md, .planning/phases/06-capability-aware-fallback/06-REVIEW.md, .planning/phases/06-capability-aware-fallback/06-SECURITY.md, .planning/phases/06-capability-aware-fallback/06-VERIFICATION.md</files>
  <action>Review request traversal, primary preservation, auth ordering, observability cardinality, and regression coverage. Run focused and full quality gates, fix findings, mark validation complete, and record a covered-file fingerprint.</action>
  <verify><automated>cargo test --all-features --workspace && cargo fmt --all --check && cargo clippy --all-targets --all-features -- -D warnings && git diff --check && test -z "$(git status --short -- wiki/)"</automated><fails_when>any test/lint/review/security issue remains or validation is incomplete</fails_when></verify>
  <done>CAP-01 is fully verified with no open finding.</done>
</task>
</tasks>

<verification><automated>cargo test --all-features --workspace && cargo fmt --all --check && cargo clippy --all-targets --all-features -- -D warnings && git diff --check && test -z "$(git status --short -- wiki/)"</automated></verification>
