---
phase: 07-collaboration-preservation
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [src/codex_endpoint.rs, src/adapters/anthropic/inbound.rs, src/model/inbound_responses/response.rs, tests/inbound_anthropic_translation.rs, tests/inbound_codex_endpoint.rs, tests/inbound_codex_websocket.rs]
autonomous: true
requirements: [COLLAB-01]
estimate: {tasks: 3, confidence: medium}
must_haves:
  truths:
    - "Only request-authorized collaboration calls regain their namespace in JSON and SSE."
    - "Native ChatGPT requests and responses stay byte-for-byte opaque with the opt-in enabled."
    - "Unsupported encrypted state causes zero translated-provider requests and no hidden recovery inference."
  artifacts:
    - {path: src/model/inbound_responses/response.rs, provides: "authorized collaboration call restoration"}
    - {path: tests/inbound_anthropic_translation.rs, provides: "HTTP and streaming collaboration transcript evidence"}
---

<objective>Compose response restoration and end-to-end isolation around the request bridge.</objective>

<context>@AGENTS.md @.planning/phases/07-collaboration-preservation/07-CONTEXT.md @src/codex_endpoint.rs @src/adapters/anthropic/inbound.rs @src/model/inbound_responses/response.rs</context>

<tasks>
<task type="auto">
  <name>Task 1: Restore authorized collaboration calls</name>
  <files>src/model/inbound_responses/response.rs, src/adapters/anthropic/inbound.rs</files>
  <action>Thread the request authority map into complete JSON and StreamTranslator. Map only exact authorized flattened names back to namespace collaboration, preserve logical name/call id/arguments, add empty encrypted_function_args, and keep existing function tools unchanged.</action>
  <verify><automated>cargo test inbound_responses::response --lib</automated><fails_when>an undeclared call gains authority, argument events drift, ordinary tools change, bounds weaken, or tests fail</fails_when></verify>
  <done>JSON and SSE expose client-valid collaboration function calls.</done>
</task>
<task type="auto">
  <name>Task 2: Activate only in the translated branch</name>
  <files>src/codex_endpoint.rs</files>
  <action>Read the immutable opt-in after route resolution and pass it only to Anthropic request translation. Carry returned authority only through that response translation. Leave Responses forwarding and compact forwarding on the exact existing opaque path.</action>
  <verify><automated>cargo test --test inbound_codex_endpoint && cargo test --test inbound_codex_websocket</automated><fails_when>native or compact bytes change, route policy changes, or tests fail</fails_when></verify>
  <done>The feature has zero behavioral effect outside enabled Anthropic translation.</done>
</task>
<task type="auto">
  <name>Task 3: Port collaboration protocol transcript fixtures</name>
  <files>tests/inbound_anthropic_translation.rs, tests/inbound_codex_endpoint.rs, tests/inbound_codex_websocket.rs</files>
  <action>Add HTTP JSON and SSE/WS fixtures covering additional_tools, agent_message plaintext, flattened Anthropic calls, restored namespace metadata, ordinary-tool isolation, disabled rejection, native byte fidelity, and zero upstream calls for unreadable encrypted tasks/continuation.</action>
  <verify><automated>cargo test --test inbound_anthropic_translation && cargo test --test inbound_codex_endpoint && cargo test --test inbound_codex_websocket</automated><fails_when>any observable path lacks evidence or tests fail</fails_when></verify>
  <done>COLLAB-01 behavior is proven across every affected transport.</done>
</task>
</tasks>

<verification><automated>cargo test inbound_responses:: --lib && cargo test --test inbound_anthropic_translation && cargo test --test inbound_codex_endpoint && cargo test --test inbound_codex_websocket && cargo fmt --all --check && cargo clippy --all-targets --all-features -- -D warnings</automated></verification>
