---
phase: 04-native-compaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server.rs
  - src/codex_endpoint.rs
  - src/concurrency.rs
  - src/config.rs
  - src/adapters/responses/request.rs
  - src/adapters/responses/inbound.rs
  - tests/inbound_codex_endpoint.rs
autonomous: true
requirements: [COMP-01]
estimate: {tasks: 2, confidence: high}
must_haves:
  truths:
    - "Authenticated POST /v1/responses/compact forwards the original body to the verified native compact URL."
    - "Pinned ChatGPT and exact compatible native routes work; malformed, ambiguous, translated, non-Responses, and compact-incapable routes fail before network dispatch."
    - "Compaction reuses existing credential/account-pool, limits, timeout, safe-header, and lazy relay behavior without local history state."
  artifacts:
    - {path: src/codex_endpoint.rs, provides: "strict compact ingress and routing boundary"}
    - {path: src/adapters/responses/inbound.rs, provides: "shared operation-aware native passthrough"}
    - {path: tests/inbound_codex_endpoint.rs, provides: "end-to-end compact conformance fixtures"}
---

<objective>Implement the narrow native compaction vertical slice with pre-dispatch capability checks and byte-faithful forwarding.</objective>

<context>@AGENTS.md @.planning/phases/04-native-compaction/04-CONTEXT.md @.planning/phases/04-native-compaction/04-RESEARCH.md @src/codex_endpoint.rs @src/adapters/responses/inbound.rs @tests/inbound_codex_endpoint.rs</context>

<tasks>
<task type="tracer">
  <name>Task 1: Trace a compact request from authenticated ingress to the native upstream</name>
  <files>src/server.rs, src/codex_endpoint.rs, src/concurrency.rs, src/config.rs, src/adapters/responses/request.rs, src/adapters/responses/inbound.rs, tests/inbound_codex_endpoint.rs</files>
  <action>Register POST `/v1/responses/compact` only with the opt-in Codex endpoint. Classify it as a Codex/OpenAI error surface. After existing auth and bounded body reading, strictly require a complete object with one non-empty string model, resolve the same Phase 2 native decision, and fail before dispatch if selected provider lacks verified native compact support. Add an internal operation passed through the existing inbound pool so only the URL changes to the Responses URL plus `/compact`; preserve request bytes/headers, selected credentials, account identity, TTFB timeout, quota classification, and lazy response relay.</action>
  <verify><automated>cargo test routing:: --lib && cargo test --test inbound_codex_endpoint compact_</automated><fails_when>the route is unauthenticated, uses the ordinary URL, mutates the body, accepts unsupported routing, or tests fail</fails_when></verify>
  <done>Pinned ChatGPT and exact supported native compact requests reach the correct upstream once with byte/header fidelity.</done>
</task>
<task type="auto">
  <name>Task 2: Close adversarial, error-relay, and regression cases</name>
  <files>src/codex_endpoint.rs, src/adapters/responses/inbound.rs, tests/inbound_codex_endpoint.rs</files>
  <action>Add zero-network cases for missing/empty/wrong-type/duplicate model, malformed JSON, ambiguous or translated routes, non-Responses routes, and unsupported xAI/custom Responses backends. Cover request-size errors and upstream success/error status, body, and safe-header relay. Run the complete ordinary inbound and multi-account suites to prove the shared operation parameter did not change `/v1/responses` behavior.</action>
  <verify><automated>cargo test --test inbound_codex_endpoint && cargo test --test codex_multi_account</automated><fails_when>any rejection reaches a mock, response fidelity changes, ordinary ingress regresses, or tests fail</fails_when></verify>
  <done>Adversarial compact routing is fail closed and all shared-path regressions remain green.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Severity | Mitigation |
|-----------|----------|----------|------------|
| T-04-01-01 | Credential disclosure | high | exact backend capability and existing auth-origin gates |
| T-04-01-02 | Tampering | high | strict unique model plus byte-faithful body forwarding |
| T-04-01-03 | Denial of service | high | existing request/concurrency/TTFB bounds |
| T-04-01-04 | Information disclosure | high | existing request credential stripping and safe response-header filter |
</threat_model>

<verification><automated>cargo test --test inbound_codex_endpoint && cargo test --test codex_multi_account && cargo fmt --all --check && cargo clippy --all-targets --all-features -- -D warnings</automated></verification>
