---
phase: 08-bounded-shutdown
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [src/shutdown.rs, tests/inbound_codex_endpoint.rs, tests/inbound_codex_websocket.rs]
autonomous: true
requirements: [OPS-01]
estimate: {tasks: 2, confidence: high}
must_haves:
  truths:
    - "Work still pending at the deadline is cancelled and releases RAII-owned resources."
    - "HTTP/SSE/WebSocket transport semantics remain unchanged during ordinary operation and clean drain."
  artifacts:
    - {path: src/shutdown.rs, provides: "clean and forced lifecycle tests"}
---

<objective>Prove clean drain and forced cancellation across the process lifecycle.</objective>

<context>@AGENTS.md @.planning/phases/08-bounded-shutdown/08-CONTEXT.md @.planning/phases/08-bounded-shutdown/08-VALIDATION.md @src/shutdown.rs @tests/inbound_codex_endpoint.rs @tests/inbound_codex_websocket.rs</context>

<tasks>
<task type="auto">
  <name>Task 1: Add deterministic lifecycle tests</name>
  <files>src/shutdown.rs</files>
  <action>Use injected futures and one-shot notifications to prove no boot-time timeout, clean completion, timeout outcome, and Drop of a pending future's lease sentinel. Keep the existing single real-SIGTERM test serialized by design.</action>
  <verify><automated>cargo test --bin shunt shutdown</automated><fails_when>timing relies on races/sleeps alone or resource release is inferred without a drop assertion</fails_when></verify>
  <done>Both clean and forced paths have deterministic automated evidence.</done>
</task>
<task type="auto">
  <name>Task 2: Run transport regressions</name>
  <files>tests/inbound_codex_endpoint.rs, tests/inbound_codex_websocket.rs</files>
  <action>Run and, only if a lifecycle seam is uncovered, extend the existing HTTP/SSE and WebSocket suites. Preserve unbuffered SSE and native byte fidelity.</action>
  <verify><automated>cargo test --test inbound_codex_endpoint && cargo test --test inbound_codex_websocket</automated><fails_when>streaming, connection cancellation, or native payload behavior regresses</fails_when></verify>
  <done>All active transport families retain their established behavior.</done>
</task>
</tasks>

<verification><automated>cargo test --bin shunt shutdown && cargo test --test inbound_codex_endpoint && cargo test --test inbound_codex_websocket</automated></verification>
