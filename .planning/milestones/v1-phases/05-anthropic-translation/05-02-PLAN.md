---
phase: 05-anthropic-translation
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/model/inbound_responses/response.rs, src/adapters/anthropic/inbound.rs, src/adapters/anthropic/mod.rs, src/codex_endpoint.rs, tests/inbound_anthropic_translation.rs, tests/inbound_codex_websocket.rs]
autonomous: true
requirements: [TRANS-02, TRANS-03]
estimate: {tasks: 2, confidence: medium}
must_haves:
  truths:
    - "Anthropic JSON and SSE map to equivalent Responses text, tool, reasoning, usage, and terminal output."
    - "Streaming event order and sequence numbers are valid, arguments and frames are bounded, and exactly one terminal event is emitted."
    - "Malformed output, upstream error, and premature EOF never surface as successful completion."
  artifacts:
    - {path: src/model/inbound_responses/response.rs, provides: "bounded Anthropic-to-Responses response machine"}
    - {path: src/adapters/anthropic/inbound.rs, provides: "composition around existing Anthropic transport"}
    - {path: tests/inbound_anthropic_translation.rs, provides: "JSON and SSE lifecycle fixtures"}
---

<objective>Project Anthropic non-streaming responses and SSE into protocol-correct Responses output with bounded incremental processing and faithful terminal status.</objective>

<context>@AGENTS.md @.planning/phases/05-anthropic-translation/05-CONTEXT.md @.planning/phases/05-anthropic-translation/05-RESEARCH.md @src/codex_endpoint/frame.rs @src/codex_endpoint/websocket.rs @src/adapters/anthropic/mod.rs @/Volumes/PortableSSD/Projects/opencodex/src/adapters/anthropic.ts @/Volumes/PortableSSD/Projects/opencodex/src/bridge.ts</context>

<tasks>
<task type="tracer">
  <name>Task 1: Build shared non-stream and incremental response translation</name>
  <files>src/model/inbound_responses/response.rs, tests/inbound_anthropic_translation.rs</files>
  <action>Create stable response/item/call IDs, final response snapshots, inclusive usage normalization, and terminal classification. Convert text, tool use, and thinking to Responses items. Implement a bounded Anthropic SSE decoder/state machine that emits created/in-progress/item/content/delta/done/terminal events in order with monotonic sequence numbers; assemble tool arguments only within a fixed cap and fail on malformed arguments, bad shapes, duplicate terminals, or EOF before message_stop.</action>
  <verify><automated>cargo test inbound_responses::response --lib && cargo test --test inbound_anthropic_translation response_</automated><fails_when>JSON/SSE semantic finals differ, usage is double-counted, event order is invalid, an accumulator is unbounded, false success occurs, or tests fail</fails_when></verify>
  <done>Golden JSON and split-chunk SSE fixtures agree on text/tools/reasoning/usage and all terminal variants.</done>
</task>
<task type="auto">
  <name>Task 2: Compose with Anthropic transport for HTTP and WebSocket</name>
  <files>src/adapters/anthropic/mod.rs, src/adapters/anthropic/inbound.rs, src/codex_endpoint.rs, tests/inbound_anthropic_translation.rs, tests/inbound_codex_websocket.rs</files>
  <action>Pass the translated Messages RequestBody through the existing Anthropic Adapter so credential pools, refresh, retry, TTFB deadlines, headers, and admission remain authoritative. Convert successful JSON/SSE responses and translated upstream errors to Responses shape. Ensure the Phase 1 WebSocket pump receives ordinary Responses SSE and keeps replacement/disconnect cancellation and backpressure.</action>
  <verify><automated>cargo test --test inbound_anthropic_translation && cargo test --test inbound_codex_websocket translated_ && cargo test --test inbound_codex_websocket replacement_</automated><fails_when>transport logic is duplicated, credentials leak, HTTP/WS diverge, cancellation regresses, or tests fail</fails_when></verify>
  <done>Both inbound transports run translated Anthropic turns through the existing operational stack.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Severity | Mitigation |
|-----------|----------|----------|------------|
| T-05-02-01 | Memory exhaustion | high | bounded SSE blocks and tool-argument accumulators |
| T-05-02-02 | False completion | high | explicit lifecycle state and mandatory terminal validation |
| T-05-02-03 | Cross-protocol data leak | high | no Anthropic signatures become OpenAI encrypted content |
| T-05-02-04 | Credential/header leak | high | reuse mature Anthropic transport and response header filtering |
</threat_model>

<verification><automated>cargo test inbound_responses::response --lib && cargo test --test inbound_anthropic_translation && cargo test --test inbound_codex_websocket && cargo fmt --all --check && cargo clippy --all-targets --all-features -- -D warnings</automated></verification>

