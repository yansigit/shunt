---
phase: 02-native-responses-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routing.rs
  - src/codex_endpoint.rs
  - tests/inbound_codex_endpoint.rs
autonomous: true
requirements: [ROUTE-01, ROUTE-02]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A single refreshed config snapshot produces one immutable pinned, selected, or rejected native decision per HTTP turn (D-01, D-08, D-10)."
    - "Exact route and exact same-model map selection reaches one Responses adapter without rewriting the request bytes (D-02, D-03, D-04, D-05)."
    - "Missing, malformed, non-string, unmatched, prefix-only, and [1m]-lookup models retain the pinned provider decision (D-01, D-02, D-10)."
  artifacts:
    - path: src/routing.rs
      provides: "Typed native inbound resolver and table-driven eligibility tests"
    - path: src/codex_endpoint.rs
      provides: "HTTP turn integration using resolver decision from one refreshed snapshot"
    - path: tests/inbound_codex_endpoint.rs
      provides: "Exact native HTTP tracer fixture with byte-preserving request assertion"
  key_links:
    - from: src/codex_endpoint.rs
      to: src/routing.rs
      via: "forward_turn invokes shared native resolver before adapter dispatch"
    - from: src/codex_endpoint.rs
      to: tests/inbound_codex_endpoint.rs
      via: "selected provider receives the original body fixture"
  prohibitions:
    - statement: "Do not silently use prefix routes or silently choose a member of an ambiguous chain."
      status: unverified
      flagged: true
    - statement: "Do not rewrite the native body or model while resolving."
      status: unverified
      flagged: true
---

<objective>
Establish the production tracer slice for exact native Responses routing over HTTP.

Purpose: Make one real exact route travel from bounded model inspection through the shared resolver and existing passthrough boundary while preserving pinned compatibility.
Output: typed resolver contract, HTTP integration, and focused resolver/tracer tests.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-native-responses-routing/02-CONTEXT.md
@.planning/phases/02-native-responses-routing/02-RESEARCH.md
@.planning/phases/02-native-responses-routing/02-PATTERNS.md
@src/routing.rs
@src/codex_endpoint.rs
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Trace one exact native HTTP model through shared resolution</name>
  <files>src/routing.rs, src/codex_endpoint.rs, tests/inbound_codex_endpoint.rs</files>
  <read_first>src/routing.rs, src/config.rs, src/codex_endpoint.rs, src/adapters/responses/inbound.rs, tests/inbound_codex_endpoint.rs, 02-CONTEXT.md</read_first>
  <action>Define the internal native decision (pinned, selected, rejected) and `resolve_native_inbound` at the existing routing seam. Inspect only exact `[models.upstream_model]` and legacy `[[routes]]` declarations, apply existing `[1m]` lookup normalization without changing the original model, require one Responses adapter, reject translation-required mappings, and return pinned for missing/malformed/unmatched input (D-01 through D-05, D-10). Replace only the HTTP pinned Route construction with this resolver using the request's one `state.refreshed()` snapshot. Wire a selected exact Responses route to the existing raw passthrough dispatcher and add one real mock-upstream test asserting selected provider and exact body bytes. Keep gateway-owned rejection in the OpenAI Responses error shape.</action>
  <verify><automated>cargo test routing:: --lib && cargo test --test inbound_codex_endpoint exact_native_route</automated><fails_when>either command exits non-zero, no exact-route test is discovered, or the mock receives altered body bytes</fails_when></verify>
  <acceptance_criteria>One exact same-model route reaches the selected Responses provider; the HTTP request body and model bytes are unchanged; the route decision is captured from one refreshed snapshot; gateway rejection is OpenAI-shaped.</acceptance_criteria>
  <done>The exact native HTTP tracer is runnable and proves selection, fidelity, and gateway error handling.</done>
  <produced_artifacts>Native decision enum/resolver, HTTP resolver call site, exact-route HTTP fixture and test evidence.</produced_artifacts>
</task>
<task type="auto">
  <name>Task 2: Lock resolver edge-case and fallback contract</name>
  <files>src/routing.rs, tests/inbound_codex_endpoint.rs</files>
  <read_first>src/routing.rs, src/config.rs, tests/inbound_codex_endpoint.rs, 02-RESEARCH.md</read_first>
  <action>Add table-driven tests for exact legacy route, exact single-entry model map, pinned fallback for no match/missing/malformed/non-string model, ignored prefix-only match, `[1m]` lookup without body rewrite, duplicate exact declaration, multi-provider ambiguity, non-Responses adapter, and model-translation mapping. Assert decision provider/adapter/model/upstream_model fields from source definitions rather than built-in provider names. Add the unmatched HTTP assertion proving Phase 1 pinned behavior remains unchanged (D-01, D-02, D-03, D-04, D-05, D-10, D-11).</action>
  <verify><automated>cargo test routing:: --lib && cargo test --test inbound_codex_endpoint pinned_fallback</automated><fails_when>any table case is missing/fails or an unmatched request dispatches to a non-pinned provider</fails_when></verify>
  <acceptance_criteria>All listed exact/fallback/rejection cases are automated and no prefix, ambiguous member, incompatible adapter, or alias rewrite is silently dispatched.</acceptance_criteria>
  <done>The resolver matrix and pinned fallback integration tests pass.</done>
  <produced_artifacts>Resolver decision matrix tests and pinned fallback integration fixture.</produced_artifacts>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Client -> HTTP ingress | Untrusted compressed/uncompressed body and model classification input |
| Gateway -> upstream | Selected route and opaque request bytes cross provider boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01-01 | Tampering | native resolver | high | mitigate | Exact-only typed decisions, translation rejection, and field-level tests prevent route/body substitution. |
| T-02-01-02 | Information disclosure | HTTP passthrough | high | mitigate | Preserve existing credential/header filtering and assert request bytes without client credentials. |
| T-02-01-03 | Denial of service | model inspection | medium | mitigate | Reuse bounded body and zstd inspection before resolver invocation. |
</threat_model>

<verification><automated>cargo test routing:: --lib && cargo test --test inbound_codex_endpoint exact_native_route && cargo test --test inbound_codex_endpoint pinned_fallback</automated><fails_when>any focused resolver or tracer command fails</fails_when></verification>
