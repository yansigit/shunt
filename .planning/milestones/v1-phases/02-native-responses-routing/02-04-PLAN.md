---
phase: 02-native-responses-routing
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - docs/m11-inbound-codex-endpoint.md
  - docs/codex-configuration.md
  - docs/upstreams-failover.md
  - README.md
  - site/src/content/docs/reference/configuration.md
  - site/src/content/docs/guides/inbound-codex-endpoint.md
  - site/src/content/docs/guides/configuration.mdx
  - .planning/phases/02-native-responses-routing/02-VALIDATION.md
autonomous: true
requirements: [ROUTE-01, ROUTE-02]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "English source docs state exact native selection; prefix-only, non-exact, and unmatched models use pinned fallback, while exact ambiguous, translated, or incompatible targets reject before dispatch (D-01 through D-12)."
    - "02-VALIDATION.md maps every plan task to an automated command and preserves wave/sampling metadata (D-11)."
    - "Generated wiki remains untouched (D-12)."
  artifacts:
    - path: .planning/phases/02-native-responses-routing/02-VALIDATION.md
      provides: "Final task-to-command validation map"
  key_links:
    - from: docs/codex-configuration.md
      to: src/routing.rs
      via: "documented exact declarations match resolver behavior"
    - from: 02-VALIDATION.md
      to: 02-01-PLAN.md
      via: "all phase task IDs and commands are mapped"
  prohibitions:
    - statement: "Do not document prefixes, generic fallback chains, translation, or provider hops as native ingress behavior."
      status: unverified
      flagged: true
    - statement: "Do not edit generated wiki files."
      status: unverified
      flagged: true
---
<objective>Update English/source documentation and finalize the Phase 02 validation map before translation and final quality-gate work.</objective>
<context>@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-native-responses-routing/02-CONTEXT.md
@.planning/phases/02-native-responses-routing/02-RESEARCH.md
@.planning/phases/02-native-responses-routing/02-VALIDATION.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Document exact native routing in English/source surfaces</name>
  <files>docs/m11-inbound-codex-endpoint.md, docs/codex-configuration.md, docs/upstreams-failover.md, README.md, site/src/content/docs/reference/configuration.md, site/src/content/docs/guides/inbound-codex-endpoint.md, site/src/content/docs/guides/configuration.mdx</files>
  <read_first>AGENTS.md, docs/m11-inbound-codex-endpoint.md, docs/codex-configuration.md, docs/upstreams-failover.md, README.md, site/src/content/docs/reference/configuration.md, site/src/content/docs/guides/inbound-codex-endpoint.md, site/src/content/docs/guides/configuration.mdx, 02-CONTEXT.md</read_first>
  <action>Update English M11, configuration, failover, README, and Nimbus source pages to explain exact `[models.upstream_model]`/`[[routes]]` native selection. State that prefix-only, non-exact, and unmatched models use the pinned `[server.codex_endpoint].provider` fallback, while only exact ambiguous, translated, or incompatible declarations reject before dispatch. Document provider-aware credentials, opaque HTTP bytes, bounded WS framing, and no post-output hop; do not add keys, alter credential writeback semantics, or edit wiki (D-01 through D-07, D-09, D-10, D-12).</action>
  <verify><automated>test -z "$(git status --short -- wiki/)" && git diff --check && for f in docs/m11-inbound-codex-endpoint.md docs/codex-configuration.md docs/upstreams-failover.md README.md site/src/content/docs/reference/configuration.md site/src/content/docs/guides/inbound-codex-endpoint.md site/src/content/docs/guides/configuration.mdx; do test -s "$f" || exit 1; git diff --numstat -- "$f" | grep -Eq '^[0-9]+[[:space:]]+[0-9]+' || exit 1; done; grep -q 'prefix-only' docs/codex-configuration.md; grep -q 'pinned' docs/m11-inbound-codex-endpoint.md; grep -q 'unmatched' README.md</automated><fails_when>any wiki status output exists, any English/source file is absent or unchanged, semantic fallback assertions are missing, whitespace differs, or the command exits non-zero</fails_when></verify>
  <acceptance_criteria>All seven English/source surfaces are changed and state the exact-vs-fallback-vs-rejection contract without introducing unsupported native routing semantics.</acceptance_criteria>
  <done>English/source documentation is synchronized and its focused guard passes.</done>
  <produced_artifacts>Seven updated English/source documentation files.</produced_artifacts>
</task>
<task type="auto">
  <name>Task 2: Finalize validation metadata for all Phase 02 tasks</name>
  <files>.planning/phases/02-native-responses-routing/02-VALIDATION.md</files>
  <read_first>02-VALIDATION.md, 02-01-PLAN.md, 02-02-PLAN.md, 02-03-PLAN.md, 02-04-PLAN.md, 02-05-PLAN.md</read_first>
  <action>Update the validation matrix with exact task IDs 02-01-01 through 02-05-02, their wave numbers, threat references, automated commands, and pending execution statuses. Keep `status: draft`, `wave_0_complete: true`, and `nyquist_compliant: true` because every task has an automated verify command and sampling continuity is structurally complete; leave final pass/fail statuses pending execution (D-11).</action>
  <verify><automated>test "$(grep -c '^| 02-' .planning/phases/02-native-responses-routing/02-VALIDATION.md)" -eq 10 && grep -q '02-05-02' .planning/phases/02-native-responses-routing/02-VALIDATION.md && grep -q 'nyquist_compliant: true' .planning/phases/02-native-responses-routing/02-VALIDATION.md</automated><fails_when>the matrix does not contain exactly ten task rows, any final-plan task ID is absent, Nyquist metadata is inconsistent, or the command exits non-zero</fails_when></verify>
  <acceptance_criteria>02-VALIDATION.md owns the complete ten-task map with draft/pending lifecycle metadata and no missing automated verification row.</acceptance_criteria>
  <done>The validation map is finalized for execution and structurally Nyquist-compliant.</done>
  <produced_artifacts>Updated 02-VALIDATION.md with ten mapped task rows.</produced_artifacts>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Documentation -> operator | Incorrect contract can cause unsafe provider/configuration use |
| Validation -> implementation | Missing mapping can silently leave behavior untested |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-04-01 | Tampering | English docs | medium | mitigate | Per-file diff, semantic fallback checks, and wiki-empty assertion. |
| T-02-04-02 | Repudiation | validation matrix | medium | mitigate | Exact task IDs, commands, wave metadata, and sampling continuity are required. |
</threat_model>
<verification><automated>git diff --check && test -z "$(git status --short -- wiki/)"</automated><fails_when>documentation diff or wiki guard fails</fails_when></verification>

## Source Audit

| Source | ID | Coverage | Plan |
|---|---|---|---|
| GOAL | — | Exact model identifiers select compatible native routes while pinned ChatGPT remains default | 02-01, 02-02, 02-03 |
| REQ | ROUTE-01 | Exact configured model reaches Responses-native provider without body alteration | 02-01, 02-02, 02-03 |
| REQ | ROUTE-02 | Ambiguous/translated/incompatible targets reject pre-dispatch with pinned compatibility default | 02-01, 02-02, 02-03 |
| RESEARCH | — | Typed resolver, provider-aware credentials, bounded inspection, hot reload, replay boundary, tests, docs | 02-01, 02-02, 02-03, 02-04, 02-05 |
| CONTEXT | D-01..D-12 | Every locked selection, fidelity, dispatch, verification, and documentation decision | 02-01, 02-02, 02-03, 02-04, 02-05 |
