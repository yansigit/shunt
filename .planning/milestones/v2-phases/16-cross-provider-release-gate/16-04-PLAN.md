---
phase: phase-16-cross-provider-release-gate
plan: 04
type: execute
wave: 3
depends_on: [16-01, 16-02, 16-03]
autonomous: false
files_modified:
  - .planning/phases/16-cross-provider-release-gate/16-SMOKE.md
requirements: [REL-04]
must_haves:
  truths:
    - "REL-04/concurrency: root owns the eight-attempt total budget; record an attempt before dispatch so interruption cannot authorize a retry."
    - "D-04/D-05/REL-04: live requests, if any, are opt-in, serialized, no-retry, <=8 total, <=60s and <=128 output tokens each, with planned paid total <=US$1 and immutable source credentials."
    - "D-06: missing credentials, unknown cost/reasoning/input bounds, unsafe refresh/writeback, unavailable service, or budget exhaustion are explicit skipped/blocked outcomes."
  artifacts:
    - path: .planning/phases/16-cross-provider-release-gate/16-SMOKE.md
      provides: Preflight ledger and redacted live/skip records owned by root.
---
<objective>Execute only the approved bounded live-smoke preflight and requests, or record honest skips/blocks, without production access.</objective>
<execution_context>
@.planning/phases/16-cross-provider-release-gate/16-CONTEXT.md
@.planning/phases/16-cross-provider-release-gate/16-RESEARCH.md
@/tmp/shunt-phase12-isolated-run.cjs
</execution_context>
<threat_model>ASVS level 1; block high. Credential exfiltration and destination spoofing are mitigated by preflight canonical-origin checks, redaction, immutable byte/mtime snapshots, and isolated homes/non-10100 ports. Cost/availability uncertainty fails closed. Root owns the finite budget; no automatic retries or refreshes.</threat_model>
<tasks>
<task type="auto">
<name>Task 1: Root-owned preflight and bounded smoke execution</name>
<files>.planning/phases/16-cross-provider-release-gate/16-SMOKE.md</files>
<read_first>16-CONTEXT.md; 16-RESEARCH.md; /tmp/shunt-phase12-isolated-run.cjs; provider auth source definitions; docs/provider-release-evidence.md</read_first>
<action>Root must establish credential presence without reading contents, canonical destination, exact model/wire, complete input/output/reasoning/token and price bounds, and read-only credential behavior before each request. Serialize at most eight synthetic no-tool requests with no retries, 60-second timeout, 128 output-token cap, and US$1 planned total. Run every stateful process through the wrapper, snapshot production config and backup/invalid inventory before/after, redact all output, stop on mutation or budget uncertainty, and record pass/skip/blocked with reason. Do not call 10100, production home, login, refresh, purchase, or unknown-cost paths.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test check_cli opencode_go_cli_negative -- --test-threads=1</automated><fails_when>The named filter selects zero tests, any live request exceeds a bound, retries, follows redirects, exposes secrets, mutates source credentials/production state, or an unsafe/unknown path is reported as pass.</fails_when></verify>
<acceptance_criteria>SMOKE.md contains complete preflight fields and honest per-attempt outcomes; zero live calls is valid when all paths skip.</acceptance_criteria>
<done>REL-04 evidence is complete and budget/accounting is auditable by root.</done>
</task>
</tasks>
## Artifacts this phase produces

- 16-SMOKE.md with preflight, attempt accounting, per-tuple outcomes and mutation checks.

## Root execution routing

This plan executes inline in the root orchestrator, not a delegated executor.
The user already approved its bounds. Root handles the auto task using the same
execute-plan contract and records results; no new user action is required unless
scope or authority changes. Credential presence preflight does not expose
contents; an eligible request may read an existing authorized credential only
into isolated memory, never logs or committed files. Go remains ineligible.

The automated Go CLI check proves only its existing negative behavior; it is
not evidence of live cost, timeout, redirect, retry or credential immutability.
Those require separate preflight/attempt records. Include every budgeted outbound
generation attempt, including transport failures and adapter retries; disable
all retry layers or skip. No repeated sweep after interruption. Unknown complete
bounds yield an explicit skip before credential access or outbound generation.
