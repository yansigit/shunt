---
phase: 11-antigravity-protocol-and-credential-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - src/auth/antigravity/auth.rs
  - src/auth/shared.rs
  - src/adapters/gemini/mod.rs
  - tests/antigravity_catalog.rs
autonomous: true
requirements:
  - ANT-01
must_haves:
  truths:
    - "Only exact daily or production HTTPS Cloud Code Assist origins pass credential-bearing inference (D-01)."
    - "Suffix/lookalike hosts, plaintext, unexpected ports/paths/query/fragments, and off-origin redirect hops fail before bearer transmission."
  artifacts:
    - path: src/auth/antigravity/auth.rs
      provides: canonical inference-origin predicate
    - path: src/auth/shared.rs
      provides: redirect policy that validates each hop before credentials
    - path: tests/antigravity_catalog.rs
      provides: origin and redirect loopback evidence
  key_links:
    - from: src/auth/shared.rs
      to: src/auth/antigravity/auth.rs
      via: redirect callback applies the same exact canonical-origin predicate
      pattern: Policy::custom|inference_base_url
  prohibitions:
    - statement: "Never attach a bearer to an off-origin redirect."
      status: flagged
      verification: unverified
---

<objective>Make native Antigravity destination and redirect credential safety fail closed at every boundary.</objective>
<tasks>
<task type="tracer" tdd="true"><name>Task 1: Enforce canonical origin before bearer attachment</name><files>src/auth/antigravity/auth.rs, src/adapters/gemini/mod.rs, tests/antigravity_catalog.rs</files><read_first>src/auth/antigravity/auth.rs, src/adapters/gemini/mod.rs, tests/antigravity_catalog.rs, .planning/phases/11-antigravity-protocol-and-credential-hardening/11-CONTEXT.md</read_first><behavior>ANT-01/D-01: exact canonical daily/prod HTTPS origins pass; lookalike/suffix/plaintext/query/fragment/port/path variants fail before any Authorization header or mock hit.</behavior><action>Write the antigravity_native_origin table first, then centralize an exact parsed-origin predicate shared by inference construction and credential-bearing request creation. Normalize approved production to daily only as existing policy requires; preserve loopback test overrides without making them production-approved. Reject all lookalikes and malformed URL components before attaching the synthetic bearer.</action><verify><automated>cargo test --all-features antigravity_native_origin</automated><fails_when>non-zero exit, or zero origin cases are selected</fails_when></verify><acceptance_criteria>Every accepted URL has HTTPS and exact approved host/shape; every rejected URL causes zero bearer-bearing upstream requests.</acceptance_criteria><done>Canonical-origin admission is enforced and tested.</done></task>
<task type="auto" tdd="true"><name>Task 2: Reject unsafe redirect hops before credentials cross origin</name><files>src/auth/shared.rs, src/auth/antigravity/auth.rs, tests/antigravity_catalog.rs</files><read_first>src/auth/shared.rs, src/auth/antigravity/auth.rs, tests/antigravity_catalog.rs</read_first><behavior>ANT-01/D-01: a redirect from an approved endpoint to an off-origin endpoint is rejected without forwarding Authorization; approved same-origin redirects follow only when exact predicate remains true.</behavior><action>Extend the existing hardened redirect-client pattern with a Cloud Code Assist policy callback that validates every target URL before forwarding or reconstructing bearer headers. Add loopback redirect fixtures for same-origin success and off-origin rejection, including a suffix host. Keep redirect count bounded and diagnostics redacted.</action><verify><automated>cargo test --all-features antigravity_native_origin</automated><fails_when>non-zero exit, or captured off-origin request contains Authorization</fails_when></verify><acceptance_criteria>Off-origin redirect attempts terminate as protocol/auth errors and the receiving mock observes no bearer; same-origin behavior remains bounded.</acceptance_criteria><done>Redirect-boundary credential safety is proven.</done></task>
</tasks>
<threat_model>ASVS level 1; block high. High information-disclosure threat is bearer exfiltration through redirect/lookalike origin; mitigate exact origin allowlist and no-bearer redirect tests. Medium SSRF threat from malformed endpoint; mitigate parse-before-send and strict path/query/fragment policy. Low DoS threat from redirect loops; retain bounded redirect count.</threat_model>
<verification><automated>cargo test --all-features antigravity_native_origin</automated><fails_when>non-zero exit or any bearer-bearing unsafe-origin request is observed</fails_when></verification>
<success_criteria>ANT-01 canonical destination and redirect rules are fail-closed and hermetically evidenced.</success_criteria>
## Artifacts this phase produces

Canonical Antigravity origin predicate and credential-safe redirect policy/tests.
