---
phase: 14-command-code-product-separation
plan: "04"
type: execute
wave: 3
depends_on: ["03"]
files_modified:
  - src/adapters/command_code/mod.rs
  - src/model/mod.rs
  - src/adapters/command_code/router_tests.rs
  - src/adapters/command_code/ndjson.rs
  - src/adapters/command_code/relay.rs
  - src/model/command_code_response.rs
  - src/model/command_code_response/validation.rs
  - tests/command_code_translate/response.rs
  - tests/command_code_translate.rs
  - tests/command_code_conformance.rs
autonomous: true
requirements:
  - CCS-05
  - CCS-06
must_haves:
  truths:
    - "One checked semantic machine serves streaming and unary output so the two modes cannot drift; text/reasoning relay incrementally and tool assembly is bounded (D-08)."
    - "Every malformed class fails explicitly: junk/null-only/empty bodies, invalid UTF-8, malformed/oversized records, incomplete tools, provider error finishes, duplicate terminals, conflicting finishes, and EOF without a supported terminal record (CCS-06; source tolerance deliberately not ported)."
    - "Terminal grammar per the evidence ledger: first terminal-class record authoritative; the documented finish-step(usage)->finish(totalUsage) companion pair accepted exactly once; any second terminal-class record except the one supported finish-step->finish companion, finish-step after finish, duplicate same-kind, or error-vs-non-error conflict fails closed (D-08, source 703-710/643-649)."
    - "Byte budgets are named consts exercised below/at/above: per-record cap, residual cap, cumulative semantic output, tool count and argument bytes; wire framing and semantic budgets are independently asserted (D-09, CCS-08 boundary)."
    - "Usage accepts only representable nonnegative integers with checked arithmetic; fractional, negative, and overflowing values fail explicitly (CCS-08 precision)."
  artifacts:
    - path: src/adapters/command_code/ndjson.rs
      provides: "expanded bounded NDJSON Decoder: line framing, below/at/above record caps, strict UTF-8, residual fail-closed, named byte consts"
    - path: src/model/command_code_response.rs
      provides: "checked response machine (text/reasoning deltas, tool start/argument/end, usage, terminal gating, final_ndjson_checked + transport_close_checked); split into a second file if it exceeds ~500 lines"
    - path: tests/command_code_translate.rs
      provides: "response-machine unit fixtures incl. the terminal grammar matrix and usage precision matrix"
    - path: src/adapters/command_code/router_tests.rs
      provides: "gateway fixtures: incremental streaming order, unary twin, byte-bound sweep, cancellation mid-body"
  key_links:
    - from: src/adapters/command_code/ndjson.rs
      to: src/model/command_code_response.rs
      via: "each bounded record feeds exactly one machine transition; transport cannot emit semantics"
      pattern: "transport_close_checked|final_ndjson_checked"
  prohibitions:
    - statement: "No EOF success synthesis, no response repair, no heuristic completion, no SSE-prefix/junk tolerance inherited from source (CCS-06)."
      status: flagged
      verification: unverified
---

<objective>Build the checked NDJSON response contract: bounded decoding, the shared semantic machine with exact terminal grammar, and byte/precision boundary fixtures proving every failure mode explicit.</objective>

<tasks>
Execution ownership: pure response fixtures may use tests/command_code_translate/response.rs, included by the existing integration target, and strict usage/identity helpers may live in src/model/command_code_response/validation.rs. These splits keep modules below the preferred 500-line limit; all filters still execute under --test command_code_translate. Input usage normalization follows the ledger: checked subtraction of cache reads+writes from inclusive inputTokens, no clamping.
Task 2/3 additionally own src/adapters/command_code/relay.rs: the private owned byte-stream/decoder/machine tuple shared by unary and SSE, plus paused-time transport deadline fixtures. These transport-unit fixtures are not claimed as network captures; actual wire fixtures remain exclusively router_tests.rs.
Execution clarification (source rechecked 2026-09-08 at 055c3ecf, command-code.ts:430-444): the exact usage detail keys are inputTokenDetails.cacheReadTokens and inputTokenDetails.cacheWriteTokens, not cacheRead/cacheWrite. Validate present usage/detail objects and individual fields strictly, with checked integer arithmetic. Preserve first-terminal usage authority when accepting a companion; do not double-count or overwrite it with the companion's totalUsage. Tests must distinguish these exact spellings and differing companion totals.
Fixture ownership contract: all subscription gateway wire fixtures (command_code_tracer_*, command_code_lifetime_*, command_code_matrix_*, and gateway command_code_bounds_*) live exclusively in src/adapters/command_code/router_tests.rs, declared cfg(test), and execute through --lib. Resolver-only fixtures live in the crate-local auth module. tests/command_code_conformance.rs contains public config/admission tests only, executed by the full workspace gate; it never owns the named wire filters. Translation fixtures live in tests/command_code_translate.rs and execute via --test command_code_translate. This contract supersedes ambiguous conformance-file artifact descriptions below. Each filtered invocation must visibly run at least one test and all named fixtures assigned to that group.

<task type="auto" tdd="true">
<name>Task 1: Response machine — deltas, tool assembly, usage, terminal grammar</name>
<files>src/model/mod.rs, src/adapters/command_code/mod.rs, src/model/command_code_response.rs, src/adapters/command_code/ndjson.rs, tests/command_code_translate.rs</files>
<read_first>14-02 Task 0 ledger response sections (events, terminal grammar, usage fields); src/model/openai_chat_response/checked.rs (validate_chunk pattern); 14-CONTEXT.md D-08/D-09; 14-EDGE-COVERAGE.json CCS-06 row (unclassified — flagged)</read_first>
<behavior>CCS-05/CCS-06/D-08. Machine consumes one record at a time: text-delta/reasoning-delta emit increments; tool-call maps to start/argument/end with authentic ids; usage (inputTokens/outputTokens, inputTokenDetails.cacheRead/Write) only representable nonnegative integers — fractional/negative/overflow fail explicitly (CCS-08 precision); finish events obey the pinned grammar (first terminal-class authoritative; finish-step->finish companion pair once; duplicates/conflicts fail); error records and error finishReason produce failed-turn semantics keeping usage; unknown parseable record types have one explicit bounded classification (typed unknown, never silently ignored); empty/null-only body fails. Fixtures named command_code_translate_machine_*.</behavior>
<action>Write red fixtures first, then implement the machine mirroring the checked.rs pattern with terminal gating and final_ndjson_checked/transport_close_checked twins. Keep records as the sole driver — the transport may not emit semantics. Split the module if it grows past ~500 lines (project style), updating mod wiring. No repair or synthesis paths anywhere.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test command_code_translate command_code_translate_machine -- --test-threads=1</automated><fails_when>non-zero exit; any terminal-grammar case passes that the ledger forbids; a fractional/negative/overflow usage passes; an unknown record is silently dropped; empty/null-only yields success; or the filter selects zero tests</fails_when></verify>
<done>The semantic machine enforces the pinned grammar and precision rules with explicit failure for every malformed class.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Streaming/unary relay over the gateway — incremental order, unary twin</name>
<files>src/adapters/command_code/mod.rs, src/adapters/command_code/ndjson.rs, src/model/command_code_response.rs, tests/command_code_conformance.rs, src/adapters/command_code/router_tests.rs</files>
<read_first>14-02 Task 1 harness + unfold relay pattern from Phase 13; src/keepalive.rs with_pings; 14-CONTEXT.md D-06/D-08</read_first>
<behavior>CCS-05/D-08. Gateway fixtures named command_code_tracer_response_*: streaming text/reasoning relay reaches the client incrementally through keepalive pings with exactly one authoritative terminal; the unary collect path uses the same machine (final twin) so modes cannot drift; a mid-body provider error record produces exactly one error event then termination; a stream cut by EOF after records but without a terminal record fails. In-batch validation never promises inspection of future bytes after transport cancellation.</behavior>
<action>Write red fixtures, then wire the unfold relay owning (byte stream, Decoder, machine, finished flag, residual) wrapped in with_pings, mirroring Phase 13 lifetime discipline (dropping the client drops the tuple; no registry). Switch 14-02's minimal unary path onto the machine so both modes share one contract. Preserve non-buffering for streaming clients.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib command_code_tracer_response -- --test-threads=1</automated><fails_when>non-zero exit; text is buffered instead of incremental; more than one terminal or success after EOF; the error record yields success; the unary and streaming machines diverge; or the filter selects zero tests</fails_when></verify>
<done>Both output modes share one checked machine over the real gateway with single-terminal semantics.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: Byte-boundary sweep — wire vs semantic budgets below/at/above</name>
<files>src/adapters/command_code/ndjson.rs, src/model/command_code_response.rs, tests/command_code_translate.rs, tests/command_code_conformance.rs, src/adapters/command_code/router_tests.rs</files>
<read_first>14-EDGE-COVERAGE.json CCS-08 boundary + precision rows; 14-CONTEXT.md D-09; existing byte-bound patterns in Phase 13 sse tests</read_first>
<behavior>CCS-06/CCS-08/D-09. Fixtures named command_code_bounds_* exercise each named const below/at/above: per-record byte cap (record split across reads, at-cap accepted, above fails); residual cap; cumulative semantic output (below/at/above); tool count; argument bytes; UTF-8 split across network reads within a record decodes losslessly, invalid UTF-8 fails; slow-drip read deadline fires. Wire framing bounds and semantic accumulation budgets are asserted independently, and truncation never repairs content.</behavior>
<action>Write red fixtures, then ensure each budget is a named const in ndjson.rs/machine and enforced at the exact boundary (at-cap accepted, one-over fails). Add the deadline using existing adapter read-deadline primitives. All fixtures carry contiguous filter substrings.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test command_code_translate command_code_bounds -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib command_code_bounds -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings</automated><fails_when>non-zero exit; an at-cap case fails or an above-cap case passes; a bound is implicit rather than a named const; repair synthesizes content; or a filter selects zero tests</fails_when></verify>
<done>Every declared bound has below/at/above coverage with explicit failure and no repair.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundaries: upstream wire bytes (fully untrusted). High tampering threat: a crafted stream masquerading as a successful turn via duplicate terminal, EOF, or unknown-record abuse — mitigate the pinned grammar, typed unknown classification, and fail-closed EOF proven red-first. High DoS threat: unbounded records/accumulation exhausting memory — mitigate named below/at/above-enforced byte consts for records, residual, semantic output, tool count, and arguments, plus read deadlines. Medium information threat: raw upstream error bodies leaking into client diagnostics — mitigate neutral redacted error mapping per the ledger.
</threat_model>
<verification><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test command_code_translate -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test command_code_conformance -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings && node /tmp/shunt-phase12-isolated-run.cjs env 'RUSTFLAGS=-D warnings' cargo test --all-features --workspace</automated><fails_when>any chained invocation exits non-zero or any filter selects zero tests</fails_when></verification>
<success_criteria>The subscription response contract is bounded, checked, grammar-exact, and shared by both output modes, with every malformed class and byte boundary failing explicitly.</success_criteria>

## Artifacts this phase produces

The inbound contract: expanded decoder, shared semantic machine, and boundary fixtures — the last semantic piece before lifetime/replay in 14-05.
