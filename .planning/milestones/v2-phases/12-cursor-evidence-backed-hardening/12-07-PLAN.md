---
phase: 12-cursor-evidence-backed-hardening
plan: "07"
type: execute
wave: 6
depends_on:
  - "05"
  - "06"
files_modified:
  - src/retry.rs
  - src/adapters/cursor/mod.rs
autonomous: true
requirements:
  - CUR-07
must_haves:
  truths:
    - "Local construction/header failures never retry; only bounded pre-output transport failures are retry candidates; ambiguous post-send failures and emitted tools prevent redispatch (D-08)."
    - "RetrySafety classification for Cursor is wired at the existing AdapterFailure seam with TODO(#126) tightening resolved; no new retry machinery is added (D-08)."
    - "No-failover behavior is fixture-proven at the seam, with this plan as explicit owner (failover consult tests)."
  artifacts:
    - path: src/retry.rs
      provides: resolved Cursor RetrySafety declarations and replay-safety table tests
    - path: src/adapters/cursor/mod.rs
      provides: classification seam assertions and the no-failover consult proof
  key_links:
    - from: src/retry.rs
      to: src/proxy/failover.rs
      via: failover consults RetrySafety before choosing a fallback
      pattern: RetrySafety|failover
  prohibitions:
    - statement: "Do not add a Commitment wrapper or a redispatch seam that does not already exist (D-08 orchestrator constraint)."
      status: flagged
      verification: unverified
    - statement: "Do not retry after any output emission or ambiguous post-send failure (D-08)."
      status: flagged
      verification: unverified
---

<objective>Prove classification and replay-safety boundaries for Cursor at the existing bounded seams, including explicit no-failover ownership, without new retry machinery.</objective>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Resolve Cursor RetrySafety taxonomy (CUR-07/D-08)</name>
<files>src/retry.rs</files>
<read_first>src/retry.rs :22-155 and TODO(#126, cursor) at :34/:206, src/adapters/cursor/mod.rs error construction, src/proxy/failover.rs :11, tests/retry.rs, .planning/phases/11-antigravity-protocol-and-credential-hardening/11-CONTEXT.md D-17 precedent</read_first>
<behavior>CUR-07/D-08: cursor uses ConnectOnly semantics (Antigravity D-17 precedent) whenever a request could have reached the upstream; Idempotent is not used for Cursor tool turns; ambiguous post-send yields NonIdempotentPost or no-retry, never retry.</behavior>
<action>Replace TODO(#126, cursor) markers with explicit RetrySafety resolution per the Phase 11 precedent; add table tests asserting each failure class maps to the intended safety value and that emitting tools blocks redispatch where the seam reads the emitted-tool flag. No new retry attempts; classification only. Preserve all existing tests.</action>
<verify><automated>cargo test --all-features cursor_retry_safety -- --test-threads=1</automated><fails_when>non-zero exit, TODO(#126, cursor) markers remain, or zero selected tests</fails_when></verify>
<acceptance_criteria>No TODO(#126, cursor) markers remain; classification table complete across seam and failover consumers.</acceptance_criteria>
<done>Classification resolved with precedent-consistent semantics.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Adapter seam classification and no-failover proof (CUR-07/D-08)</name>
<files>src/adapters/cursor/mod.rs</files>
<read_first>src/adapters/cursor/mod.rs failure tagging sites and TODO(#170) notes :125-126, finalized src/retry.rs declarations, src/proxy/failover.rs consumer path</read_first>
<behavior>CUR-07/D-08: construction/header errors never tagged retry-safe; UpstreamStatus classification matches declarations; emitted tools block redispatch; the failover consult for Cursor is explicitly proven not to trigger redispatch for this adapter's post-output cases (this plan owns the no-failover tests).</behavior>
<action>Loopback fixtures assert the final classification as seen by the proxy: construction/header error never a retry candidate; upstream status classification as declared; emitted-tool fact blocks redispatch at the existing failover consult. TODO(#170) stays present and honest if the streaming pre-response seam is still absent; do not invent one. Doc-comment cross-references D-08 and the milestone record.</action>
<verify><automated>cargo test --all-features cursor_failure_classification -- --test-threads=1</automated><fails_when>non-zero exit, a classified retry occurs post-output, or zero selected tests</fails_when></verify>
<acceptance_criteria>Construction-only, headers, upstream status, emitted-tool, and no-failover contexts all covered with observable output at the proxy seam.</acceptance_criteria>
<done>Bounded pre-output classification and explicit no-failover ownership proven end-to-end.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundary is retry decisioning over untrusted upstream failure signals. High tampering threat: corrupted upstream errors could look transient and replay tool execution; mitigate emitted-tool blocks and NonIdempotentPost semantics. Medium integrity threat: misclassification skips failover for auth costs; mitigate per-variant table tests. Low DoS threat: bounded attempts only; mitigate no-new-tries constraint.
</threat_model>
<verification><automated>cargo test --all-features cursor_retry_safety -- --test-threads=1 && cargo test --all-features cursor_failure_classification -- --test-threads=1</automated><fails_when>either invocation exits non-zero or selects zero tests</fails_when></verification>
<success_criteria>CUR-07 classification, replay-safety boundaries, and no-failover ownership proven with TODO #126 Cursor resolved.</success_criteria>

## Artifacts this phase produces

Resolved RetrySafety plus loopback classification and no-failover proofs.
