---
phase: 12-cursor-evidence-backed-hardening
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/cursor/mod.rs
autonomous: true
requirements:
  - CUR-01
  - CUR-02
must_haves:
  truths:
    - "The AgentService Run destination constant is enforced by a regression test so replacement requires captured or live evidence (D-01)."
    - "Per-request model/mode/endpoint/capability facts are request-local; concurrent mixed-model requests cannot mutate or share mutable observation state (D-02)."
  artifacts:
    - path: src/adapters/cursor/mod.rs
      provides: destination regression and concurrency request-isolation proof fixtures
  prohibitions:
    - statement: "Do not change AGENT_BASE_URL or invent destination fallback (D-01)."
      status: flagged
      verification: unverified
    - statement: "Do not infer wire support from model families (D-02)."
      status: flagged
      verification: unverified
---

<objective>Pin the proven Run destination and prove request-local capability facts survive concurrent mixed-model traffic.</objective>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Destination regression assertion (CUR-01)</name>
<files>src/adapters/cursor/mod.rs</files>
<read_first>src/adapters/cursor/agent.rs lines 1-70 and Run endpoint construction, src/adapters/cursor/mod.rs wiremock harness :520-560, .planning/phases/12-cursor-evidence-backed-hardening/12-CONTEXT.md D-01</read_first>
<behavior>CUR-01/D-01: every dispatched request targets https://agentn.global.api5.cursor.sh/agent.v1.AgentService/Run regardless of model, mode, fast flag, or client version; retired api2 endpoints never appear on the active path.</behavior>
<action>Adapter-dispatch-level loopback fixture (the WireMock adapter harness — labeled honestly as adapter level, NOT the whole gateway router; whole-router parity is owned by the 12-01 tracer) asserting the exact method+path of every Run POST across a mixed batch (three models); add a test-time regression asserting AGENT_BASE_URL and the endpoint suffix equal the proven values; explicit request headers captured per D-02.</action>
<verify><automated>cargo test --all-features cursor_destination -- --test-threads=1</automated><fails_when>non-zero exit, destination byte mismatch, or zero selected tests</fails_when></verify>
<acceptance_criteria>Exact absolute Run URL asserted per request and as a constant regression; no other host on the active path.</acceptance_criteria>
<done>Destination pinning is enforced by tests, not convention.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Request-local fact isolation under concurrency (CUR-02)</name>
<files>src/adapters/cursor/mod.rs</files>
<read_first>src/adapters/cursor/mod.rs dispatch and TurnGuard ownership, src/adapters/cursor/agent.rs read state, .planning/phases/12-cursor-evidence-backed-hardening/12-RESEARCH.md Section 4.1</read_first>
<behavior>CUR-02/D-02: interleaved requests differing in model, fast flag, and capability each observe only their own facts; no shared mutable observation state; events pair unambiguously to their originating request.</behavior>
<action>Add a concurrency fixture using tokio join (no detached tasks) with per-request expected facts, then assert isolation and exact pairing. If the proof requires a private ownership change near the unary block :334-339, keep it private and disclose the decision in the summary; public config keys stay untouched (D-09), legacy modules untouched.</action>
<verify><automated>cargo test --all-features cursor_request_isolation -- --test-threads=1</automated><fails_when>non-zero exit, cross-request fact mismatch, or zero selected tests</fails_when></verify>
<acceptance_criteria>Interleaved requests each surface only their own captured facts with unambiguous pairing.</acceptance_criteria>
<done>Request-local fact isolation is proven, not assumed.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundary is local dispatch state. High tampering threat: cross-request fact leakage misroutes models; mitigate concurrency mismatch assertions and strict pairing. Low DoS threat: mixed-model burst; mitigate bounded task count.
</threat_model>
<verification><automated>cargo test --all-features cursor_destination -- --test-threads=1 && cargo test --all-features cursor_request_isolation -- --test-threads=1</automated><fails_when>either invocation exits non-zero or selects zero tests; the sequential chain stops at first failure</fails_when></verification>
<success_criteria>CUR-01 and CUR-02 enforced hermetically without public surface change.</success_criteria>

## Artifacts this phase produces

Destination pin regression plus request-isolation concurrency fixtures.
