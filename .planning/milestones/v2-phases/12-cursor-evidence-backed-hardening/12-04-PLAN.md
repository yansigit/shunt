---
phase: 12-cursor-evidence-backed-hardening
plan: "04"
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - src/adapters/cursor/mod.rs
autonomous: true
requirements:
  - CUR-04
must_haves:
  truths:
    - "Nameless tools, missing or non-object schemas, undecodable images, and unsupported argument representations fail explicitly before dispatch (D-04)."
    - "Silent drop behavior at extract_cursor_tools :256-278 and decode_selected_images :176-190 is replaced by explicit CursorError surfaces; native MCP tool handoff provenance is preserved (D-04, D-01)."
  artifacts:
    - path: src/adapters/cursor/mod.rs
      provides: explicit unsupported pre-dispatch admission with loopback rejection fixtures
  key_links:
    - from: src/adapters/cursor/mod.rs
      to: src/adapters/cursor/client.rs
      via: unsupported forms surface CursorError in the gateway error shape
      pattern: CursorError|unsupported
  prohibitions:
    - statement: "Do not revive the retired tool bridge by assumption (D-04, D-09)."
      status: flagged
      verification: unverified
    - statement: "Do not silently default missing schemas or drop undecodable images (D-04)."
      status: flagged
      verification: unverified
---

<objective>Replace silent pre-dispatch drops with explicit unsupported errors for tools, schemas, images, and argument forms, without inventing tool support.</objective>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Explicit unsupported admission errors (CUR-04)</name>
<files>src/adapters/cursor/mod.rs</files>
<read_first>src/adapters/cursor/mod.rs extract_cursor_tools :256-278, decode_selected_images :176-190, :95 dispatch order, src/adapters/cursor/client.rs CursorError, .planning/phases/12-cursor-evidence-backed-hardening/12-PATTERNS.md mod.rs admission analog</read_first>
<behavior>CUR-04/D-04: each rejected form produces a distinct single pre-dispatch error naming the unsupported aspect: nameless tool, missing/non-object schema, undecodable base64 image, unsupported argument representation, unsupported guidance form. Valid forms unchanged.</behavior>
<action>Replace silent filter_map drop and .ok()? drop with explicit error returns in the admission path; loopback fixtures assert dispatch never fires for rejected forms, errors surface in the gateway error shape, and a healthy request immediately before/after still succeeds (no sticky rejected state). Preserve the documented native MCP handoff provenance (Phase 9-11 decisions).</action>
<verify><automated>cargo test --all-features cursor_admission -- --test-threads=1</automated><fails_when>non-zero exit, any silent-drop fixture missing, or zero selected tests</fails_when></verify>
<acceptance_criteria>All listed unsupported forms fail with explicit pre-dispatch errors; valid tools/images remain functional; zero dispatch on rejection.</acceptance_criteria>
<done>Admission gap closed with exhaustive loopback evidence.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Legacy off-path and honesty assertions (CUR-04/D-01)</name>
<files>src/adapters/cursor/mod.rs</files>
<read_first>src/adapters/cursor/tool_bridge.rs (legacy, read-only), src/adapters/cursor/mod.rs admission call order, .planning/phases/12-cursor-evidence-backed-hardening/12-CONTEXT.md D-01/D-09</read_first>
<behavior>D-01/D-04: legacy proto.rs/stream.rs/tool_bridge.rs/tool_use_xml.rs/response.rs and old CursorHttpClient are untouched and provably off the active dispatch path.</behavior>
<action>Add behavior-based negative fixtures that admission rejects before any legacy bridge function is reachable (call-order assertion via mock gateway route names), plus doc-comment honesty about retained legacy scope via D-01. Legacy modules remain untouched; never delete them. Disclose any retired-module ambiguity in the summary.</action>
<verify><automated>cargo test --all-features cursor_admission_legacy_offpath -- --test-threads=1</automated><fails_when>non-zero exit, legacy path reachable from active fixtures, or zero selected tests</fails_when></verify>
<acceptance_criteria>Legacy tool bridge provably off-path; legacy modules compile and pass tests unmodified.</acceptance_criteria>
<done>Retired/active boundary is pinned by assertions.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundary is inbound client tool/image payloads. High tampering threat: malformed tool/image data could reach dispatch; mitigate explicit pre-dispatch rejection fixtures. High repudiation threat: silent drop hides bad input; mitigate distinct named errors. Medium DoS threat: adversarially sized base64 image; mitigate existing decode bound and bounded fixtures.
</threat_model>
<verification><automated>cargo test --all-features cursor_admission -- --test-threads=1 && cargo test --all-features cursor_admission_legacy_offpath -- --test-threads=1</automated><fails_when>either invocation exits non-zero or selects zero tests; sequential chain stops at first failure</fails_when></verification>
<success_criteria>CUR-04 explicit unsupported-form admission proven with zero silent-drop paths and legacy kept off-path.</success_criteria>

## Artifacts this phase produces

Explicit unsupported-form admission errors plus retired-module off-path assertions.
