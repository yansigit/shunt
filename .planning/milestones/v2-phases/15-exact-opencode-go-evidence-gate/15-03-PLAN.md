---
phase: phase-15-exact-opencode-go-evidence-gate
plan: 03
type: execute
wave: 3
depends_on: [15-02]
autonomous: true
files_modified:
  - README.md
  - site/src/content/docs/providers/opencode-go.md
  - site/src/content/docs/reference/configuration.md
  - site/src/lib/i18n.ts
  - docs/opencode-go-evidence-gate.md
  - tests/opencode_go_docs.rs
requirements: [OGO-01, OGO-02, OGO-03]
must_haves:
  truths:
    - "D-07/OGO-03: the English provider, configuration, and engineering-note surfaces state that any future admitted tuple must reuse the matching existing Chat contract and may send a stable opaque conversation-scoped x-opencode-session only to the canonical destination; the empty set emits neither session nor credential today."
    - "D-10/REL-05: README and provider/configuration pages agree in English (this plan) with the locale mirrors owned by 15-05; navigation is a single structured entry in site/src/lib/i18n.ts covering all locales."
  artifacts:
    - path: "site/src/content/docs/providers/opencode-go.md"
      provides: "English zero-support provider guidance for opencode-go"
    - path: "site/src/lib/i18n.ts"
      provides: "NAVIGATION entry plus embedded label/translations for the provider page (single nav source for all locales)"
    - path: "docs/opencode-go-evidence-gate.md"
      provides: "engineering note connecting the evidence gate to D-01..D-11 and the wiki regeneration boundary"
    - path: "tests/opencode_go_docs.rs"
      provides: "per-file deterministic assertions; one test fn per required file, each failing when that exact file is missing or lacks required tokens"
  key_links:
    - from: "README.md"
      to: "site/src/content/docs/reference/configuration.md"
      via: "same additive key SHUNT_OPENCODE_GO_API_KEY and zero-admission contract"
  prohibitions:
    - statement: "No surface claims any candidate is supported or live-verified; zero-support wording stays uniform across English surfaces."
      status: unresolved
      verification: judgment
    - statement: "Generated wiki is never hand-edited and no locale files are touched here (15-05 owns the nine locale files)."
      status: unresolved
      verification: judgment
    - statement: "No session producers, dynamic catalogs, or speculative multi-wire dispatch are documented as shipping behavior."
      status: unresolved
      verification: judgment
---

<objective>Publish synchronized English zero-support guidance, navigation, and the engineering note for the empty evidence gate, with per-file verification that fails on any missing required surface.</objective>
<execution_context>
@.planning/phases/15-exact-opencode-go-evidence-gate/15-CONTEXT.md
@.planning/phases/15-exact-opencode-go-evidence-gate/15-EVIDENCE.md
</execution_context>
<threat_model>
ASVS level 1; block high severity. Trust boundaries: published documentation consumers who may make purchasing/rollout decisions from these pages. High spoofing threat: a positive-support claim (supported, live-verified, or a candidate promoted early) would misrepresent the phase contract; mitigate by per-file assertions rejecting positive claims and requiring the exact zero-admission tokens. Medium tampering threat: a dead nav link or missing locale entry in site/src/lib/i18n.ts breaks discovery without failing a build direction; mitigate by asserting the i18n.ts structure explicitly. Surface scope only — this plan changes no runtime behavior.
</threat_model>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: English documentation with per-file RED/GREEN assertions</name>
  <files>README.md, site/src/content/docs/providers/opencode-go.md, site/src/content/docs/reference/configuration.md, site/src/lib/i18n.ts, docs/opencode-go-evidence-gate.md, tests/opencode_go_docs.rs</files>
  <read_first>README.md; site/src/content/docs/providers (existing page shape); site/src/content/docs/reference/configuration.md; site/src/lib/i18n.ts (NAVIGATION structure); docs/ (note conventions); .planning/phases/15-exact-opencode-go-evidence-gate/15-EVIDENCE.md; existing doc assertion conventions</read_first>
  <behavior>Each required English file has one dedicated test function (opencode_go_docs_readme, opencode_go_docs_provider, opencode_go_docs_configuration, opencode_go_docs_nav_i18n, opencode_go_docs_engineering_note) asserting within THAT file's new Go section (not unrelated provider prose): the env key SHUNT_OPENCODE_GO_API_KEY appears; the canonical URL appears; a zero-support/unsupported statement appears; no positive-support claim token appears; the nav test asserts the NAVIGATION entry plus provider label exists in i18n.ts. A missing file or missing token fails that test by name. Navigation is the exception: assert only its Go link and four locale labels, not configuration prose, URLs, or env keys. Reject specific affirmative Go support claims, not the substring supported inside unsupported.</behavior>
  <action>First create or extend the per-file tests described below, before editing documentation. Run them and record behavioral RED for missing Go content (not a compile error). Then update the content and rerun to GREEN. Write deterministic pure-Rust file-reading assertions (fs::read_to_string of the workspace-relative path; missing file = failure with the file named). Core test name opencode_go_docs so the shared verify filter selects every per-file function with a single -- separator; per-file names stay contiguous as filter substrings of it. Document the exact opt-in key SHUNT_OPENCODE_GO_API_KEY, canonical https://opencode.ai/zen/go/v1 destination, kind opencode_go, the four candidate-only records, the empty admitted set, pre-credential rejection, strict authoritative terminal policy, and the future hermetic + credential-safe promotion conditions. Explain that x-opencode-session is conditional and absent today (D-07). Add the providers/opencode-go page, a NAVIGATION entry with embedded translations in site/src/lib/i18n.ts (covering all locales via the existing single-source structure), the matching configuration.md section, and the new engineering note docs/opencode-go-evidence-gate.md tying the gate to D-01..D-11 and noting wiki/ is regenerated, never hand-edited (guide links point at the provider page). Update the affected README quickstart/provider mention if present. Scope positive-claim checks to the new Go section only and preserve unrelated provider claims. Use the entire opencode_go_docs integration binary without a substring filter. Inspect its reported test count and named results: require at least 5 tests, including every newly owned per-file function; a zero-test run is never accepted. Temporarily remove one required Go token to confirm a behavioral failure, restore it, and rerun GREEN.</action>
  <verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test opencode_go_docs -- --test-threads=1</automated><fails_when>Any owned file lacks its required Go contract, an owned assertion fails, fewer than 5 tests are reported, any named per-file assertion is absent, or the mutation check does not fail.</fails_when></verify>
  <acceptance_criteria>English surfaces precisely describe the empty evidence gate, conditional OGO-03 behavior, and the engineering note carries the decision references. Verification fails if any required surface is missing required content; single-token alternation gaps are impossible because one file maps to one test.</acceptance_criteria>
  <done>Owned documentation and executable per-file coverage pass together; RED, mutation failure, restored GREEN, and actual test counts are recorded.</done>
</task>
</tasks>
