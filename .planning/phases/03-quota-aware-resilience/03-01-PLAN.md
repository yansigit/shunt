---
phase: 03-quota-aware-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/accounts.rs
  - src/adapters/responses/pool.rs
  - src/adapters/responses/inbound.rs
  - tests/inbound_codex_endpoint.rs
  - tests/codex_multi_account.rs
autonomous: true
requirements: [RES-01]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "A bounded, valid structured quota rejection prevents the affected account from being selected again during the current turn, while a status-only 429 remains eligible for ordinary transient rotation."
    - "The raw inbound Responses path performs quota classification before any response bytes or WebSocket events are exposed and preserves the final upstream response when all candidates fail."
    - "When every eligible account fails, the final upstream status, body, and safe Retry-After header are preserved verbatim across a hard-quota/transient combination."
    - "The translating Responses pool consumes the same typed classification and cooldown decision rather than maintaining a second quota policy."
  artifacts:
    - path: src/accounts.rs
      provides: "Typed quota decision boundary and shared cooldown reason/timing inputs"
    - path: src/adapters/responses/pool.rs
      provides: "Pre-stream bounded inspection and shared first/retry outcome wiring"
    - path: src/adapters/responses/inbound.rs
      provides: "Raw Responses account rotation using the typed quota decision"
    - path: tests/inbound_codex_endpoint.rs
      provides: "End-to-end hard-exhaustion rotation and verbatim final response fixture"
    - path: tests/codex_multi_account.rs
      provides: "All-eligible-accounts-exhausted hard/transient fixture with final status/body/header assertions"
  key_links:
    - from: src/adapters/responses/inbound.rs
      to: src/adapters/responses/pool.rs
      via: "forward_codex_passthrough calls the shared pre-stream classifier before classify_first"
    - from: src/adapters/responses/pool.rs
      to: src/accounts.rs
      via: "typed quota decision selects account-scoped cooldown and transient rotation behavior"
    - from: tests/inbound_codex_endpoint.rs
      to: src/adapters/responses/inbound.rs
      via: "mock upstream returns a structured hard-quota body for account one and success for account two"
    - from: tests/codex_multi_account.rs
      to: src/adapters/responses/inbound.rs
      via: "hard-quota then transient responses exhaust every candidate and assert final status/body/safe Retry-After fidelity"
---

<objective>Establish the production tracer for body-aware quota classification across the existing Responses account pool.</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-quota-aware-resilience/03-CONTEXT.md
@.planning/phases/03-quota-aware-resilience/03-RESEARCH.md
@src/accounts.rs
@src/adapters/responses/pool.rs
@src/adapters/responses/inbound.rs
@tests/inbound_codex_endpoint.rs
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Trace one hard-quota response through bounded inspection and account rotation</name>
  <files>src/accounts.rs, src/adapters/responses/pool.rs, src/adapters/responses/inbound.rs, tests/inbound_codex_endpoint.rs, tests/codex_multi_account.rs</files>
  <read_first>src/accounts.rs, src/adapters/responses/pool.rs, src/adapters/responses/inbound.rs, tests/inbound_codex_endpoint.rs, 03-CONTEXT.md, 03-RESEARCH.md</read_first>
  <action>Introduce an internal typed quota decision at the existing Codex account-pool boundary. Inspect only pre-stream failure responses with a fixed byte budget and bounded read completion; accept hard exhaustion only for HTTP 429/402 carrying one exact trusted structured discriminator (`usage_limit_exceeded` or `insufficient_quota`), and classify status-only or ambiguous evidence as transient/unverified (RES-01, D-01 through D-04). Feed the decision into the existing `FirstOutcome`/`RetryOutcome` path so a proven exhausted account receives the centralized bounded cooldown and is excluded from the current candidate order, while transient 429s retain ordinary rotation and 401 refresh behavior. Preserve the original response status, safe headers, and body for the final raw passthrough response, and keep the post-output no-replay boundary unchanged. Add the real Wiremock tracer with account one returning exact hard-quota JSON and account two returning the existing SSE success, asserting account one is not called again and the client receives account two's result. Also add an all-eligible-accounts-exhausted integration fixture in `tests/codex_multi_account.rs`: account one returns hard-quota evidence, account two returns a transient 429 with a safe `Retry-After`, and the client must receive account two's exact final status/body plus the validated safe Retry-After header (D-05, D-10).</action>
  <verify><automated>cargo test --test inbound_codex_endpoint hard_quota_rotates_without_revisit && cargo test --test codex_multi_account -- exhausted_pool_preserves_final_body</automated><fails_when>either focused test is missing/fails, account one is retried after proven hard-quota evidence, the final status/body/header differs from account two, classification occurs after client-visible output, or the command exits non-zero</fails_when></verify>
  <acceptance_criteria>A proven structured quota response causes one bounded account-scoped cooldown and one-way progression to the next eligible account; status-only 429 remains transient; when all eligible accounts fail across hard and transient outcomes, the last upstream status/body and safe Retry-After header are preserved verbatim for the client.</acceptance_criteria>
  <done>Runnable end-to-end fixtures prove bounded inspection, typed classification, cooldown wiring, account rotation, all-account exhaustion fidelity, and safe Retry-After exposure.</done>
</task>
<task type="auto">
  <name>Task 2: Reuse the quota decision on translated and refreshed pool paths</name>
  <files>src/adapters/responses/pool.rs, src/adapters/responses/inbound.rs, src/accounts.rs</files>
  <read_first>src/adapters/responses/pool.rs, src/adapters/responses/inbound.rs, src/accounts.rs, tests/codex_multi_account.rs, 03-RESEARCH.md</read_first>
  <action>Route the translating Responses pool and the 401 refreshed-retry branch through the same typed body probe and quota decision used by the tracer. Keep `classify_codex` authentication, permission, server-error, ordinary 4xx, and route-level failover semantics unchanged; apply account cooldown only after a proven hard-exhaustion decision and retain existing last-response precedence. Keep all body reads bounded, cancellation-safe, and before stream/event delivery, with no credential-file writeback or new public configuration.</action>
  <verify><automated>cargo test adapters::responses::pool:: --lib && cargo test --test codex_multi_account</automated><fails_when>translated or refreshed paths diverge from the shared decision, unrelated status behavior changes, or either command exits non-zero</fails_when></verify>
  <acceptance_criteria>Raw, translated, and refreshed account-pool paths share one quota policy and preserve existing authentication, non-quota failover, and streaming boundaries.</acceptance_criteria>
  <done>The shared account-pool tracer is wired across every existing Responses path without a second retry driver.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Upstream error body -> quota policy | Untrusted provider status, headers, and bounded bytes influence account availability |
| Gateway pool -> client response | Classified upstream response may be relayed to an OpenAI Responses client |
| Gateway -> credential store | Account selection must not trigger credential-file writes |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01-01 | Tampering | quota classifier | high | mitigate | Exact structured evidence, bounded parsing, and fail-closed ambiguous handling are covered by the tracer and adversarial expansion tests. |
| T-03-01-02 | Denial of service | body inspection | high | mitigate | Fixed byte/read bounds, cancellation propagation, and pre-stream placement prevent unbounded memory or stalled turns. |
| T-03-01-03 | Information disclosure | response relay | medium | mitigate | Existing safe-header filtering and verbatim final-body assertions remain in the inbound integration fixture. |
</threat_model>

<verification><automated>cargo test --test inbound_codex_endpoint hard_quota_rotates_without_revisit && cargo test adapters::responses::pool:: --lib && cargo test --test codex_multi_account</automated><fails_when>any focused quota, pool, or account integration command fails</fails_when></verification>

<nyquist_validation>
| Task | Automated evidence | Sampling |
|---|---|---|
| 03-01-01 | `cargo test --test inbound_codex_endpoint hard_quota_rotates_without_revisit` | End-to-end hard-quota rotation and no-revisit assertion |
| 03-01-02 | `cargo test adapters::responses::pool:: --lib && cargo test --test codex_multi_account` | Shared translated/refreshed path and regression sampling |
</nyquist_validation>

## Source Audit

| Source | ID | Coverage | Plan |
|---|---|---|---|
| GOAL | — | Distinguish transient throttling from exhausted quota in existing account paths | 03-01, 03-02 |
| REQ | RES-01 | Provider status/body evidence yields hard exhaustion only when trusted and bounded | 03-01, 03-02 |
| REQ | RES-02 | Shared bounded retry timing is completed in 03-02 | 03-02 |
| RESEARCH | — | Shared classifier, pre-stream inspection, pool wiring, raw final response fidelity | 03-01 |
| CONTEXT | D-01..D-05 | Status-only transient, exact evidence, unchanged unrelated statuses, shared pool, no replay | 03-01, 03-02 |
</source_audit>
