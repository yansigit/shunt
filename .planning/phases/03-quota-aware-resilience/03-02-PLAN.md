---
phase: 03-quota-aware-resilience
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/accounts.rs
  - src/retry.rs
  - src/adapters/responses/pool.rs
  - tests/retry.rs
  - tests/codex_multi_account.rs
  - tests/failover.rs
autonomous: true
requirements: [RES-01, RES-02]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "Retry-After delta-seconds and supported HTTP-date forms produce finite, deterministic waits with safe rounding and immediate-positive handling for zero or past deadlines."
    - "Overlong, malformed, overflowed, or excessive Retry-After values are ignored or bounded without wall-clock underflow or unbounded sleeping."
    - "Route-level 401/403/404/5xx and unrelated account-pool statuses preserve their existing failover and final-response precedence."
    - "Adversarial quota bodies (duplicates, malformed JSON, invalid UTF-8, truncation, timeout, and oversized payloads) fail closed to transient/unverified behavior."
    - "When every eligible account is exhausted, the final upstream status, body, and safe Retry-After header remain verbatim across a hard-quota/transient combination."
  artifacts:
    - path: src/accounts.rs
      provides: "Single bounded Retry-After parser and quota evidence tests"
    - path: src/retry.rs
      provides: "Existing retry policy consuming the centralized parser and honoring budget"
    - path: tests/retry.rs
      provides: "Delta/date, clamp, underflow, and retry-budget integration fixtures"
    - path: tests/failover.rs
      provides: "Unchanged route failover regression coverage"
  key_links:
    - from: src/retry.rs
      to: src/accounts.rs
      via: "next_backoff delegates all Retry-After parsing to accounts::retry_after"
    - from: src/adapters/responses/pool.rs
      to: src/accounts.rs
      via: "bounded quota decision and cooldown duration share one centralized timing contract"
    - from: tests/failover.rs
      to: src/proxy/failover.rs
      via: "status matrix asserts unrelated route advancement and remembered-failure precedence"
---

<objective>Harden the shared timing and classifier boundaries with adversarial fixtures while locking unrelated failover semantics.</objective>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-quota-aware-resilience/03-CONTEXT.md
@.planning/phases/03-quota-aware-resilience/03-RESEARCH.md
@.planning/phases/03-quota-aware-resilience/03-01-PLAN.md
@src/accounts.rs
@src/retry.rs
@src/adapters/responses/pool.rs
@src/proxy/failover.rs
@tests/retry.rs
@tests/failover.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Make Retry-After finite, standards-compliant, and table-driven</name>
  <files>src/accounts.rs, src/retry.rs, tests/retry.rs</files>
  <behavior>
    - Test integer zero, whitespace, decimal delta-seconds, and upward-safe rounding.
    - Test IMF-fixdate, RFC850, and asctime HTTP dates in the future plus now/past dates.
    - Test malformed, overlong, overflowed, and finite-maximum-exceeding values.
    - Test `next_backoff` honors an in-budget value and surfaces immediately when the parsed value exceeds policy budget.
  </behavior>
  <read_first>src/accounts.rs, src/retry.rs, tests/retry.rs, 03-RESEARCH.md</read_first>
  <action>Replace the current numeric/date helper with one centralized parser that trims non-negative decimal delta-seconds, rounds safely, accepts the standards-supported HTTP-date forms through the existing `httpdate` dependency, rejects overlong or malformed inputs, and clamps honored waits to a finite internal maximum. Normalize zero and past deadlines to the smallest positive internal cooldown boundary where account bookkeeping requires a future instant, while retaining zero semantics for retry policy decisions where appropriate. Keep `retry::next_backoff` as the sole policy consumer so values above `max_backoff` return `ExceedsBudget` and no caller sleeps indefinitely. Do not add adapter-local parsing or public configuration keys (D-06 through D-09).</action>
  <verify><automated>cargo test accounts::retry_after --lib && cargo test retry:: --lib && cargo test --test retry</automated><fails_when>any table case is absent/fails, date arithmetic underflows, an excessive value sleeps unboundedly, or any command exits non-zero</fails_when></verify>
  <acceptance_criteria>Every accepted Retry-After form is bounded and deterministic; invalid/excessive input is safely ignored or surfaced within policy; existing retry-budget behavior remains explicit.</acceptance_criteria>
  <done>The one shared Retry-After parser and its retry-policy contract are covered by focused unit and integration tests.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Close adversarial body and unrelated failover gaps</name>
  <files>src/adapters/responses/pool.rs, src/accounts.rs, tests/codex_multi_account.rs, tests/failover.rs</files>
  <behavior>
    - Test exact hard-quota codes/types, status-only 429, 402 billing, mismatched discriminators, and provider transient-rate wording precedence.
    - Test duplicate nested keys, conflicting discriminators, malformed/empty/invalid UTF-8, truncation, slow/aborted, and oversized bodies as transient/unverified.
    - Test 401 refresh, 5xx rotation, ordinary 400 relay, and route 401/403/404/5xx remembered-failure precedence.
    - Test no second account attempt after the first streamed event or WebSocket terminal boundary.
    - Test an all-eligible-exhausted hard-quota/transient fixture preserves the final upstream status/body and validated safe Retry-After header verbatim.
  </behavior>
  <read_first>src/adapters/responses/pool.rs, src/accounts.rs, src/proxy/failover.rs, tests/codex_multi_account.rs, tests/failover.rs, 03-RESEARCH.md</read_first>
  <action>Complete the body-aware classifier's adversarial table and integration coverage. Use an explicit bounded read outcome that distinguishes complete valid bytes from overflow, timeout, cancellation, invalid UTF-8, parse failure, duplicate keys, or conflicting root/nested evidence; every unsafe outcome remains transient/unverified. Keep provider-specific text handling narrow and let stable transient request-rate evidence win over broad quota wording. Retain an explicit all-eligible-exhausted integration fixture: account one returns exact hard-quota evidence, account two returns a transient 429 with a safe Retry-After, and the client receives account two's final upstream status/body and validated safe Retry-After header verbatim. Add route-level regression assertions without changing `is_advance_status` or remembered failure priority, and retain the streaming/WebSocket no-replay boundary. Verify that the account cooldown is provider/account scoped, memory-only, and never performs credential-file writeback or introduces generalized health scoring (D-01 through D-05, D-10 through D-12).</action>
  <verify><automated>cargo test adapters::responses::pool:: --lib && cargo test --test codex_multi_account -- exhausted_pool_preserves_final_body && cargo test --test failover</automated><fails_when>any adversarial input is treated as hard exhaustion, the all-eligible fixture is absent or changes final status/body/header fidelity, route failover changes for unrelated statuses, a streamed turn replays, or any command exits non-zero</fails_when></verify>
  <acceptance_criteria>Unsafe or ambiguous bodies cannot trigger hard-quota cooldown; after every eligible account fails, the hard/transient fixture preserves the final upstream status, body, and safe Retry-After header verbatim; all required account and route status regressions pass with bounded pre-output behavior.</acceptance_criteria>
  <done>RES-01 and RES-02 adversarial coverage is complete, all-eligible exhaustion response fidelity is proven, and unrelated failover semantics are proven unchanged.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| HTTP headers/date -> timer | Untrusted Retry-After input influences sleep and cooldown deadlines |
| Provider body -> account availability | Untrusted bytes influence whether an account is suppressed |
| Route status -> provider failover | Upstream status influences cross-route dispatch |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-02-01 | Denial of service | Retry-After parser | high | mitigate | Length limits, finite clamp, safe rounding, and explicit policy-budget tests prevent indefinite waits. |
| T-03-02-02 | Tampering | JSON classifier | high | mitigate | Duplicate/conflicting evidence, malformed bytes, and bounded-read failures fail closed with adversarial fixtures. |
| T-03-02-03 | Availability | route failover | medium | mitigate | Existing status matrix and remembered-failure assertions guard unrelated route behavior. |
</threat_model>

<verification><automated>cargo test accounts::retry_after --lib && cargo test retry:: --lib && cargo test --test retry && cargo test adapters::responses::pool:: --lib && cargo test --test codex_multi_account && cargo test --test failover</automated><fails_when>any timing, classifier, pool, or failover regression command fails</fails_when></verification>

<nyquist_validation>
| Task | Automated evidence | Sampling |
|---|---|---|
| 03-02-01 | `cargo test accounts::retry_after --lib && cargo test retry:: --lib && cargo test --test retry` | All parser/date/budget boundary tables |
| 03-02-02 | `cargo test adapters::responses::pool:: --lib && cargo test --test codex_multi_account && cargo test --test failover` | Adversarial body, account, stream, and route matrix |
</nyquist_validation>

## Source Audit

| Source | ID | Coverage | Plan |
|---|---|---|---|
| GOAL | — | Bounded transient retry/cooldown versus hard exhaustion | 03-01, 03-02 |
| REQ | RES-01 | Exact bounded evidence and fail-closed adversarial behavior | 03-01, 03-02 |
| REQ | RES-02 | Delta/date parsing, underflow handling, and finite clamp | 03-02 |
| RESEARCH | — | OpenCodex fixtures, bounded body mechanics, route separation, quality gates | 03-01, 03-02, 03-03 |
| CONTEXT | D-01..D-12 | Classification, timing, pool, no replay, scope, and verification constraints | 03-01, 03-02, 03-03 |
</source_audit>
