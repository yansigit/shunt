---
phase: 08-bounded-shutdown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/config.rs, src/shutdown.rs, src/main.rs, src/reload.rs]
autonomous: true
requirements: [OPS-01]
estimate: {tasks: 3, confidence: high}
must_haves:
  truths:
    - "The drain deadline begins on the first shutdown signal, never at process boot."
    - "A clean Axum drain propagates its result before the configured deadline."
  artifacts:
    - {path: src/shutdown.rs, provides: "bounded drain coordinator"}
    - {path: src/config.rs, provides: "validated shutdown_timeout_seconds contract"}
---

<objective>Add a validated process-level shutdown deadline and coordinator.</objective>

<context>@AGENTS.md @.planning/phases/08-bounded-shutdown/08-CONTEXT.md @.planning/phases/08-bounded-shutdown/08-RESEARCH.md @src/shutdown.rs @src/main.rs @src/config.rs @src/reload.rs</context>

<tasks>
<task type="auto">
  <name>Task 1: Add the bounded server configuration</name>
  <files>src/config.rs, src/reload.rs</files>
  <action>Add server.shutdown_timeout_seconds with a 30-second default and validate the inclusive 1..=3600 range. Update every explicit ServerConfig initializer, warn when a reload changes the boot-captured value, and add focused default/valid/invalid and reload tests.</action>
  <verify><automated>cargo test shutdown_timeout --lib</automated><fails_when>zero/unbounded or excessive values pass, defaults drift, or existing config breaks</fails_when></verify>
  <done>The deadline is public, finite, validated, and backward compatible.</done>
</task>
<task type="auto">
  <name>Task 2: Notify and bound graceful drain</name>
  <files>src/shutdown.rs</files>
  <action>Notify a one-shot receiver at the first signal, pin the server future, and apply one timeout only after notification. Return clean or timed-out outcome without process::exit; preserve first-signal process-group cleanup and the continuously subscribed second-signal watcher.</action>
  <verify><automated>cargo test --bin shunt shutdown</automated><fails_when>timer starts at boot, clean errors are swallowed, timeout leaves the owned future alive, or second-signal behavior changes</fails_when></verify>
  <done>Graceful drain is finite and cancellation-by-drop is deterministic.</done>
</task>
<task type="auto">
  <name>Task 3: Wire the coordinator into serve</name>
  <files>src/main.rs</files>
  <action>Capture the validated deadline before moving config, connect the first-signal notification to Axum graceful shutdown, await through the bounded coordinator, and log clean versus forced completion.</action>
  <verify><automated>cargo test --bin shunt shutdown && cargo check --all-features</automated><fails_when>serve errors stop propagating or the process can wait indefinitely after the first signal</fails_when></verify>
  <done>Main uses the new lifecycle coordinator for every run.</done>
</task>
</tasks>

<verification><automated>cargo test --lib shutdown_timeout && cargo test --bin shunt shutdown && cargo check --all-features</automated></verification>
