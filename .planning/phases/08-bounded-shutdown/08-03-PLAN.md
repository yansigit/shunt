---
phase: 08-bounded-shutdown
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified: [README.md, README.ko.md, README.ja.md, README.zh-CN.md, docs/running.md, docs/bounded-shutdown.md, site/src/content/docs/reference/configuration.md, site/src/content/docs/ko/reference/configuration.md, site/src/content/docs/ja/reference/configuration.md, site/src/content/docs/zh-cn/reference/configuration.md, site/src/content/docs/getting-started/installation.mdx, site/src/content/docs/ko/getting-started/installation.mdx, site/src/content/docs/ja/getting-started/installation.mdx, site/src/content/docs/zh-cn/getting-started/installation.mdx, .planning/phases/08-bounded-shutdown/08-VALIDATION.md]
autonomous: true
requirements: [OPS-01]
estimate: {tasks: 2, confidence: high}
must_haves:
  truths:
    - "Operators can predict the first-signal drain and deadline-expiry behavior."
    - "Every quality, review, security, and documentation gate passes."
  artifacts:
    - {path: docs/bounded-shutdown.md, provides: "process lifecycle contract"}
    - {path: .planning/phases/08-bounded-shutdown/08-VERIFICATION.md, provides: "OPS-01 evidence"}
---

<objective>Document, review, secure, and verify bounded shutdown.</objective>

<context>@AGENTS.md @.planning/phases/08-bounded-shutdown/08-CONTEXT.md @.planning/phases/08-bounded-shutdown/08-VALIDATION.md</context>

<tasks>
<task type="auto">
  <name>Task 1: Document the lifecycle contract in every maintained locale</name>
  <files>README.md, README.ko.md, README.ja.md, README.zh-CN.md, docs/running.md, docs/bounded-shutdown.md, site/src/content/docs/reference/configuration.md, site/src/content/docs/ko/reference/configuration.md, site/src/content/docs/ja/reference/configuration.md, site/src/content/docs/zh-cn/reference/configuration.md, site/src/content/docs/getting-started/installation.mdx, site/src/content/docs/ko/getting-started/installation.mdx, site/src/content/docs/ja/getting-started/installation.mdx, site/src/content/docs/zh-cn/getting-started/installation.mdx</files>
  <action>Document the 30-second default, valid range, first-signal admission fence, shared HTTP/SSE/WS deadline, runtime cancellation/resource release, normal-return telemetry behavior, and second-signal escape hatch. Never edit wiki.</action>
  <verify><automated>git diff --check && test -z "$(git status --short -- wiki/)"</automated><fails_when>deadline or forced behavior is ambiguous, locales drift, or wiki changes</fails_when></verify>
  <done>All operator surfaces state one consistent shutdown contract.</done>
</task>
<task type="auto">
  <name>Task 2: Review and verify the milestone implementation</name>
  <files>.planning/phases/08-bounded-shutdown/08-VALIDATION.md, .planning/phases/08-bounded-shutdown/08-REVIEW.md, .planning/phases/08-bounded-shutdown/08-SECURITY.md, .planning/phases/08-bounded-shutdown/08-VERIFICATION.md</files>
  <action>Review deadline timing, error propagation, task/lease cancellation, signal races, streaming behavior, and config bounds. Run focused/full gates, fix findings, mark validation complete, and record the covered-file fingerprint.</action>
  <verify><automated>cargo test --all-features --workspace && cargo fmt --all --check && cargo clippy --all-targets --all-features -- -D warnings && git diff --check && test -z "$(git status --short -- wiki/)"</automated><fails_when>any test/lint/review/security issue remains</fails_when></verify>
  <done>OPS-01 is verified with no open finding.</done>
</task>
</tasks>

<verification><automated>cargo test --all-features --workspace && cargo fmt --all --check && cargo clippy --all-targets --all-features -- -D warnings && git diff --check && test -z "$(git status --short -- wiki/)"</automated></verification>
