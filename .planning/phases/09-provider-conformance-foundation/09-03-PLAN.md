---
phase: 09-provider-conformance-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - src/adapters/responses/codex_ws.rs
  - src/adapters/responses/codex_continuation.rs
  - src/adapters/responses/websocket.rs
  - src/model/responses.rs
  - src/model/responses_request.rs
  - tests/responses_translate.rs
  - tests/codex_websocket_fallback.rs
  - tests/inbound_codex_endpoint.rs
  - tests/inbound_anthropic_translation.rs
autonomous: true
requirements:
  - PRES-02
  - SAFE-06
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Safe full-input recovery preserves tool-call/result adjacency, stable identities, session data, and opaque continuation metadata exactly once."
    - "Continuation is stored only after genuine completion and only when item, byte, and metadata limits all pass; overflow discards reuse atomically."
    - "Missing tool identity, missing reasoning identity, or malformed arguments fail closed instead of creating synthetic continuation state."
    - "Existing account selection, quota, compaction, and opt-in collaboration regressions remain green without durable history."
  artifacts:
    - path: "src/adapters/responses/codex_continuation.rs"
      provides: "Fallible bounded append-only continuation construction"
    - path: "src/adapters/responses/codex_ws.rs"
      provides: "Atomic continuation capture/discard on completion or overflow"
    - path: "src/model/responses_request.rs"
      provides: "Strict request-side tool and authentic metadata validation"
  key_links:
    - from: "src/adapters/responses/websocket.rs"
      to: "src/adapters/responses/codex_continuation.rs"
      via: "One-shot previous-response recovery serializes the authentic full input then returns to the ordinary commitment gate"
      pattern: "previous_response_not_found|full_input"
    - from: "src/adapters/responses/websocket.rs"
      to: "src/retry.rs"
      via: "Continuation recovery consumes the same shared commitment predicate as same-provider retry, route failover, and WebSocket fallback"
      pattern: "Commitment|may_redispatch"
    - from: "src/adapters/responses/codex_ws.rs"
      to: "src/adapters/responses/codex_continuation.rs"
      via: "Clean terminal capture builds and stores continuation only after bounded validation"
      pattern: "capture_continuation|build_transcript"
---

<objective>
Harden the existing Codex continuation and recovery path so only bounded, authentic, correctly paired state can be reused.

Purpose: Make D-11 and SAFE-06 observable without adding durable history, generated signatures, or behavior from later provider phases.
Output: Safe-recovery integration evidence, bounded continuation capture, strict identity/argument translation, and preservation sweeps for account, compaction, and collaboration behavior.
</objective>

<execution_context>
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.13.0+gsd.b7406b293fc4.wrapper.2/gsd-core/workflows/execute-plan.md
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.13.0+gsd.b7406b293fc4.wrapper.2/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-provider-conformance-foundation/09-CONTEXT.md
@.planning/phases/09-provider-conformance-foundation/09-RESEARCH.md
@.planning/phases/09-provider-conformance-foundation/09-PATTERNS.md
@.planning/phases/09-provider-conformance-foundation/09-VALIDATION.md
@.planning/phases/09-provider-conformance-foundation/09-01-SUMMARY.md
@.planning/phases/09-provider-conformance-foundation/09-02-SUMMARY.md
@src/adapters/responses/codex_continuation.rs
@src/adapters/responses/codex_ws.rs
@src/model/responses_request.rs

<interfaces>
- `decide_with_signature(stored, current_body, current_signature) -> Option<Decision>` already requires an equal non-input signature and an append-only normalized transcript.
- `build_transcript(request_input, output_items) -> Vec<Value>` is currently infallible and unbounded.
- `capture_continuation(event, response_id, output_items, turn_state)` currently retains response state incrementally before later storage.
- `recover_previous_response_not_found(...)` already permits exactly one full-input recovery and then uses the normal fallback/commitment gate.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove exact, single-shot recovery across a tool turn</name>
  <files>src/adapters/responses/websocket.rs, tests/codex_websocket_fallback.rs</files>
  <behavior>
    - D-11: `previous_response_not_found` causes exactly one full-input request containing the original adjacent tool call and result with unchanged call IDs.
    - D-11: the recovery retains stable session identity and opaque continuation bytes byte-for-byte and exactly once.
    - D-05: any output or replay-unsafe tool activity from the failed recovery closes redispatch and prevents another request.
  </behavior>
  <action>Add a real-gateway `continuation_recovery` body-matcher case before production edits, using structural synthetic data with no account/project/credential values per D-04. Assert exactly two upstream attempts total, the second request contains the complete paired transcript once, and a second recovery is impossible. Make recovery consume the exact shared `Commitment::may_redispatch()` predicate introduced in Plan 09-01 before issuing its one allowed full-input request; client-visible output or replay-unsafe tool activity must block recovery through the same predicate used by same-provider retry, route failover, and WebSocket fallback. Change production only if the failing fixture exposes a concrete mismatch per D-14. Never synthesize a thought signature or duplicate a tool call/result. This implements D-01, D-03, D-05, D-11, D-13, D-14, and SAFE-04.</action>
  <verify>
    <automated>cargo test --all-features continuation_recovery</automated>
    <fails_when>non-zero exit, or the filter reports zero matching tests</fails_when>
  </verify>
  <done>A hermetic gateway test proves safe recovery preserves the authentic paired transcript and opaque continuation/session state exactly once without reopening commitment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound continuation capture and discard overflow atomically</name>
  <files>src/adapters/responses/codex_ws.rs, src/adapters/responses/codex_continuation.rs</files>
  <behavior>
    - D-07: continuation item count, serialized transcript bytes, response ID bytes, and turn-state bytes pass below/exact caps and reject cap-plus-one.
    - D-11: overflow never truncates and reuses opaque metadata; it disables reuse for the next turn.
    - D-06: only a genuinely completed current response may be delivered when reuse is discarded; failure clears every continuation candidate.
  </behavior>
  <action>Write small-limit unit cases first. Make transcript construction/capture fallible and atomically accounted with crate-private defaults: 10,000 retained items, 32 MiB serialized transcript bytes, and 64 KiB each for response ID and opaque turn-state metadata, with injectable smaller limits for tests. Include the crossing item in checked accounting; on overflow discard the complete candidate rather than truncating it. Store continuation only after the strict terminal machinery from Plan 09-02 confirms genuine completion, and clear it on every parser/provider/cancellation failure. Do not add persistence or public configuration. This is the bounded repair required by D-06, D-07, D-08, D-11, D-13, and D-14.</action>
  <verify>
    <automated>cargo test --all-features continuation_bounds</automated>
    <fails_when>non-zero exit, or the filter reports zero matching tests</fails_when>
  </verify>
  <done>Continuation retention has explicit item/byte/metadata caps with exact boundary tests, and no overflowed, truncated, failed, or cancelled state can be reused.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reject invented tool and reasoning identities while preserving existing feature suites</name>
  <files>src/model/responses.rs, src/model/responses_request.rs, tests/responses_translate.rs, tests/inbound_codex_endpoint.rs, tests/inbound_anthropic_translation.rs</files>
  <behavior>
    - D-11: missing/empty tool call ID, missing/empty function name, malformed completed arguments, or continuation metadata without a non-empty reasoning ID is a typed error.
    - D-11: valid opaque metadata and paired tool IDs retain byte-for-byte values and order.
    - PRES-02: account selection, quota, compaction, and opt-in collaboration suites retain current behavior.
  </behavior>
  <action>Add `authentic_tool_identity` failing translation cases before removing synthetic IDs, empty identifiers, empty-object argument fallbacks, and unkeyed reasoning metadata. Preserve valid opaque values without decoding/re-encoding changes. Extend only the nearest existing compaction/collaboration fixture when a required adjacency assertion is absent; otherwise treat the existing suites as characterization and make no production change. Run the multi-account, compaction, and collaboration suites as the PRES-02 gate. This follows D-01, D-03, D-04, D-11, D-13, D-14, D-15, and D-16.</action>
  <verify>
    <automated>cargo test --all-features authentic_tool_identity</automated>
    <fails_when>non-zero exit, or the filter reports zero matching tests</fails_when>
    <automated>cargo fmt --all --check</automated>
    <fails_when>non-zero exit reports formatting drift</fails_when>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <fails_when>non-zero exit reports a compiler or Clippy warning/error</fails_when>
    <automated>cargo test --all-features --workspace</automated>
    <fails_when>non-zero exit reports any unit, integration, or documentation test failure</fails_when>
  </verify>
  <done>Invalid or invented tool/reasoning identity cannot enter continuation or client output, valid pairs remain exact, and all existing account/quota/compaction/collaboration behavior stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider transcript → continuation store | Untrusted identities, arguments, and opaque metadata may be retained for a later request. |
| Stored continuation → recovery request | Replayed state can duplicate tool effects or cross account/session boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-08 | Tampering | tool/reasoning identity translation | high | mitigate | Reject missing, malformed, or invented identities and preserve valid opaque bytes exactly. |
| T-09-09 | Elevation of Privilege | full-input recovery | high | mitigate | One-shot recovery, stable session/credential ownership, and monotonic commitment prevent duplicate tool execution. |
| T-09-10 | Denial of Service | continuation capture | high | mitigate | Checked item/byte/metadata caps and atomic discard bound retained state. |
| T-09-11 | Information Disclosure | continuation fixtures | low | accept | Fixtures use structural synthetic content and never include credentials, account/project IDs, or private prompts per D-04. |
</threat_model>

<verification>
- `cargo test --all-features continuation_recovery`
- `cargo test --all-features continuation_bounds`
- `cargo test --all-features authentic_tool_identity`
- `cargo test --all-features --test codex_multi_account --test inbound_codex_endpoint --test inbound_anthropic_translation`
- `cargo test --all-features --test inbound_codex_endpoint --test inbound_codex_websocket --test codex_websocket_fallback --test codex_multi_account`
- `cargo fmt --all --check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo test --all-features --workspace`
- Confirm no durable state, signing subsystem, credential writeback, later-provider adapter, or Google AI Studio Web artifact was added.
</verification>

<success_criteria>
- PRES-02 and SAFE-06 are proven at pure translation and real-gateway boundaries.
- Safe recovery sends one exact paired transcript; unsafe or repeated recovery sends nothing further.
- Continuation state is bounded and retained only after clean completion.
- Current account, quota, compaction, and opt-in collaboration suites pass without semantic change.
- D-12, D-15, and D-16 exclusions remain intact.
- The final-wave format, warnings-denied Clippy, and full all-features workspace gates pass.
</success_criteria>

<output>
Create `.planning/phases/09-provider-conformance-foundation/09-03-SUMMARY.md` when done.
</output>
