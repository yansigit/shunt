---
phase: 09-provider-conformance-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/adapters/responses/http.rs
  - src/adapters/responses/ws_stream.rs
  - src/model/responses.rs
  - tests/responses_translate.rs
  - tests/inbound_codex_endpoint.rs
  - tests/inbound_codex_websocket.rs
  - docs/m1-responses-translation.md
autonomous: true
requirements:
  - PRES-01
  - PRES-04
  - SAFE-01
  - SAFE-02
  - SAFE-03
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Translated Responses HTTP and defensive WebSocket relays succeed only after one genuine provider success terminal."
    - "Malformed input, explicit provider failure, duplicate terminal, post-terminal semantic input, and premature EOF produce one error outcome and never synthesized success."
    - "Streaming remains incremental, while explicitly non-streaming collection, SSE residual/event batches, and translated response state are bounded and boundary-tested."
    - "Gateway-owned Codex ingress errors remain OpenAI Responses-shaped and other ingress errors remain Anthropic-shaped."
    - "The native Codex regression sweep executes HTTP/SSE, inbound and outbound WebSocket v2, compression, continuation, cancellation, and terminal suites."
  artifacts:
    - path: "src/model/responses.rs"
      provides: "Strict Responses event and terminal state machine with bounded aggregate translation state"
      contains: "AnthropicSseMachine"
    - path: "src/adapters/responses/http.rs"
      provides: "Byte-strict bounded SSE parser and bounded non-streaming collector"
      contains: "SseParser"
    - path: "src/adapters/responses/ws_stream.rs"
      provides: "Bare-close and terminal parity for defensive WebSocket translation"
    - path: "docs/m1-responses-translation.md"
      provides: "Accurate engineering contract for incomplete and malformed upstream responses"
  key_links:
    - from: "src/adapters/responses/http.rs"
      to: "src/model/responses.rs"
      via: "Every complete bounded event is strictly decoded and applied before any client terminal is emitted"
      pattern: "SseParser|AnthropicSseMachine"
    - from: "src/adapters/responses/ws_stream.rs"
      to: "src/model/responses.rs"
      via: "Channel EOF asks the same machine whether a trustworthy terminal was observed"
      pattern: "final_json|finish"
    - from: "tests/inbound_codex_endpoint.rs"
      to: "src/error.rs"
      via: "Real Codex ingress asserts the Responses error envelope while the Anthropic surface retains its own shape"
      pattern: "assert_openai_error_shape"
---

<objective>
Make the existing Responses translation path byte-bounded and terminal-strict in streaming and non-streaming client modes.

Purpose: Repair the concrete D-06/D-07 violations exposed by research while preserving ordinary ChatGPT/Codex HTTP, SSE, compression, cancellation, and ingress error behavior required by D-01.
Output: Failing-first parser, terminal, bounds, and cross-ingress error regressions; narrow adapter/state-machine repairs; and the matching engineering-doc correction.
</objective>

<execution_context>
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.13.0+gsd.b7406b293fc4.wrapper.2/gsd-core/workflows/execute-plan.md
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.13.0+gsd.b7406b293fc4.wrapper.2/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-provider-conformance-foundation/09-CONTEXT.md
@.planning/phases/09-provider-conformance-foundation/09-RESEARCH.md
@.planning/phases/09-provider-conformance-foundation/09-PATTERNS.md
@.planning/phases/09-provider-conformance-foundation/09-VALIDATION.md
@.planning/phases/09-provider-conformance-foundation/09-01-SUMMARY.md
@src/codex_endpoint/frame.rs
@src/adapters/responses/http.rs
@src/model/responses.rs
@tests/responses_translate.rs

<interfaces>
From src/codex_endpoint/frame.rs:
- `BoundedSseFrameBuffer::feed(&[u8]) -> Result<Vec<Vec<u8>>, SseFrameError>` is the local byte-first pattern: split delimiters, cap candidate bytes, cap frames per feed, dispose on overflow.

From src/model/responses.rs:
- `AnthropicSseMachine::apply(ResponseEvent)` maps genuine provider events.
- Existing `finish()` and `final_json()` synthesize completion on EOF; Phase 9 must split terminal validation from successful finalization.
- `parse_sse_events(&str) -> Vec<ResponseEvent>` currently drops malformed JSON and must become a fallible strict boundary or be replaced at adapter call sites.

From src/adapters/responses/http.rs:
- `stream_response(...)` consumes `bytes_stream()` incrementally.
- `SseParser` currently retains an unbounded `Vec<u8>` and uses lossy UTF-8.
- The non-streaming path currently collects through `Response::text()`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject incomplete and conflicting terminal transcripts before success commits</name>
  <files>src/model/responses.rs, src/adapters/responses/http.rs, tests/responses_translate.rs, tests/inbound_codex_endpoint.rs, tests/inbound_codex_websocket.rs</files>
  <behavior>
    - D-06: `response.completed`, `response.done`, and `response.incomplete` are the only accepted success-terminal families already recognized by the current translator; a bare EOF is a protocol failure.
    - D-06: `error` and `response.failed` remain provider failures; a second terminal or semantic event after a terminal fails exactly once and emits no clean terminal.
    - PRES-04: a locally generated failure on Codex ingress uses the OpenAI Responses envelope, while the same gateway-owned class on `/v1/messages` uses the Anthropic envelope.
    - D-01: a normal complete transcript produces the same ordered text, usage, finish reason, and single terminal as before.
  </behavior>
  <action>Write failing regressions first, including inversion of the test that currently accepts a synthesized completion. Give `AnthropicSseMachine` explicit terminal state and fallible EOF/finalization APIs so success cannot be manufactured after an incomplete transcript and terminal validation happens before `message_stop` or a successful JSON response is emitted. Propagate one typed protocol failure through streaming and non-streaming HTTP translation. Add real Codex HTTP/SSE and inbound WebSocket v2 terminal/error-shape assertions, including `tests/inbound_codex_websocket.rs`, using existing helpers; add the paired Anthropic-surface assertion and do not alter `src/error.rs` unless the new regression demonstrates drift. Preserve unknown well-formed event compatibility separately from malformed events. This is the production repair authorized by D-14 for D-01, D-03, D-06, D-13, PRES-01, and PRES-04.</action>
  <verify>
    <automated>cargo test --all-features responses_terminal</automated>
    <fails_when>non-zero exit, or the filter reports zero matching tests</fails_when>
  </verify>
  <done>Complete transcripts remain compatible, and every incomplete, provider-failed, duplicate-terminal, or post-terminal transcript yields exactly one correctly shaped error with no successful terminal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound and strictly decode Responses SSE and accumulated output</name>
  <files>src/adapters/responses/http.rs, src/model/responses.rs, tests/responses_translate.rs</files>
  <behavior>
    - D-07: split chunks and CRLF delimiters parse incrementally without waiting for upstream completion.
    - SAFE-01: residual/event bytes, events processed per feed, client-requested JSON accumulation, and aggregate translated content/tool arguments reject exact-cap-plus-one while below-cap and exact-cap pass.
    - D-06: invalid UTF-8, malformed JSON, malformed tool arguments, or oversized state produces one typed error and clears parser state.
    - SAFE-02: a streaming success body is never collected in full; only `stream: false` uses the bounded collector.
  </behavior>
  <action>Start with small-limit constructors in tests so below/exact/plus-one cases are cheap. Replace the lossy string parser with a byte-oriented, fallible parser following `BoundedSseFrameBuffer`: checked aggregate accounting, a bounded delimiter candidate, a per-feed event-count cap, strict UTF-8/JSON, and disposed state after failure. Define crate-private defaults with documented rationale: 8 MiB per SSE event/residual, 256 events per feed, and 32 MiB for total client-requested non-streaming response/translated aggregate state; permit tests to inject smaller caps. Replace `Response::text()` with incremental bounded collection only on the non-streaming path. Count tool-argument bytes in aggregate and reject malformed completed arguments instead of substituting an empty object. Do not add a config key or parser dependency. This implements D-03, D-06, D-07, D-13, D-14, and keeps D-15/D-16 excluded.</action>
  <verify>
    <automated>cargo test --all-features responses_bounds</automated>
    <fails_when>non-zero exit, or the filter reports zero matching tests</fails_when>
  </verify>
  <done>Every retained Responses parser/translation byte and per-feed event count introduced or exercised here has a named internal limit with below/exact/plus-one tests, and streaming first-byte delivery remains incremental.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce HTTP and defensive WebSocket terminal parity and document it</name>
  <files>src/adapters/responses/ws_stream.rs, src/adapters/responses/http.rs, docs/m1-responses-translation.md</files>
  <behavior>
    - D-06: defensive WebSocket channel close succeeds only after the same authoritative terminal required by HTTP.
    - D-06: a bare close or parser failure returns one error in streaming mode and a gateway error in non-streaming mode, with no synthetic `message_stop`.
    - D-01: genuine complete HTTP and WebSocket transcripts remain equivalent in content and finish meaning.
  </behavior>
  <action>Add focused `responses_transport_terminal` tests beside each adapter before changing production code. Make `ws_stream.rs` use the strict machine finalization from Task 1 instead of treating a bare channel close as success; ensure streaming and non-streaming paths each surface only one terminal outcome. Update only the inaccurate robustness/terminal section in `docs/m1-responses-translation.md` to state that EOF without a genuine provider terminal is an upstream protocol error. Review README.md, README.ko.md, README.ja.md, README.zh-CN.md and all English/ko/ja/zh-cn site surfaces for impact and record the result in the execution summary; because this plan adds no public provider/configuration semantic, edit them only if an existing claim is factually contradicted. Never edit `wiki/`. This follows D-01, D-04, D-06, D-14, and D-15.</action>
  <verify>
    <automated>cargo test --all-features responses_transport_terminal</automated>
    <fails_when>non-zero exit, or the filter reports zero matching tests</fails_when>
  </verify>
  <done>HTTP and defensive WebSocket translation share the trustworthy-terminal rule, the engineering note matches observable behavior, and the required documentation surfaces have been explicitly considered without adding support claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider bytes → SSE parser | Arbitrary bytes, framing, JSON, and event amplification enter retained parser state. |
| Parser events → client terminal | Malformed or conflicting provider state can be misrepresented as successful completion. |
| Gateway error → ingress client | The same internal failure must use the protocol envelope expected by each ingress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Denial of Service | SSE residual/event/aggregate buffers | high | mitigate | Checked named caps, disposed-on-overflow state, and exact boundary tests. |
| T-09-05 | Tampering | terminal state machine | high | mitigate | Explicit monotonic terminal state rejects malformed, duplicate, and post-terminal input before success emission. |
| T-09-06 | Spoofing | ingress error envelope | medium | mitigate | Real-router paired assertions pin OpenAI versus Anthropic gateway-owned error shapes. |
| T-09-07 | Information Disclosure | malformed-event diagnostics | low | accept | Errors expose structural protocol context only; fixtures contain no credentials or private provider data per D-04. |
</threat_model>

<verification>
- `cargo test --all-features responses_terminal`
- `cargo test --all-features responses_bounds`
- `cargo test --all-features responses_transport_terminal`
- `cargo test --all-features --test responses_translate --test inbound_codex_endpoint`
- `cargo test --all-features --test inbound_codex_endpoint --test inbound_codex_websocket --test codex_websocket_fallback`
- Confirm these suites exercise native HTTP/SSE, inbound and outbound WebSocket v2, compression, continuation, cancellation, and trustworthy terminal behavior for PRES-01.
- Inspect the diff to confirm `stream_response` still emits translated chunks incrementally and only explicit non-streaming mode collects bounded bytes.
</verification>

<success_criteria>
- PRES-01, PRES-04, SAFE-01, SAFE-02, and SAFE-03 have focused behavioral evidence.
- Malformed/oversized/duplicate/cut Responses streams never become clean completion.
- All new accumulation is bounded without a public config or dependency.
- Ordinary complete ChatGPT/Codex HTTP, SSE, compression, and error semantics remain unchanged except for the concrete fail-closed repairs authorized by D-14.
- No credential persistence, later-provider behavior, Google AI Studio Web surface, or generated wiki content changes (D-12, D-15, D-16).
</success_criteria>

<output>
Create `.planning/phases/09-provider-conformance-foundation/09-02-SUMMARY.md` when done.
</output>
