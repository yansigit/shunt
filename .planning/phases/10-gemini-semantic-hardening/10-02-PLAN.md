---
phase: 10-gemini-semantic-hardening
plan: "02"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/adapters/gemini/mod.rs
  - src/adapters/gemini/sse.rs
  - tests/gemini_conformance.rs
autonomous: true
requirements: [GEM-02, GEM-03, GEM-04]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Gemini SSE survives arbitrary byte and UTF-8 splits, CRLF, comments, and supported multiline data without reordering events."
    - "Malformed, invalid-UTF-8, oversized, over-amplified, or unterminated frames produce one protocol error and dispose parser state."
    - "Streaming stays lazy while explicitly non-streaming responses are the only bounded accumulation path."
    - "Transport EOF and `[DONE]` cannot substitute for the provider finish required by the semantic machine."
  artifacts:
    - path: "src/adapters/gemini/sse.rs"
      provides: "Private bounded incremental byte SSE decoder"
    - path: "src/adapters/gemini/mod.rs"
      provides: "Lazy checked streaming relay and bounded unary collector"
    - path: "tests/gemini_conformance.rs"
      provides: "Real-gateway framing, terminal, unary, and provider-error evidence"
  key_links:
    - from: "src/adapters/gemini/sse.rs"
      to: "src/model/gemini.rs"
      via: "each complete decoded JSON data event enters the checked semantic machine in wire order"
      pattern: "feed|process_chunk"
    - from: "src/adapters/gemini/mod.rs"
      to: "tests/gemini_conformance.rs"
      via: "the real `/v1/messages` body owns decoder/machine state until completion or drop"
      pattern: "Body::from_stream|bytes_stream"
---

<objective>
Replace line-oriented, unbounded Gemini response handling with a strict bounded byte pipeline feeding Plan 10-01's checked state.

Purpose: Deliver framing and body-lifetime portions of GEM-02 through GEM-04 without buffering streaming output or accepting EOF as success.
Output: Gemini-local SSE decoder, bounded unary collector, adapter integration, and real-gateway transcript tests.
</objective>

<execution_context>
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.13.0+gsd.b7406b293fc4.wrapper.2/gsd-core/workflows/execute-plan.md
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.13.0+gsd.b7406b293fc4.wrapper.2/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-gemini-semantic-hardening/10-CONTEXT.md
@.planning/phases/10-gemini-semantic-hardening/10-RESEARCH.md
@.planning/phases/10-gemini-semantic-hardening/10-VALIDATION.md
@.planning/phases/10-gemini-semantic-hardening/10-01-SUMMARY.md
@.planning/phases/09-provider-conformance-foundation/09-02-SUMMARY.md
@src/adapters/gemini/mod.rs
@src/adapters/responses/http.rs
<interfaces>
From Plan 10-01:
- Checked Gemini semantic apply/close/final-render APIs accept direct or one-level Code Assist response objects and withhold success until transport closure.

From `src/adapters/responses/http.rs`:
- Phase 9's byte-oriented bounded SSE parser is an analog for checked event/residual accounting and disposed-parser behavior; keep Gemini framing local because its event payload contract differs.

From `src/adapters/gemini/mod.rs`:
- `forward` currently chooses `streamGenerateContent?alt=sse` only for streaming clients and `generateContent` for unary clients; keep those destinations and the Code Assist envelope unchanged per D-02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decode Gemini SSE incrementally with hard byte and event bounds</name>
  <files>src/adapters/gemini/sse.rs, src/adapters/gemini/mod.rs</files>
  <behavior>
    - D-05: arbitrary chunk/code-point splits, LF/CRLF/mixed blank delimiters, comments, unknown fields, optional field space, and multiline `data:` preserve event order.
    - D-06 and D-08: invalid UTF-8/JSON, oversize event/residual, too many events per feed, and non-whitespace residual at EOF fail once and dispose the parser.
    - D-07: empty data is a no-op; `[DONE]` is only a boundary after accepted provider finish and later data is rejected.
  </behavior>
  <action>Write focused decoder RED tests using injected small limits, then add a private `sse` module modeled on Phase 9's checked byte parser. Frame complete SSE events across all specified blank-line encodings, join supported `data:` lines with newline, ignore comments/unknown SSE fields, decode UTF-8 only after a complete event, parse one JSON value, and enforce checked event/residual/events-per-feed caps per D-05, D-06, and D-08. A bound or decoding failure clears retained bytes and permanently rejects later feed. Keep default limits crate-private and derived from the proven Phase 9 resource ceiling rather than adding config.</action>
  <verify>
    <automated>cargo test --all-features gemini_sse_bounds</automated>
    <fails_when>the command exits non-zero or reports zero matching tests</fails_when>
  </verify>
  <done>Decoder tests accept all supported framing/split cases at exact bounds and reject every cap-plus-one, malformed, disposed, and incomplete case deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stream checked semantic events lazily through the real gateway</name>
  <files>src/adapters/gemini/mod.rs, src/adapters/gemini/sse.rs, tests/gemini_conformance.rs</files>
  <behavior>
    - D-04 and D-08: streaming consumes chunks lazily with backpressure and retains only decoder plus bounded semantic state.
    - D-07: clean EOF finalizes only after one supported provider finish; cut, malformed, duplicate, or post-terminal bytes return one stream error.
    - D-09 and D-12: direct/wrapped content and embedded provider errors flow through the same checked machine and never append synthetic success.
  </behavior>
  <action>Add real-Axum/mock-upstream failing tests for early incremental delivery, arbitrary framing splits, embedded provider errors, malformed frames, and cut streams. Replace `append_gemini_events` and the line buffer with the bounded decoder feeding the Plan 10-01 checked semantic machine. Keep `reqwest::bytes_stream` inside the downstream body unfold so backpressure and D-04 drop ownership remain intact; emit exactly one Anthropic error outcome where the transport permits it and never continue after disposal. Preserve Code Assist request endpoint/envelope selection per D-02 and D-16.</action>
  <verify>
    <automated>cargo test --all-features --test gemini_conformance gemini_streaming_framing</automated>
    <fails_when>the command exits non-zero or reports zero matching tests</fails_when>
  </verify>
  <done>The real streaming endpoint delivers ordered deltas before terminal and fails closed for every framing, provider-error, and premature-closure case without detached buffering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bound unary collection and render it from the shared checked state</name>
  <files>src/adapters/gemini/mod.rs, tests/gemini_conformance.rs</files>
  <behavior>
    - D-08: unary response bytes accept the exact internal cap and reject cap plus one, including absent or false `Content-Length`.
    - D-09: unary uses the same checked semantic apply and closure path as streaming and does not discard generated events/errors.
    - D-03 and D-12: invalid JSON, malformed structure, and embedded provider error map to Anthropic errors without credential/project material.
  </behavior>
  <action>Replace unbounded `response.text()` with incremental checked byte accumulation capped before extension, optionally rejecting an oversized truthful `Content-Length` early without trusting it as sufficient. Decode and parse only after bounded collection, then feed the JSON through the same checked semantic state and transport-close method used by SSE before calling checked final rendering. Add real-gateway direct/wrapped parity, exact/plus-one, lying/missing length, malformed JSON, and embedded-error tests per D-03, D-08, D-09, and D-12.</action>
  <verify>
    <automated>cargo test --all-features --test gemini_conformance gemini_unary_bounds</automated>
    <fails_when>the command exits non-zero or reports zero matching tests</fails_when>
  </verify>
  <done>Unary responses are bounded before allocation growth and expose exactly the same validated content, tools, usage, finish, and error meaning as streaming equivalents.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider bytes → SSE frames | Arbitrarily split and attacker-sized bytes enter retained parser memory. |
| Decoded event → downstream body | A malformed or incomplete event could be mistaken for a successful Anthropic transcript. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Denial of Service | SSE residual/event/unary buffers | high | mitigate | Checked pre-extension caps, events-per-feed limit, disposed parser, and exact/cap-plus-one tests. |
| T-10-06 | Tampering | framing/terminal decoder | high | mitigate | Complete-event UTF-8/JSON validation and provider-finish-plus-clean-closure requirement. |
| T-10-07 | Information Disclosure | adapter errors | medium | mitigate | Synthetic credential/project markers are asserted absent from downstream error bodies. |
</threat_model>

<verification>
- `cargo test --all-features gemini_sse_bounds`
- `cargo test --all-features --test gemini_conformance gemini_streaming_framing`
- `cargo test --all-features --test gemini_conformance gemini_unary_bounds`
- `cargo test --all-features --test gemini_translate --test gemini_conformance`
</verification>

<success_criteria>
- GEM-02 through GEM-04 execute at the real `/v1/messages` boundary.
- Streaming is incremental and bounded; unary is explicitly and boundedly accumulated.
- Every malformed, oversized, invalid-UTF-8, duplicate, post-terminal, or premature-EOF transcript fails without synthesized success.
- No public provider/configuration semantics, credential persistence, Antigravity policy, or AI Studio Web artifact changes.
</success_criteria>

<output>
Create `.planning/phases/10-gemini-semantic-hardening/10-02-SUMMARY.md` when done.
</output>
