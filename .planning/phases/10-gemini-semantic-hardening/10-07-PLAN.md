---
phase: 10-gemini-semantic-hardening
plan: "07"
type: execute
wave: 7
depends_on: ["10-06"]
files_modified:
  - src/adapters/gemini/mod.rs
  - src/auth/mod.rs
  - src/server.rs
  - docs/v2-gemini-semantic-hardening.md
autonomous: true
gap_closure: true
requirements: [GEM-01, GEM-02, GEM-03, GEM-04, GEM-05]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A hermetic GoogleOauth Code Assist turn sends one synthetic bearer and project-bearing `/v1internal` envelope through unary, streaming, permitted pre-header retry, and cancellation."
    - "The OAuth fixture neither reads live credentials nor writes or rewrites any credential file."
    - "The engineering record names the exact OAuth, tool-order, Part, and post-DONE evidence now executed."
    - "All focused Gemini, retry/failover, scope, format, lint, and full workspace gates pass after gap closure."
  artifacts:
    - path: "src/adapters/gemini/mod.rs"
      provides: "Narrow private resolved-credential dispatch seam shared by production and hermetic tests"
    - path: "src/server.rs"
      provides: "In-process `/v1/messages` router evidence with a private synthetic credential resolver"
    - path: "docs/v2-gemini-semantic-hardening.md"
      provides: "Corrected implementation and hermetic evidence claims"
  key_links:
    - from: "src/auth/mod.rs"
      to: "src/server.rs"
      via: "`DefaultCredentialResolver` implements the crate-private resolver boundary and every production constructor installs it"
      pattern: "CredentialResolver|DefaultCredentialResolver|resolve_credential"
    - from: "src/server.rs"
      to: "src/adapters/gemini/mod.rs"
      via: "the real `/v1/messages` router carries one request-local resolver in `AppState`; Gemini resolves through it once before endpoint/envelope/retry construction"
      pattern: "build_router_with_dependencies|resolve_route_credential|v1internal|NonIdempotentPost"
    - from: "docs/v2-gemini-semantic-hardening.md"
      to: "src/server.rs"
      via: "named OAuth lifetime and post-DONE behavioral evidence"
      pattern: "v1internal|Google OAuth|DONE|cancellation"
---

<objective>
Close the Google OAuth request-lifetime evidence gap, reconcile documentation, and run the final release gate.

Purpose: Prove GEM-01/GEM-05 through the real Code Assist dispatch path without live credentials or writeback, then establish complete GEM-01 through GEM-05 evidence.
Output: A narrow hermetic credential seam, gateway-level OAuth lifetime tests, accurate engineering claims, and green repository gates.
</objective>

<execution_context>
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.13.0+gsd.b7406b293fc4.wrapper.2/gsd-core/workflows/execute-plan.md
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.13.0+gsd.b7406b293fc4.wrapper.2/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-gemini-semantic-hardening/10-CONTEXT.md
@.planning/phases/10-gemini-semantic-hardening/10-RESEARCH.md
@.planning/phases/10-gemini-semantic-hardening/10-VERIFICATION.md
@.planning/phases/10-gemini-semantic-hardening/10-03-SUMMARY.md
@.planning/phases/10-gemini-semantic-hardening/10-06-SUMMARY.md
@src/adapters/gemini/mod.rs
@src/auth/mod.rs
@src/server.rs
@docs/v2-gemini-semantic-hardening.md
@scripts/check_phase10_scope.sh

<interfaces>
From `src/adapters/gemini/mod.rs` and `src/auth/mod.rs`:
- `forward` resolves `Credential::GoogleOauth { access_token, project_id }` before endpoint/envelope construction and captures token, payload, and endpoint before `send_with_retry_with_safety`.
- Google OAuth selects `/v1internal:{generateContent|streamGenerateContent}` plus `{model, project, request}` and `Authorization: Bearer`; API-key tests currently select `/v1beta` and `x-goog-api-key` instead.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one synthetic Google OAuth Code Assist lifetime end to end</name>
  <reversibility rating="costly">The test seam touches credential-to-adapter wiring, so it must remain private and preserve the production resolver as the sole normal entry.</reversibility>
  <files>src/auth/mod.rs, src/server.rs, src/adapters/gemini/mod.rs</files>
  <behavior>
    - D-01, D-02, and D-15: unary and streaming requests use `/v1internal`, one selected synthetic bearer, and one identical project-bearing envelope for the full response lifetime.
    - D-13 and D-15: a permitted pre-header transport retry reuses the exact bearer, endpoint, model, project, and serialized request meaning on every attempt.
    - D-04, D-13, and D-14: downstream cancellation drops upstream work and releases gateway capacity without credential re-resolution or redispatch.
    - D-03: the fixture uses no live credential source and creates, migrates, refreshes, or writes no credential file.
  </behavior>
  <action>Implement one exact private dependency boundary, with no test-only public escape hatch. In `src/auth/mod.rs`, define crate-private `CredentialResolver` with an object-safe boxed-future `resolve(config, route, client)` method and `DefaultCredentialResolver`, whose implementation delegates directly to the existing `resolve_credential`. In `src/server.rs`, make `AppState` privately own `Arc&lt;dyn CredentialResolver&gt;`; keep `AppState::new`, `AppState::from_shared`, and public `build_router` signatures unchanged and make each install `DefaultCredentialResolver`. Add private `AppState::from_shared_with_credential_resolver`, private `build_router_with_dependencies(config, http_client, resolver)`, and crate-private `AppState::resolve_route_credential(&amp;Route)`; ensure `refreshed()` clones the same resolver Arc so a request snapshot cannot swap identity. In `src/adapters/gemini/mod.rs`, replace the direct free-function call with exactly one `state.resolve_route_credential(&amp;route).await` before endpoint, envelope, and retry-closure construction; downstream code remains the production path for both default and injected resolvers.

Inside `src/server.rs`'s existing `#[cfg(test)]` module, define `SyntheticGoogleOauthResolver` implementing that same private trait. It returns `Credential::GoogleOauth` with synthetic split bearer/project markers and increments an `AtomicUsize`; construct the actual application with `build_router_with_dependencies`, configure the Gemini provider's ordinary `AuthMode::GoogleOauth` and loopback base URL, and submit Anthropic requests to the returned Router at `/v1/messages` via `tower::ServiceExt::oneshot`. Drive unary and streaming success, a deterministic raw-loopback first connection that closes before response headers followed by a successful retry, and a held streaming body whose downstream Body is dropped while the one-slot concurrency limit is occupied. Capture each upstream request and assert `/v1internal:{method}`, Bearer-only authentication, exact model/project envelope, byte-equivalent payload meaning across retry, resolver count exactly one per client request, no retry after headers/body activity, observed upstream drop, and capacity reacquisition per D-01 through D-04 and D-13 through D-15. Name the cases with the shared `gemini_google_oauth_code_assist_lifetime` filter. The synthetic resolver, dependency builder, and tests stay inside crate privacy; introduce no environment variable, public config/API, alternate auth mode, live credential read, refresh, migration, or credential-file write.</action>
  <verify>
    <automated>cargo test --all-features gemini_google_oauth_code_assist_lifetime</automated>
    <fails_when>the command exits non-zero or reports zero matching tests</fails_when>
  </verify>
  <done>Hermetic unary, streaming, safe-retry, and cancellation cases prove one synthetic bearer/project and byte-equivalent `/v1internal` request lifetime, with no live credential read, file write, or public seam.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile evidence claims and replay the Phase 10 release gate</name>
  <files>docs/v2-gemini-semantic-hardening.md</files>
  <action>Audit the engineering record against Plans 10-06 and 10-07. Replace the previously overstated loopback claim with the exact hermetic GoogleOauth `/v1internal` unary, streaming, pre-header retry, and cancellation evidence. State that parallel results are selected by unique ID but emitted in original call order, known unsupported Part variants/incompatible combinations fail in both modes, and every completed SSE frame after `[DONE]` fails while only the tested raw-whitespace residual is accepted. Keep D-02, D-03, and D-16 exclusions explicit. Confirm the existing English and ko/ja/zh-cn operator prose already states the resulting observable fail-closed contract; do not edit those pages unless the implementation diverged from that existing claim, in which case stop and report the unexpected documentation-scope change. Then run every focused gap filter, both full Gemini suites, retry/failover, the scope audit, formatting, warnings-denied Clippy, and the serial all-feature workspace suite. Treat zero selected tests, any ignored new test, scope failure, or secret-bearing fixture/diagnostic as failure; never weaken a test or edit generated `wiki/`.</action>
  <verify>
    <automated>cargo test --all-features gemini_parallel_tool_result_identity &amp;&amp; cargo test --all-features --test gemini_translate gemini_known_part_strictness &amp;&amp; cargo test --all-features gemini_post_done_frames &amp;&amp; cargo test --all-features gemini_google_oauth_code_assist_lifetime &amp;&amp; cargo test --all-features --test gemini_translate --test gemini_conformance &amp;&amp; cargo test --all-features --test retry --test failover &amp;&amp; bash scripts/check_phase10_scope.sh &amp;&amp; cargo fmt --all --check &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo test --all-features --workspace -- --test-threads=1</automated>
    <fails_when>any focused, exclusion, format, lint, or workspace command exits non-zero or a focused filter selects zero tests</fails_when>
  </verify>
  <done>Documentation names only implemented evidence, all gap/baseline suites pass, and every Phase 10 exclusion remains intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google OAuth credential → Code Assist origin | A bearer and project cross into the selected upstream request and must not change or escape. |
| Test evidence → operator documentation | Inaccurate claims can present unverified behavior as guaranteed. |
| Phase diff → release gate | Forbidden credentials, dependencies, provider surfaces, or generated content could enter during closure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-G07-02 | Information Disclosure | OAuth test seam and diagnostics | high | mitigate | Keep resolver injection crate-private, use synthetic split markers, assert absence from diagnostics, and introduce no live-credential or file-write path. |
| T-10-G07-03 | Spoofing | bearer/project affinity | high | mitigate | Capture identity and envelope once before retry and assert equality on every request attempt. |
| T-10-G07-04 | Denial of Service | held streaming response | medium | mitigate | Drop downstream ownership, observe upstream cancellation, and reacquire the bounded gateway slot under timeout. |
| T-10-G07-05 | Repudiation | engineering evidence claims | medium | mitigate | Name exact hermetic cases and their proven boundaries. |
| T-10-G07-06 | Tampering | final phase diff | high | mitigate | Run the pinned phase-range scope audit plus repository gates. |
</threat_model>

<verification>
- `cargo test --all-features gemini_google_oauth_code_assist_lifetime`
- `cargo test --all-features gemini_parallel_tool_result_identity`
- `cargo test --all-features --test gemini_translate gemini_known_part_strictness`
- `cargo test --all-features gemini_post_done_frames`
- `cargo test --all-features --test gemini_conformance`
- `bash scripts/check_phase10_scope.sh`
- `cargo fmt --all --check`, warnings-denied Clippy, and the serial all-feature workspace suite
</verification>

<success_criteria>
- Real Code Assist adapter dispatch proves immutable synthetic OAuth bearer/project affinity for unary, streaming, safe retry, and cancellation.
- GEM-01 through GEM-05 have passing focused and repository-wide evidence, and the engineering record states it accurately.
- No live credentials, credential writeback, public configuration/API, new dependency, Antigravity policy, or Google AI Studio Web surface is introduced.
</success_criteria>

<output>
Create `.planning/phases/10-gemini-semantic-hardening/10-07-SUMMARY.md` when done.
</output>
