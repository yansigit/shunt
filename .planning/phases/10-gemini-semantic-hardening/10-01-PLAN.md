---
phase: 10-gemini-semantic-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/model/gemini.rs
  - src/model/gemini_request.rs
  - src/model/gemini_request/tests.rs
  - tests/gemini_translate.rs
autonomous: true
requirements: [GEM-02, GEM-03, GEM-04]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A complete direct or Code Assist-wrapped Gemini transcript has one ordered semantic meaning in streaming and non-streaming modes."
    - "Provider success requires one supported finish reason and clean transport closure; EOF, duplicate terminals, or semantic data after finish cannot become success."
    - "Text, reasoning, tools, usage, and embedded provider errors retain their order and authoritative meaning."
    - "Only authentic, exactly paired tool identities and thought signatures survive a call/result round trip."
  artifacts:
    - path: "src/model/gemini.rs"
      provides: "Checked request-local Gemini semantic state machine and renderers"
      contains: "GeminiSseMachine"
    - path: "src/model/gemini_request.rs"
      provides: "Fail-closed authentic Gemini tool-history reconstruction"
    - path: "tests/gemini_translate.rs"
      provides: "Direct/wrapped parity, terminal, ordering, usage, error, and signature fixtures"
  key_links:
    - from: "src/model/gemini.rs"
      to: "tests/gemini_translate.rs"
      via: "the same checked apply/finalize state produces incremental SSE events or bounded final JSON"
      pattern: "process_chunk|finish|final_json"
    - from: "src/model/gemini_request.rs"
      to: "src/model/gemini.rs"
      via: "opaque tool-use IDs round-trip exact call names, arguments, and authentic signatures"
      pattern: "thoughtSignature|functionCall|functionResponse"
---

<objective>
Create the checked semantic oracle that every Gemini transport path will share.

Purpose: Deliver GEM-02 through GEM-04 at the translation boundary before adapter wiring, eliminating synthetic completion, stream/unary drift, ambiguous candidates, and invented tool metadata.
Output: One fallible request-local state machine, authentic tool-history reconstruction, and exhaustive semantic fixtures.
</objective>

<execution_context>
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.13.0+gsd.b7406b293fc4.wrapper.2/gsd-core/workflows/execute-plan.md
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.13.0+gsd.b7406b293fc4.wrapper.2/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-gemini-semantic-hardening/10-CONTEXT.md
@.planning/phases/10-gemini-semantic-hardening/10-RESEARCH.md
@.planning/phases/10-gemini-semantic-hardening/10-VALIDATION.md
@.planning/phases/09-provider-conformance-foundation/09-02-SUMMARY.md
@.planning/phases/09-provider-conformance-foundation/09-03-SUMMARY.md
@src/model/gemini.rs
@src/model/gemini_request.rs
@tests/gemini_translate.rs

<interfaces>
From `src/model/gemini.rs`:
- `GeminiSseMachine::new`, `process_chunk`, `finish`, and `final_json` are the current translation entry points; preserve fixture-friendly compatibility only where production can use checked counterparts.
- `SseEvent { event, data }` is the Anthropic SSE rendering unit.
- `translate_gemini_error_val` and `map_gemini_error` preserve existing Anthropic error mapping.

From `src/model/gemini_request.rs`:
- `translate_request_for_model` constructs the inner GenerateContent request.
- Existing opaque `call_gemini_v1_...` IDs carry authentic `thoughtSignature` state across the Anthropic tool round trip.

From `src/retry.rs` through Phase 9:
- `Commitment` is crate-private and monotonic; Gemini has no body-time redispatch seam, but successful visible/tool events must have an equivalent semantic commitment classification.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one successful wrapped transcript through both client renderings</name>
  <reversibility rating="costly">The checked semantic entry points become the contract consumed by Gemini and later Antigravity transport work.</reversibility>
  <files>src/model/gemini.rs, tests/gemini_translate.rs</files>
  <behavior>
    - D-09: equivalent direct and `{response: ...}` inputs produce one normalized ordered trace and equivalent streamed/final Anthropic meaning.
    - D-10: reasoning, visible text, function calls, usage, and finish remain in upstream order for the supported single candidate.
    - D-07: one supported provider finish is recorded but `message_stop` is emitted only after checked transport closure.
    - D-12: a top-level or wrapped provider error yields one Anthropic error and no successful message.
  </behavior>
  <action>Write failing semantic-parity fixtures first, using one complete Code Assist-wrapped transcript and its direct Gemini equivalent. Evolve `GeminiSseMachine` into a fallible request-local state machine with explicit open, pending-success, emitted-success, provider-failed, and protocol-failed terminal states. Accept exactly one wrapper level, validate the supported single-candidate structure before mutation, preserve part order, and expose checked apply/transport-close/final-JSON paths so streaming and unary consume the same state per D-07, D-09, D-10, and D-12. Keep existing stop-reason mappings where well-defined; do not synthesize STOP at EOF.</action>
  <verify>
    <automated>cargo test --all-features --test gemini_translate semantic_parity</automated>
    <fails_when>the command exits non-zero or reports zero matching tests</fails_when>
  </verify>
  <done>One complete transcript proves direct/wrapped and stream/unary equivalence, incremental event order, one authoritative finish, and one provider-error outcome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject ambiguous and incomplete Gemini semantics before emission</name>
  <files>src/model/gemini.rs, tests/gemini_translate.rs</files>
  <behavior>
    - D-06 and D-10: malformed candidates/content/parts/supported scalar fields, multiple candidates, and incompatible part kinds fail without partial successful output.
    - D-07: EOF or `[DONE]` without an accepted finish, duplicate/conflicting finish, and supported data after finish fail closed.
    - D-12: provider error before or after content is terminal and never followed by a synthetic success event.
    - D-08: reconstructed unary content, tool state, signature metadata, and usage accounting obey checked aggregate bounds at exact and cap-plus-one cases.
  </behavior>
  <action>Add failing tables for every structural and terminal case from `10-RESEARCH.md`. Prevalidate each candidate and all parts atomically before emitting its events; ignore unknown fields only when they do not ambiguously claim a supported shape. Validate usage counts as non-negative integers, treat usage-only frames as non-terminal, require an explicit supported finish reason, and dispose the machine after any protocol/provider failure. Add injected small test limits and checked arithmetic for retained non-streaming content, tool metadata, usage, and opaque IDs per D-06, D-07, D-08, D-10, and D-12. Streaming mode must not retain a second full content copy.</action>
  <verify>
    <automated>cargo test --all-features --test gemini_translate gemini_semantic_strictness</automated>
    <fails_when>the command exits non-zero or reports zero matching tests</fails_when>
  </verify>
  <done>Malformed, ambiguous, oversized, conflicting, or incomplete semantic transcripts return one typed failure without partial tool batches, silent loss, or synthesized success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve only authentic tool calls, results, and thought signatures</name>
  <files>src/model/gemini.rs, src/model/gemini_request.rs, src/model/gemini_request/tests.rs, tests/gemini_translate.rs</files>
  <behavior>
    - D-11: each valid function call keeps its non-blank name, exact JSON arguments, opaque ID, and authentic non-empty signature from the same part.
    - D-11: the next `tool_result` reconstructs the matching Gemini `functionResponse`; orphan, foreign, malformed, or ambiguous IDs/names fail before dispatch.
    - D-11: required Gemini 3 signatures are never randomized, moved by name/argument matching, or replaced by a placeholder.
    - D-11: response translation wraps only the exact non-empty signature present on that upstream call part; request translation decodes that value byte-exactly without substituting or relocating it.
    - D-10: a malformed later call in one candidate prevents emission of the entire call batch.
    - GEM-02: a client `tool_result` is relayed as the paired `functionResponse` in the next Code Assist request; an upstream assistant-side `functionResponse` is rejected because Anthropic assistant output has no faithful tool-result block.
  </behavior>
  <action>Write round-trip and negative fixtures first. Define D-11 authenticity narrowly and restart-independently: Shunt must emit only an exact non-empty `thoughtSignature` observed on the same upstream function-call part, preserve its bytes through the existing opaque base64 ID across processes, and decode that exact value on the next request without generating, signing, substituting, moving, or claiming provenance beyond exact preservation. The reversible ID is client-carried continuation data; syntactically valid client-authored payloads are not distinguishable without forbidden durable/configured state and remain upstream-validated, while malformed/empty/oversized IDs fail locally. Require atomic `functionCall` objects with non-blank names and object-compatible arguments and enforce private signature/encoded-ID bounds. Remove response-side random fallback and request-side placeholder synthesis for models that require authentic signatures. Preserve evidenced unsigned legacy behavior without treating a generated join ID as signature material. Make malformed/orphan result IDs and fallback `unknown_tool` reconstruction return a typed pre-dispatch error. Define GEM-02 function-result relay as the valid Anthropic direction: a client `tool_result` becomes the exact paired `functionResponse` in the next Code Assist request; reject an upstream assistant-side `functionResponse` because Anthropic assistant content has no faithful `tool_result` representation. This implements D-10 and D-11 without persistence, instance affinity, or credential changes.</action>
  <verify>
    <automated>cargo test --all-features gemini_tool_signature_roundtrip</automated>
    <fails_when>the command exits non-zero or reports zero matching tests</fails_when>
  </verify>
  <done>Valid sequential and parallel tool turns round-trip exact identities/signatures, while incomplete or invented metadata is rejected before any successful tool representation or next dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini JSON → Anthropic semantics | Untrusted provider structures can become client-visible text, billable usage, tools, or success terminals. |
| Anthropic tool history → Gemini request | Client-returned opaque IDs can attempt to forge provider signature metadata or pair a result to the wrong call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | candidate/part validation | high | mitigate | Prevalidate supported shapes and complete call batches before state mutation or event emission. |
| T-10-02 | Spoofing | client-carried tool-use ID | medium | transfer | Shunt guarantees exact upstream-value preservation and local shape/bound validation; provenance of syntactically valid client-authored continuation data is validated by Google because no durable/configured trust key is authorized. |
| T-10-03 | Repudiation | finish/error state | medium | mitigate | Explicit monotonic terminal states reject duplicate or conflicting outcomes deterministically. |
| T-10-04 | Denial of Service | retained semantic state | high | mitigate | Checked aggregate accounting with exact/cap-plus-one tests and no streaming content reconstruction. |
</threat_model>

<verification>
- `cargo test --all-features --test gemini_translate`
- `cargo test --all-features gemini_tool_signature_roundtrip`
- Confirm production-facing checked APIs reject EOF without a supported provider finish and embedded errors cannot reach final JSON success.
</verification>

<success_criteria>
- GEM-02, GEM-03, and semantic portions of GEM-04 are covered by failing-first fixtures.
- Direct/wrapped and streaming/unary meanings derive from one checked state.
- Tool/signature metadata is authentic, bounded, exact-part paired, and fail-closed.
- No Antigravity policy, Google AI Studio Web surface, credential writeback, public configuration, or new dependency is introduced.
</success_criteria>

<output>
Create `.planning/phases/10-gemini-semantic-hardening/10-01-SUMMARY.md` when done.
</output>
