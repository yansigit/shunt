---
phase: 10-gemini-semantic-hardening
plan: "03"
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/adapters/gemini/mod.rs
  - src/model/gemini_request.rs
  - src/model/gemini_request/tests.rs
  - tests/gemini_conformance.rs
autonomous: true
requirements: [GEM-01, GEM-03, GEM-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One Gemini turn keeps the same OAuth bearer, selected project, endpoint, and byte-equivalent request meaning through completion or cancellation."
    - "A non-idempotent generation retries only a bounded transient transport failure before response headers and never a returned status or body-time failure."
    - "Visible output or tool activity closes redispatch monotonically; no body repair, account switch, provider hop, or partial replay occurs."
    - "Dropping a downstream stream promptly drops upstream/parser state and releases response-owned capacity without leaking identity markers."
  artifacts:
    - path: "src/adapters/gemini/mod.rs"
      provides: "Non-idempotent immutable Code Assist dispatch and response-owned cancellation"
      contains: "RetrySafety::NonIdempotentPost"
    - path: "tests/gemini_conformance.rs"
      provides: "Real-gateway identity, retry hit-count, parity, tool-result, and cancellation evidence"
    - path: "src/model/gemini_request.rs"
      provides: "Fail-closed call/result pairing before request dispatch"
  key_links:
    - from: "src/adapters/gemini/mod.rs"
      to: "src/retry.rs"
      via: "generation dispatch uses explicit non-idempotent acceptance safety"
      pattern: "send_with_retry_with_safety|NonIdempotentPost"
    - from: "src/adapters/gemini/mod.rs"
      to: "tests/gemini_conformance.rs"
      via: "mock hit counts and captured headers/envelopes prove identity affinity and no forbidden redispatch"
      pattern: "gemini_identity_retry|gemini_response_drop"
---

<objective>
Harden Gemini request lifetime, retry safety, tool-result dispatch, and cancellation at the real gateway boundary.

Purpose: Complete GEM-01, GEM-03, and GEM-05 using Phase 9 retry/commitment and body-drop primitives without changing Code Assist identity or persistence semantics.
Output: Explicit non-idempotent dispatch, immutable identity/project evidence, complete gateway parity matrix, and cancellation proof.
</objective>

<execution_context>
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.13.0+gsd.b7406b293fc4.wrapper.2/gsd-core/workflows/execute-plan.md
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.13.0+gsd.b7406b293fc4.wrapper.2/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-gemini-semantic-hardening/10-CONTEXT.md
@.planning/phases/10-gemini-semantic-hardening/10-RESEARCH.md
@.planning/phases/10-gemini-semantic-hardening/10-VALIDATION.md
@.planning/phases/10-gemini-semantic-hardening/10-02-SUMMARY.md
@.planning/phases/09-provider-conformance-foundation/09-01-SUMMARY.md
@.planning/phases/09-provider-conformance-foundation/09-04-SUMMARY.md
@src/adapters/gemini/mod.rs
@src/retry.rs
@src/model/gemini_request.rs
<interfaces>
From `src/retry.rs`:
- `send_with_retry_with_safety(policy, provider, RetrySafety::NonIdempotentPost, attempt)` permits transient pre-header transport retry but never response-status retry.
- `Commitment::{mark_client_visible, mark_replay_unsafe_tool, may_redispatch}` is the Phase 9 monotonic vocabulary; Gemini has no valid mid-body recovery seam.

From `src/adapters/gemini/mod.rs`:
- Credential resolution currently occurs once before request translation and captures `access_token`, `project_id`, endpoint, and payload outside the retry closure; preserve that affinity.
- The OAuth Code Assist branch must remain `v1internal:{generateContent|streamGenerateContent}` with `{model, project, request}` and bearer authentication.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make Gemini generation explicitly non-idempotent and identity-stable</name>
  <files>src/adapters/gemini/mod.rs, tests/gemini_conformance.rs</files>
  <behavior>
    - D-01 and D-15: a permitted transient pre-header retry reuses identical bearer, project, endpoint, model, and JSON request meaning.
    - D-13: returned 429/502/503/504/529 statuses each perform exactly one total request; a classified transient connect/timeout before headers may retry within the configured budget.
    - D-03: credentials are resolved only through existing `google_oauth` policy and synthetic identity markers never appear downstream.
  </behavior>
  <action>Add failing real-gateway hit-count/capture fixtures with synthetic split token and project markers. Replace the idempotent-default helper with `send_with_retry_with_safety(..., RetrySafety::NonIdempotentPost, ...)`. Keep credential/project resolution and exact payload construction outside the attempt closure, cloning only the captured immutable values per D-01, D-03, D-13, and D-15. Prove byte-equivalent parsed JSON and identical destination/auth across a permitted transport retry, no response-status retry, and no credential-file read/write beyond the existing resolver.</action>
  <verify>
    <automated>cargo test --all-features --test gemini_conformance gemini_identity_retry</automated>
    <fails_when>the command exits non-zero or reports zero matching tests</fails_when>
  </verify>
  <done>Gemini generation uses explicit non-idempotent safety, preserves one token/project/payload identity across the only permitted retry class, and never retries a returned status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove no post-header replay and complete call/result gateway parity</name>
  <files>src/model/gemini_request.rs, src/model/gemini_request/tests.rs, tests/gemini_conformance.rs</files>
  <behavior>
    - D-14: malformed/truncated body, embedded provider error, visible text, or function call causes zero second upstream requests and zero route hops.
    - D-11: a streamed call returned as `tool_result` reconstructs the exact next `functionResponse` with paired ID/name/arguments/signature semantics.
    - GEM-02: an upstream assistant-side `functionResponse` fails explicitly rather than being misrepresented as assistant text or a tool block.
    - D-09 and D-12: equivalent streaming and unary real-gateway transcripts agree on content, tools, usage, finish, and provider error.
  </behavior>
  <action>Extend the real gateway suite with exact upstream hit counters for every body-time failure and commitment class, plus a two-request call/client-result/next-functionResponse sequence. Add a captured upstream `functionResponse` negative fixture proving both client modes reject it explicitly: GEM-02's function-result relay is the client-result-to-next-request direction because Anthropic assistant responses cannot faithfully carry `tool_result` blocks. Keep Gemini body processing free of retry/recovery calls; classify visible output and tool activity consistently with Phase 9 `Commitment` if a reachable redispatch seam is encountered, otherwise assert structurally that the adapter owns no such seam. Verify the request translator rejects orphan/ambiguous results before network dispatch and the streamed/unary normalized outputs match per D-09, D-11, D-12, and D-14. Do not add INVALID_ARGUMENT repair, project rediscovery, account rotation, or partial-response recovery.</action>
  <verify>
    <automated>cargo test --all-features --test gemini_conformance gemini_no_post_header_replay</automated>
    <fails_when>the command exits non-zero or reports zero matching tests</fails_when>
  </verify>
  <done>Every body-time or post-output path has one upstream attempt, valid tool results preserve their exact pairing into the next request, and real streaming/unary outcomes are equivalent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove downstream cancellation releases the entire Gemini response lifetime</name>
  <files>src/adapters/gemini/mod.rs, tests/gemini_conformance.rs</files>
  <behavior>
    - D-04: dropping a downstream streaming body drops the pending upstream body, decoder, semantic state, and captured response-lifetime ownership within a bounded timeout.
    - D-01: cancellation ends the selected identity/project lifetime without switching or redispatching.
    - D-14: cancellation performs zero recovery, route hop, or second generation request.
  </behavior>
  <action>Using the Phase 9 response-drop analog, add a pending mock Gemini stream whose receiver/drop signal is observable, begin one real `/v1/messages` stream, consume enough to prove establishment, then drop the client body and assert upstream drop plus capacity reacquisition within a timeout. Keep the byte stream, decoder, semantic machine, and response-lifetime values owned directly by Axum's downstream body; avoid detached producer tasks/channels. If existing RAII already satisfies D-04, retain characterization without extra production machinery. Assert one upstream hit and sanitized errors/log-shaped data.</action>
  <verify>
    <automated>cargo test --all-features --test gemini_conformance gemini_response_drop_releases</automated>
    <fails_when>the command exits non-zero or reports zero matching tests</fails_when>
  </verify>
  <done>Client cancellation observably drops all response-owned Gemini work and releases capacity promptly with no retry, provider hop, identity leak, or credential writeback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Retry outcome → billable redispatch | Ambiguous status/body failures can duplicate a non-idempotent generation. |
| OAuth resolver → Code Assist destination | Bearer and project must remain paired and confidential across attempts and errors. |
| Downstream body drop → upstream ownership | Abandoned streams can retain sockets, parser memory, and admission capacity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08 | Repudiation | non-idempotent retry | high | mitigate | Explicit safety enum and exact mock hit counts distinguish permitted pre-header retry from forbidden status/body replay. |
| T-10-09 | Information Disclosure | bearer/project capture | high | mitigate | Existing destination-bound auth only, synthetic split-marker absence assertions, and no credential diagnostics. |
| T-10-10 | Denial of Service | cancelled response ownership | medium | mitigate | Body-owned lazy stream plus observable drop/capacity reacquisition under timeout. |
| T-10-11 | Tampering | tool-result pairing | high | mitigate | Exact opaque ID/name pairing validation before the next request is dispatched. |
</threat_model>

<verification>
- `cargo test --all-features --test gemini_conformance gemini_identity_retry`
- `cargo test --all-features --test gemini_conformance gemini_no_post_header_replay`
- `cargo test --all-features --test gemini_conformance gemini_response_drop_releases`
- `cargo test --all-features --test retry --test failover --test gemini_conformance`
</verification>

<success_criteria>
- GEM-01 and GEM-05 have real-gateway attempt-count and capture evidence.
- GEM-03 parity includes the full call/result next-turn path and provider errors.
- Cancellation releases upstream work and capacity without detached tasks or identity changes.
- Existing Code Assist endpoint/envelope, OAuth source, public config, and credential persistence stay unchanged per D-02, D-03, and D-16.
</success_criteria>

<output>
Create `.planning/phases/10-gemini-semantic-hardening/10-03-SUMMARY.md` when done.
</output>
