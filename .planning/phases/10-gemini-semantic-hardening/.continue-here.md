---
context: phase
phase: 10-gemini-semantic-hardening
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-09-07T02:53:42.567Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered by an actual failing test. Acknowledge them before proceeding.

- [ ] RETRY SEMANTICS: Do not broaden `reqwest` retry classification merely to make the loopback fixture green. Prove that any newly accepted error is pre-header and safe under the Phase 9 non-idempotent commitment contract, or change the fixture to an already-approved transient failure.
- [ ] WARNINGS GATE: The current `TcpStream::set_linger` fixture is deprecated. Phase 10 cannot complete while `-D warnings` fails.
- [ ] CREDENTIAL BOUNDARY: Do not change credential-file writeback behavior or public config/provider semantics while finishing 10-07.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Test-driven retry expansion | A reset fixture produced a non-`is_connect` `SendRequest/ConnectionReset`; temporary debugging was added to the shared classifier. | blocking | Remove debug output, characterize the exact reqwest error, and require a focused safety regression before changing shared retry policy. |
| Green deterministic evidence described as live proof | Google does not publish the complete private `/v1internal` contract and no live OAuth run was performed. | advisory | Documentation must say hermetic real-router evidence and preserve the explicit live-proof uncertainty. |
| Wholesale OpenCodex port | OpenCodex has useful provider patterns but also current global-state, EOF, tool-repair, and no-progress regressions. | advisory | Port individual invariants with Shunt-native tests; never copy an adapter as a unit. |

<current_state>

Phase 10 Plan 10-06 is complete and committed. Plan 10-07 Task 1 is mid-GREEN after RED commit `a8b9727`. Partial changes add a crate-private `CredentialResolver`, carry it through `AppState`, route Gemini credential resolution through the state, and add real-router synthetic OAuth tests. The focused filter currently reports 2 pass and 1 fail: unary/streaming and cancellation pass; pre-header retry receives 502. `src/retry.rs` contains a temporary `RETRY_DEBUG` print. No Task 2 docs or release gates have been completed.

</current_state>

<completed_work>

- Phase 9: complete and verified.
- Phase 10 Plans 10-01 through 10-05: complete.
- Phase 10 Plan 10-06: complete; ID-based parallel result matching, strict known-Part handling, and post-DONE rejection committed through `8907d3d`.
- Phase 10 Plan 10-07 RED fixtures: committed as `a8b9727`.
- Upstream research: detailed in `.planning/research/UPSTREAM-ISSUES-HANDOFF.md` using GitHub CLI, web search, official docs, local OpenCodex source, and parallel GPT-5.6 Luna agents.

</completed_work>

<remaining_work>

- Task 1: safely complete the private resolver seam and retry fixture; remove debug output and deprecated API; run the complete `gemini_google_oauth_code_assist_lifetime` filter.
- Task 2: update `docs/v2-gemini-semantic-hardening.md`, 10-VALIDATION, 10-VERIFICATION, ROADMAP, and Plan 10-07 summary with exact—not overstated—evidence.
- Run Phase 10 scope, focused Gemini, retry/failover, format, warnings-denied Clippy, and serial all-feature workspace gates.
- Continue Phases 11–16 in roadmap order using the research handoff.

</remaining_work>

<decisions_made>

- Preserve one OAuth bearer/project/endpoint/envelope for the full request lifetime and permitted retry.
- Keep the test seam crate-private; production constructors install only `DefaultCredentialResolver`.
- Treat OpenCodex as a pattern/evidence source, not a drop-in implementation.
- Generic `openai_chat` is a necessary new provider kind; Command Code API-key and subscription are separate products; OpenCode Go is exact-tuple gated.
- Google AI Studio Web stays excluded. Credential writeback behavior stays unchanged absent explicit approval.

</decisions_made>

<blockers>

- Technical: reset-before-headers test yields `reqwest::Error` with `SendRequest/ConnectionReset`, but `is_connect()` is false, so it is not retried and the router returns 502.
- Technical: `set_linger` is deprecated and fails the repository warning policy.
- No human action or live credential is required; the evidence is intentionally hermetic.

</blockers>

## Required Reading (in order)

1. `.planning/research/UPSTREAM-ISSUES-HANDOFF.md` — current upstream evidence and port/avoid decisions.
2. `.planning/phases/10-gemini-semantic-hardening/10-07-PLAN.md` — exact remaining task contract.
3. `.planning/phases/10-gemini-semantic-hardening/10-06-SUMMARY.md` — the three closed verification gaps.
4. `.planning/phases/10-gemini-semantic-hardening/10-VERIFICATION.md` — stale baseline report; update only after re-verification.
5. `.planning/phases/10-gemini-semantic-hardening/10-RESEARCH.md` — Phase 10 decisions and exclusions.
6. `.planning/METHODOLOGY.md` if present — project analytical lenses.

## Critical Anti-Patterns (do NOT repeat these)

- Do not retain `RETRY_DEBUG`, secrets, tokens, project IDs, or live credential fixtures.
- Do not classify every pre-response request error as retryable; builder/configuration errors must remain local failures.
- Do not mark Phase 10 complete from the old verification report; it predates Plan 10-06 and the unfinished 10-07 work.
- Do not update generated `wiki/`.
- Do not copy OpenCodex global model evidence, unbounded account rotation, permissive EOF, or heuristic no-progress behavior.

## Infrastructure State

- No server, watcher, or external async job is intentionally running.
- Branch: `codex/opencodex-provider-compatibility`.
- Shared dirty working directory must be preserved exactly for the successor task.
- `.gsd/` and `.planning/milestone.lock` are untracked orchestration state; do not accidentally stage them.
- `.planning/config.json` only records `_auto_chain_active: false` after pausing.

<context>

The implementation is close but not complete. The safest next move is a narrow retry-fixture diagnosis, not a provider-wide refactor. Once Phase 10 is green and reverified, use the upstream research to plan each remaining phase independently. The recurring design rule is exact evidence at the request/transport boundary, bounded state, and no redispatch after output or tool commitment.

</context>

<next_action>

Start with: run `git diff -- src/retry.rs src/server.rs`, remove the temporary debug print, reproduce the single failing retry test with tracing, and decide fixture-versus-classifier based on the Phase 9 retry contract before editing.

</next_action>
