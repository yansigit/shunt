---
phase: 13-generic-openai-chat-completions
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/model/openai_chat_request.rs
  - src/config.rs
  - src/adapters/openai_chat/mod.rs
  - tests/openai_chat_translate.rs
  - tests/openai_chat_conformance.rs
autonomous: true
requirements:
  - CHAT-02
  - CHAT-03
  - CHAT-04
must_haves:
  truths:
    - "Translation rejects null/non-object requests and invalid/empty required message representations with typed 400s before any dispatch, and accepts a single-message input explicitly (CHAT-03 edge row, E-02)."
    - "Text content preserves Unicode byte-for-byte with no normalization; length budgets are UTF-8 byte counts via checked arithmetic; malformed UTF-8 fails at the decode boundary (CHAT-03 encoding row)."
    - "Translation is pure and request-local: concurrent requests cannot exchange model, content, or generation controls; endpoint construction from an accepted root is deterministic and produces identical URL bytes under repetition and interleaving (CHAT-03 concurrency row, CHAT-02 edge row)."
    - "Endpoint construction appends exactly one /chat/completions to an accepted API root and rejects ambiguous query, fragment, userinfo, or otherwise malformed URL configurations at boot before credential lookup (CHAT-02, D-04)."
    - "Declared tools, tool_choice, parallel assistant tool calls, and tool-result messages survive translation with stable call identities; orphaned, duplicate, missing-identity, and incomplete-arguments tool inputs are typed request errors (CHAT-04, D-05)."
  artifacts:
    - path: src/model/openai_chat_request.rs
      provides: "Full whitelist translation: roles, text, image, generation controls (temperature/top_p/stop/max_tokens/stream/tool_choice), tools and tool-result pairing, endpoint-path builder, byte-budget checks, typed request-error variants"
    - path: src/config.rs
      provides: "openai_chat base-url grammar validation accepted at boot (reject query/fragment/userinfo/ambiguity before credentials)"
    - path: tests/openai_chat_translate.rs
      provides: "Pure translation unit suite: role/image/controls/tools matrices, empty and null rejections, UTF-8 byte budgets, URL grammar, determinism probes"
    - path: tests/openai_chat_conformance.rs
      provides: "Router-level fixtures proving translated wire bodies (no Responses-only keys; tools and results present) and boot rejection of bad base URLs"
  key_links:
    - from: src/config.rs
      to: src/model/openai_chat_request.rs
      via: "Boot-validated base URL grammar is the same builder the request path uses, so config and dispatch cannot disagree about the endpoint"
      pattern: "chat/completions"
  prohibitions:
    - statement: "No Responses-only or Anthropic-only fields are forwarded upstream; no silent normalization of Unicode text; no speculative URL repair."
      status: flagged
      verification: unverified
---

<objective>Expand the proven tracer slice into complete, strictly validated request translation: full role/text/image/generation whitelist, tools and tool-result pairing with typed 400s, and boot-enforced endpoint grammar with exactly one /chat/completions.</objective>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Role/text/image/generation-control whitelist with byte budgets and emptiness rules</name>
<files>src/model/openai_chat_request.rs, tests/openai_chat_translate.rs, tests/openai_chat_conformance.rs</files>
<read_first>13-01 Task 1 artifacts; src/model/ (existing Anthropic request types and field names); tests/ for the existing translation test style; .planning/phases/13-generic-openai-chat-completions/13-AI-SPEC.md scenario tables for empty/encoding rows; 13-EDGE-COVERAGE.json CHAT-03 rows; 13-CONTEXT.md D-05</read_first>
<behavior>CHAT-03. A null or non-object request body is rejected with a typed 400 before dispatch. A request with zero messages, or a message whose required text content is neither a nonempty representation nor a valid supported empty case, is rejected. A single system+user text pair translates to the Chat messages array with roles preserved. Image blocks in user messages translate to the Chat image_url shape (data URLs passed through unchanged; remote URLs passed through unchanged; non-image block types in image position are rejected). Generation controls present on the Anthropic request (temperature, top_p, stop sequences, max tokens, stream) map to their Chat fields; unknown or Responses-only fields never appear in the upstream body — proven by asserting the exact serialized outbound JSON. Multibyte content (e.g. a CJK string) round-trips byte-identically with no NFC/NFD normalization; a content block exceeding the named UTF-8 byte budget is rejected before dispatch with a checked length computation; malformed UTF-8 input bytes fail at the decode boundary rather than being lossily coerced.</behavior>
<action>Write the red pure-translation tests in tests/openai_chat_translate.rs first (null body, empty messages, single message, image mapping, each generation control, absent controls, exact outbound JSON snapshot, CJK byte preservation, over-budget content). Test malformed UTF-8 as raw HTTP bytes in the router fixture: serde_json::Value cannot contain invalid UTF-8. Extend translate_request from the 13-01 skeleton: enumerate the accepted Anthropic request fields, map them one-to-one, and construct the outbound serde_json::Value or typed struct with deny-by-default field handling. Introduce named consts for text/content byte budgets in the model module and use checked conversions; return typed request-error variants the adapter already surfaces as Anthropic-shaped 400s (failure: None). Add one router-level fixture asserting the mock upstream receives exactly the whitelisted body and none of the forbidden keys. No normalization pass anywhere; lengths are bytes.len() semantics via checked arithmetic.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test openai_chat_translate -- --test-threads=1</automated><fails_when>non-zero exit; a forbidden key survives into the outbound body; a null/empty case passes translation; a CJK string changes bytes; an over-budget or malformed-UTF-8 input is accepted; or the filter selects zero tests</fails_when></verify>
<acceptance_criteria>Every accepted Anthropic request field maps to its Chat counterpart, every rejected case returns a typed 400 before dispatch, and byte budgets plus byte-preserving Unicode are proven by pure tests with one wire-level confirmation.</acceptance_criteria>
<done>CHAT-03 translation is complete and testable in isolation from transport.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Tools, tool_choice, parallel tool calls, and paired tool results with stable identities</name>
<files>src/model/openai_chat_request.rs, tests/openai_chat_translate.rs, tests/openai_chat_conformance.rs</files>
<read_first>Task 1 artifacts; src/model/ existing Anthropic tool-use/tool-result types; 13-RESEARCH.md tool-mapping section; 13-AI-SPEC.md tool scenarios; 13-EDGE-COVERAGE.json CHAT-04 row; 13-CONTEXT.md D-05</read_first>
<behavior>CHAT-04/D-05. Declared tools translate to the Chat tools array with names, descriptions, and JSON schemas preserved. tool_choice (auto/any/tool-by-name) maps to the Chat value. An assistant message containing parallel tool_use blocks translates to one assistant message with multiple tool_calls, each with stable id, name, and arguments serialized once from the Anthropic tool_use.input object. A following user message with tool_result blocks translates to role tool entries whose tool_call_id matches the originating use. An orphaned tool_result with no preceding matching tool_use, a duplicate tool_use id within a turn, a tool_use missing its identity, and a tool_use with incomplete arguments are each rejected with typed 400s before dispatch. Anthropic tool_use.input must be a JSON object; serialize it once with serde_json::to_string to form Chat function.arguments. Reject null/scalar/array/string inputs, including an incomplete-JSON string; never invent an empty object. Compare parsed argument values in fixtures, not object key order. Concurrent translation of two tool-heavy requests preserves per-call pairing with no shared state.</behavior>
<action>Write the red tests first in tests/openai_chat_translate.rs (declaration mapping, each tool_choice variant, parallel calls with interleaved results, and the four rejection cases) plus one router fixture in tests/openai_chat_conformance.rs asserting the upstream sees the paired tool_calls/tool messages with matching ids. Implement in the model module: single pass over messages building a tool_use id registry request-locally, validating pairing order and completeness, emitting tool_call entries with arguments serialized once from the validated input object. Reuse the typed request-error variants; add tool-specific ones if needed. Translation remains a pure function of its input; no statics or shared maps.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test openai_chat_translate tool -- --test-threads=1</automated><fails_when>non-zero exit; ids are reordered, dropped, or regenerated; an orphaned/duplicate/identity-less/incomplete-arguments case reaches dispatch instead of a 400; or the filter selects zero tests</fails_when></verify>
<acceptance_criteria>Tool declarations, choices, parallel calls, and paired results cross the boundary with stable identities, and every malformed tool input is a typed 400 before any network send.</acceptance_criteria>
<done>CHAT-04 request-side semantics are complete.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: Endpoint grammar — exactly one /chat/completions, ambiguity rejected at boot</name>
<files>src/config.rs, src/adapters/openai_chat/mod.rs, src/model/openai_chat_request.rs, tests/openai_chat_translate.rs, tests/openai_chat_conformance.rs</files>
<read_first>Task 1 artifacts; src/config.rs:2080-2147 (base_url error family); 13-RESEARCH.md URL grammar section; 13-EDGE-COVERAGE.json CHAT-02 row; 13-CONTEXT.md D-04; the 13-01 partial append in src/adapters/openai_chat/mod.rs (replaceable per plan)</read_first>
<behavior>CHAT-02/D-04. The accepted API root produces exactly one /chat/completions path: a root already ending in /chat/completions is used as-is without doubling; a root with a trailing slash normalizes; a root with a base path (e.g. https://host/api/v1) appends. Configurations containing a query string, a fragment, userinfo (user:pass@), or other ambiguous malformed URLs are rejected at boot with a config error before any credential lookup or network activity. Construction is pure and deterministic: building the URL twice, or from two threads concurrently, yields identical bytes (CHAT-02 edge row). The grammar lives in one place shared by config validation and request construction.</behavior>
<action>Write red tests in tests/openai_chat_translate.rs for the URL builder (accept cases, double-append prevention, determinism under repetition) and red boot-rejection cases in tests/openai_chat_conformance.rs (query, fragment, userinfo each rejected at config load). Move the one-time append from the 13-01 adapter into a single grammar function in the model module (or config, if the existing error family lives there); config validation calls it at boot and errors with the existing base_url error style. The adapter calls the same function at request time. No speculative repair, no silent trimming beyond the documented trailing-slash rule.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test openai_chat_translate endpoint -- --test-threads=1</automated><fails_when>non-zero exit; any ambiguous URL boots successfully; /chat/completions appears twice in a built path; two constructions differ; or the filter selects zero tests</fails_when></verify>
<acceptance_criteria>Every accepted root yields exactly one canonical chat-completions path, every ambiguous root fails at boot before credentials, and the grammar is a single shared deterministic function.</acceptance_criteria>
<done>CHAT-02 is closed with config- and request-side enforcement in one place.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundaries: inbound Anthropic request bodies and operator base-URL configuration. High tampering threat: unvalidated request fields could smuggle Responses-only keys or oversized payloads to the upstream; mitigate the deny-by-default whitelist, byte budgets, and exact-body wire assertions. High spoofing threat: an ambiguous base URL (userinfo, query, fragment) could redirect the credential-bearing request to an unintended destination; mitigate boot-time grammar rejection before any credential resolution. Medium DoS threat: unbounded tool arguments; mitigate the named argument byte budget carried into 13-04. Low information threat: user content echoed in errors; mitigate provider-neutral typed messages.
</threat_model>
<verification><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test openai_chat_translate -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test openai_chat_conformance openai_chat -- --test-threads=1</automated><fails_when>any chained invocation exits non-zero; a test filter selects zero tests; or any new rejection is silent instead of typed</fails_when></verification>
<success_criteria>Requests cross the boundary through one pure, byte-exact, identity-stable translation with boot-enforced endpoint grammar, all proven by a pure unit suite plus wire-level router fixtures.</success_criteria>

## Artifacts this phase produces

Complete request-side translation: whitelist mapping, tools/pairing with typed 400s, shared URL grammar, and the pure openai_chat_translate test suite.
