---
phase: 13-generic-openai-chat-completions
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.rs
  - src/routing.rs
  - src/proxy/failover.rs
  - src/proxy/capability.rs
  - src/adapters/mod.rs
  - src/adapters/openai_chat/mod.rs
  - src/adapters/openai_chat/sse.rs
  - src/model/mod.rs
  - src/model/openai_chat_request.rs
  - src/model/openai_chat_response.rs
  - tests/openai_chat_conformance.rs
autonomous: true
requirements:
  - CHAT-01
  - CHAT-05
  - CHAT-06
must_haves:
  truths:
    - "A real Anthropic Messages client can send a text turn to a configured openai_chat provider through the full gateway router and receive the upstream JSON completion translated into the Anthropic response contract, proven by a red-first loopback fixture over server::build_router (D-01, CHAT-01)."
    - "The same router relays a streaming text turn incrementally with exactly one authoritative terminal, and an upstream stream that ends by EOF without finish_reason or [DONE] emits exactly one Anthropic-shaped error event, never a synthesized success (D-07, CHAT-06)."
    - "Every Chat send pins RetrySafety::ConnectOnly and the upstream client is built with redirect(Policy::none()); the generation POST is never re-dispatched after it could have reached the upstream, proven by exactly-one-request and zero-redirect-target-request fixtures (D-09, D-03)."
    - "The openai_chat provider kind resolves its configured api_key_env per request; concurrent requests cannot exchange credentials, models, or generation controls (CHAT-01/unclassified compound edge row: request-local resolution, asserted behaviorally)."
    - "Gateway-owned errors on this ingress use the Anthropic error shape with failure: None (PRES-04); the Codex-endpoint OpenAI-shape exception does not apply."
  artifacts:
    - path: src/config.rs
      provides: "ProviderKind::OpenAiChat (serde kind string openai_chat) with doc-comment, accepted with AuthMode::ApiKey + api_key_env, and its exhaustive-match companions updated"
    - path: src/routing.rs
      provides: "AdapterKind::OpenAiChat and one From<ProviderKind> mapping arm"
    - path: src/proxy/failover.rs
      provides: "dispatch arm OpenAiChat => crate::adapters::openai_chat::OpenAiChatAdapter, count_tokens list entry with CountTokens::Estimate"
    - path: src/adapters/openai_chat/mod.rs
      provides: "OpenAiChatAdapter, forward, OPENAI_CHAT_RETRY_SAFETY: RetrySafety = RetrySafety::ConnectOnly, redirect(Policy::none()) client, bounded unary collect, stream::unfold SSE relay with keepalive::with_pings"
    - path: src/adapters/openai_chat/sse.rs
      provides: "bounded Decoder (data-line framing, [DONE] detection, per-event byte cap, residual/UTF-8/duplicate-terminal failures), provider-neutral error messages"
    - path: src/model/openai_chat_request.rs
      provides: "translate_request whitelist skeleton (system/user/assistant text, model, stream flag) expanded by 13-02"
    - path: src/model/openai_chat_response.rs
      provides: "shared semantic machine skeleton (text deltas, finish, usage, terminal gating) with final_json_checked and transport_close_checked, expanded by 13-03"
    - path: tests/openai_chat_conformance.rs
      provides: "real-router loopback harness (gateway on 127.0.0.1:0, mock Chat upstream, synthetic env API key) with the tracer fixtures"
  key_links:
    - from: src/proxy/failover.rs
      to: src/adapters/openai_chat/mod.rs
      via: "AdapterKind::OpenAiChat dispatch arm invokes OpenAiChatAdapter::forward"
      pattern: "OpenAiChatAdapter|AdapterKind::OpenAiChat"
    - from: src/model/openai_chat_response.rs
      to: src/adapters/openai_chat/mod.rs
      via: "one machine serves JSON (final_json_checked) and SSE (per-frame) modes so output modes cannot drift (D-06/D-07)"
      pattern: "final_json_checked|transport_close_checked"
  prohibitions:
    - statement: "The adapter never retries after the request could have reached the upstream, and never follows redirects; BeforeHeaders classification is reserved for genuine pre-send connect failures (D-03/D-09)."
      status: flagged
      verification: unverified
---

<objective>Ship the thinnest real working slice: an openai_chat provider kind wired through config, routing, capability/discovery/dispatch, and a real-router loopback proving one unary and one streaming text turn end to end. Everything later in the phase expands this proven slice.</objective>

Plan-size note (conscious justification): eleven files_modified exceeds the
10-file plan guideline because this is an inherent vertical tracer — kind,
adapter, dispatch, model and harness are one slice whose files are only
meaningful together. Kept deliberately as minimal wiring; plans 13-02 onward
split by concern.
<tasks>
<task type="auto">
<name>Task 0: Preflight protocol-evidence gate — official Chat grammar consumed before any fixture or production code</name>
<files>read-only gate — consumes 13-PROTOCOL-EVIDENCE.md, modifies no files</files>
<read_first>.planning/phases/13-generic-openai-chat-completions/13-PROTOCOL-EVIDENCE.md (complete); 13-CONTEXT.md D-04/D-06</read_first>
<action>Read 13-PROTOCOL-EVIDENCE.md in full before writing any test fixture or production line in this plan. Confirm the doc separates (a) official Chat Completions facts with exact source links and per-source provenance, (b) gateway hardening beyond the official contract, and (c) provider reasoning extensions that are NOT official OpenAI fields. Log one execution line stating the three-way split before proceeding to Task 1. Consume the recorded fetched evidence; another network fetch is not a prerequisite for hermetic execution. Halt if the evidence is missing or contradicts the fixture contract.</action>
<verify><automated>test -s .planning/phases/13-generic-openai-chat-completions/13-PROTOCOL-EVIDENCE.md && rg -c "developers.openai.com" .planning/phases/13-generic-openai-chat-completions/13-PROTOCOL-EVIDENCE.md && rg -q "^## Official facts" .planning/phases/13-generic-openai-chat-completions/13-PROTOCOL-EVIDENCE.md && rg -q "^## Gateway hardening" .planning/phases/13-generic-openai-chat-completions/13-PROTOCOL-EVIDENCE.md && rg -q "^## Provider extensions" .planning/phases/13-generic-openai-chat-completions/13-PROTOCOL-EVIDENCE.md && rg -q "NO reasoning_content/reasoning textual" .planning/phases/13-generic-openai-chat-completions/13-PROTOCOL-EVIDENCE.md</automated><fails_when>the evidence doc is missing, carries no official-source links, lacks one of the three sections, or omits the explicit no-reasoning-fields official finding</fails_when></verify>
<done>Official protocol evidence is consumed and logged before any fixture or production code exists for openai_chat.</done>
</task>
<task type="tracer" tdd="true">
<name>Task 1: Tracer — openai_chat kind wired end to end, unary text turn over the whole gateway router</name>
<files>src/config.rs, src/routing.rs, src/proxy/failover.rs, src/proxy/capability.rs, src/adapters/mod.rs, src/adapters/openai_chat/mod.rs, src/adapters/openai_chat/sse.rs, src/model/mod.rs, src/model/openai_chat_request.rs, src/model/openai_chat_response.rs, tests/openai_chat_conformance.rs</files>
<read_first>Task 0 gate output (13-PROTOCOL-EVIDENCE.md already consumed); src/routing.rs:10-40 (AdapterKind + From arm pattern); src/config.rs:1765-1818 (ProviderKind/AuthMode), src/config.rs:2080-2147 (base_url error family); src/proxy/failover.rs:283-290 (count_tokens list), 349-376 (dispatch arms), 620-641 (headers_for_route slot stripping); src/adapters/mod.rs:27-65 (AdapterError, AdapterFailure, Adapter trait, with_admission); src/adapters/gemini/mod.rs:113-126 (local error shape), 162-201 (collect_unary_response), 416-457 (send + retry safety); src/adapters/gemini/sse.rs (Decoder shape); src/gateway/telemetry_ingest.rs:371-384 (Policy::none() rationale); src/retry.rs:137-160 (ConnectOnly) and the CURSOR_RETRY_SAFETY const at :152; tests/gemini_conformance.rs:60-149 (loopback harness); .planning/phases/13-generic-openai-chat-completions/13-PATTERNS.md (all sections); 13-CONTEXT.md D-01/D-02/D-03/D-06/D-07/D-09; 13-RESEARCH.md orchestrator corrections</read_first>
<behavior>CHAT-01/CHAT-05/D-01/D-09. Writing the failing router fixture first: a loopback mock Chat upstream returns a JSON chat.completion (content string, usage, finish_reason stop) and a gateway configured with kind = "openai_chat", auth = "api_key", api_key_env pointing at a synthetic env key serves POST /v1/messages and returns the Anthropic response contract with matching text, usage, and end_turn. The mock asserts it received exactly one request, at path .../chat/completions, with Authorization Bearer <synthetic> and no inbound authorization or x-api-key slot (stripping proven at the wire, not assumed). Config rejects the kind combined with non-API-key auth modes at boot. A second fixture proves request-local isolation: two concurrent requests with different api_key_env values each reach the upstream with their own bearer and their own model (CHAT-01/unclassified).</behavior>
<action>Name every new fixture function with its verify-filter substring as a contiguous part (openai_chat_tracer_unary_happy_path, openai_chat_tracer_unary_isolation, openai_chat_tracer_streaming_relay, openai_chat_tracer_streaming_eof_failclosed, openai_chat_tracer_streaming_duplicate_terminal) so no verify filter can select zero tests. Write the red fixture in tests/openai_chat_conformance.rs over the real router (build_router pattern from gemini_conformance.rs, loopback 127.0.0.1:0, synthetic SHUNT_OPENAI_CHAT_CONFORMANCE_KEY). Then wire production: add ProviderKind::OpenAiChat with a doc comment (serde snake_case yields kind "openai_chat"), accept it under AuthMode::ApiKey + api_key_env only; add AdapterKind::OpenAiChat with one From arm; add the failover dispatch arm and the count_tokens entry with CountTokens::Estimate; update every other exhaustive match the new variants surface (capability filtering, discovery/model listing, server error paths) so the crate compiles with no wildcard concealment. Create src/adapters/openai_chat/mod.rs with OpenAiChatAdapter delegating to forward: build the upstream client with reqwest redirect(Policy::none()), resolve the configured key once per request, strip nothing itself (failover already stripped inbound slots — assert that, do not re-handle), append /chat/completions once to the base URL (trailing-slash trim only; full ambiguity grammar is 13-02 Task 3 and this partial path must be replaceable without architectural change), assemble the outbound body via translate_request whitelist (system/user text, model, stream flag; no Responses-only or Anthropic-only keys), send with OPENAI_CHAT_RETRY_SAFETY = RetrySafety::ConnectOnly wrapped in upstream_timeout::wait, classify AdapterFailure::BeforeHeaders only for genuine pre-send connect errors, and return gateway-owned errors in the Anthropic shape with failure: None. Create src/adapters/openai_chat/sse.rs with a bounded Decoder mirroring the Gemini one (per-event byte cap, [DONE], residual and UTF-8 failures, provider-neutral messages). Create src/model/openai_chat_request.rs and openai_chat_response.rs: the response machine owns the terminal contract from day one — final_json_checked validates choices/usage/finish for the unary path; transport_close_checked fails closed on EOF without finish_reason or [DONE]. Local bounds get named consts from the start. No stub-success branch anywhere; every accepted shape is real.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test openai_chat_conformance openai_chat_tracer_unary -- --test-threads=1</automated><fails_when>non-zero exit; the unary fixture shows untranslated or Responses-shaped output; the mock sees a second request, a wrong path, a stripped-slot survivor, or the wrong bearer; the two concurrent requests exchange credentials or models; the kind is accepted with a non-API-key auth mode; or the filter selects zero tests</fails_when></verify>
<acceptance_criteria>The red fixture passes only after wiring lands; the unary text path works through the full router with credential stripping, destination, usage, and finish proven at the wire; ConnectOnly and Policy::none are pinned in code; gateway errors are Anthropic-shaped. Fixture functions carry the openai_chat_tracer_unary substring so the verify filter cannot select zero tests.</acceptance_criteria>
<done>The tracer proves the architecture end to end and becomes the skeleton every later task expands.</done>
<reversibility rating="one-way">kind string "openai_chat" and the provider table are public operator config; renaming after adoption breaks user configs. The user already approved this public addition (13-CONTEXT D-01), so no checkpoint is inserted — the rating is recorded, not re-asked.</reversibility>
</task>
<task type="auto" tdd="true">
<name>Task 2: Streaming text tracer — incremental relay with exactly one terminal, EOF fail-closed</name>
<files>src/adapters/openai_chat/mod.rs, src/adapters/openai_chat/sse.rs, src/model/openai_chat_response.rs, tests/openai_chat_conformance.rs</files>
<read_first>Task 1 artifacts; src/adapters/gemini/mod.rs:520-640 (unfold relay tuple and error-event pattern); src/keepalive.rs:46 (with_pings); 13-AI-SPEC.md Section 4 (streaming relay guidance); 13-CONTEXT.md D-06/D-07/D-08</read_first>
<behavior>CHAT-06/D-06/D-07. With stream: true the gateway forces stream_options include_usage upstream, relays delta.content frames as incremental Anthropic SSE text blocks through keepalive pings, maps finish_reason stop to the Anthropic terminal with usage, and treats [DONE] as the framing terminal. A stream cut by EOF after text but without finish_reason or [DONE] produces exactly one Anthropic-shaped error event and never a clean message_stop; a duplicate [DONE] or residual bytes after [DONE] also fail closed in this slice. Dropping the client connection mid-stream drops the owning unfold tuple (no registry, no detached task).</behavior>
<action>Name these fixtures with their filter substring as a contiguous part (openai_chat_tracer_streaming_relay, openai_chat_tracer_streaming_eof_failclosed, openai_chat_tracer_streaming_duplicate_terminal). Then write the red streaming fixtures (happy relay; EOF-without-terminal; duplicate terminal) in tests/openai_chat_conformance.rs against the same router. Then implement the SSE relay in the adapter as a futures_util::stream::unfold tuple owning (byte stream, Decoder, machine, finished flag, pending residual) wrapped in keepalive::with_pings, with configured upstream idle deadlines and bounded unary/error-body collection from this first slice, where the machine — not the transport — owns terminal decisions and emits Anthropic message_start/content_block/message_delta/message_stop framing plus exactly one error event then termination on failure. The unary path switches to machine.final_json_checked so both output modes share the one contract. Preserve the streaming invariant: the upstream body is never fully buffered for streaming clients.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test openai_chat_conformance openai_chat_tracer_streaming -- --test-threads=1</automated><fails_when>non-zero exit; text is buffered instead of relayed incrementally; more than one terminal or any success terminal after EOF; include_usage missing upstream; or the filter selects zero tests</fails_when></verify>
<acceptance_criteria>Streaming and unary text turns both pass through the one semantic machine with exactly one trustworthy terminal each; EOF, duplicate terminal, and residual failures are one typed error. Streaming fixture functions carry the openai_chat_tracer_streaming substring.</acceptance_criteria>
<done>Both output modes are proven real over the router before any expansion plan runs.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundaries: operator config (base URL, api_key_env) and upstream wire bytes. High spoofing/tampering threat: a redirect or mis-parsed path could carry the bearer off the configured destination; mitigate redirect(Policy::none()) from the first send, one-time path append, and wire-level assertions on the mock. High tampering threat: an EOF stream masquerading as success; mitigate machine-owned terminal gating proven red-first. Medium DoS threat: oversized SSE frames; mitigate the named per-event byte cap in sse.rs. Medium information threat: bearer leakage into diagnostics; mitigate existing redaction paths and fixture-only synthetic keys.
</threat_model>
<verification><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test openai_chat_conformance openai_chat_tracer -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings</automated><fails_when>any chained invocation exits non-zero; a test filter selects zero tests; or clippy/fmt report the new code</fails_when></verification>
<success_criteria>An operator-configured openai_chat provider serves real unary and streaming Anthropic Messages turns through the full gateway router with credential stripping, destination pinning, and single-terminal semantics proven at the wire.</success_criteria>

## Artifacts this phase produces

Real-router openai_chat vertical slice: kind/adapter/dispatch wiring, bounded SSE framing, the shared response machine skeleton, and the loopback conformance harness every later plan extends.
