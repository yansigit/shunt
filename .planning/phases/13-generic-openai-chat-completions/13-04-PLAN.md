---
phase: 13-generic-openai-chat-completions
plan: "04"
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - src/model/openai_chat_response.rs
  - src/adapters/openai_chat/mod.rs
  - src/adapters/openai_chat/sse.rs
  - tests/openai_chat_translate.rs
  - tests/openai_chat_conformance.rs
  - tests/failover.rs
  - tests/retry.rs
autonomous: true
requirements:
  - CHAT-06
  - CHAT-08
must_haves:
  truths:
    - "Interleaved indexed tool-call deltas assemble by index with identity completion across deltas: a call's name or identity may arrive in a later delta, argument strings accumulate raw with exactly one JSON parse at the assembly boundary, and an unnamed call at terminal is a hard error (CHAT-06, D-05)."
    - "Concurrent assemblies preserve per-request index state with no cross-request leakage; interleaved upstream chunks never reorder, drop, or merge distinct calls (CHAT-06 concurrency row, D-05)."
    - "Tool count and tool argument bytes are bounded by named limits with limit-minus-one accepted, exact-limit accepted, and limit-plus-one rejected (CHAT-06, CHAT-09/boundary)."
    - "Authentication resolves only the configured API-key source per request, inbound credential slots are stripped before dispatch, and the bearer reaches no destination other than the configured upstream — proven by a loopback 3xx redirect refusal fixture where the credential does not survive the refusal (CHAT-08, D-03)."
    - "RetrySafety::ConnectOnly holds: a post-send timeout produces a typed failure with no fallback retry, and AdapterFailure::BeforeHeaders classification appears only for demonstrably pre-send failures, pinned by classification fixtures (D-09)."
  artifacts:
    - path: src/model/openai_chat_response.rs
      provides: "Indexed tool-delta assembly state machine: index→identity→name→raw-args accumulation, single close-time JSON parse, unnamed-at-terminal error, bounded tool state"
    - path: src/adapters/openai_chat/mod.rs
      provides: "Assembly invocation inside the streaming relay; ConnectOnly + redirect(Policy::none()) reasserted on the send path; no-fallback timeout classification"
    - path: tests/openai_chat_conformance.rs
      provides: "Interleaved delta fixtures, byte-boundary tool fixtures, redirect-refusal fixture, post-send timeout classification fixture"
    - path: tests/failover.rs
      provides: "OpenAiChat failover-classification extensions (no retry after send)"
    - path: tests/retry.rs
      provides: "ConnectOnly assertion extensions for the OpenAI Chat adapter"
  key_links:
    - from: src/model/openai_chat_response.rs
      to: src/adapters/openai_chat/mod.rs
      via: "The relay feeds every delta through one assembly state machine before any tool block is emitted"
      pattern: "tool_assembly"
    - from: src/adapters/openai_chat/mod.rs
      to: src/retry.rs
      via: "OPENAI_CHAT_RETRY_SAFETY = RetrySafety::ConnectOnly gates classification so post-send ambiguity never retries"
      pattern: "OPENAI_CHAT_RETRY_SAFETY"
  prohibitions:
    - statement: "No JSON parse of tool arguments before the assembly boundary; no retry or fallback after a request could have reached the upstream; no credential persistence into any redirect destination; descriptorless classification claims stay flagged-unverified until fixtures run."
      status: flagged
      verification: unverified
---

<objective>Add streaming tool-delta assembly with interleaved-index fidelity and named byte bounds, then pin the auth/destination and retry-classification guarantees at the wire: configured-key-only auth, redirect refusal without credential survival, and ConnectOnly classification.</objective>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Interleaved indexed tool-delta assembly with cross-delta identity completion</name>
<files>src/model/openai_chat_response.rs, src/adapters/openai_chat/mod.rs, src/adapters/openai_chat/sse.rs, tests/openai_chat_translate.rs, tests/openai_chat_conformance.rs</files>
<read_first>13-01/13-03 machine artifacts; 13-RESEARCH.md tool-delta section and orchestrator corrections; 13-AI-SPEC.md tool-delta scenarios; 13-EDGE-COVERAGE.json CHAT-04/CHAT-06 rows; 13-CONTEXT.md D-05</read_first>
<behavior>CHAT-06/D-05. Upstream chunks carrying tool_call deltas with an index field assemble per index: the first delta may open with index alone (identity and/or name deferred), later deltas complete identity or name, and argument fragments accumulate as raw strings. If the same index arrives with a different identity after one is established, assembly fails closed. Argument fragments are type-checked and byte-bounded incrementally, but never JSON-parsed before completion; at the assembly boundary (finish or terminal event) exactly one JSON parse validates the accumulated arguments, and an incomplete or unparseable argument string is a typed failure. A call still unnamed at terminal is a hard error, never a synthesized placeholder. Two calls whose deltas strictly interleave (A-args, B-args, A-args, B-args) emit two correctly ordered tool_use blocks with unmerged fragments. A concurrent second request with different tool calls shares no assembly state.</behavior>
<action>Name every new fixture function with a contiguous assembly substring (assembly_identity_deferral, assembly_interleave, assembly_identity_conflict, assembly_unnamed_terminal) so the verify filter cannot select zero tests. Then write the red tests first: pure tests in tests/openai_chat_translate.rs for the assembly state machine (identity-across-deltas, interleaved indices, differing-identity conflict, single parse at close, unnamed-at-terminal); router fixtures in tests/openai_chat_conformance.rs streaming the interleaved delta script and asserting the emitted Anthropic tool_use blocks. Implement the assembly state inside the response machine keyed by upstream index, invoked from the relay in the adapter per chunk. Track a named tool-count limit and argument byte budget as consts; the boundary parse consumes the accumulated raw string once. Emit tool_use blocks only at the boundary, preserving arrival order.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test openai_chat_translate assembly -- --test-threads=1</automated><fails_when>non-zero exit; fragments are parsed per delta; interleaved calls merge or reorder; an unnamed call at terminal succeeds; identity conflict is accepted; or the filter selects zero tests</fails_when></verify>
<acceptance_criteria>Interleaved indexed tool deltas assemble with stable identities, deferred names, raw-argument accumulation, and a single validated boundary parse. Assembly fixture functions carry a contiguous assembly substring.</acceptance_criteria>
<done>CHAT-06 tool assembly semantics are complete.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Named tool bounds — count and argument byte limits with ±1 boundary probes</name>
<files>src/model/openai_chat_response.rs, src/adapters/openai_chat/sse.rs, tests/openai_chat_translate.rs, tests/openai_chat_conformance.rs</files>
<read_first>Task 1 artifacts; 13-EDGE-COVERAGE.json CHAT-09 boundary row; 13-AI-SPEC.md bounds section; existing named-limit patterns in the crate (CURSOR_RETRY_SAFETY const style, per-event byte cap)</read_first>
<behavior>CHAT-09/boundary. The tool count limit and per-call argument byte budget are named consts. A stream with limit-minus-one tool calls succeeds; exactly the limit succeeds; limit-plus-one fails with a typed error at the moment the excess call opens. Argument accumulation crossing one byte below, exactly at, and one byte past the budget behaves the same way, with the budget accounting including incomplete residual bytes. All three probes are explicit fixtures, not derived from a single boundary test.</behavior>
<action>Name every new probe function with a contiguous bound substring (bound_tool_count_minus_one, bound_tool_count_at, bound_tool_count_plus_one, bound_args_minus_one, bound_args_at, bound_args_plus_one) so the verify filter cannot select zero tests. Then write red boundary tests in tests/openai_chat_translate.rs for both limits (minus-one/at/plus-one each) plus one router fixture with an at-limit stream. Wire the limits into the assembly state and the residual accounting so a budget breach during accumulation (including a multibyte character split across the boundary) is caught. Reuse the typed error variants; messages are provider-neutral and name the limit.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test openai_chat_translate bound -- --test-threads=1</automated><fails_when>non-zero exit; any of the six boundary probes is missing from the suite; limit-plus-one is accepted; a split multibyte argument evades the budget; or the filter selects zero tests</fails_when></verify>
<acceptance_criteria>Both named limits are enforced with explicit minus-one/at/plus-one coverage including residual bytes. Probe functions carry the bound substring so the verify filter cannot select zero tests.</acceptance_criteria>
<done>Tool state cannot grow unbounded; the CHAT-09/boundary tool bounds are closed with tri-probe evidence.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: Auth/destination pinning and ConnectOnly classification fixtures</name>
<files>src/adapters/openai_chat/mod.rs, tests/openai_chat_conformance.rs, tests/failover.rs, tests/retry.rs</files>
<read_first>13-01 artifacts; src/retry.rs:137-160; src/proxy/failover.rs:620-641 (headers_for_route); 13-RESEARCH.md orchestrator corrections (classification wording); 13-EDGE-COVERAGE.json CHAT-08 row; 13-CONTEXT.md D-03/D-09; existing tests/failover.rs and tests/retry.rs patterns</read_first>
<behavior>CHAT-08/D-03/D-09. A loopback upstream that answers the initial request with a 3xx redirect to a second loopback server records that the second server received zero requests; the gateway surfaces a typed failure, not the redirect response. A post-send timeout (upstream accepts the connection and request bytes then stalls past the named pre-header/stream timeout) yields a typed failure with no fallback retry attempt — the mock records exactly one request. The classification fixtures in tests/failover.rs and tests/retry.rs pin that OpenAiChat failures are ConnectOnly-gated and that BeforeHeaders is asserted only for a demonstrably pre-send connect error (connection refused before any bytes). Two concurrent requests with different configured keys each deliver only their own bearer (extends the CHAT-08/concurrency isolation fixture with redirect refusal in the loop).</behavior>
<action>Name every new fixture function with a contiguous openai_chat_auth substring (openai_chat_auth_redirect_refusal, openai_chat_auth_postsend_timeout, openai_chat_auth_isolation) so the verify filter cannot select zero tests. Then write the red fixtures first: redirect-refusal and post-send-timeout cases in tests/openai_chat_conformance.rs; classification extensions in tests/failover.rs and tests/retry.rs following their existing fixture styles. In the adapter, verify the timeout is configured on the client send path (upstream_timeout::wait) and that the error mapping distinguishes connect-refused (BeforeHeaders) from everything post-send (non-retryable typed failure). No new credential handling: the configured key resolution and slot stripping from 13-01 are asserted, not re-implemented. Assert the credential never appears in the redirect target's request log.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test openai_chat_conformance openai_chat_auth -- --test-threads=1</automated><fails_when>non-zero exit; the redirect target receives any request; the stalled upstream produces a second request; a post-send timeout is classified as retryable or BeforeHeaders; or the filter selects zero tests</fails_when></verify>
<acceptance_criteria>Destination pinning, credential isolation, and ConnectOnly classification are proven at the wire with exactly-one-request evidence and no redirect credential survival. Auth fixture functions carry the openai_chat_auth substring.</acceptance_criteria>
<done>CHAT-08 and D-09 guarantees are fixture-pinned; the slice is behaviorally complete pending conformance breadth.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundaries: upstream SSE tool-delta bytes, HTTP redirects, and the configured credential source. High spoofing threat: a redirect could exfiltrate the bearer to a third host; mitigate redirect(Policy::none()) plus the wire fixture proving no credential reaches the redirect target. High tampering threat: interleaved deltas could splice arguments between calls or carry conflicting identities; mitigate index-keyed assembly, identity-conflict failure, and single boundary parse. Medium DoS threat: unbounded tool state; mitigate the named count/argument limits with ±1 probes. High availability/replay threat: ambiguous post-send timeout could trigger a duplicate generation; mitigate ConnectOnly gating and exactly-one-request fixtures.
</threat_model>
<verification><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test openai_chat_translate -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test openai_chat_conformance openai_chat -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test failover -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test retry -- --test-threads=1</automated><fails_when>any chained invocation exits non-zero; a filter selects zero tests; or a classification fixture contradicts the ConnectOnly gate</fails_when></verification>
<success_criteria>Tool deltas assemble faithfully under named bounds, and credential/destination/retry guarantees are proven at the wire with no post-send replay path.</success_criteria>

## Artifacts this phase produces

Indexed tool-delta assembly with named limits and the auth/destination/ConnectOnly fixture set across conformance, failover, and retry suites.
