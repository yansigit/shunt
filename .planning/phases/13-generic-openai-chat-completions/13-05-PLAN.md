---
phase: 13-generic-openai-chat-completions
plan: "05"
type: execute
wave: 5
depends_on: ["04"]
files_modified:
  - tests/openai_chat_conformance.rs
  - tests/openai_chat_translate.rs
  - .planning/phases/13-generic-openai-chat-completions/13-VALIDATION.md
  - README.md
  - README.ko.md
  - README.ja.md
  - README.zh-CN.md
  - docs/openai-chat-translation.md
  - site/src/content/docs/providers/openai-chat.md
  - site/src/content/docs/reference/configuration.md
  - site/src/content/docs/ko/providers/openai-chat.md
  - site/src/content/docs/ko/reference/configuration.md
  - site/src/content/docs/ja/providers/openai-chat.md
  - site/src/content/docs/ja/reference/configuration.md
  - site/src/content/docs/zh-cn/providers/openai-chat.md
  - site/src/content/docs/zh-cn/reference/configuration.md
autonomous: true
requirements:
  - CHAT-08
  - CHAT-09
must_haves:
  truths:
    - "All twelve AI-SPEC scenarios pass as hermetic conformance fixtures, including long-context boundary, cancellation before headers and mid-stream in both output modes, and a provider-error scenario (CHAT-09, D-10)."
    - "Cancellation while awaiting headers (after the upstream receives the request) and during streaming/unary collection closes the upstream transport and releases the admission permit within a bounded deadline; no zero-bytes or exactly-one-read promise is made about bytes already in flight (CHAT-09, E-07)."
    - "The three named-bound triples from E-12 (event/residual/aggregate byte caps) each have minus-one/at/plus-one coverage, completing CHAT-09's boundary requirement alongside 13-04's tool bounds (CHAT-09)."
    - "Every user-facing doc surface is updated in the same change set: README.md plus ko/ja/zh-CN translations, a docs/ engineering note, and the site page in English plus its three locale copies; wiki/ remains untouched and unregenerated in this plan (AGENTS documentation rule, D-11)."
  artifacts:
    - path: tests/openai_chat_conformance.rs
      provides: "The complete 12-scenario matrix: normal, streaming, image, tool-heavy, long-context boundary, cancellation (pre-header, mid-stream unary, mid-stream SSE), provider error, tool-delta, usage-only chunk, bounds"
    - path: README.md
      provides: "openai_chat provider kind documented in all four root READMEs (English source plus maintained translations)"
    - path: docs/openai-chat-translation.md
      provides: "Phase-13 engineering note describing the adapter, translation, machine, and guarantees"
    - path: site/src/content/docs/providers/openai-chat.md
      provides: "English provider/config reference updates plus ko/ja/zh-cn copies, with locale-safe anchors verified against built dist ids or plain English links"
  key_links:
    - from: tests/openai_chat_conformance.rs
      to: README.md
      via: "Documented operator configuration (kind openai_chat, api_key_env, base_url grammar) matches exactly what the conformance fixtures configure"
      pattern: "openai_chat"
  prohibitions:
    - statement: "No wiki/ hand edits; no locale page invents an English anchor without verifying the built id; no doc claims live provider availability beyond synthetic fixtures; no Computer UI claims."
      status: flagged
      verification: unverified
---

<objective>Close the phase: the full 12-scenario conformance matrix including cancellation proofs and named-bound triples, then every affected documentation surface in English and the three maintained locales, finishing with the chained full gates.</objective>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Conformance matrix — cancellation proofs, long-context boundary, provider-error scenario</name>
<files>tests/openai_chat_conformance.rs</files>
<read_first>All prior plan artifacts; 13-AI-SPEC.md sections 5–7 (12 scenarios, 7 evaluation dimensions); 13-EDGE-COVERAGE.json E-07 and E-12 rows; 13-CONTEXT.md D-10/D-11; existing conformance harness shape</read_first>
<behavior>CHAT-09/D-10. The matrix covers: (1) normal unary text; (2) streaming text; (3) image input; (4) tool-heavy turn (declaration + parallel calls + results, end to end); (5) long-context boundary at the named aggregate cap (minus-one, at, plus-one for the aggregate byte budget — E-12 row); (6) cancellation before headers — the upstream records the request then stalls headers, the client disconnects, and the test observes upstream EOF/closure and admission capacity reclamation within a deadline; (7) cancellation during unary aggregation — after the upstream signals body bytes sent, the downstream disconnects while waiting for the aggregated response; observe upstream closure and permit release within a deadline; (8) cancellation mid-stream SSE — same ownership proof for the unfold tuple; (9) provider error (embedded 200-error) surfaces correctly; (10) usage-only trailing chunk; (11) tool-delta assembly end to end; (12) named event/residual cap boundary behavior. Each cancellation fixture asserts ownership: no registry entry, no orphaned task, exactly the asserted upstream interaction.</behavior>
<action>Write the missing red fixtures against the existing router harness, reusing its mock-upstream controls. For cancellation, use the harness's drop/connection-close hooks and assert upstream closure and successful admission of a subsequent request within a bounded deadline; for the aggregate boundary, stream bodies at each of the three sizes. Map each fixture to its AI-SPEC scenario number in a test name or comment so the matrix is auditable. No scenario is satisfied by an existing test alone unless it already asserts the exact truth above; extend rather than weaken.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test openai_chat_conformance -- --test-threads=1</automated><fails_when>non-zero exit; any of the twelve scenarios lacks a named fixture; a cancellation fixture cannot prove upstream closure and admission release within its deadline; or the suite silently skips a scenario</fails_when></verify>
<acceptance_criteria>All twelve scenarios run as explicit hermetic fixtures with ownership proofs for cancellation and boundary evidence for the aggregate cap.</acceptance_criteria>
<done>CHAT-09's conformance requirement is complete.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Named-bound triples for event, residual, and aggregate caps (E-12 completion)</name>
<files>tests/openai_chat_conformance.rs, tests/openai_chat_translate.rs</files>
<read_first>Task 1 fixtures; the named consts in sse.rs and the response machine; 13-EDGE-COVERAGE.json CHAT-09 boundary row</read_first>
<behavior>CHAT-09/E-12. For the per-event byte cap, the residual buffer cap, and the aggregate response budget: a fixture at minus-one passes, at the limit passes, and one past fails with the specific typed error. Multibyte UTF-8 split exactly at each cap behaves per the documented residual rule (incomplete bytes count toward the budget; a split character that later completes remains valid). These join the 13-04 tool-bound triples so every named limit in the adapter has tri-probe coverage.</behavior>
<action>Write the nine probe cases (3 limits × 3 positions) across the two test files using the existing harness controls and pure-machine entry points. Where a cap is shared with an existing 13-01 test, extend that test to the tri-probe shape rather than duplicating. Confirm the error message names the limit so failures are diagnosable.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test openai_chat_translate cap -- --test-threads=1</automated><fails_when>non-zero exit; any of the nine probes is missing; a one-past-limit case is accepted; or the filter selects zero tests</fails_when></verify>
<acceptance_criteria>Every named byte cap has explicit minus-one/at/plus-one coverage including split-UTF-8 residual accounting.</acceptance_criteria>
<done>E-12 and the CHAT-09 boundary row are fully evidenced.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: Documentation — README (EN + ko/ja/zh-CN), docs/ note, site pages (EN + 3 locales), full chained gates</name>
<files>README.md, README.ko.md, README.ja.md, README.zh-CN.md, docs/openai-chat-translation.md, site/src/content/docs/providers/openai-chat.md, site/src/content/docs/reference/configuration.md, site/src/content/docs/ko/providers/openai-chat.md, site/src/content/docs/ko/reference/configuration.md, site/src/content/docs/ja/providers/openai-chat.md, site/src/content/docs/ja/reference/configuration.md, site/src/content/docs/zh-cn/providers/openai-chat.md, site/src/content/docs/zh-cn/reference/configuration.md, .planning/phases/13-generic-openai-chat-completions/13-VALIDATION.md</files>
<read_first>AGENTS.md documentation section; existing README provider sections in all four locales; docs/ milestone note style; site provider page structure in English and the three locale trees; site/dist anchor-verification procedure from AGENTS.md; the new config surface (kind string, auth mode, base URL grammar)</read_first>
<behavior>CHAT-09/D-11. The openai_chat provider kind is documented wherever providers are listed: English README gains the provider with its config keys (kind = "openai_chat", auth = "api_key", api_key_env, base_url including the grammar rules) and the ko/ja/zh-CN READMEs gain faithful translations of the same change. A docs/ engineering note records the adapter design: ConnectOnly, redirect refusal, the shared response machine, terminal semantics, and named bounds. The site's provider/reference pages gain the same content in English plus ko/ja/zh-cn copies; any cross-page fragment link in a locale file targets a section verified to exist in that locale's built dist output, otherwise it links the English page. Docs claim only synthetic-fixture behavior; no live provider availability claims. The plan then runs the full chained gates.</behavior>
<action>Update each surface minimally and consistently, translating rather than copying English prose into the three locales. Build the site once (npm --prefix site run build) and verify any locale anchor against site/dist before linking it; default to English-page links where parity is uncertain. Do not touch wiki/. Then run the full chained verification below and update the 13-VALIDATION.md per-task map statuses to executed for this phase's own evidence (leaving independent-checker fields untouched).</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings && node /tmp/shunt-phase12-isolated-run.cjs env RUSTFLAGS=-D warnings cargo test --all-features --workspace && node /tmp/shunt-phase12-isolated-run.cjs npm --prefix site run build</automated><fails_when>any chained invocation exits non-zero; fmt or clippy report the new code; any workspace test fails; the site build fails; or any command escapes the isolated wrapper</fails_when></verify>
<acceptance_criteria>All four READMEs, the docs/ note, and all four site locales document the new provider with correct anchors, and the full gate chain passes under the isolated wrapper.</acceptance_criteria>
<done>The phase is complete end to end: implementation, tests, and every required doc surface.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundaries: published documentation accuracy and the full test surface as the final gate. Medium spoofing threat: docs could overstate guarantees (live availability, verified official spec compliance); mitigate wording restricted to synthetic-fixture behavior and an explicit unresolved-spec note. Medium tampering threat: locale drift leaving a page current in English only; mitigate same-change-set translation updates and dist-verified anchors. Low availability threat: gates passing while a scenario is skipped; mitigate the zero-test-filter fails_when clauses across all commands.
</threat_model>
<verification><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings && node /tmp/shunt-phase12-isolated-run.cjs env RUSTFLAGS=-D warnings cargo test --all-features --workspace && node /tmp/shunt-phase12-isolated-run.cjs npm --prefix site run build</automated><fails_when>any chained invocation exits non-zero; a filter selects zero tests; the site build fails; or the wiki/ tree is modified</fails_when></verification>
<success_criteria>The twelve-scenario matrix and complete bound triples pass, all required documentation surfaces ship in the same change set, and the full gate chain is green under the isolated wrapper.</success_criteria>

## Artifacts this phase produces

The complete conformance matrix with cancellation ownership proofs and E-12 triples, plus the full documentation set (README ×4, docs/ note, site ×4 locales) and the green chained gates.
