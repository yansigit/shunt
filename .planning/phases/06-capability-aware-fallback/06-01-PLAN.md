---
phase: 06-capability-aware-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/proxy/capability.rs, src/proxy.rs, src/proxy/failover.rs, src/metrics.rs, tests/failover.rs]
autonomous: true
requirements: [CAP-01]
estimate: {tasks: 3, confidence: medium}
must_haves:
  truths:
    - "The declared primary is unchanged while known-incompatible fallback candidates are removed before auth, credentials, or network I/O."
    - "Tools, base64/URL images, structured output, explicit reasoning effort, and the 1M hint have deterministic eligibility behavior."
    - "Exclusions are visible without high-cardinality metric labels."
  artifacts:
    - {path: src/proxy/capability.rs, provides: "bounded request requirement extraction and adapter eligibility"}
    - {path: src/proxy/failover.rs, provides: "pre-dispatch chain filtering"}
    - {path: tests/failover.rs, provides: "zero-network exclusion fixtures"}
---

<objective>Add the minimal capability gate to heterogeneous outbound failover chains.</objective>

<context>@AGENTS.md @.planning/phases/06-capability-aware-fallback/06-CONTEXT.md @.planning/phases/06-capability-aware-fallback/06-RESEARCH.md @src/proxy/failover.rs @src/routing.rs @src/model/responses_request.rs @src/model/gemini_request.rs @src/adapters/cursor/request.rs</context>

<tasks>
<task type="tracer">
  <name>Task 1: Extract bounded request requirements</name>
  <files>src/proxy/capability.rs, src/proxy.rs</files>
  <action>Build a small immutable summary from the parsed Messages body: caller tools, base64/URL images in message/tool-result content, structured output, explicit effort, and trailing 1M hint. Traverse only known arrays and ignore malformed/unknown shapes for normal adapter validation.</action>
  <verify><automated>cargo test capability::tests::extract --lib</automated><fails_when>scanning is recursive/unbounded, clear requirements are missed, malformed shapes are repaired/rejected, or tests fail</fails_when></verify>
  <done>Table fixtures pin every requirement and no-requirement baseline.</done>
</task>
<task type="auto">
  <name>Task 2: Filter fallback candidates with an internal matrix</name>
  <files>src/proxy/capability.rs, src/proxy/failover.rs, src/metrics.rs</files>
  <action>Keep routes[0] unconditionally. Retain later routes only when their adapter preserves every detected feature; exclude all later routes for a 1M requirement. Run before inbound-auth chain analysis. Warn with provider/model and a bounded reason list; increment the existing failover metric with a fixed capability_excluded state.</action>
  <verify><automated>cargo test capability::tests::eligibility --lib && cargo test capability::tests::primary --lib</automated><fails_when>primary behavior changes, excluded candidates affect auth or dispatch, reasons enter metric labels, or tests fail</fails_when></verify>
  <done>The chain contains the primary plus only known-compatible fallbacks.</done>
</task>
<task type="auto">
  <name>Task 3: Prove pre-network filtering and compatible failover</name>
  <files>tests/failover.rs</files>
  <action>Add mock-backed mixed-chain fixtures: a primary failure skips incompatible targets with zero calls and reaches a compatible later target; 1M prevents every fallback; requests without requirements retain existing chain behavior.</action>
  <verify><automated>cargo test --test failover capability</automated><fails_when>an incompatible mock receives a request, a compatible fallback is lost, primary behavior changes, or tests fail</fails_when></verify>
  <done>Network observations prove capability filtering happens before dispatch.</done>
</task>
</tasks>

<verification><automated>cargo test capability --lib && cargo test --test failover capability && cargo test --test failover && cargo fmt --all --check && cargo clippy --all-targets --all-features -- -D warnings</automated></verification>
