---
phase: 11-antigravity-protocol-and-credential-hardening
plan: "04"
type: execute
wave: 4
depends_on:
  - "11-03"
files_modified:
  - src/model/antigravity_request.rs
  - src/model/gemini_request.rs
  - src/model/gemini.rs
  - src/adapters/gemini/mod.rs
  - tests/antigravity_translate.rs
  - tests/gemini_translate.rs
autonomous: true
requirements:
  - ANT-03
  - ANT-05
  - ANT-07
must_haves:
  truths:
    - "The exact agent envelope contains model, project, Antigravity metadata, fresh agent UUID request ID, request body, and an opaque scoped session (D-05, D-06)."
    - "Session identity is stable only for the same account/conversation and distinct for unrelated scopes without prompt/account/secret leakage or persistence."
    - "Sequential calls/results retain authentic IDs and order; signatures are non-empty, matching, and bound to account/session; copied, invented, orphaned, duplicate, and mismatched signatures fail before dispatch (D-13, D-14, D-15)."
  artifacts:
    - path: src/model/antigravity_request.rs
      provides: exact envelope and scoped opaque session/request identity
    - path: src/model/gemini_request.rs
      provides: sequential tool history and signature binding
    - path: src/model/gemini.rs
      provides: strict signature/tool semantic validation
    - path: tests/antigravity_translate.rs
      provides: exact envelope/session/tool fixtures
  key_links:
    - from: src/model/antigravity_request.rs
      to: src/adapters/gemini/mod.rs
      via: validated model/project/session envelope is serialized once
      pattern: wrap_antigravity_envelope|sessionId
    - from: src/model/gemini_request.rs
      to: src/model/gemini.rs
      via: ordered tool calls and authentic signatures survive translation and response checks
      pattern: tool_use_id|thought_signature
  prohibitions:
    - statement: "Never derive session or signature identity solely from prompt text."
      status: flagged
      verification: unverified
    - statement: "Never synthesize or persist thought signatures."
      status: flagged
      verification: unverified
<objective>Implement exact Antigravity envelope/session semantics and authentic account/session-bound sequential tool history.</objective>
<tasks>
<task type="tracer" tdd="true"><name>Task 1: Build exact scoped envelope and session identity</name><files>src/model/antigravity_request.rs, src/adapters/gemini/mod.rs, tests/antigravity_translate.rs</files><read_first>src/model/antigravity_request.rs, src/adapters/gemini/mod.rs, tests/antigravity_translate.rs, .planning/phases/11-antigravity-protocol-and-credential-hardening/11-CONTEXT.md</read_first><behavior>ANT-03/D-05/D-06: exact JSON keys and fresh agent UUID format are asserted; same account/conversation reuses opaque session, unrelated/account-changed scopes differ, and no prompt/account/token substrings appear.</behavior><action>Replace prompt-only session derivation with a bounded request-local opaque scope accepting account and conversation identity from the private tuple. Preserve exact envelope field names and request body semantics, generate fresh agent-UUID request IDs, and keep session non-durable. Add table-driven antigravity_native_envelope tests including serialization exactness and cancellation/local lifetime.</action><verify><automated>cargo test --all-features antigravity_native_envelope</automated><fails_when>non-zero exit, or output shows a prompt/account/secret substring in session or envelope</fails_when></verify><acceptance_criteria>Envelope JSON matches the proven field set; session stability/difference rules and opacity tests pass.</acceptance_criteria><done>ANT-03 exact envelope/session contract is implemented.</done></task>
<task type="auto" tdd="true"><name>Task 2: Preserve ordered calls/results and authentic signature bindings</name><files>src/model/gemini_request.rs, src/model/gemini.rs, tests/antigravity_translate.rs, tests/gemini_translate.rs</files><read_first>src/model/gemini_request.rs, src/model/gemini.rs, tests/gemini_translate.rs, .planning/phases/10-gemini-semantic-hardening/10-06-PLAN.md</read_first><behavior>ANT-05/D-13/D-14/D-15: adjacent sequential calls/results map by authentic ID and order; valid signatures round-trip; foreign/account-session-copied, empty, orphan, duplicate, invented, and mismatched values fail atomically.</behavior><action>Extend the existing checked tool/signature representation with private account/session binding and validate all relations before emitting any output or dispatch. Keep external seed items separate from tool results, preserve call order independently of result arrival, enforce non-empty matching signatures, and reject every non-bijective or cross-scope case. Add focused antigravity_native_tool_signature fixtures for streaming and unary parity.</action><verify><automated>cargo test --all-features antigravity_native_tool_signature</automated><fails_when>non-zero exit, or any invalid signature case emits a tool event or selects zero tests</fails_when></verify><acceptance_criteria>Valid multi-round histories preserve exact IDs/order/signatures; every invalid relation fails pre-dispatch with no partial output.</acceptance_criteria><done>ANT-05 authentic tool/signature history is proven.</done></task>
</tasks>
<threat_model>ASVS level 1; block high. High spoofing threat is copied thought signatures authorizing another account/session; mitigate scoped opaque binding and negative fixtures. High integrity threat is tool/result reordering or duplication; mitigate atomic bijection validation before emission. Medium information-disclosure threat is prompt/token leakage in IDs; mitigate opacity assertions and redacted diagnostics.</threat_model>
<verification><automated>cargo test --all-features antigravity_native_envelope && cargo test --all-features antigravity_native_tool_signature</automated><fails_when>either command exits non-zero or a focused filter selects zero tests</fails_when></verification>
<success_criteria>ANT-03, ANT-05, and Antigravity-specific ANT-07 tool/wrapper semantics are exact, opaque, ordered, and fail closed.</success_criteria>
## Artifacts this phase produces

Scoped Antigravity envelope/session types and account/session-bound tool/signature validation.
