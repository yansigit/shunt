---
phase: 11-antigravity-protocol-and-credential-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/gemini/mod.rs
  - src/adapters/gemini/sse.rs
  - src/model/gemini.rs
  - tests/gemini_conformance.rs
autonomous: true
requirements:
  - ANT-06
  - ANT-07
must_haves:
  truths:
    - "Native Antigravity uses streamGenerateContent?alt=sse for both downstream streaming and non-streaming requests (D-09)."
    - "One bounded decoder and checked semantic machine incrementally relays streaming output and accumulates only bounded non-streaming output (D-10)."
    - "Malformed wrappers, invalid UTF-8, incomplete tools, provider errors, premature EOF, duplicate terminals, and completed frames after DONE fail closed (D-11, D-18)."
  artifacts:
    - path: src/adapters/gemini/mod.rs
      provides: always-SSE native Antigravity dispatch and shared mode rendering
    - path: src/adapters/gemini/sse.rs
      provides: bounded frame decoder with strict terminal/EOF handling
    - path: src/model/gemini.rs
      provides: checked Antigravity wrapper and semantic validation
    - path: tests/gemini_conformance.rs
      provides: loopback real-router split/coalesced parity evidence
  key_links:
    - from: src/adapters/gemini/mod.rs
      to: src/adapters/gemini/sse.rs
      via: native response bytes feed one decoder in both client modes
      pattern: streamGenerateContent|GeminiSseDecoder
    - from: src/adapters/gemini/sse.rs
      to: src/model/gemini.rs
      via: checked frames reach one semantic machine before relay or accumulation
      pattern: process_chunk_checked|finish
  prohibitions:
    - statement: "Do not call unary generateContent for native Antigravity."
      status: flagged
      verification: unverified
    - statement: "Do not buffer a successful streaming response."
      status: flagged
      verification: unverified
---

<objective>Establish the production-quality always-SSE Antigravity tracer and strict bounded terminal path before identity or replay layers.</objective>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Route native Antigravity through one always-SSE checked path</name>
<files>src/adapters/gemini/mod.rs, src/adapters/gemini/sse.rs, src/model/gemini.rs, tests/gemini_conformance.rs</files>
<read_first>src/adapters/gemini/mod.rs, src/adapters/gemini/sse.rs, src/model/gemini.rs, tests/gemini_conformance.rs, .planning/phases/11-antigravity-protocol-and-credential-hardening/11-CONTEXT.md, .planning/phases/11-antigravity-protocol-and-credential-hardening/11-VALIDATION.md</read_first>
<behavior>ANT-06/D-09: both downstream modes request the SSE endpoint; ANT-07/D-10: both consume the same checked decoder and semantic machine; bounded accumulation is limited to non-streaming.</behavior>
<action>Write a failing real-loopback router fixture named antigravity_native_sse for streaming and non-streaming requests, then change only the native Antigravity branch to construct streamGenerateContent?alt=sse and feed both modes through the existing byte decoder and Gemini semantic machine. Preserve ordinary Gemini API-key and Google OAuth endpoint behavior. Keep stream bodies lazy and incremental; use existing bounded collectors only after the client explicitly requested non-streaming. Assert exact path/query, no full-body buffering in streaming, and equivalent semantic events from one transcript per D-09/D-10/D-12.</action>
<verify><automated>cargo test --all-features antigravity_native_sse</automated><fails_when>non-zero exit, or the output reports zero matching tests</fails_when></verify>
<acceptance_criteria>Both modes hit exactly the always-SSE path; the streaming body emits incrementally; unary rendering is bounded; ordinary Gemini paths are unchanged.</acceptance_criteria>
<done>Always-SSE native Antigravity tracer passes the focused real-router parity test.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Close wrapper, framing, and terminal failure cases</name>
<files>src/adapters/gemini/sse.rs, src/model/gemini.rs, tests/gemini_conformance.rs</files>
<read_first>src/adapters/gemini/sse.rs, src/model/gemini.rs, tests/gemini_conformance.rs, .planning/phases/10-gemini-semantic-hardening/10-06-PLAN.md</read_first>
<behavior>ANT-07/D-11/D-18: malformed JSON/SSE/UTF-8, mixed or incomplete wrappers/tools, provider error finishes, premature EOF, duplicate/post-DONE frames fail without success; exact bound and cap+1 cases are observable.</behavior>
<action>Add table-driven red fixtures for wrapper shapes, semantic parity, invalid UTF-8, oversized frames, incomplete tool data, provider errors, duplicate terminals, post-DONE frames, and split/coalesced delimiters. Tighten the existing decoder and checked machine at completed-frame boundaries, preserving only the documented raw-whitespace EOF residual. Ensure no error path emits a successful terminal or partial tool result.</action>
<verify><automated>cargo test --all-features antigravity_native_sse</automated><fails_when>non-zero exit, or any malformed/boundary case is absent from the focused test output</fails_when></verify>
<acceptance_criteria>Every listed malformed, bounded, incomplete, and post-terminal input fails closed exactly once in both modes; no successful terminal follows an error.</acceptance_criteria>
<done>Strict always-SSE parser and semantic terminal contract is proven before later layers.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundary is upstream SSE bytes to Anthropic output. High tampering threat: post-terminal or malformed frames could authorize false success; mitigate with completed-frame guards and split/coalesced tests. High DoS threat: oversized frame/tool retention; mitigate explicit byte/state caps and cap+1 tests. Medium integrity threat: wrapper ambiguity; mitigate one checked semantic path and parity fixtures.
</threat_model>
<verification><automated>cargo test --all-features antigravity_native_sse</automated><fails_when>the command exits non-zero or selects no native SSE tests</fails_when></verification>
<success_criteria>ANT-06 and ANT-07 strict always-SSE behavior is implemented with bounded, terminal-strict shared parsing.</success_criteria>
## Artifacts this phase produces

Native Antigravity SSE dispatch branch, strict wrapper validation, and focused `antigravity_native_sse` fixtures.
