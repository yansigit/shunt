---
phase: 11-antigravity-protocol-and-credential-hardening
plan: "05"
type: execute
wave: 5
depends_on:
  - "11-04"
files_modified:
  - src/adapters/gemini/mod.rs
  - src/auth/mod.rs
  - src/server.rs
  - src/retry.rs
  - src/auth/antigravity/auth.rs
  - src/upstream_timeout.rs
  - src/server/antigravity_replay_tests.rs
  - src/server/antigravity_replay_tests/fixtures.rs
autonomous: true
requirements:
  - ANT-02
  - ANT-08
must_haves:
  truths:
    - "A first pre-header 401 permits at most one same-account refresh/replay while retaining project/catalog/session/payload identity (D-16)."
    - "Second 401, refresh failure, any headers/output/tool/body/parser failure, and ambiguous send never replay (D-17)."
    - "Credential-file bytes and resolver/writeback behavior remain unchanged (D-03)."
  artifacts:
    - path: src/adapters/gemini/mod.rs
      provides: narrow Antigravity pre-commit 401 replay state machine
    - path: src/auth/mod.rs
      provides: same-account refresh seam without file writeback changes
    - path: src/server/antigravity_replay_tests.rs
      provides: 401, commitment, and unchanged-file evidence
  key_links:
    - from: src/adapters/gemini/mod.rs
      to: src/retry.rs
      via: replay state consults monotonic commitment before retry
      pattern: Commitment|may_redispatch|401
    - from: src/auth/mod.rs
      to: src/server.rs
      via: refresh remains request-local and account-bound
      pattern: CredentialResolver|resolve_route_credential
  prohibitions:
    - statement: "Never replay after client-visible output, tool activity, body/parser error, or ambiguous send."
      status: flagged
      verification: unverified
---

<objective>D-03: Implement one bounded, same-account, pre-commit Antigravity 401 refresh/replay without changing credential-file writeback or persistence behavior.</objective>
<tasks>
<task type="tracer" tdd="true"><name>Task 1: Add one account-bound pre-header 401 replay</name><files>src/adapters/gemini/mod.rs, src/auth/mod.rs, src/server.rs, tests/antigravity_catalog.rs</files><read_first>src/adapters/gemini/mod.rs, src/auth/mod.rs, src/server.rs, src/retry.rs, tests/antigravity_catalog.rs, .planning/phases/10-gemini-semantic-hardening/10-03-SUMMARY.md</read_first><behavior>ANT-08/D-02/D-16: exactly one initial pre-header 401 refreshes same account and replays with unchanged project/catalog/session/payload; resolver/refresh count is bounded and file bytes unchanged.</behavior><action>Write red real-router 401 fixtures, then add a private Antigravity-only state machine around the initial send. Refresh through the existing account-bound seam, retain validated tuple fields and serialized payload, replace only bearer binding, and permit exactly one replay before commitment. Keep all other providers and generic retry behavior unchanged; do not write or migrate credential files.</action><verify><automated>cargo test --all-features antigravity_native_401</automated><fails_when>non-zero exit, or the captured attempts exceed one replay or change account/project/session/payload</fails_when></verify><acceptance_criteria>First pre-header 401 has exactly two attempts with same account/project/session/meaning and refreshed bearer; no writeback occurs.</acceptance_criteria><done>Safe one-shot same-account 401 replay is implemented.</done></task>
<task type="auto" tdd="true"><name>Task 2: Prove every committed and ambiguous 401 boundary terminates</name><files>src/adapters/gemini/mod.rs, src/retry.rs, tests/antigravity_catalog.rs</files><read_first>src/adapters/gemini/mod.rs, src/retry.rs, tests/antigravity_catalog.rs, .planning/phases/09-provider-conformance-foundation/09-03-SUMMARY.md</read_first><behavior>ANT-08/D-17: second 401, refresh error, response headers, output, tool event, body/parser error, and ambiguous connection close each produce one terminal outcome and zero replay.</behavior><action>Expand the focused table with deterministic loopback responses for every commitment boundary and ambiguous send. Assert exact attempt counts, one authoritative error terminal, no post-commit provider hop, no detached task, and unchanged synthetic credential-file bytes. Per D-19, limit evidence to the explicit Commitment transition and request-local Antigravity replay seam; do not add generalized caches/storage/global-state claims, cross-account reuse logic, or a no-progress heuristic. Reuse Commitment vocabulary rather than adding a generalized retry framework.</action><verify><automated>cargo test --all-features antigravity_native_401</automated><fails_when>non-zero exit, zero 401 tests are selected, duplicate terminal, or any committed case records a second inference request</fails_when></verify><acceptance_criteria>All listed boundaries terminate once without replay; only the single pre-header 401 case replays, with D-19 narrow-scope evidence.</acceptance_criteria><done>ANT-08 replay safety is fully characterized.</done></task>
</tasks>
<threat_model>ASVS level 1; block high. High integrity/billing threat is duplicate generation after commitment; mitigate monotonic Commitment checks and attempt-count fixtures. High spoofing threat is refresh changing account; mitigate same-account binding and tuple equality. Medium persistence threat is accidental credential writeback; mitigate unchanged-file hash/bytes assertion.</threat_model>
<verification><automated>cargo test --all-features antigravity_native_401</automated><fails_when>non-zero exit or any post-commit/ambiguous scenario replays</fails_when></verification>
<success_criteria>ANT-08 and the retry-affinity portion of ANT-02 permit only one same-account pre-commit 401 replay and fail closed thereafter.</success_criteria>
## Artifacts this phase produces

Private Antigravity 401 replay state machine and bounded commitment-boundary evidence.

## Execution adjustment

The real-router 401 matrix lives in crate-local server tests with the existing
private credential dependency, not the public integration-test crate. This
keeps the OAuth token URL test-only and avoids a new production environment
override. Its helpers are split into a small sibling fixture module. The
existing credential-store write paths remain byte-for-byte unchanged.
