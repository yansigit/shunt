---
phase: 11-antigravity-protocol-and-credential-hardening
plan: "03"
type: execute
wave: 3
depends_on:
  - "11-02"
files_modified:
  - src/auth/mod.rs
  - src/server.rs
  - src/auth/antigravity/catalog.rs
  - src/adapters/gemini/mod.rs
  - tests/antigravity_catalog.rs
autonomous: true
requirements:
  - ANT-02
  - ANT-04
must_haves:
  truths:
    - "One immutable request-local account, bearer, project, catalog, and session tuple survives response completion, cancellation, and safe retry (D-02, D-04)."
    - "Fresh per-account catalog evidence admits only exact model/effort tuples; cold, stale-only, unknown, and ambiguous negatives fail before any credential-bearing inference dispatch (D-07, D-08). Account identity/catalog resolution and its control-plane fetch may run to establish the admission decision."
    - "Concurrent credential swaps cannot pair an old request with a new account or project."
  artifacts:
    - path: src/auth/mod.rs
      provides: private account-aware credential model and resolver seam
    - path: src/server.rs
      provides: request-local resolver ownership and lifetime wiring
    - path: src/auth/antigravity/catalog.rs
      provides: fresh exact account-keyed admission
    - path: tests/antigravity_catalog.rs
      provides: affinity and catalog negative evidence
  key_links:
    - from: src/server.rs
      to: src/adapters/gemini/mod.rs
      via: one resolver result is captured before dispatch
      pattern: resolve_route_credential
    - from: src/auth/antigravity/catalog.rs
      to: src/adapters/gemini/mod.rs
      via: exact fresh catalog gate precedes credential/network inference
      pattern: fetchAvailableModels|catalog_ids
  prohibitions:
    - statement: "Do not reuse catalog or project evidence across accounts."
      status: flagged
      verification: unverified
---

<objective>Bind native Antigravity credentials and discovery evidence to one request-local account tuple and make model/effort admission exact.</objective>
<tasks>
<task type="tracer" tdd="true"><name>Task 1: Capture immutable account/project/token tuple through the real router</name><files>src/auth/mod.rs, src/server.rs, src/adapters/gemini/mod.rs, tests/antigravity_catalog.rs</files><read_first>src/auth/mod.rs, src/auth/antigravity/auth.rs, src/server.rs, src/adapters/gemini/mod.rs, tests/antigravity_catalog.rs, .planning/phases/10-gemini-semantic-hardening/10-07-PLAN.md</read_first><behavior>ANT-02/D-02/D-04: injected and production-default resolver paths resolve once per request; every captured upstream attempt retains the same distinct account fingerprint, bearer, project, catalog context, and cancellation drops ownership promptly. Same-account multi-project requests remain distinct by project, cross-account same-project requests remain distinct by account, refresh/replay freezes the initial identity, and legacy refresh-token fallback is request-local and non-durable.</behavior><action>Inspect the existing Antigravity credential/store fields first, then add a crate-private, opaque, versioned, domain-separated SHA-256 account fingerprint. When the persisted email is present in StoredAuth, normalize it and fingerprint that value; never retain, log, expose, or serialize the raw email. For legacy stores without email, fingerprint the existing refresh token with a separate domain label, captured only in the request-local resolver result; document and test that this fallback is not durable across refresh-token rotation and is never logged or exposed. Keep this account identity distinct from bearer and project_id. Freeze the initial identity for the entire request and its one permitted 401 access-token refresh/replay; refresh may replace only the access token, never account identity, project, catalog, or session. Do not alter public config, credential-file schema, persistence, or writeback. Thread the immutable tuple through AppState into the native adapter closure and add concurrent-swap, held-body, and production-default-resolver tests named antigravity_native_affinity; cover same-account multiple projects, cross-account same project, access-token refresh stability, and legacy fallback safety. Synthetic-resolver assertions are supplementary, not the sole proof. Preserve RAII ownership.</action><verify><automated>cargo test --all-features antigravity_native_affinity</automated><fails_when>non-zero exit, zero affinity tests are selected, or production-default and injected resolver paths do not each resolve exactly once per client request</fails_when></verify><acceptance_criteria>Every request's upstream observations show one non-secret account fingerprint/token/project tuple with account distinct from project and bearer; both resolver paths invoke exactly once; same-account different projects and different-account same projects never collide; refresh/replay preserves the initial identity; legacy refresh-token fallback is neither persisted nor exposed; concurrent swaps cannot mix tuples; cancellation releases tuple and admission within the bounded test timeout.</acceptance_criteria><done>Request-local account affinity and lifetime are real-router proven on both production-default and synthetic resolver paths.</done></task>
<task type="auto" tdd="true"><name>Task 2: Require fresh exact catalog model/effort admission</name><files>src/auth/antigravity/catalog.rs, src/adapters/gemini/mod.rs, tests/antigravity_catalog.rs</files><read_first>src/auth/antigravity/catalog.rs, src/adapters/gemini/mod.rs, tests/antigravity_catalog.rs, .planning/phases/11-antigravity-protocol-and-credential-hardening/COVERAGE.md</read_first><behavior>ANT-04/D-07/D-08: exact fresh positive tuples dispatch; unknown, stale-only, cold, ambiguous, bare, Claude/GPT-rewritten, and unsupported-effort inputs fail before credential-bearing inference dispatch. Account-aware credential resolution and the catalog control-plane fetch are permitted prerequisites; rejected tuples produce no Authorization-bearing inference request and no inference endpoint hit.</behavior><action>Change only native Antigravity admission from fail-open catalog fallback to authoritative fresh exact matching keyed by the resolved private account identity and project. Preserve single-flight and bounded cache fetch behavior. Add positive and negative real-router fixtures proving rejected combinations may resolve the account and call fetchAvailableModels, but never attach Authorization to or hit streamGenerateContent/generateContent; assert no clamping or rewriting. Per D-19 keep the invariant narrow to this request-local catalog/admission seam: do not generalize caches, storage, cross-account reuse, global mutable state, or no-progress heuristics; make no public config change.</action><verify><automated>cargo test --all-features antigravity_native_affinity</automated><fails_when>non-zero exit, zero affinity tests are selected, or any rejected tuple reaches an inference endpoint, carries an inference Authorization header, or bypasses the fresh exact decision</fails_when></verify><acceptance_criteria>Only exact fresh catalog model/effort declarations pass; all negative cases fail before credential-bearing inference dispatch, with account/catalog control-plane calls explicitly allowed and observed; explicit effort is never rewritten or clamped.</acceptance_criteria><done>ANT-04 exact admission is enforced before inference dispatch with account/catalog prerequisites and D-19 narrow-scope evidence.</done></task>
</tasks>
<threat_model>ASVS level 1; block high. High spoofing threat is token/project/account race; mitigate immutable tuple and concurrent swap assertions. High authorization threat is guessed model/effort causing billable dispatch; mitigate fresh exact catalog gate with zero-hit negatives. Medium privacy threat is cross-account cache reuse; mitigate account/project cache keys and isolation tests.</threat_model>
<verification><automated>cargo test --all-features antigravity_native_affinity</automated><fails_when>non-zero exit or rejected catalog inputs reach credential/network seams</fails_when></verification>
<success_criteria>ANT-02 and ANT-04 are enforced with request-local account affinity and exact fresh catalog admission.</success_criteria>
## Artifacts this phase produces

Private account-aware resolver tuple and authoritative native catalog gate with affinity fixtures.
