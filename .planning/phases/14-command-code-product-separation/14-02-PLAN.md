---
phase: 14-command-code-product-separation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-command-code-product-separation/14-PROTOCOL-EVIDENCE.md
  - src/config.rs
  - src/config/upstreams.rs
  - src/auth/mod.rs
  - src/auth/command_code.rs
  - src/adapters/mod.rs
  - src/adapters/command_code/mod.rs
  - src/adapters/command_code/ndjson.rs
  - src/routing.rs
  - src/proxy/failover.rs
  - src/proxy/capability.rs
  - src/adapters/command_code/router_tests.rs
  - tests/command_code_conformance.rs
autonomous: true
requirements:
  - CCS-01
  - CCS-02
  - CCS-05
must_haves:
  truths:
    - "A dated 14-PROTOCOL-EVIDENCE.md separates source-derived Command Code wire facts, gateway hardening, and unverified live-acceptance items before any fixture encodes assumptions (D-12, Wave 0 gate)."
    - "A real-router subscription tracer proves a text turn over POST https://api.commandcode.ai/alpha/generate with Bearer from SHUNT_COMMAND_CODE_TOKEN, canonical-destination validation before credential access and again at dispatch, and one bounded NDJSON terminal (D-03/D-04/CCS-02)."
    - "Explicit env source precedence with invalid-explicit fail-closed and absent/empty -> auth error is proven read-only; credential file bytes/mtime/inventory unchanged (CCS-01/EDGE concurrency)."
    - "EOF without a supported terminal-class record fails; the first terminal-class record is authoritative and the documented finish-step->finish companion pair is accepted once (D-08)."
  artifacts:
    - path: .planning/phases/14-command-code-product-separation/14-PROTOCOL-EVIDENCE.md
      provides: "dated evidence ledger with Source-derived facts / Gateway hardening / Unverified-live sections, finish grammar and session resolution from pinned source 055c3ecf0de6c35f59195fc434d6b08525182b7f"
    - path: src/config.rs
      provides: "ProviderKind::CommandCode + AuthMode::CommandCodeOauth with kind/auth pairing validation (only this pairing admitted)"
    - path: src/config/upstreams.rs
      provides: "flag-less AuthMap variant for CommandCodeOauth following the XaiOauth pattern"
    - path: src/auth/command_code.rs
      provides: "env-first read-only resolver (SHUNT_COMMAND_CODE_TOKEN; explicit invalid fails closed; absent/empty -> auth error; CLI ~/.commandcode/auth.json fallback deferred to 14-05)"
    - path: src/adapters/command_code/mod.rs
      provides: "CommandCodeAdapter skeleton: canonical /alpha/generate destination gate, Policy::none, COMMAND_CODE_RETRY_SAFETY = ConnectOnly, minimal envelope, bounded NDJSON framing"
    - path: src/adapters/command_code/ndjson.rs
      provides: "bounded NDJSON Decoder (line framing, per-record byte cap, UTF-8 discipline, fail-closed residual)"
    - path: src/routing.rs
      provides: "AdapterKind::CommandCode with one From arm"
    - path: src/proxy/failover.rs
      provides: "dispatch arm CommandCode => CommandCodeAdapter and count_tokens entry"
    - path: src/proxy/capability.rs
      provides: "eligibility arm: tools yes; effort admitted by the 14-03 tuple table (pending -> deny); 1m unknown -> deny"
    - path: src/adapters/command_code/router_tests.rs
      provides: "real-router loopback harness with named command_code_tracer_* fixtures"
  key_links:
    - from: src/proxy/failover.rs
      to: src/adapters/command_code/mod.rs
      via: "AdapterKind::CommandCode dispatch invokes CommandCodeAdapter::forward"
      pattern: "CommandCodeAdapter|AdapterKind::CommandCode"
    - from: src/auth/command_code.rs
      to: src/adapters/command_code/mod.rs
      via: "destination check completes before credential resolution; re-checked at dispatch"
      pattern: "SHUNT_COMMAND_CODE_TOKEN|CommandCodeOauth"
  prohibitions:
    - statement: "No live credential read, no writeback/refresh/copy, no CLI fallback yet (14-05 scope), no public loopback bypass permitting live tokens off-origin (D-02/D-04)."
      status: flagged
      verification: unverified
---

<objective>Ship the thinnest real subscription slice: dated protocol evidence, command_code kind/auth wiring, env-first read-only auth, canonical-destination gating, and a real-router tracer proving one text turn over /alpha/generate RED before GREEN. Later plans expand envelope, response semantics, and lifetime.</objective>

Plan-size note (conscious justification): twelve files_modified mirrors the
13-01 tracer precedent — kind, adapter, dispatch, harness and evidence are one
vertical slice whose files are only meaningful together.

<tasks>
Fixture ownership contract: all subscription gateway wire fixtures (command_code_tracer_*, command_code_lifetime_*, command_code_matrix_*, and gateway command_code_bounds_*) live exclusively in src/adapters/command_code/router_tests.rs, declared cfg(test), and execute through --lib. Resolver-only fixtures live in the crate-local auth module. tests/command_code_conformance.rs contains public config/admission tests only, executed by the full workspace gate; it never owns the named wire filters. Translation fixtures live in tests/command_code_translate.rs and execute via --test command_code_translate. This contract supersedes ambiguous conformance-file artifact descriptions below. Each filtered invocation must visibly run at least one test and all named fixtures assigned to that group.

<task type="auto">
<name>Task 0: Wave 0 gate — dated protocol-evidence ledger before any fixture</name>
<files>.planning/phases/14-command-code-product-separation/14-PROTOCOL-EVIDENCE.md</files>
<read_first>.planning/phases/14-command-code-product-separation/14-RESEARCH.md (complete); 14-CONTEXT.md D-05/D-08/D-12; pinned adjacent source read-only: /Volumes/PortableSSD/Projects/opencodex at 055c3ecf0de6c35f59195fc434d6b08525182b7f — src/adapters/command-code.ts:253-276 (session hashing), ; tests/command-code-provider.test.ts:643-649 finish-step(usage)+finish(totalUsage), :703-731 terminal comments; src/providers/command-code-efforts.ts (exact arrays); src/oauth/command-code.ts (auth.json schema)</read_first>
<action>Write 14-PROTOCOL-EVIDENCE.md with exactly three sections — "## Source-derived facts", "## Gateway hardening", "## Unverified live acceptance" — each fact carrying source path, revision 055c3ecf0de6c35f59195fc434d6b08525182b7f, and access date. Record: both finish-step and finish are terminal-class, first-wins, commonly co-occurring as finish-step(stop,usage) then finish(stop,totalUsage) (source 703-710 + test 643-649); error finishReason = failed turn; session = randomUUID when no identity else SHA-256 of identity only (not credentials) rendered UUID-shaped; source hashes first-user-text as last-resort — Shunt REJECTS prompt-only identity and uses explicit conversation identity with a documented request-local UUID v4 fallback that is NOT multi-turn stable; auth.json schema {apiKey: nonempty, userId?}; the exact effort arrays as pinned by root (Luna/Muse low..max with ultra rejected, GLM-5.3 low/high/max, Google low/medium/high, GLM-5.x and deepseek high/max, case-sensitive ids). Label minimal-constant-config live acceptance and version 0.52.1 currency as Phase 16 gated in the Unverified section. Include OpenCodex MIT attribution. Halt the plan chain if the ledger is missing or contradicts the fixtures.</action>
<verify><automated>test -s .planning/phases/14-command-code-product-separation/14-PROTOCOL-EVIDENCE.md && rg -q "^## Source-derived facts" .planning/phases/14-command-code-product-separation/14-PROTOCOL-EVIDENCE.md && rg -q "^## Gateway hardening" .planning/phases/14-command-code-product-separation/14-PROTOCOL-EVIDENCE.md && rg -q "^## Unverified live acceptance" .planning/phases/14-command-code-product-separation/14-PROTOCOL-EVIDENCE.md && rg -q "055c3ecf0de6c35f59195fc434d6b08525182b7f" .planning/phases/14-command-code-product-separation/14-PROTOCOL-EVIDENCE.md && rg -q "MIT" .planning/phases/14-command-code-product-separation/14-PROTOCOL-EVIDENCE.md && rg -q "finish-step" .planning/phases/14-command-code-product-separation/14-PROTOCOL-EVIDENCE.md</automated><fails_when>the ledger is missing, lacks a section, omits the pinned revision, omits attribution, or omits the finish grammar</fails_when></verify>
<done>The evidence ledger exists and every later fixture cites it instead of encoding assumptions.</done>
</task>
<task type="tracer" tdd="true">
<name>Task 1: RED tracer — command_code kind/auth wiring to one real text turn</name>
<files>src/config.rs, src/config/upstreams.rs, src/auth/mod.rs, src/auth/command_code.rs, src/adapters/mod.rs, src/adapters/command_code/mod.rs, src/adapters/command_code/ndjson.rs, src/routing.rs, src/proxy/failover.rs, src/proxy/capability.rs, tests/command_code_conformance.rs, src/adapters/command_code/router_tests.rs</files>
<read_first>Task 0 ledger; 14-01-PLAN.md Task 2 harness (reuse shape); src/config.rs:1765-1818 (ProviderKind/AuthMode patterns); src/config/upstreams.rs XaiOauth AuthMap variant; src/auth/mod.rs:63 (Credential variants), :551 (with_credential_timeout); src/routing.rs:10-40; src/proxy/failover.rs:349-376 dispatch arms; src/proxy/capability.rs:75,112,~236; src/adapters/openai_chat/mod.rs (redirect::Policy::none, RETRY_SAFETY const pattern); src/model/openai_chat_request/endpoint.rs:8-43; 14-CONTEXT.md D-01..D-04, D-08..D-13; 14-PATTERNS.md</read_first>
<behavior>CCS-02/D-01..D-04/D-08. First commit RED fixtures named with contiguous verify-filter substrings (command_code_tracer_unary, command_code_tracer_destination_negatives, command_code_tracer_env_auth): a loopback mock NDJSON upstream emits text-delta then finish(stop,totalUsage) and the gateway (kind command_code) serves POST /v1/messages returning the Anthropic-shaped text response. The mock asserts path /alpha/generate on the canonical origin it stands for, Bearer <synthetic from SHUNT_COMMAND_CODE_TOKEN>, and stream:true. Destination negatives: http scheme, lookalike host, wrong path suffix, userinfo, query, fragment, and a 302 redirect response invalid configured destinations yield zero credential lookups and zero requests; the 302 requires one initial credential snapshot and zero redirected requests or new lookups; the credential-absent case yields an authentication error, never a SKIP. Then wire production so GREEN: ProviderKind::CommandCode + AuthMode::CommandCodeOauth admitted only as that pair; flag-less AuthMap variant; env-first resolver with explicit-invalid fail-closed and absent/empty -> AdapterError auth failure; AdapterKind::CommandCode From arm; dispatch + count_tokens; capability arms (tools yes; effort pending -> deny; 1m unknown -> deny); exhaustive-match updates with no wildcard concealment.</behavior>
<action>Create src/adapters/command_code/router_tests.rs as a #[cfg(test)] crate-local module declared from the adapter mod.rs. Reuse server::build_router_with_test_client and a test-only reqwest client with a locally generated CA and DNS resolution pinned to a fresh loopback TLS listener, preserving https://api.commandcode.ai/alpha/generate in the request URL. Do not expose the seam publicly or change host DNS. Keep lookup counters in crate-local auth tests; the external integration target must not import cfg(test)/pub(crate) helpers. Create tests/command_code_conformance.rs only for public-surface tests. Follow the 13-01 precedent: write the red fixtures first and record the RED output, then implement the vertical slice. Build the upstream client with reqwest redirect(Policy::none()); validate the canonical HTTPS origin and exact /alpha/generate path BEFORE resolving credentials and again at dispatch (both gates observable via a lookup counter the fixtures assert). The minimal envelope in this slice is the constant config object + empty memory + null taste/skills + standard/agent mode + params {model, messages, tools:[]} per D-05/RESEARCH — full envelope fields are 14-03. Decoder: bounded line framing with per-record cap, strict UTF-8, residual fail-closed, no SSE-prefix or junk tolerance. Terminal: first terminal-class record authoritative; finish-step->finish companion pair accepted once; any second terminal record except that companion, duplicate same-kind, or conflicting finishReason fails. EOF without terminal-class record fails. RetrySafety::ConnectOnly const COMMAND_CODE_RETRY_SAFETY. All stateful commands via node /tmp/shunt-phase12-isolated-run.cjs with quoted 'RUSTFLAGS=-D warnings'; one cargo at a time; never port 10100 or /Users/user/.opencodex.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib command_code_tracer -- --test-threads=1</automated><fails_when>non-zero exit; the RED step was never recorded; the mock sees a non-canonical path or missing bearer; any invalid configured destination performs a lookup, or any redirect causes a follow-up/new lookup; explicit-invalid env silently falls back instead of failing closed; absent env returns SKIP instead of an auth error; duplicate/second terminal or EOF-without-terminal yields success; or the filter selects zero tests</fails_when></verify>
<acceptance_criteria>RED recorded before GREEN; the subscription tracer works end to end over the real router with destination-before-credential ordering, env-first fail-closed auth, and single-terminal NDJSON semantics proven at the wire.</acceptance_criteria>
<reversibility rating="one-way">the kind string "command_code" and auth pairing are public operator config; user-approved per 14-CONTEXT D-02.</reversibility>
<done>The subscription vertical slice is proven and becomes the skeleton 14-03..14-05 expand.</done>
</task>
<task type="auto">
<name>Task 2: CCS-01 credential matrix — explicit/invalid/absent + read-only proof</name>
<files>src/auth/command_code.rs, tests/command_code_conformance.rs</files>
<read_first>Task 1 artifacts; 14-EDGE-COVERAGE.json CCS-01 rows; 14-CONTEXT.md D-03/D-13; src/auth/mod.rs with_credential_timeout pattern</read_first>
<behavior>CCS-01/D-03. Matrix fixtures named command_code_tracer_env_matrix_*: (a) explicit valid env authenticates; (b) explicit present-but-invalid env fails closed WITHOUT falling back to any other source; (c) absent env is an auth error (not SKIP — only an explicit live harness may SKIP, and none runs here); (d) empty-string env treated as invalid explicit auth error, never eligible for file fallback; (e) concurrent requests take immutable credential snapshots and never mutate or fall back (EDGE concurrency resolution). File-side proof: if the CLI file path exists in the test home it is byte/mtime/inventory unchanged after every request.</behavior>
<action>Extend the Task 1 resolver with snapshot semantics (one immutable credential read per request, with_credential_timeout bounded). Add the matrix fixtures asserting resolver outcomes and unchanged temp-file state via SHA-256/mtime before/after. No CLI file fallback implementation in this plan — Task 1 explicitly does not read the file; 14-05 adds it.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib command_code_tracer_env_matrix -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings</automated><fails_when>non-zero exit; invalid explicit env falls back instead of failing; absent env yields SKIP; snapshots mutate files or cross-talk; a filter selects zero tests; fmt/clippy flag new code</fails_when></verify>
<done>The CCS-01 credential surface is explicit, fail-closed, and proven read-only at the resolver boundary.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundaries: operator config, environment-sourced tokens, upstream wire bytes, and the filesystem credential path. High tampering threat: a redirect or lookalike origin exfiltrating the bearer — mitigate destination validation before credentials and at dispatch plus redirect(Policy::none()) and zero-follow-up fixtures. High information threat: live credential reads or writeback — mitigate read-only resolver, temp-file-only fixtures, and the byte/mtime/inventory assertions. High spoofing threat: an EOF or duplicate-terminal stream masquerading as success — mitigate first-terminal-authoritative machine semantics proven red-first. Medium DoS: oversized NDJSON records — mitigate the named per-record byte cap. Medium repudiation: source-derived fixtures mislabeled as live captures — mitigate the dated evidence ledger and synthetic-content labeling.
</threat_model>
<verification><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib command_code_tracer -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings && node /tmp/shunt-phase12-isolated-run.cjs env 'RUSTFLAGS=-D warnings' cargo test --all-features --workspace</automated><fails_when>any chained invocation exits non-zero or any filter selects zero tests</fails_when></verification>
<success_criteria>The subscription product has a proven real-router text-turn tracer with dated protocol evidence, canonical destination gating ordered before credentials, fail-closed env auth, and bounded single-terminal NDJSON semantics.</success_criteria>

## Artifacts this phase produces

The subscription vertical slice: kind/auth/dispatch wiring, evidence ledger, env-first read-only auth, and the command_code_conformance harness that 14-03..14-05 expand.
