---
phase: 14-command-code-product-separation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/presets.rs
  - tests/command_code_api_conformance.rs
autonomous: true
requirements:
  - CCK-01
  - CCK-02
  - CCK-03
must_haves:
  truths:
    - "The preset table gains a commandcode row over OpenAiChat at base https://api.commandcode.ai/provider/v1 with AuthMode::ApiKey and env SHUNT_COMMANDCODE_API_KEY; no existing preset row changes (D-01)."
    - "A real-router tracer proves an API-key text turn reaches .../provider/v1/chat/completions with its own bearer, and concurrent API-key vs subscription-mode-style requests cannot exchange credentials or destinations (CCK-02/EDGE concurrency)."
    - "CCK-03 scenarios (unary, streaming, tools, long-context below limit, auth error, drop) pass through the actual gateway using the commandcode preset without duplicating Chat translation."
  artifacts:
    - path: src/config/presets.rs
      provides: "commandcode preset row (kind OpenAiChat, canonical base, ApiKey auth, api_key_env SHUNT_COMMANDCODE_API_KEY, generic Chat tool semantics unchanged) plus sibling-preservation test extension"
    - path: tests/command_code_api_conformance.rs
      provides: "real-router loopback harness for the API-key product with named tracer fixtures"
  key_links:
    - from: src/config/presets.rs
      to: tests/command_code_api_conformance.rs
      via: "preset row drives the conformance fixture's provider config"
      pattern: "commandcode|SHUNT_COMMANDCODE_API_KEY"
  prohibitions:
    - statement: "No subscription auth mode is reachable from the commandcode preset and no bearer-based product inference exists (D-01)."
      status: flagged
      verification: unverified
---

<objective>Ship the commandcode API-key product as a table-driven preset over the proven Phase 13 Chat slice: preset row, sibling-preservation proof, and a real-router conformance tracer for CCK-01/02/03 with RED before GREEN.</objective>

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Preset row — commandcode over OpenAiChat with sibling preservation</name>
<files>src/config/presets.rs, tests/command_code_api_conformance.rs</files>
<read_first>.planning/phases/14-command-code-product-separation/14-CONTEXT.md D-01/D-02; 14-RESEARCH.md sections "Two products" and "Validation Architecture"; src/config/presets.rs:19-90 (preset table), :118-138 (ordered-name test), :152-174 (sibling-preservation test); 13-01-PLAN.md artifact contract for the openai_chat slice</read_first>
<behavior>CCK-01/D-01. A config selecting preset name commandcode resolves kind openai_chat, base https://api.commandcode.ai/provider/v1, auth api_key with api_key_env SHUNT_COMMANDCODE_API_KEY. Adding the row does not perturb any existing preset row's fields, ordering, or names (sibling-preservation test extension). The preset cannot be combined with any subscription auth mode (kind/auth pairing rejected at boot), proving products stay separate without bearer inference.</behavior>
<action>Write the failing real-router missing-preset tracer and sibling-preservation fixture first in tests/command_code_api_conformance.rs, named command_code_api_preset_tracer and command_code_api_preset_sibling_preservation so its verify filter cannot select zero tests. Record their genuine missing-preset RED before adding any production row. Then per D-01 add the commandcode row to the ordered preset table with the canonical /provider/v1 base and the distinct env name (SHUNT_COMMANDCODE_API_KEY vs the subscription SHUNT_COMMAND_CODE_TOKEN naming, so the two products cannot share a source), extend the ordered-name and sibling-preservation tests to cover the new row, and rely on existing kind/auth pairing validation (no new pairing rules). No change to any other row's fields or ordering. No new config keys.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test command_code_api_conformance command_code_api_preset -- --test-threads=1</automated><fails_when>non-zero exit; any existing preset row's fields change; the row accepts a subscription auth mode; the env name collides with SHUNT_COMMAND_CODE_TOKEN; or the filter selects zero tests</fails_when></verify>
<done>The commandcode preset row exists table-driven with proven sibling preservation and distinct credential naming.</done>
</task>
<task type="tracer" tdd="true">
<name>Task 2: Characterization — real-router API-key text turn, no-crossover concurrency</name>
<files>tests/command_code_api_conformance.rs</files>
<read_first>Task 1 artifacts; tests/openai_chat_conformance.rs (real-router harness: build_router pattern, 127.0.0.1:0 loopback, synthetic env key); 14-CONTEXT.md D-01/D-04/D-12/D-13; 14-EDGE-COVERAGE.json CCK-01/02/03 rows</read_first>
<behavior>CCK-01/CCK-02/D-12/D-13. Over the real gateway router on 127.0.0.1:0 with a mock Chat upstream asserting path .../provider/v1/chat/completions and Authorization Bearer <synthetic>: (a) command_code_api_tracer_unary returns the Anthropic-shaped text response; (b) command_code_api_tracer_isolation runs a concurrent API-key request and a request configured toward a subscription auth mode and proves distinct resolved credentials and destinations with no cross-product bearer (EDGE CCK-02 concurrency resolution); (c) command_code_api_tracer_auth_error maps an upstream 401 to the gateway-owned error shape without retry (CCK-03 error scenario). No live credentials, no port 10100, no /Users/user/.opencodex.</behavior>
<action>The real-router missing-preset tracer recorded genuine RED in Task 1. Add these existing-behavior characterization assertions without requiring another RED: never introduce a deliberate failure to manufacture test history. Reuse the Phase 13 harness verbatim in shape; add fixtures named with contiguous verify-filter substrings (command_code_api_tracer_unary, command_code_api_tracer_isolation, command_code_api_tracer_auth_error). The isolation fixture uses one-shot synthetic env keys and a second provider entry pinned to a non-admitted auth pairing, asserting boot rejection rather than runtime leakage, plus two concurrent live API-key requests each reaching the upstream with its own bearer (request-local resolution). Keep all upstream requests post-Phase-13 semantics: ConnectOnly, Policy::none inherited, not re-tested here beyond one wire assertion.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test command_code_api_conformance command_code_api_tracer -- --test-threads=1</automated><fails_when>non-zero exit; the mock sees the wrong path or bearer; concurrent requests exchange credentials; an auth error is retried or leaks upstream shape; or the filter selects zero tests</fails_when></verify>
<done>The API-key product tracer has Task 1 RED-then-GREEN evidence and Task 2 characterization over the real router with isolation and auth-error semantics proven at the wire.</done>
</task>
<task type="auto">
<name>Task 3: CCK-03 scenario sweep — unary/stream/tools/long-context/drop over the preset</name>
<files>tests/command_code_api_conformance.rs</files>
<read_first>Task 2 harness; tests/openai_chat_conformance.rs streaming/tool/cancellation fixtures; 14-EDGE-COVERAGE.json CCK-03 row (drop closes upstream work, capacity released, parallel tools keep identities); 14-CONTEXT.md D-11/D-14</read_first>
<behavior>CCK-03/D-11. The commandcode preset serves: streaming text relay with single terminal; strict-paired tool turn (Phase 13 policy unchanged, D-07); long-context request below the configured bound accepted and passed through intact; a client-disconnect drop closes the upstream connection and releases request capacity (real listener drop assertion); each fixture named with its filter substring (command_code_api_scenarios_*).</behavior>
<action>Extend the harness from Task 2 with the CCK-03 sweep fixtures mirroring the Phase 13 suite's structure (do not copy its fixtures blindly; assert commandcode-specific config resolution). For the drop scenario use the established owned-server-task drop pattern from Phase 13 cancellation tests and await server shutdown proof. Run all commands through node /tmp/shunt-phase12-isolated-run.cjs.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test command_code_api_conformance command_code_api_scenarios -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings</automated><fails_when>non-zero exit on any chained invocation; a fixture filter selects zero tests; streaming buffers instead of relaying; tool pairing is weakened relative to Phase 13; the drop leaves upstream work or capacity unreleased; fmt/clippy flag new code</fails_when></verify>
<done>CCK-01/02/03 are fully executed over the actual gateway with the preset, and the API-key product is complete pending the final 14-06 gate.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundaries: operator config (preset row, env names) and upstream wire bytes. High spoofing threat: a lookalike base URL or path prefix could send the bearer off the canonical destination; mitigate by pinning the canonical base in the preset and the Phase 13 endpoint grammar assertions at the wire. High tampering threat: credential crossover between concurrent products; mitigate request-local resolution with a wire-level isolation fixture. Medium information threat: bearer or prompt leakage into logs; mitigate existing redaction and synthetic fixture keys only. No new public endpoints are added by this plan.
</threat_model>
<verification><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test command_code_api_conformance -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings && node /tmp/shunt-phase12-isolated-run.cjs env 'RUSTFLAGS=-D warnings' cargo test --all-features --workspace</automated><fails_when>any chained invocation exits non-zero or any test filter selects zero tests</fails_when></verification>
<success_criteria>The commandcode API-key product is a complete table-driven preset serving real unary/streaming/tool turns through the actual gateway with distinct credential handling proven at the wire, siblings preserved, and all CCK requirements executed.</success_criteria>

## Artifacts this phase produces

The API-key half of the product split: preset row with preservation proof and a reusable real-router conformance harness later plans extend only when their requirements touch this product.
