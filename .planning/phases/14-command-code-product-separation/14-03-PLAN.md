---
phase: 14-command-code-product-separation
plan: "03"
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - src/adapters/command_code/mod.rs
  - src/proxy/capability.rs
  - src/adapters/command_code/request.rs
  - src/adapters/command_code/efforts.rs
  - src/config/presets.rs
  - tests/command_code_translate.rs
autonomous: true
requirements:
  - CCS-03
  - CCS-04
must_haves:
  truths:
    - "The subscription envelope is the exact proprietary shape with a minimal constant config (no cwd/date/environment/directory/git scanning), memory empty, taste/skills null, standard/agent mode, and params carrying exact model, messages, joined system, max_tokens default 64000, stream true, optional temperature, and reasoning_effort only when the tuple is admitted (D-05)."
    - "Headers are exactly Bearer, UA cli, x-command-code-version: 0.52.1 (constant, dated provenance), x-cli-environment: production, x-taste-learning: false, x-co-flag: false, x-session-id; no x-project-slug is ever emitted (D-05)."
    - "Session identity is credential-scoped SHA-256 over explicit conversation identity (inbound x-claude-code-session-id when present), rendered UUID-shaped; no-ID case is a request-local UUID v4 documented as NOT multi-turn stable; prompt-text-only and shared-cohort hashing are rejected (D-06, source 253-276 evidence)."
    - "Effort arrays match the pinned source exactly and are case-sensitive; unknown model or unsupported effort fails before generation; no alias, clamp, refresh, or remap (D-05)."
    - "Tool history preserves adjacent call/result units with authentic identities, explicit non-executed error-text for missing results, orphan-result user carrier, result-image carrier user message, and rejects duplicate/ambiguous identities before dispatch (D-07)."
  artifacts:
    - path: src/adapters/command_code/request.rs
      provides: "envelope assembly, exact headers, session derivation (SHA-256 credential-scoped, UUID-shaped), and tool-history compilation"
    - path: src/adapters/command_code/efforts.rs
      provides: "ten admitted source effort rows: zai-org/GLM-5.3 [low,high,max]; deepseek/deepseek-v4-pro and -flash [high,max]; zai-org/GLM-5, GLM-5.1, GLM-5.2, GLM-5.2-Fast [high,max]; meta/muse-spark-1.2, -1.2-contributor, meta/muse-spark-1.1 [low,medium,high,xhigh,max]. All three reporter-only rows (Luna, Gemini 3.7 Flash, vision-exp) excluded per corrected ledger; ultra rejected."
    - path: src/config/presets.rs
      provides: "subscription preset row command-code (kind command_code, canonical origin base, CommandCodeOauth) with sibling preservation"
    - path: tests/command_code_translate.rs
      provides: "unit fixtures for envelope, headers, session, efforts, and tool history"
  key_links:
    - from: src/adapters/command_code/efforts.rs
      to: src/proxy/capability.rs
      via: "capability admission consults the effort table so unsupported tuples fail before generation (replaces the 14-02 pending deny)"
      pattern: "effort|reasoning_effort"
    - from: src/adapters/command_code/request.rs
      to: src/adapters/command_code/mod.rs
      via: "forward delegates outbound body/headers to the request module"
      pattern: "x-session-id|x-command-code-version"
  prohibitions:
    - statement: "No prompt-text-only session hashing, no shared-cache-cohort fallback, no x-project-slug, no workspace path or git state in the envelope, no effort clamp/alias, no public version override config (D-05/D-06)."
      status: flagged
      verification: unverified
---

<objective>Complete the subscription request surface: exact envelope and identity headers, credential-scoped conversation session, pinned effort table, subscription preset row, and the tool-history compiler — each proven by unit fixtures against the evidence ledger.</objective>

<tasks>
Evidence correction (2026-09-08): Task 1 must explicitly reject gpt-5.6-luna, google/gemini-3.7-flash and deepseek/deepseek-v4-flash-vision-exp. The pinned source labels these exact three entries reporter-only, confirmed by git blame; they were mistakenly admitted in earlier planning. Preserve the D-05 unknown-combination fail-closed policy; no model table has shipped yet. The admitted table has ten rows. Other subagent-provider models are unrelated.
<task type="auto" tdd="true">
<name>Task 1: Effort table + subscription preset — exact tuples, sibling preservation</name>
<files>src/proxy/capability.rs, src/adapters/command_code/mod.rs, src/adapters/command_code/efforts.rs, src/config/presets.rs, tests/command_code_translate.rs</files>
<read_first>Task 0/1 artifacts of 14-02 (14-PROTOCOL-EVIDENCE.md effort section); src/providers/command-code-efforts.ts at 055c3ecf0de6c35f59195fc434d6b08525182b7f (exact arrays re-verified at execution time); src/config/presets.rs:19-90,152-174; 14-CONTEXT.md D-05</read_first>
<behavior>CCS-03/D-05. Every pinned model accepts exactly its pinned ladder and rejects every other value including ultra; model ids are case-sensitive; an unknown model fails before generation; effort omission is admitted only for models whose source row admits it (re-verify at execution; if the source makes effort mandatory for all rows, omit fails too). The subscription preset row command-code resolves kind command_code with the canonical origin and CommandCodeOauth; existing preset rows remain field-identical (sibling-preservation extension).</behavior>
<action>Write failing fixtures first (named command_code_translate_effort_* and command_code_translate_preset_subscription) so the verify filter cannot select zero tests, then implement efforts.rs as a table of exact arrays matching the pinned source at the pinned revision — no family generalization, no remap, no dynamic refresh — and wire capability admission to consult it (replacing 14-02's pending-effort deny with tuple-exact admission). Add the subscription preset row and extend ordered-name/sibling tests. No new public config keys; no version override.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test command_code_translate command_code_translate_effort -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test command_code_translate command_code_translate_preset_subscription -- --test-threads=1</automated><fails_when>non-zero exit; any array diverges from the pinned source; ultra is remapped or clamped; an unknown tuple passes; sibling rows change; or a filter selects zero tests</fails_when></verify>
<done>Effort tuples are exact and preset-level admission consults them before any generation.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Envelope + headers + credential-scoped session</name>
<files>src/adapters/command_code/request.rs, src/adapters/command_code/mod.rs, tests/command_code_translate.rs</files>
<read_first>14-02 Task 0 ledger envelope/session sections; src/adapters/command-code.ts buildRequest at 055c3ecf0de6c35f59195fc434d6b08525182b7f (field names re-verified); src/proxy.rs:30 (x-claude-code-session-id); src/config/session.rs primitives; 14-CONTEXT.md D-05/D-06; 14-EDGE-COVERAGE.json CCS-03 row</read_first>
<behavior>CCS-03/D-05/D-06. Outbound body: {config: minimal constant object, memory: "", taste: null, skills: null, permissionMode: "standard", mode: "agent", params: {model, messages, tools, system (joined), max_tokens (default 64000), stream: true, temperature?, reasoning_effort?}}. Headers exactly as pinned; the constant UA/version values carry ledger provenance. Session: with explicit conversation identity -> UUID-shaped SHA-256 over (credential identity, conversation id); same credential + same conversation across turns yields the same session (positive continuation case); different conversation or different credential yields a different session; no raw identity, prompt text, credential, path, or account name is derivable from the session value; no-ID case -> request-local UUID v4 asserted NON-stable across independent requests (documented honestly, never claimed as multi-turn stable). No prompt-text-only hashing and no cohort fallback anywhere.</behavior>
<action>Write red fixtures first (command_code_translate_envelope_*, command_code_translate_headers_*, command_code_translate_session_*) then implement request.rs owning envelope/headers/session assembly, with mod.rs delegating. Session derivation follows the evidence ledger's stronger-than-source rule per D-06 (credential scoping; source hashed identity only — never credentials — and Shunt additionally excludes root prompt and any cohort fallback). Ignore prompt text as an identity source; do not reject an ordinary no-ID request, which uses its request-local UUID. Keep the minimal config constant named and asserted to contain no cwd/environment/directory/git keys.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test command_code_translate command_code_translate_session -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test command_code_translate command_code_translate_envelope -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test command_code_translate command_code_translate_headers -- --test-threads=1</automated><fails_when>non-zero exit; any envelope/header field diverges from the ledger; x-project-slug appears; the session leaks identity or is stable across distinct conversations; prompt text is used as an identity key; the no-ID case claims stability; or a filter selects zero tests</fails_when></verify>
<done>The exact envelope, headers, and credential-scoped opaque session are implemented and unit-proven with positive continuation cases.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: Tool-history compiler — adjacent units, missing/orphan carriers, image carriers, duplicate rejection</name>
<files>src/adapters/command_code/request.rs, tests/command_code_translate.rs</files>
<read_first>14-02 ledger tool-history section; src/adapters/command-code.ts wireMessages at 055c3ecf0de6c35f59195fc434d6b08525182b7f (re-verified); 14-CONTEXT.md D-07; 14-EDGE-COVERAGE.json CCS-04 row (unclassified — flagged); Phase 13 Chat pairing policy (untouched)</read_first>
<behavior>CCS-04/D-07. Adjacent assistant tool-call + tool-result pairs port with authentic toolCallId/toolName; result outputs render {output:{type:"text"|"error-text", value}}; a missing result becomes an explicit non-executed error-text carrier (positive case asserted, not rejection-only); an orphan result is retained as visible context in a user text carrier; result images appear in a following user message as {type:"image", image, mediaType?}; original ordering is preserved across all of the above; duplicate toolCallId or ambiguous identity fails closed before dispatch. Unsupported opaque history also fails before dispatch. Phase 13 Chat strict pairing remains unchanged.</behavior>
<action>Write red fixtures first (command_code_translate_tools_*) covering: happy adjacent pair; missing-result error-text; orphan-result carrier; result-image carrier; ordering preservation with mixed units; duplicate-id rejection; unsupported-opaque rejection; positive plaintext subagent tool + continuation history case. Then implement the compiler in request.rs, keeping it product-local (never routed through the Chat compiler). Positive subagent/continuation cases are required inputs — rejection-only coverage does not close CCS-04/CCS-08.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test command_code_translate command_code_translate_tools -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings</automated><fails_when>non-zero exit; identities are renamed or reordered; missing results are silently dropped; orphan results are discarded; duplicate ids pass; subagent/continuation lacks a positive case; Chat pairing changes; or a filter selects zero tests</fails_when></verify>
<done>The tool-history contract is compiler-proven with authentic identities, explicit carriers, ordering, and fail-closed ambiguity handling.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundaries: inbound request history (untrusted client data) and operator config. High tampering threat: crafted history could inject tool identities or reorder units to smuggle instructions upstream — mitigate authentic-identity preservation, ordering assertions, and duplicate/ambiguous fail-closed checks proven by fixtures. High information threat: session derivation or envelope fields leaking prompt text, credential material, or workspace paths — mitigate credential-scoped one-way derivation, the no-cwd/git constant config assertion, and the absent x-project-slug assertion. Medium spoofing: an invented model id could select an unadvertised upstream model — mitigate case-sensitive exact-table admission before generation.
</threat_model>
<verification><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test command_code_translate -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings && node /tmp/shunt-phase12-isolated-run.cjs env 'RUSTFLAGS=-D warnings' cargo test --all-features --workspace</automated><fails_when>any chained invocation exits non-zero or any filter selects zero tests</fails_when></verification>
<success_criteria>The subscription request surface exactly matches the evidence ledger — envelope, headers, session, efforts, tool history — with all positive cases implemented and every divergence failing closed before generation.</success_criteria>

## Artifacts this phase produces

The complete outbound contract for the subscription product, unit-proven and wired into capability admission, ready for the response machine in 14-04.
