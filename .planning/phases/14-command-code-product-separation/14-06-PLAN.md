---
phase: 14-command-code-product-separation
plan: "06"
type: execute
wave: 5
depends_on: ["01", "05"]
files_modified:
  - src/adapters/command_code/router_tests.rs
  - site/src/lib/i18n.ts
  - site/src/content/docs/guides/providers.mdx
  - site/src/content/docs/ko/guides/providers.mdx
  - site/src/content/docs/ja/guides/providers.mdx
  - site/src/content/docs/zh-cn/guides/providers.mdx
  - .planning/phases/14-command-code-product-separation/14-VALIDATION.md
  - tests/command_code_conformance.rs
  - README.md
  - README.ko.md
  - README.ja.md
  - README.zh-CN.md
  - docs/m16-command-code.md
  - THIRD-PARTY-NOTICES.md
  - site/src/content/docs/providers/command-code.md
  - site/src/content/docs/ko/providers/command-code.md
  - site/src/content/docs/ja/providers/command-code.md
  - site/src/content/docs/zh-cn/providers/command-code.md
  - site/src/content/docs/reference/configuration.md
  - site/src/content/docs/ko/reference/configuration.md
  - site/src/content/docs/ja/reference/configuration.md
  - site/src/content/docs/zh-cn/reference/configuration.md
autonomous: true
requirements:
  - CCK-03
  - CCS-08
must_haves:
  truths:
    - "The full conformance matrix executes: positive plaintext subagent and continuation cases, tool-heavy conversations, long-context below/at configured bound, auth-error matrix, error-finish handling, and product-drop scenarios for both products over the real gateway (D-12)."
    - "Docs update in one PR across README (en+ko+ja+zh-cn), a new docs milestone note, the site provider page (en+3 locales), and configuration reference rows for both new products with locale anchors verified — wiki untouched, no hand-edit (D-14)."
    - "Final gates run through node /tmp/shunt-phase12-isolated-run.cjs: fmt, clippy -D warnings, full workspace suite with env 'RUSTFLAGS=-D warnings', npm --prefix site run build, plus owned CLI/curl smoke (D-13/D-14)."
    - "Live provider acceptance is NOT claimed; the minimal-envelope and version-currency items stay explicitly labelled as the Phase 16 gate in 14-VALIDATION.md (D-14/RESEARCH)."
  artifacts:
    - path: src/adapters/command_code/router_tests.rs
      provides: "remaining CCS-08/CCK-03 matrix fixtures (subagent/continuation positive, tool-heavy, long-context bounds, auth-error matrix, error finish)"
    - path: docs/m16-command-code.md
      provides: "milestone note in the established docs/m*-*.md style describing both products, config, and safety semantics"
    - path: site/src/content/docs/providers/command-code.md
      provides: "provider page for both products with en+ko+ja+zh-cn copies and locale-safe links"
  key_links:
    - from: tests/command_code_conformance.rs
      to: .planning/phases/14-command-code-product-separation/14-VALIDATION.md
      via: "every matrix group maps to an executed task id and filter recorded in the validation table"
      pattern: "command_code|CCK|CCS"
  prohibitions:
    - statement: "No live credential reads, no port 10100 or /Users/user/.opencodex access, no wiki hand-editing, no locale drift, no unverified live-availability claims (D-13/D-14)."
      status: flagged
      verification: unverified
---

<objective>Close the phase: execute the full scenario matrix for both products, update every affected documentation surface with its maintained locales, run all final gates and the owned smoke, and record the honest validation state including the Phase 16 live gate.</objective>

<tasks>
Fixture ownership contract: all subscription gateway wire fixtures (command_code_tracer_*, command_code_lifetime_*, command_code_matrix_*, and gateway command_code_bounds_*) live exclusively in src/adapters/command_code/router_tests.rs, declared cfg(test), and execute through --lib. Resolver-only fixtures live in the crate-local auth module. tests/command_code_conformance.rs contains public config/admission tests only, executed by the full workspace gate; it never owns the named wire filters. Translation fixtures live in tests/command_code_translate.rs and execute via --test command_code_translate. This contract supersedes ambiguous conformance-file artifact descriptions below. Each filtered invocation must visibly run at least one test and all named fixtures assigned to that group.

<task type="auto" tdd="true">
<name>Task 1: Full conformance matrix — subagent/continuation/tool-heavy/long-context/auth/error</name>
<files>tests/command_code_conformance.rs, src/adapters/command_code/router_tests.rs</files>
<read_first>All prior plan artifacts; 14-EDGE-COVERAGE.json CCS-08 rows; 14-CONTEXT.md D-12/D-14; 14-VALIDATION.md per-task map</read_first>
<behavior>CCS-08/CCK-03/D-12. Fixtures named command_code_matrix_*: positive plaintext subagent tool turn and a continuation history turn (positive cases required, rejection-only coverage insufficient); tool-heavy conversation with many authentic adjacent units; long-context below and at the configured bound accepted, above rejected before generation; auth-error matrix (env invalid/absent, file missing/malformed) yielding the correct gateway error shapes; provider error finish consumed as failed turn with usage retained. Each product's unauthenticated 401 path renders the gateway-owned error shape for its inbound protocol.</behavior>
<action>Write red fixtures first, then close any behavioral gap they expose (no weakening of earlier plans' semantics). Reuse the established harness; keep filters contiguous. Phase 13 (2875 passing) is not reopened — only new files/tests are added.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib command_code_matrix -- --test-threads=1</automated><fails_when>non-zero exit; any positive subagent/continuation case is missing; long-context above bound generates; an auth-error case SKIPs instead of failing; or the filter selects zero tests</fails_when></verify>
<done>The full scenario matrix executes green over the real gateway for both products.</done>
</task>
<task type="auto">
<name>Task 2: Docs — README+3 locales, milestone note, site provider page+3 locales, configuration reference+3 locales</name>
Task 2 additionally owns site/src/lib/i18n.ts and site/src/content/docs/guides/providers.mdx plus site/src/content/docs/ko/guides/providers.mdx, site/src/content/docs/ja/guides/providers.mdx and site/src/content/docs/zh-cn/guides/providers.mdx. Update all four guide pages and verify the built locale pages in this task. Before acceptance also run rg -q SHUNT_COMMAND_CODE_TOKEN README.ja.md and rg -q SHUNT_COMMAND_CODE_TOKEN README.zh-CN.md; both must succeed, supplementing the automated checks below.
Task 2 also owns THIRD-PARTY-NOTICES.md: preserve the full pinned OpenCodex MIT copyright/permission/warranty notice from the protocol ledger in this shipped, non-planning surface for source-derived translations. Verify against the pinned source LICENSE, identify the adapted subsystem, and link from docs/m16-command-code.md. Attribution only under .planning is insufficient because release/PR filtering may omit it. Preserve existing notices if the file appears before execution.
<files>README.md, README.ko.md, README.ja.md, README.zh-CN.md, docs/m16-command-code.md, site/src/content/docs/providers/command-code.md, site/src/content/docs/ko/providers/command-code.md, site/src/content/docs/ja/providers/command-code.md, site/src/content/docs/zh-cn/providers/command-code.md, site/src/content/docs/reference/configuration.md, site/src/content/docs/ko/reference/configuration.md, site/src/content/docs/ja/reference/configuration.md, site/src/content/docs/zh-cn/reference/configuration.md</files>
<read_first>AGENTS.md Documentation section; 14-CONTEXT.md D-14; README.md provider table; docs/ milestone-note style (e.g. docs/m15* if present); site/src/content/docs/providers/ page shape (e.g. an existing provider page); site/src/content/docs/reference/configuration.md preset/env conventions; anchor rule: locale ids from built site/dist HTML, else link the English page</read_first>
<behavior>D-14/AGENTS.md. Document both products accurately: preset names, env vars (SHUNT_COMMANDCODE_API_KEY for API-key; SHUNT_COMMAND_CODE_TOKEN for subscription), endpoints, supported model/effort table (exact ten admitted arrays; Luna, Gemini 3.7 Flash and vision-exp excluded as reporter-only/unverified), read-only credential sources, no version override, and the Phase 16 live-availability caveat. Every English change ships with ko/ja/zh-cn copies in the same task; no fragment link is added without confirming the locale anchor exists (or linking the English page). wiki/ is not touched. No unverified live-availability claims.</behavior>
<action>Update README.md first, then mirror the change into the three locale READMEs (translate, do not machine-diff); write docs/m16-command-code.md in the established milestone-note style; create the site provider page with three locale copies; add configuration reference rows for the new preset/env names with locale copies. Update the provider guide and sidebar labels in site/src/lib/i18n.ts with all maintained locales. Confirm locale fragment IDs against built site/dist/<locale>/.../index.html; use fragment-free links when no matching translated section exists. Docs are the only file surface shared with root's ownership boundary — root owns commits, this plan owns content.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs npm --prefix site run build && rg -q "SHUNT_COMMANDCODE_API_KEY" README.md && rg -q "SHUNT_COMMAND_CODE_TOKEN" README.md && rg -q "SHUNT_COMMANDCODE_API_KEY" README.ko.md && rg -q "SHUNT_COMMAND_CODE_TOKEN" README.ko.md && rg -q "SHUNT_COMMANDCODE_API_KEY" README.ja.md && rg -q "SHUNT_COMMANDCODE_API_KEY" README.zh-CN.md && rg -q "SHUNT_COMMAND_CODE_TOKEN" site/src/content/docs/reference/configuration.md && test -s docs/m16-command-code.md && test -s site/src/content/docs/ko/providers/command-code.md && test -s site/src/content/docs/ja/providers/command-code.md && test -s site/src/content/docs/zh-cn/providers/command-code.md</automated><fails_when>site build fails; an English doc surface changed without its three locale copies; an env name is wrong or swapped between products; wiki/ files were modified; or a locale file is empty</fails_when></verify>
<done>All affected doc surfaces carry accurate en+ko+ja+zh-cn content with no locale drift and no wiki edits.</done>
</task>
<task type="auto">
<name>Task 3: Final gates + owned CLI/curl smoke + validation-state recording</name>
<files>tests/command_code_conformance.rs (read-only if green), .planning/phases/14-command-code-product-separation/14-VALIDATION.md (draft update)</files>
<read_first>AGENTS.md Build & Run; 14-VALIDATION.md current draft; D-13/D-14; prior phase verify chains (13-*-SUMMARY.md prior_verify_commands)</read_first>
<behavior>D-13/D-14. The full gate chain passes through node /tmp/shunt-phase12-isolated-run.cjs: cargo fmt --all --check; cargo clippy --all-targets --all-features -- -D warnings; env 'RUSTFLAGS=-D warnings' cargo test --all-features --workspace; npm --prefix site run build. Owned smoke: the isolated binary serves a loopback (non-10100) gateway and curl unary and streaming API-key turns against a mock return the expected shape, captured path/headers/body are asserted, and malformed request/config cases fail correctly. For the ordinary binary, subscription smoke covers config validation and invalid-explicit-token errors (no CLI-file fallback and no network generation); successful subscription transport is separately exercised by the crate-local canonical-host TLS router harness. No public origin bypass or system DNS mutation is permitted. 14-VALIDATION.md draft gains the real task-id table with commands (frontmatter stays draft; nyquist_compliant stays false until actual validation).</behavior>
<action>Run the chained gates one cargo at a time, quoted 'RUSTFLAGS=-D warnings', zero-filtered-test guards on every filter mention. Smoke uses the established isolation wrapper with fresh OPENCODEX_HOME and non-10100 ports; before/after, verify /Users/user/.opencodex config mtime+SHA-256 unchanged and backup inventory unchanged per AGENTS.md (read-only checks). Update the 14-VALIDATION.md per-task table (root owns commits; the draft content edit is planner-owned). Record the Phase 16 live gate explicitly in Manual-Only Verifications.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings && node /tmp/shunt-phase12-isolated-run.cjs env 'RUSTFLAGS=-D warnings' cargo test --all-features --workspace && node /tmp/shunt-phase12-isolated-run.cjs npm --prefix site run build</automated><fails_when>any chained invocation exits non-zero; any test filter in this phase's plans selects zero tests; the smoke hits port 10100 or /Users/user/.opencodex; or the live home's mtime/SHA-256 changes unexpectedly</fails_when></verify>
<done>All final gates and the owned smoke pass; the validation draft records executed task ids, commands, and the explicit Phase 16 gate.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundaries: documentation surfaces (public), the smoke harness (network-listening), and the live home boundary. High information threat: docs advertising unverified capabilities or leaking live-credential paths — mitigate pinned-facts-only content, all three reporter-only rows excluded, and the Phase 16 caveat. High integrity threat: smoke touching production user state — mitigate the isolation wrapper, fresh OPENCODEX_HOME, non-10100 ports, and before/after mtime/SHA-256 + inventory checks that halt on drift. Medium tampering: locale drift leaving a stale security note in one language — mitigate same-task locale mirroring and build-gated site output. Docs-only surfaces add no runtime attack surface.
</threat_model>
<verification><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings && node /tmp/shunt-phase12-isolated-run.cjs env 'RUSTFLAGS=-D warnings' cargo test --all-features --workspace && node /tmp/shunt-phase12-isolated-run.cjs npm --prefix site run build</automated><fails_when>any chained invocation exits non-zero</fails_when></verification>
<success_criteria>The phase closes green: full scenario matrix executed, all doc surfaces updated with maintained locales, final gates and owned smoke passed, and the validation state honestly records the Phase 16 live gate.</success_criteria>

## Artifacts this phase produces

Phase closure artifacts: complete matrix coverage, documentation parity across all maintained locales, and the executed validation record.
