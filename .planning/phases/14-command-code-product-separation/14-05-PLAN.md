---
phase: 14-command-code-product-separation
plan: "05"
type: execute
wave: 4
depends_on: ["04"]
files_modified:
  - src/auth/command_code.rs
  - src/adapters/command_code/request.rs
  - src/adapters/command_code/mod.rs
  - src/adapters/command_code/router_tests.rs
  - src/adapters/command_code/router_tests/lifetime.rs
  - src/adapters/command_code/router_tests/replay.rs
  - tests/command_code_conformance.rs
autonomous: true
requirements:
  - CCS-01
  - CCS-02
  - CCS-07
must_haves:
  truths:
    - "CLI file fallback ~/.commandcode/auth.json is read-only with schema {apiKey: nonempty, userId?}, timeout-bounded, no write/refresh/copy; explicit env still takes precedence and present-but-invalid explicit never falls back (D-03/CCS-01)."
    - "Destination validation runs before credential lookup and again at dispatch; invalid configured destinations (http, lookalike host, wrong suffix, userinfo, query, fragment) produce zero credential reads and requests; a 30x redirect permits only the initial credential snapshot and zero redirected requests/new lookups (D-04/CCS-02)."
    - "Generation POST pins RetrySafety::ConnectOnly: only proven pre-connect failure retries; post-send timeout is single-attempt; a safe pre-connect replay retains the same credential and session; no refresh or rotation (D-10/CCS-07)."
    - "Cancellation before headers and mid-body in both modes drops upstream work, parser state, and gateway capacity through ownership with no detached recovery task (D-11)."
  artifacts:
    - path: src/auth/command_code.rs
      provides: "read-only CLI file fallback with credential timeout, env precedence unchanged, file-unchanged guarantees"
    - path: src/adapters/command_code/request.rs
      provides: "destination gate invoked pre-credential and at dispatch"
    - path: src/adapters/command_code/mod.rs
      provides: "ConnectOnly send discipline, replay identity retention, ownership-based cancellation"
    - path: src/adapters/command_code/router_tests.rs
      provides: "destination negatives, credential matrix, replay discipline, real-socket cancellation fixtures"
  key_links:
    - from: src/adapters/command_code/request.rs
      to: src/auth/command_code.rs
      via: "destination gate precedes resolver call; resolver never called on non-canonical destination"
      pattern: "alpha/generate|SHUNT_COMMAND_CODE_TOKEN"
    - from: src/retry.rs
      to: src/adapters/command_code/mod.rs
      via: "COMMAND_CODE_RETRY_SAFETY = RetrySafety::ConnectOnly governs all send attempts"
      pattern: "ConnectOnly"
  prohibitions:
    - statement: "No post-send/ambiguous replay, no output or tool-activity replay, no credential writeback/refresh/rotation, no public loopback bypass for live tokens; test-only TLS/DNS seam must not be a public off-origin bypass (D-04/D-10)."
      status: flagged
      verification: unverified
---

<objective>Harden the subscription lifetime: CLI-file fallback, full destination/credential matrices, ConnectOnly replay discipline with identity retention, and ownership-based cancellation proven over real sockets.</objective>

<test_isolation_contract>Credential tests inject an explicit temporary file path through a crate-private test helper; they must never resolve the developer's real ~/.commandcode/auth.json or mutate process-global HOME during parallel tests. Production keeps the fixed read-only CLI path. Bound file bytes as well as lookup duration and validate a nonempty header-safe bearer; never log source contents. Auth fixture tests live crate-locally when private injection is required. The isolated top-level runner alone does not redirect the Command Code CLI home.</test_isolation_contract>

<tasks>
Implementation layout clarification: real-socket lifetime fixtures may live in
the private router_tests/lifetime.rs child module, declared only by the existing
cfg(test) router_tests module. This keeps each Rust file focused and under the
preferred 500-line boundary. Wire ownership remains the router test module tree;
no public conformance seam or production endpoint override is added.

Fixture ownership contract: all subscription gateway wire fixtures (command_code_tracer_*, command_code_lifetime_*, command_code_matrix_*, and gateway command_code_bounds_*) live exclusively in src/adapters/command_code/router_tests.rs, declared cfg(test), and execute through --lib. Resolver-only fixtures live in the crate-local auth module. tests/command_code_conformance.rs contains public config/admission tests only, executed by the full workspace gate; it never owns the named wire filters. Translation fixtures live in tests/command_code_translate.rs and execute via --test command_code_translate. This contract supersedes ambiguous conformance-file artifact descriptions below. Each filtered invocation must visibly run at least one test and all named fixtures assigned to that group.

<task type="auto" tdd="true">
<name>Task 1: CLI file fallback + destination-at-dispatch ordering</name>
<files>src/auth/command_code.rs, src/adapters/command_code/request.rs, tests/command_code_conformance.rs, src/adapters/command_code/router_tests.rs</files>
<read_first>14-02 Task 0 ledger auth.json section; src/oauth/command-code.ts at 055c3ecf0de6c35f59195fc434d6b08525182b7f (schema only, re-verified); src/auth/mod.rs:513/551 patterns; 14-CONTEXT.md D-03/D-04; 14-EDGE-COVERAGE.json CCS-02 row (unclassified — flagged)</read_first>
<behavior>CCS-01/CCS-02/D-03/D-04. When no explicit env source is present, the resolver reads a fixed ~/.commandcode/auth.json inside the test home: {apiKey: nonempty string, userId?} — missing file, empty apiKey, malformed JSON, and unreadable file each yield a normal authentication error; a valid file authenticates without modification. Precedence: valid explicit env wins even when a valid file exists; invalid explicit env fails closed (never reading the file). File state (bytes/mtime/inventory) is unchanged after every scenario. Destination-at-dispatch: even when pre-credential validation passed, a mutated destination at dispatch re-fails before the bearer is attached (gate observable via lookup counter). Reuse the crate-local test-only TLS/DNS injection seam created in 14-02 is confined to the test harness and is never a public loopback bypass for live tokens.</behavior>
<action>Write red fixtures first (command_code_lifetime_file_*, command_code_lifetime_destination_dispatch_*), then implement the fallback with with_credential_timeout-style bounds and no write path; re-invoke the destination gate at dispatch in request.rs. No schema discovery from real credentials — the ledger's schema is authoritative. Ask-before-changing writeback rules is moot: there is no writeback.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib command_code_lifetime_file -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib command_code_lifetime_destination_dispatch -- --test-threads=1</automated><fails_when>non-zero exit; the file is read while an invalid explicit env is set; any scenario mutates file bytes/mtime/inventory; a dispatch-time destination change attaches the bearer; or a filter selects zero tests</fails_when></verify>
<done>Read-only fallback with strict precedence and dispatch-time destination re-validation is proven.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Replay discipline — ConnectOnly, pre-connect identity retention, no post-send replay</name>
<files>src/adapters/command_code/mod.rs, tests/command_code_conformance.rs, src/adapters/command_code/router_tests.rs</files>
<read_first>src/retry.rs:142/160 (ConnectOnly + CURSOR_RETRY_SAFETY pattern); 14-02/14-04 adapter artifacts; 14-CONTEXT.md D-10; 14-EDGE-COVERAGE.json CCS-07 rows</read_first>
<behavior>CCS-07/D-10. Fixtures named command_code_lifetime_replay_*: a genuine pre-connect connect error is classified BeforeHeaders and retried exactly once on a fresh socket with the SAME credential snapshot and SAME session id (identity retention asserted at the wire); a post-send timeout yields a single attempt with no second request; observed output/tool activity is never replayed; a 302 response is a terminal failure with zero follow-ups; no refresh/rotation call exists on the retry path.</behavior>
<action>Write red fixtures, then assert/extend the send path: COMMAND_CODE_RETRY_SAFETY = ConnectOnly already pinned in 14-02; prove classification and identity retention end to end. Pre-connect replay reuses the request's captured credential+session; nothing rebuilds credentials mid-retry. No changes to the shared retry vocabulary.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib command_code_lifetime_replay -- --test-threads=1</automated><fails_when>non-zero exit; a post-send failure triggers a second request; the retry exchanges credential or session; a redirect is followed; or the filter selects zero tests</fails_when></verify>
<done>Replay is conservatively bounded with provable credential/session retention and no unsafe post-send behavior.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: Cancellation ownership — real sockets, both modes, upstream closure and capacity reuse</name>
<files>src/adapters/command_code/mod.rs, tests/command_code_conformance.rs, src/adapters/command_code/router_tests.rs</files>
<read_first>Phase 13 cancellation fixtures (owned-server-task drop pattern); 14-CONTEXT.md D-11; 14-EDGE-COVERAGE.json CCS-07 concurrency row</read_first>
<behavior>CCS-07/D-11. Real-listener fixtures named command_code_lifetime_cancel_*: dropping the client before headers and mid-body in BOTH streaming and unary modes closes the upstream connection (mock observes EOF/socket closure) and frees request capacity (a subsequent request on the same gateway instance is admitted and served); no detached recovery task or persistent lease registry appears; parser state dies with the owning tuple.</behavior>
<action>Write red fixtures, then verify/extend the unfold tuple ownership from 14-04 for both modes with the established await-owned-server-task proof pattern. Capacity reuse is proven by serving a follow-up request after the cancel completes, not by inferring from internal counters.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib command_code_lifetime_cancel -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings</automated><fails_when>non-zero exit; upstream work persists after cancel; capacity is not reusable; a recovery task leaks; or a filter selects zero tests</fails_when></verify>
<done>Cancellation drops all owned work through existing lifetime discipline with proven capacity reuse.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundaries: filesystem credential path, network destination, and socket lifetime. High information threat: the bearer escaping to a non-canonical origin via redirect, mutation, or fallback confusion — mitigate pre-credential and at-dispatch gates with lookup-counter proof, Policy::none, and zero-follow-up fixtures. High tampering threat: replay of a generation after observed output could duplicate upstream effects — mitigate ConnectOnly classification proven at the wire with single-attempt fixtures. High integrity threat: credential file mutation during resolution — mitigate read-only code paths and bytes/mtime/inventory assertions. Medium DoS: leaked upstream work after cancel — mitigate ownership drop with real-socket closure and capacity-reuse proof. Test-only TLS/DNS injection stays harness-confined so no public off-origin bypass exists.
</threat_model>
<verification><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib command_code_lifetime -- --test-threads=1 && node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings && node /tmp/shunt-phase12-isolated-run.cjs env 'RUSTFLAGS=-D warnings' cargo test --all-features --workspace</automated><fails_when>any chained invocation exits non-zero or any filter selects zero tests</fails_when></verification>
<success_criteria>Subscription lifetime safety is fully proven: read-only credential sources with strict precedence, destination gates before credentials and at dispatch, ConnectOnly replay with identity retention, and ownership-based cancellation.</success_criteria>

## Artifacts this phase produces

The complete lifetime contract for both products' shared safety vocabulary, ready for the full-matrix/docs plan in 14-06.
