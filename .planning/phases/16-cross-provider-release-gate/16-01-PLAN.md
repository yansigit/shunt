---
phase: phase-16-cross-provider-release-gate
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - docs/provider-release-evidence.md
  - tests/release_matrix.rs
requirements: [REL-01, REL-02]
must_haves:
  truths:
    - "REL-01/adjacency: duplicate provider/auth/exact-model/wire identities are rejected."
    - "REL-01/empty: missing rows and empty required scenarios fail; Go admissions are explicitly empty."
    - "REL-01/ordering: rows and scenario mappings are deterministic independent of test order."
    - "REL-01/idempotency: repeated hermetic validation has identical results and sends no live requests."
    - "REL-01/concurrency: owned test servers clean up on cancellation; Cargo and live-budget ownership are serialized."
    - "D-01/REL-01: every finite supported provider/auth/exact-model/wire tuple has deterministic unique identity, named scenario evidence, or a not_applicable rationale; OpenCode Go remains admitted=[] per D-02."
    - "D-10/REL-02: each source-derived row records repository, pinned revision, inspected date, and sanitization status without secrets or private identifiers."
  artifacts:
    - path: docs/provider-release-evidence.md
      provides: Sanitized machine-readable release ledger with exact tuple rows and scenario mappings.
    - path: tests/release_matrix.rs
      provides: Deterministic parser, duplicate/empty/missing-field rejection, and zero-Go-admission assertions.
  key_links:
    - from: src/config/presets.rs and actual adapter wires
      to: docs/provider-release-evidence.md
      via: finite inventory expansion, with Cursor active agent URL recorded from agent.rs.
  prohibitions:
    - statement: "No family inference, arbitrary-model claims, Go promotion, or host model dispatch is treated as protocol evidence."
      status: unresolved
      verification: judgment
---
<objective>Build the exact cross-provider evidence ledger and focused validator that closes the demonstrated traceability gap without changing runtime behavior.</objective>
<execution_context>
@.planning/phases/16-cross-provider-release-gate/16-CONTEXT.md
@.planning/phases/16-cross-provider-release-gate/16-PATTERNS.md
@.planning/phases/16-cross-provider-release-gate/16-PLANNER-INPUTS.md
@.planning/phases/16-cross-provider-release-gate/16-RESEARCH.md
</execution_context>
<threat_model>ASVS level 1; block high. Spoofing risk from duplicate or family-inferred tuple rows is mitigated by canonical four-field identity and duplicate rejection. Tampering risk from fabricated provenance or Go support is mitigated by required source metadata, sanitized fields, and admitted=[] validation. Information disclosure risk is mitigated by rejecting secret-like/private-content fields.</threat_model>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: RED-first release ledger validator</name>
<files>tests/release_matrix.rs</files>
<read_first>tests/opencode_go_evidence.rs; tests/opencode_go_docs.rs; src/config/presets.rs; src/adapters/cursor/agent.rs; 16-PATTERNS.md</read_first>
<behavior>Test parser/validate functions reject duplicate tuple identities, null or empty required scenario entries, missing provenance fields, unsanitized secret markers, and non-empty Go admissions; tests compile against a temporary fixture path before the ledger exists and fail behaviorally.</behavior>
<action>Create a focused integration test with env!("CARGO_MANIFEST_DIR") path resolution and one fenced JSON parser. Encode deterministic ordering, idempotent repeated validation, exact tuple uniqueness, required scenario keys (normal, stream, tools, terminal, malformed, truncation, auth, cancellation, retry), explicit not_applicable rationale, provenance schema, and D-02 empty Go admission. Include a mutation test that removes each required field and asserts failure. Do not add a generalized catalog or runtime seam.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test release_matrix release_matrix -- --test-threads=1</automated><fails_when>The filter selects zero tests, RED is a compile failure, or validation accepts duplicate/empty/secret-bearing/non-empty-Go fixtures.</fails_when></verify>
<acceptance_criteria>Validator tests compile and fail only on behavioral assertions while the artifact is absent or invalid.</acceptance_criteria>
<done>RED evidence is captured for all ledger invariants and no runtime files are modified.</done>
</task>
<task type="auto">
<name>Task 2: Populate finite exact-tuple ledger and provenance map</name>
<files>docs/provider-release-evidence.md; tests/release_matrix.rs</files>
<read_first>Task 1; src/config/presets.rs; src/config/*.rs; src/adapters/*; tests/gemini_conformance.rs; tests/openai_chat_conformance.rs; tests/command_code_api_conformance.rs; tests/codex_websocket_fallback.rs; tests/failover.rs; tests/retry.rs; tests/passthrough.rs; tests/inbound_codex_endpoint.rs; tests/inbound_codex_websocket.rs; src/adapters/cursor/*_tests.rs; tests/opencode_go_evidence.rs</read_first>
<action>Write the sanitized ledger rows from documented exact routes and configured model declarations only; distinguish preset seed from active wire, record Cursor https://agentn.global.api5.cursor.sh/agent.v1.AgentService/Run, include custom Gemini/Antigravity/generic Vercel contracts, and keep arbitrary models out. Link named existing tests for each applicable scenario and give rationale for every not_applicable. Record source repository/revision/date/sanitization for translated material and explicit capture/live fields; keep Go admitted=[] and no live inference per D-01/D-02/D-03. Extend tests only as needed for exact row count/order and run mutation failure then restore GREEN.</action>
<verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test release_matrix release_matrix -- --test-threads=1</automated><fails_when>Any row lacks named evidence or rationale, rows are nondeterministically ordered, Cursor uses the preset URL, provenance is incomplete, secrets/private identifiers appear, Go admissions become non-empty, or zero tests are selected.</fails_when></verify>
<acceptance_criteria>REL-01 and REL-02 are machine-checked for the finite supported surface with separate source/capture/live/static classes.</acceptance_criteria>
<done>Ledger validator passes with a recorded mutation failure and restored GREEN result.</done>
</task>
</tasks>
## Artifacts this phase produces

- docs/provider-release-evidence.md: permanent release ledger, retained when planning is archived.
- tests/release_matrix.rs: parser and validator helpers plus named happy-path and mutation tests (exact helper names chosen locally; no public symbols).
- Recorded RED/mutation/GREEN results in the plan summary.

The test reads only the durable docs artifact, never an active planning directory,
git-dirty state, or the external isolation wrapper. Existing source fixtures remain
separate from live results. Missing applicable protocol coverage requires focused
behavior tests at the existing seam, not a not_applicable label hiding a gap;
surface any new source-file ownership needed before editing outside this plan.
