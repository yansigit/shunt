---
phase: 12-cursor-evidence-backed-hardening
plan: "03"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - src/adapters/cursor/request.rs
  - src/adapters/cursor/mod.rs
autonomous: true
requirements:
  - CUR-03
must_haves:
  truths:
    - "Stable structured call/result identity is implemented (or genuinely fixed where absent) across ordinary, compacted, recovered, and multi-round histories — proven by an authentic outgoing-to-inbound roundtrip, not by byte-identical prompt text alone (A2, D-03)."
    - "The identity guard is integrated in mod.rs span-visibility-of-dispatch: unsupported opaque continuation errors explicitly before any dispatch, with the error propagated through the adapter path (D-03)."
  artifacts:
    - path: src/adapters/cursor/request.rs
      provides: structured identity propagation across four history kinds plus renderer stateless habit
    - path: src/adapters/cursor/mod.rs
      provides: continuation-guard integration and no-dispatch loopback proof
  key_links:
    - from: src/adapters/cursor/request.rs
      to: src/adapters/cursor/mod.rs
      via: guard rejection short-circuits dispatch in the adapter core
      pattern: cursor_continuation_guard
  prohibitions:
    - statement: "No invented call/result IDs, no durable continuation store, no heuristic identity guessing from prompt text (D-03, D-09)."
      status: flagged
      verification: unverified
---

<objective>Implement stable structured call/result identity where it is absent and integrate the explicit continuation guard with no-dispatch proof.</objective>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Structured identity implementation and roundtrip proof (CUR-03)</name>
<files>src/adapters/cursor/request.rs</files>
<read_first>src/adapters/cursor/request.rs rendering and history extraction, src/adapters/cursor/mod.rs history-to-outgoing conversion, src/adapters/cursor/model.rs inbound tool_use/tool_result id fields, .planning/phases/12-cursor-evidence-backed-hardening/12-RESEARCH.md Section 4.4, .planning/phases/12-cursor-evidence-backed-hardening/12-CONTEXT.md code_context + D-03</read_first>
<behavior>CUR-03/D-03: inspect the actual identity chain first — inbound request id versus conversation identity versus the emitted tool call ID — then scope what is authentic: (a) ordinary tool_use/tool_result histories preserve the client-provided ids end-to-end (outgoing-to-inbound roundtrip), (b) compacted, recovered, and multi-round histories retain that call/result identity across turns, (c) where the renderer currently lacks structured identity field use, it is fixed to carry ids structurally rather than flattening prompt text, with no invented IDs anywhere. Byte-identical re-render may be asserted only as a supporting statelessness property, never as the identity proof.</behavior>
<action>Read the actual structs first (request_id, conversation id, tool call id field names/slots on this wire). Then write the failing roundtrip fixture: a request with a tool_use followed by its tool_result re-renders preserving both ids exactly as provided, including across a compacted/recovered/multi-round variant. Implement the minimal structural change in request.rs so ids persist structurally through rendering; no store, no synthesized ids. Update honest provenance comments. If a history kind structurally cannot carry a paired id, that kind is unsupported and handled by Task 2, not fabricated here.</action>
<verify><automated>cargo test --all-features cursor_history_identity -- --test-threads=1</automated><fails_when>non-zero exit, any fixture shows a missing/unpaired id, or zero selected tests</fails_when></verify>
<acceptance_criteria>Authentic ids round-trip structurally across all four supported history kinds; no invented ids; statelessness assertion secondary but passing.</acceptance_criteria>
<done>Structured identity implementation is in place with roundtrip evidence.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Continuation guard integration in mod.rs (CUR-03/D-03)</name>
<files>src/adapters/cursor/request.rs, src/adapters/cursor/mod.rs</files>
<read_first>src/adapters/cursor/request.rs entry points, src/adapters/cursor/mod.rs call sites around :95 and dispatch ownership, src/adapters/cursor/client.rs CursorError variants</read_first>
<behavior>D-03: opaque/unreconstructable continuation produces an explicit CursorError before dispatch, propagated through the adapter interface; never re-rendered as a fresh conversation; malformed stable ID absence is also explicitly unsupported, not silently dropped.</behavior>
<action>Add the guard in request.rs classification, then wire the error into mod.rs at the pre-dispatch seam so the adapter interface propagation is proven: loopback fixture asserts HTTP 200 never emitted, no Run POST reaches the gateway (mock route observes zero dispatch), and the error surfaces in the gateway error shape. Keep every existing test green; no public config or store; document that the guard belongs to mod.rs dispatch ownership and request.rs classification solely (no file-sharing with other plans — dependency edges handle serialization).</action>
<verify><automated>cargo test --all-features cursor_continuation_guard -- --test-threads=1</automated><fails_when>non-zero exit, any Run POST observed on rejection, or zero selected tests</fails_when></verify>
<acceptance_criteria>Unsupported opaque continuation errors explicitly with zero dispatch attempts, proven end-to-end through mod.rs.</acceptance_criteria>
<done>The guard closes silent-fresh-conversation with adapter-integrated proof.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundary is client history input. High tampering threat: malformed history could repeat tool execution or mispair results; mitigate explicit rejection and roundtrip identity assertions. Medium spoofing threat: fabricated call ids; mitigate no-invented-id rule tests. Low DoS threat: oversized history; mitigate bounded fixtures only.
</threat_model>
<verification><automated>cargo test --all-features cursor_history_identity -- --test-threads=1 && cargo test --all-features cursor_continuation_guard -- --test-threads=1</automated><fails_when>either invocation exits non-zero or selects zero tests; sequential chain stops at first failure</fails_when></verification>
<success_criteria>CUR-03 structured identity and integrated continuation rejection both proven hermetically.</success_criteria>

## Artifacts this phase produces

Structured identity propagation and the adapter-integrated continuation guard.
