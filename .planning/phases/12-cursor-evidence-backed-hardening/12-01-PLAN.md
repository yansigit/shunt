---
phase: 12-cursor-evidence-backed-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/cursor/agent.rs
  - src/adapters/cursor/connect.rs
  - src/adapters/cursor/test_frames.rs
  - src/adapters/cursor/router_parity_tests.rs
  - src/server.rs
  - Cargo.toml
autonomous: true
requirements:
  - CUR-05
  - CUR-06
must_haves:
  truths:
    - "A tiny production end-to-end terminal fix ships first: a stream that ends by EOF without an authoritative terminal emits exactly one error event and never a synthesized success End, proven red-first over a genuine whole-gateway-router loopback test before any later terminal work in 12-05/12-06."
    - "Wire-fact fixtures for output-token deltas and absolute context occupancy are asserted with a schema-derived local test decoder — independent of the current production decoder, which does not yet consume them — with dated OpenCodex schema provenance before any usage implementation (D-10, E1)."
    - "Byte corpora cover field-number zero, varint overflow, truncated length-delimited fields, unsupported wire encodings, and corrupt versus valid gzip END trailers before 12-06 flips them (W0-2)."
  artifacts:
    - path: src/adapters/cursor/agent.rs
      provides: EOF-without-authoritative-terminal error tracer (production change, exactly one error, no success terminal)
    - path: src/adapters/cursor/router_parity_tests.rs
      provides: whole-gateway-router loopback tracer fixtures over a hermetic TLS loopback (owned by 12-01; later extended only by 12-08 cancellation probe, serialized transitively via deps 05/07)
    - path: src/server.rs
      provides: reuses the existing build_router_with_dependencies client/resolver seam (server.rs :247) for test-only client construction; no production-signature edit, production behavior unchanged
    - path: Cargo.toml
      provides: test-only dev-dependencies for the router harness (rustls test server + cert generation); no runtime dependency or config change
    - path: src/adapters/cursor/test_frames.rs
      provides: schema-derived usage/terminal wire-fact encoding fixtures with dated provenance (W0-1)
    - path: src/adapters/cursor/connect.rs
      provides: corpus byte helpers with corrupt-gzip versus valid-gzip contrast (W0-2)
  key_links:
    - from: src/adapters/cursor/agent.rs
      to: src/adapters/cursor/router_parity_tests.rs
      via: tracer drives the production EOF path end-to-end through server::build_router over a TLS loopback with pinned DNS and a synthetic SHUNT_CURSOR_AUTH_FILE credential — the full gateway router, not the adapter-only wiremock unit
      pattern: cursor_terminal_tracer|FLAG_END
  prohibitions:
    - statement: "Corrupt gzip junk is rejected; only valid gzip-decoded JSON and valid empty trailers decode (Q3)."
      status: flagged
      verification: unverified
    - statement: "Do not claim CLI cache/input usage fields as proof of new protobuf field numbers; do not weaken the cursor.sh https origin guard or the pinned AGENT_BASE_URL (D-01)."
      status: flagged
      verification: unverified
---

<objective>Lead with a real production EOF-terminal tracer proven over the whole gateway router, then land the W0-1/W0-2 evidence fixtures that later plans consume.</objective>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Tracer — EOF without an authoritative terminal errors once, proven over the full gateway router</name>
<files>src/adapters/cursor/agent.rs, src/adapters/cursor/router_parity_tests.rs</files>
<read_first>src/adapters/cursor/agent.rs ReadState ingest :285-330, terminal_event :349-357, FLAG_END handling :387-397, and terminal_event characterization tests :1124-1148; src/adapters/cursor/connect.rs FLAG_END handling; src/server.rs build_router :225-250 and AppState::new :88-105; src/auth/mod.rs :582 SHUNT_CURSOR_AUTH_FILE; tests/antigravity_catalog.rs loopback pattern :47-140; .planning/phases/12-cursor-evidence-backed-hardening/12-RESEARCH.md Sections 4.1-4.2; 12-CONTEXT.md D-05</read_first>
<behavior>CUR-05/CUR-06/D-05 (moved out of 12-05): a stream that ends by EOF (Ok(None) from the body) without an authoritative terminal produces exactly one explicit error event and never CursorStreamEvent::End; a stream that received the authoritative END terminal before EOF completes with exactly one success End. Exactly one terminal-or-error outcome per turn — never End-then-error. Detection is bounded to bytes and frames actually received before EOF; the plan promises no detection of arbitrary future bytes after stream cancellation (a cancelled read stops observing the wire; buffered pre-cancellation bytes remain covered). This closes the terminal-synthesis hole at the EOF path first; duplicate/post-terminal and idle handling stay owned by 12-05.</behavior>
<action>Write the failing whole-gateway-router fixture first: create src/adapters/cursor/router_parity_tests.rs and wire it by adding exactly one #[cfg(test)]-gated declaration line inside agent.rs (no mod.rs wiring line needed — 12-02 owns mod.rs in the same wave), keeping all test TLS/DNS/cert code out of the production build. The module spins up the real axum server by reusing the existing build_router_with_dependencies client/resolver seam (server.rs :247) with a test-only reqwest client, pins agentn.global.api5.cursor.sh to loopback HTTPS (an in-test-generated root certificate is added to the test client — no existing helper, so 12-01 builds it test-only; no origin-guard weakening, AGENT_BASE_URL bytes pinned unchanged), and injects a synthetic credential via SHUNT_CURSOR_AUTH_FILE. Then implement the minimal agent.rs production fix: the EOF path pushes a success terminal only when an authoritative terminal was received from the wire (FLAG_END reached ingest), otherwise exactly one error event — gated through terminal_event with an explicit terminal-received parameter or equivalent minimal seam. Update the characterization test terminal_event_clean_eof_ends_even_without_output :1137-1141 in this plan to the stricter expectation and disclose the flip in the summary. Preserve pacing comments :20-23 and :293-303 untouched. Idle semantics stay with 12-05.</action>
<verify><automated>cargo test --all-features cursor_terminal_tracer -- --test-threads=1</automated><fails_when>non-zero exit, any fixture shows a synthesized success End after EOF, more than one error event, missing gateway-router loopback coverage (adapter-only harness labeled as router), or the filter selects zero tests</fails_when></verify>
<acceptance_criteria>EOF-without-terminal yields exactly one error and no success End through the full gateway router; terminal-then-EOF yields exactly one End; the changed characterization test is disclosed; red-first evidence exists.</acceptance_criteria>
<done>The tracer proves the production EOF fix over the whole gateway router before any corpus work.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: W0-1 usage/terminal wire-fact encoding fixtures</name>
<files>src/adapters/cursor/test_frames.rs</files>
<read_first>src/adapters/cursor/test_frames.rs, .planning/phases/12-cursor-evidence-backed-hardening/12-RESEARCH.md Section 5, .planning/phases/12-cursor-evidence-backed-hardening/12-CONTEXT.md D-02/D-09</read_first>
<behavior>W0-1/E1/D-10: fixtures encode InteractionUpdate field 8 TokenDeltaUpdate field 1 (int32 tokens) and ConversationStateStructure field 5 ConversationTokenDetails field 1 (uint32 used_tokens) per OpenCodex rev 055c3ecf0de6c35f59195fc434d6b08525182b7f; checkpoints 10000 then 10300 plus delta 42 combine to context 10300, never 20300 (CUR-05/D-02). Derived input is labeled inferred; absence is null or omitted, never zero filled as authentic. These are encoding assertions validated by a schema-derived local test decoder — NOT claims that the unchanged production decoder consumes them today; production consumption flips only in the 12-05 usage relay.</behavior>
<action>Extend test_frames with dated provenance builders for: single output delta, two-context-checkpoint transcript with one delta, TurnEndedUpdate terminal transcript, absent-usage transcript, negative int32 output-delta boundary. Quote rev and execution date in comments; assert what the schema proves using the local schema-derived test decoder; explicitly document that the current production decoder does not parse these fields yet and will only begin to via 12-05. No fake pass-through through the production decoder.</action>
<verify><automated>cargo test --all-features cursor_w0_usage_facts -- --test-threads=1</automated><fails_when>non-zero exit, occupancy math totals 20300, an assertion relies on the unchanged production decoder, or the filter selects zero tests</fails_when></verify>
<acceptance_criteria>All five W0-1 cases pass encoding assertions via the schema-derived test decoder with dated provenance and absolute-context semantics; no production-decode claim exists.</acceptance_criteria>
<done>Usage/terminal wire facts are encoding-locked for the 12-05 relay.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: W0-2 framing/protobuf corpora with gzip contrast</name>
<files>src/adapters/cursor/connect.rs</files>
<read_first>src/adapters/cursor/connect.rs corpus helpers :327-360 and 64 MiB bounds :116-118, .planning/phases/12-cursor-evidence-backed-hardening/12-PATTERNS.md connect.rs section</read_first>
<behavior>W0-2/D-06: named corpora cover malformed END-trailer JSON, corrupt-gzip junk in a gzip-flagged frame (rejected), valid gzip-decoded JSON (succeeds), valid empty END trailer (succeeds, Q3), truncated length-delimited protobuf, varint overflow, field-number zero, and an unsupported wire-encoding byte (wire-type mismatch where the encoding contradicts the field). Well-formed unknown field numbers carrying supported wire encodings explicitly remain decodable — invalid encodings, not unknown fields, are the failure class.</behavior>
<action>Extend the corpus helpers beside gzip/incompressible_bytes; each case comments its expected post-12-06 outcome. This task proves corpus construction only: assert valid gzip JSON and valid empty trailer decode today; corrupt/malformed cases are built and named, consumed by 12-06. Production parse behavior is untouched here beyond the Task 1 EOF tracer.</action>
<verify><automated>cargo test --all-features cursor_w0_corpora -- --test-threads=1</automated><fails_when>non-zero exit, valid gzip JSON or valid empty trailer fails today, or zero selected tests</fails_when></verify>
<acceptance_criteria>All named corpus helpers exist; the two valid cases decode today; corrupt gzip is paired against valid gzip as an explicit contrast pair.</acceptance_criteria>
<done>Corpora exist as red evidence with zero production framing change.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundary is loopback wire bytes inside tests (production change is the EOF-without-terminal error seam only); the test client adds a locally generated root certificate and DNS pin for one host — no production origin guard, credential handling, or config semantics change, and no live opencodex state is touched. Medium tampering threat: corpus bytes could mask a success path; mitigate per-case expected-outcome comments and tracer red-first evidence. Low DoS threat: oversized corpus frames; mitigate existing 64 MiB bound and bounded fixtures.
</threat_model>
<verification><automated>cargo test --all-features cursor_terminal_tracer -- --test-threads=1 && cargo test --all-features cursor_w0_usage_facts -- --test-threads=1 && cargo test --all-features cursor_w0_corpora -- --test-threads=1</automated><fails_when>the sequential chain exits non-zero at the first failing invocation; each invocation must select nonzero tests</fails_when></verification>
<success_criteria>EOF-without-terminal production fix shipped and proven over the whole gateway router; W0 encoding/corpora evidence locked with dated provenance and gzip correspondence.</success_criteria>

## Artifacts this phase produces

Whole-gateway-router EOF terminal tracer, honest schema-derived encoding fixtures, and gzip-contrast framing corpora.
