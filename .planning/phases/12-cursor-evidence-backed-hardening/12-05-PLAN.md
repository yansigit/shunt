---
phase: 12-cursor-evidence-backed-hardening
plan: "05"
type: execute
wave: 4
depends_on:
  - "01"
  - "04"
files_modified:
  - src/adapters/cursor/agent.rs
  - src/adapters/cursor/mod.rs
autonomous: true
requirements:
  - CUR-05
must_haves:
  truths:
    - "EOF-without-terminal handling already shipped by the 12-01 tracer; this plan rejects duplicate END/post-terminal frames within a received batch before any success terminal and makes idle timeout never synthesize a successful terminal (D-05)."
    - "Output-token deltas stream incrementally and context occupancy is relayed as absolute occupancy with derived/absent input labeled honestly (E1, D-09)."
  artifacts:
    - path: src/adapters/cursor/agent.rs
      provides: within-batch duplicate/post-terminal rejection and idle-synthesis rejection completing the terminal seam started by the 12-01 EOF tracer
    - path: src/adapters/cursor/mod.rs
      provides: incremental usage relay replacing the hardcoded unary block
  key_links:
    - from: src/adapters/cursor/agent.rs
      to: src/adapters/cursor/connect.rs
      via: trailer/terminal state gates the single End emission
      pattern: terminal_event|ConnectEndError
  prohibitions:
    - statement: "Do not add a no-progress or repetition heuristic (D-09, CUR-08)."
      status: flagged
      verification: unverified
    - statement: "Do not claim derived input or cache counts as authentic upstream measurements (E1)."
      status: flagged
      verification: unverified
---

<objective>Complete terminal discipline beyond the 12-01 EOF tracer (batch duplicates and idle cannot synthesize or trail success) and deliver the schema-evidenced incremental usage relay.</objective>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Duplicate/post-terminal batch rejection and idle cannot synthesize success (CUR-05/D-05)</name>
<files>src/adapters/cursor/agent.rs</files>
<read_first>src/adapters/cursor/agent.rs ReadState ingest :285-330, terminal_event :349-357, existing FLAG_END handling :387-397, the 12-01 tracer EOF seam and router_parity_tests.rs fixtures, pacing comments :20-23 and :293-303, src/adapters/cursor/connect.rs trailer handling, W0 corpora from 12-01</read_first>
<behavior>CUR-05/D-05 (EOF path already shipped by the 12-01 tracer): (a) within the received batch, a second END-flagged frame or bytes after the accepted terminal are rejected before any success End is emitted — one error instead, never success-then-error (exactly-one-terminal discipline); (b) a stream reaching the idle timer with output but no supported terminal produces an explicit error, never CursorStreamEvent::End. Detection stays bounded to bytes received before the boundary (EOF, cancellation, or idle fire); no promise of observing arbitrary post-cancellation bytes. Pacing comments untouched. Per the measured 1708-line agent.rs, do NOT imply a near-500-line cleanup; if shape demands space, extract a narrow private helper module with explicit ownership (named in the summary), never a large refactor.</behavior>
<action>Red-first using W0-1/W0-2 corpora and the 12-01 router fixtures: gate the terminal path so batch-level validation (received duplicate END or post-terminal frames inside ReadState) flips the outcome to a single error BEFORE the success End can be pushed; and the idle path (:332) cannot push a success-synthesizing terminal_event for a stream that never received the authoritative terminal. Timeout with zero output remains the internal error already present. Update the idle characterization test terminal_event_idle_timeout_after_output_ends_cleanly :1143-1148 to the stricter expectation and disclose the flip. Divergence from the 12-01 tracer boundary: EOF-without-terminal was already fixed there; this task owns only within-batch duplicate/post-terminal rejection and the idle synthesis path. Preserve pacing comments surgically.</action>
<verify><automated>cargo test --all-features cursor_terminal_dedupe_idle -- --test-threads=1</automated><fails_when>non-zero exit, a success End precedes a duplicate/post-terminal error, idle with output but no terminal synthesizes End, or zero selected tests</fails_when></verify>
<acceptance_criteria>Duplicate END/post-terminal bytes yield one error with no success End ever emitted first; idle-no-terminal rejects explicitly; clean single-END transcripts unchanged and single-terminal.</acceptance_criteria>
<done>The remaining terminal-manufacture holes (within-batch duplicates, idle synthesis) are closed after the 12-01 EOF tracer.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Incremental usage relay per W0-1 (CUR-05/E1)</name>
<files>src/adapters/cursor/mod.rs, src/adapters/cursor/agent.rs</files>
<read_first>src/adapters/cursor/mod.rs unary usage block :334-339, src/adapters/cursor/agent.rs interaction envelope handling, W0-1 fixtures, .planning/phases/12-cursor-evidence-backed-hardening/12-RESEARCH.md Section 5 honesty notes</read_first>
<behavior>CUR-05/E1/D-05: streaming output deltas relay incrementally during the turn; context checkpoints combine with deltas as absolute occupancy (10300 not 20300); no input_tokens: 1 hardcode remains; derived/absent input metrics are labeled (null or omitted), never claimed authentic; negative and overflow int32 deltas handled explicitly.</behavior>
<action>Replace the hardcoded unary usage block with a relay accepting only schema-proven fields (TokenDeltaUpdate output, ConversationTokenDetails context occupancy), citing OpenCodex rev + date and the CLI follow-up caveat honestly (aggregate CLI cache fields are not wire fields). Do not transplant StreamUnifiedChatWithTools field claims. Validate against all W0-1 fixture cases including negative/overflow; use estimated-language in derived-labeled outputs (docs beyond 12-08 parity). agent.rs edits follow the same narrow-helper discipline as Task 1.</action>
<verify><automated>cargo test --all-features cursor_usage_relay -- --test-threads=1</automated><fails_when>non-zero exit, the hardcoded 1/1 fixture remains, occupancy math is additive (20300), or zero selected tests</fails_when></verify>
<acceptance_criteria>All W0-1 usage cases pass with incremental output, absolute-context semantics, and honest derived/absent labeling; no invented fields.</acceptance_criteria>
<done>Usage flows only what the schema proves, honestly labeled.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundary is upstream Connect bytes to Anthropic output. High tampering threat: silence could authorize false completion; mitigate EOF/idle error fixtures. Medium DoS threat: flood of token deltas; mitigate streaming (no accumulation) and existing 64 MiB cap. Medium information threat: fabricated billing metrics; mitigate labeling discipline and W0-1 assertions.
</threat_model>
<verification><automated>cargo test --all-features cursor_terminal_dedupe_idle -- --test-threads=1 && cargo test --all-features cursor_usage_relay -- --test-threads=1</automated><fails_when>either invocation exits non-zero or selects zero tests; sequential chain stops at first failure</fails_when></verification>
<success_criteria>CUR-05 terminal strictness completed beyond the tracer EOF fix plus the schema-evidenced usage relay, fixture-proven.</success_criteria>

## Artifacts this phase produces

Duplicate/post-terminal batch rejection, idle-synthesis rejection, and the incremental usage relay.
