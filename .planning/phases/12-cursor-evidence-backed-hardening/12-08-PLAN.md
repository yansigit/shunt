---
phase: 12-cursor-evidence-backed-hardening
plan: "08"
type: execute
wave: 7
depends_on:
  - "05"
  - "07"
files_modified:
  - src/adapters/cursor/agent.rs
  - src/adapters/cursor/router_parity_tests.rs
  - README.md
  - README.ko.md
  - README.ja.md
  - README.zh-CN.md
  - docs/
  - site/src/content/docs/reference/
autonomous: true
requirements:
  - CUR-05
  - CUR-08
must_haves:
  truths:
    - "Cancellation before headers and during both client response modes releases the paced sender, upstream body, parser state, and gateway capacity via existing RAII ownership (D-07) — this plan owns the cancellation tests."
    - "No semantic no-progress/repetition heuristic, no new public config, no durable continuation store exists after the phase (D-09)."
    - "Every observable change ships with English plus ko/ja/zh-cn docs parity in the same PR (D-11)."
  artifacts:
    - path: src/adapters/cursor/agent.rs
      provides: cancellation RAII release proof fixtures (TurnGuard scope)
    - path: src/adapters/cursor/router_parity_tests.rs
      provides: "router-side cancellation probe extending the 12-01 gateway-router loopback module, transitively serialized via plan deps 05/07 on 01"
    - path: docs/
      provides: phase 12 hardening note with honest evidence language
    - path: site/src/content/docs/reference/
      provides: reference parity for new error surfaces and usage labeling
  key_links:
    - from: src/adapters/cursor/agent.rs
      to: src/adapters/cursor/mod.rs
      via: TurnGuard + CursorAgentTurn ownership is the release scope (no registry)
      pattern: TurnGuard|from_response_for_test
  prohibitions:
    - statement: "Do not hand-edit or regenerate wiki/ in this phase (D-11)."
      status: flagged
      verification: unverified
    - statement: "Do not use a global cancellation registry or detached recovery task (D-07)."
      status: flagged
      verification: unverified
---

<objective>Prove cancellation release via ownership/RAII, assert CUR-08/D-09 absences, and deliver strict locale-parity docs.</objective>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Cancellation release proof (CUR-05/D-07)</name>
<files>src/adapters/cursor/agent.rs, src/adapters/cursor/router_parity_tests.rs</files>
<read_first>src/adapters/cursor/agent.rs TurnGuard :244-252, from_response_for_test :255-268, paced sender :163-283, the 12-01 router_parity_tests.rs gateway-router loopback harness, .planning/phases/12-cursor-evidence-backed-hardening/12-RESEARCH.md Section 4.6 sender review</read_first>
<behavior>CUR-05/D-07: cancel during header wait, during streaming read, and during non-streaming accumulation each releases the paced sender JoinHandle, half-closes the upstream body, drops parser state, and frees gateway capacity (no leaked guard). Real-router parity with a live cancellation from the router side is included here so release is proven through the loopback router, not just unit holders.</behavior>
<action>Extend the TurnGuard/from_response_for_test harness with three cancellation-phase fixtures plus one live-router cancel probe run over the 12-01 gateway-router loopback harness (router_parity_tests.rs extension — transitively serialized after 12-01 via this plan's 05/07 dependency chain, never concurrent edits); assert observable release effects (sender dropped, join abort, turn count back to zero, no detached task). Fix any missing RAII coverage (e.g., heartbeat timer outliving the turn) with ownership-only changes; still no registry. Preserve pacing comments; disclose the review outcome in the summary.</action>
<verify><automated>cargo test --all-features cursor_cancellation_release -- --test-threads=1</automated><fails_when>non-zero exit, leaked timer/sender after cancel, or zero selected tests</fails_when></verify>
<acceptance_criteria>All three cancellation phases (plus router-side cancel probe) release sender, body, and parser without leaked timers or registry.</acceptance_criteria>
<done>Cancellation release proven through ownership scope with router parity.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: CUR-08 absence assertions (D-09)</name>
<files>src/adapters/cursor/agent.rs, src/adapters/cursor/mod.rs</files>
<read_first>src/adapters/cursor/agent.rs post-12-05 idle/EOF paths, .planning/phases/12-cursor-evidence-backed-hardening/12-CONTEXT.md D-09, .planning/research/PITFALLS.md Cursor section</read_first>
<behavior>CUR-08/D-09: the hardened stream contains no semantic repetition/no-progress success heuristic, no new public config keys, no durable continuation store; idle/EOF error instead of success; repeated-semantic payloads never affect terminalization.</behavior>
<action>Add behavior-based negative fixtures: idle streams never flip to success regardless of inactivity patterns; repeated-semantic payloads have no terminal effect; the Cursor config surface is unchanged (parse existing config and assert no new keys consumed). Absence asserted behaviorally, not by grep; no candidates invented for deletion; legacy untouched.</action>
<verify><automated>cargo test --all-features cursor_no_heuristics -- --test-threads=1</automated><fails_when>non-zero exit, any idle/repetition success response, or zero selected tests</fails_when></verify>
<acceptance_criteria>Idle, repetition, and no-durable-store assertions pass with unchanged public config.</acceptance_criteria>
<done>CUR-08 enforced behaviorally.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: Docs parity with strict build gate (D-11)</name>
<files>README.md, README.ko.md, README.ja.md, README.zh-CN.md, docs/, site/src/content/docs/reference/</files>
<read_first>12-05/12-06 acceptance criteria, README.md cursor section, docs/ index, site reference cursor pages, AGENTS.md Documentation section, CONTRIBUTING.md</read_first>
<behavior>D-11: README (EN + ko/ja/zh-CN), docs/, and site reference pages document explicit unsupported errors, strict terminal semantics, honesty-labeled usage relay, strict framing/protobuf/argument rejections, and classifier semantics — conformance language only, never live availability; anchors only where the built locale dist actually contains them, else link the English page.</behavior>
<action>Update English surfaces first, then the maintained locale copies in the same PR; verify anchors against built dist/<locale>/.../index.html before adding fragment links. Verification is strictly chained with && and no suppression, no cd tricks, no ';': the focused cargo check runs, then the site build via npm --prefix site run build. If locale parity would introduce broken links, fix links rather than suppressing the build. wiki/ untouched.</action>
<verify><automated>cargo check --workspace --all-features && npm --prefix site run build</automated><fails_when>either chained invocation fails; any masked failure or missing locale parity is a defect, not a pass</fails_when></verify>
<acceptance_criteria>English and locale pages updated in the same PR; strict cargo check and site build both exit zero; wiki untouched.</acceptance_criteria>
<done>All four doc surfaces carry parity under a strict chained gate.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundary is downstream cancellation signals. High DoS threat: unclosed timers burn capacity; mitigate RAII release fixtures. Medium tampering threat: docs overclaim live availability; mitigate conformance-only wording across all locales. Low information threat: credential defaults in examples; mitigate synthetic placeholders only.
</threat_model>
<verification><automated>cargo test --all-features cursor_cancellation_release -- --test-threads=1 && cargo test --all-features cursor_no_heuristics -- --test-threads=1 && cargo check --workspace --all-features && npm --prefix site run build</automated><fails_when>the chained sequence fails at any step; each test invocation must select nonzero tests</fails_when></verification>
<success_criteria>Orderly release semantics, absence proofs, and full docs parity under a strict chained gate.</success_criteria>

## Artifacts this phase produces

Cancellation RAII proofs, CUR-08 behavioral absences, and locale-parity docs.
