---
phase: 12-cursor-evidence-backed-hardening
plan: "06"
type: execute
wave: 5
depends_on:
  - "01"
  - "05"
files_modified:
  - src/adapters/cursor/connect.rs
  - src/adapters/cursor/agent.rs
autonomous: true
requirements:
  - CUR-06
must_haves:
  truths:
    - "Malformed END-trailer JSON, corrupt gzip, truncated payload, and wrong flag order fail explicitly while valid gzip-decoded JSON and valid empty trailers succeed (Q3, D-06); duplicate/post-terminal and EOF-without-terminal terminal discipline already shipped via the 12-01 tracer and 12-05 batch rejection."
    - "Malformed protobuf at iter_fields fails on exact classes — field-number zero, varint overflow, truncated length-delimited field, unsupported wire encoding — while well-formed unknown field numbers on supported wire types remain decodable (D-06)."
    - "Nested Struct/Value/list/MCP argument parsing is strict: bounded size, valid UTF-8, and no Null/empty-string coercion into typed positions; nested depth and aggregate limits enforced with cap+1 tests."
  artifacts:
    - path: src/adapters/cursor/connect.rs
      provides: explicit END-trailer error mapping preserving auth-code passthrough
    - path: src/adapters/cursor/agent.rs
      provides: strict protobuf decode surfaces and strict nested argument parsing
  key_links:
    - from: src/adapters/cursor/connect.rs
      to: src/adapters/cursor/agent.rs
      via: explicit error replaces the None-success seam
      pattern: parse_connect_error|ConnectEndError
  prohibitions:
    - statement: "Do not alter the 64 MiB decompression bounds or add new cap vocabulary (A3)."
      status: flagged
      verification: unverified
    - statement: "Do not convert an undecodable trailer or invalid wire encoding into clean completion (D-06)."
      status: flagged
      verification: unverified
---

<objective>Close the None-success framing seam and make protobuf + nested argument decoding strict using the W0-2 corpora.</objective>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: END-trailer and flag-order rejection table (CUR-06/D-06)</name>
<files>src/adapters/cursor/connect.rs, src/adapters/cursor/agent.rs</files>
<read_first>src/adapters/cursor/connect.rs decode_frame :31-118, parse_connect_error :252-278, FLAG order :4-5, agent.rs END handling, the W0-2 corpora from 12-01, the shipped 12-01 EOF tracer and 12-05 dedupe seams</read_first>
<behavior>CUR-06/D-06: W0-2 corpora outcomes hold: malformed trailer JSON, corrupt-gzip junk, truncated payload, and wrong flag order reject; valid gzip-decoded JSON and valid empty END trailer succeed per proven convention (Q3/A3). Duplicate END/post-terminal and EOF-without-terminal behavior was already shipped — the 12-01 EOF tracer and the 12-05 within-batch rejection own those classes; this task consumes the corpora error classes without re-implementing them.</behavior>
<action>Red-first with the corpora: change the caller seam so a present-but-malformed parse_connect_error payload yields an explicit error while a genuinely empty valid trailer stays valid. Preserve auth-code passthrough (401/403/429/400) at :266-278; keep decompression bounds untouched. Connect the corrupt-gzip versus valid-gzip contrast pair as the headline assertion pair.</action>
<verify><automated>cargo test --all-features cursor_framing_rejection -- --test-threads=1</automated><fails_when>non-zero exit, any corrupt/malformed case flips to success, valid gzip JSON or empty trailer fails, or zero selected tests</fails_when></verify>
<acceptance_criteria>Every malformed/corrupt vector errors exactly once; proven valid conventions preserved.</acceptance_criteria>
<done>The None-success seam is closed against the corpora.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Strict protobuf and nested argument parsing (CUR-06/D-06)</name>
<files>src/adapters/cursor/agent.rs</files>
<read_first>src/adapters/cursor/agent.rs iter_fields :687 and call sites :761-869, nested Struct/Value/list argument decode sites, MCP argument rendering, the W0-2 protobuf corpora</read_first>
<behavior>CUR-06/D-06: iter_fields errors exactly on field-number zero, varint overflow, truncated length-delimited fields, and unsupported wire encodings; well-formed unknown field numbers with supported wire types remain decodable (unknown != invalid). Nested Struct/Value/list and MCP argument payloads enforce: bounded encoded size, valid UTF-8, no Null or empty-string coercion into typed/required positions, bounded list/Struct depth and aggregate cap with cap+1 observable tests.</behavior>
<action>Red-first table fixtures for the four protobuf failure classes (using 12-01 corpora) plus wire-encoding contrast fixtures proving well-formed unknown fields still decode. Change iter_fields to surface truncation as an explicit error consumed at :761-869 call sites, mapped into CursorError per the endpoint error-shape rule (Anthropic shape; OpenAI Responses shape only on the inbound Codex endpoint). Add the nested-argument strictness suite (size cap+1, depth cap+1, UTF-8 invalid bytes rejected, Null/empty coercions rejected). Keep malformed-corpus decode time bounded.</action>
<verify><automated>cargo test --all-features cursor_proto_wire_strict -- --test-threads=1</automated><fails_when>non-zero exit, silent truncation persists, a valid unknown field fails, or zero selected tests</fails_when></verify>
<acceptance_criteria>All four protobuf failure classes error; unknown-but-wellformed fields decodable; nested Struct/Value/list/MCP parsing strict on limits, UTF-8, and coercion with cap+1 coverage.</acceptance_criteria>
<done>Protobuf and nested-argument seams are strict and fixture-proven.</done>
</task>
</tasks>
<threat_model>
ASVS level 1; block high severity. Trust boundary is upstream Connect frame bytes. High tampering threat: malformed trailer could impersonate success; mitigate corpora-backed rejection tests. High DoS threat: huge declared lengths and deep nested values; mitigate existing 64 MiB cap plus new bounded depth/size cap+1 tests. Medium repudiation threat: silent truncation hides corruption; mitigate explicit error surfaces in the gateway error shape.
</threat_model>
<verification><automated>cargo test --all-features cursor_framing_rejection -- --test-threads=1 && cargo test --all-features cursor_proto_wire_strict -- --test-threads=1</automated><fails_when>either invocation exits non-zero or selects zero tests</fails_when></verification>
<success_criteria>CUR-06 framing, protobuf, and nested-argument strictness exhaustive and fixture-driven with bounds intact.</success_criteria>

## Artifacts this phase produces

Explicit trailer/protobuf error surfaces plus strict nested argument parsing.
