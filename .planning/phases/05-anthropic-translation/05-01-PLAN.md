---
phase: 05-anthropic-translation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/routing.rs, src/codex_endpoint.rs, src/model/mod.rs, src/model/inbound_responses/mod.rs, src/model/inbound_responses/request.rs, tests/inbound_anthropic_translation.rs]
autonomous: true
requirements: [TRANS-01, TRANS-03]
estimate: {tasks: 2, confidence: medium}
must_haves:
  truths:
    - "One exact Anthropic route selects translation while every native Responses route remains byte-faithful."
    - "Instructions, messages, tools, paired tool results, images, and supported reasoning controls produce a valid Anthropic Messages request."
    - "Lossy, stateful, malformed, ambiguous, or unsupported input fails before credentials or network dispatch."
  artifacts:
    - {path: src/model/inbound_responses/request.rs, provides: "strict Responses-to-Messages request translator"}
    - {path: src/routing.rs, provides: "exact native-or-Anthropic inbound route decision"}
    - {path: tests/inbound_anthropic_translation.rs, provides: "request and pre-dispatch conformance tests"}
---

<objective>Route exact Anthropic targets through a dedicated, fail-closed Responses request translator while preserving native passthrough unchanged.</objective>

<context>@AGENTS.md @.planning/phases/05-anthropic-translation/05-CONTEXT.md @.planning/phases/05-anthropic-translation/05-RESEARCH.md @src/codex_endpoint.rs @src/routing.rs @src/adapters/anthropic/mod.rs @tests/inbound_codex_endpoint.rs</context>

<tasks>
<task type="tracer">
  <name>Task 1: Translate a complete Responses request into Anthropic Messages</name>
  <files>src/model/mod.rs, src/model/inbound_responses/mod.rs, src/model/inbound_responses/request.rs, tests/inbound_anthropic_translation.rs</files>
  <action>Add a typed, strict translator for the accepted Phase 5 surface. Preserve instruction/message order, function-call/result pairing and IDs, inline URL/data-URL images, function schemas and choice, generation limits, and bounded reasoning controls. Produce path-specific OpenAI-facing validation errors for malformed JSON arguments, orphan/duplicate calls, remote ids, stateful continuation, hosted/custom tools, encrypted reasoning, invalid roles/content, and unsupported semantic controls.</action>
  <verify><automated>cargo test inbound_responses::request --lib && cargo test --test inbound_anthropic_translation request_</automated><fails_when>accepted content is lost, invalid Anthropic history is built, unsupported behavior is silently dropped, or tests fail</fails_when></verify>
  <done>Pure fixtures prove every accepted request feature and every fail-closed category.</done>
</task>
<task type="auto">
  <name>Task 2: Add exact translated dispatch without changing native passthrough</name>
  <files>src/routing.rs, src/codex_endpoint.rs, tests/inbound_anthropic_translation.rs, tests/inbound_codex_endpoint.rs</files>
  <action>Extend the narrow inbound decision so one unambiguous exact mapping may select AdapterKind::Anthropic, while prefix routes, default inference, multiple providers, and other adapter kinds remain rejected or pinned exactly as documented. Parse only after route selection requires translation, before credential resolution. Pin no-network rejection and native request byte-identity behavior.</action>
  <verify><automated>cargo test routing:: --lib && cargo test --test inbound_anthropic_translation request_ && cargo test --test inbound_codex_endpoint</automated><fails_when>a non-exact route translates, an unsupported request dispatches, native bytes change, or tests fail</fails_when></verify>
  <done>Exact Anthropic dispatch is isolated and native routing behavior is unchanged.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Severity | Mitigation |
|-----------|----------|----------|------------|
| T-05-01-01 | Credential disclosure | high | route and feature validation complete before credential resolution |
| T-05-01-02 | Request confusion | high | strict duplicate-aware parsing and exact route selection |
| T-05-01-03 | State injection | high | reject foreign continuation/encrypted state before dispatch |
| T-05-01-04 | Resource exhaustion | high | reuse inbound byte limits and bound translated structures |
</threat_model>

<verification><automated>cargo test inbound_responses::request --lib && cargo test --test inbound_anthropic_translation request_ && cargo test --test inbound_codex_endpoint && cargo fmt --all --check && cargo clippy --all-targets --all-features -- -D warnings</automated></verification>

