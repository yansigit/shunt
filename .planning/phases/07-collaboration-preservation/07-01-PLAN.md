---
phase: 07-collaboration-preservation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/config.rs, src/model/inbound_responses/request.rs, src/model/inbound_responses/collaboration.rs, src/model/inbound_responses.rs]
autonomous: true
requirements: [COLLAB-01]
estimate: {tasks: 3, confidence: medium}
must_haves:
  truths:
    - "Absent or disabled collaboration keeps translated and native behavior unchanged."
    - "Enabled translated requests preserve V2 collaboration catalogs and plaintext agent messages without leaking encrypted tasks."
    - "Only the Responses-only schema encryption marker is removed."
  artifacts:
    - {path: src/model/inbound_responses/collaboration.rs, provides: "bounded V2 request bridge and encrypted-task guard"}
    - {path: src/config.rs, provides: "explicit collaboration opt-in"}
---

<objective>Add the request-side opt-in collaboration bridge for exact Anthropic routes.</objective>

<context>@AGENTS.md @.planning/phases/07-collaboration-preservation/07-CONTEXT.md @.planning/phases/07-collaboration-preservation/07-RESEARCH.md @src/config.rs @src/model/inbound_responses/request.rs</context>

<tasks>
<task type="auto">
  <name>Task 1: Add an explicit default-off endpoint opt-in</name>
  <files>src/config.rs</files>
  <action>Add `collaboration: bool` to CodexEndpointConfig with serde default false. Extend config fixtures to prove absent, false, and true behavior without changing provider validation.</action>
  <verify><automated>cargo test codex_endpoint --lib</automated><fails_when>the default changes, unknown/provider validation weakens, or tests fail</fails_when></verify>
  <done>Collaboration translation can be enabled only by explicit configuration.</done>
</task>
<task type="auto">
  <name>Task 2: Build the bounded V2 request authority bridge</name>
  <files>src/model/inbound_responses/collaboration.rs, src/model/inbound_responses.rs, src/model/inbound_responses/request.rs</files>
  <action>Detect collaboration namespace function tools in top-level and additional_tools catalogs, flatten authorized names for Anthropic, strip only schema-position encrypted markers, and retain a request authority map for response restoration. Accept plaintext agent_message items as separate user turns.</action>
  <verify><automated>cargo test inbound_responses::collaboration --lib</automated><fails_when>catalogs are lost, collisions pass, schemas are corrupted, task content merges into assistant state, or tests fail</fails_when></verify>
  <done>Supported collaboration requests translate deterministically with bounded authority metadata.</done>
</task>
<task type="auto">
  <name>Task 3: Prove disabled/native isolation and encrypted fail-closed behavior</name>
  <files>src/model/inbound_responses/collaboration.rs, src/model/inbound_responses/request.rs</files>
  <action>Port synthetic Fernet wire fixtures and detect a trailing agent_message whose task content is only routing metadata plus structurally valid ciphertext. Reject it and all translated continuation state before dispatch. Keep ordinary encrypted-looking text and plaintext tasks usable.</action>
  <verify><automated>cargo test inbound_responses::collaboration --lib && cargo test inbound_responses::request --lib</automated><fails_when>ciphertext enters an error/log, false positives reject plaintext, recovery performs work, native bytes are parsed, or tests fail</fails_when></verify>
  <done>Unsupported opaque tasks fail once, locally, without disclosure or network work.</done>
</task>
</tasks>

<verification><automated>cargo test inbound_responses:: --lib && cargo fmt --all --check && cargo clippy --all-targets --all-features -- -D warnings</automated></verification>
