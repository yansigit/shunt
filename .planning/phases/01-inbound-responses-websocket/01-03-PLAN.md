---
phase: 01-inbound-responses-websocket
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - tests/fixtures/responses_ws.rs
  - src/codex_endpoint/frame.rs
  - tests/inbound_codex_websocket.rs
autonomous: true
requirements:
  - CONF-01
estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - Focused Rust fixtures cover OpenCodex-proven warmup completion frames, frame splitting, CRLF/multiline SSE, terminal variants, malformed input, premature EOF, and safe-header projection (per D-09, CONF-01).
    - An upstream stream ending with [DONE] prior to a terminal event is detected as premature EOF and emits a 502 websocket_protocol_error frame (per D-07, CONF-01).
    - Upstream chunks arriving after a terminal event are strictly discarded, preventing post-terminal frame pollution (per D-03, CONF-01).
    - Oversized SSE frames exceeding 4 MiB fail boundedly while preserving any terminal frames encountered prior to the limit (per D-06, CONF-01).
  artifacts:
    - tests/fixtures/responses_ws.rs (or unit tests in src/codex_endpoint/frame.rs) with ported OpenCodex fixtures
    - tests/inbound_codex_websocket.rs with protocol edge tests
  key_links:
    - tests/fixtures/responses_ws.rs fixtures derive directly from OpenCodex test cases in tests/ws-endpoint.test.ts and tests/sse-client-frame-bounds.test.ts without duplicating outbound Shunt tests (per D-09).
---

<objective>
Port OpenCodex conformance fixtures into focused Rust test cases covering warmup frame structure, split-chunk reassembly, multiline data parsing, CRLF delimiters, premature EOF, post-terminal frame discarding, safe header allowlists, and oversized stream bounds.

Purpose: Validates transport and framing behavior against proven wire fixtures while preventing regression of edge cases.
Output: Rust protocol conformance fixtures and unit/integration edge test suites.

## must_haves
- Warmup completion fixture matches OpenCodex: two frames (created + completed), empty string id, correct sequence numbers, no upstream call (per D-05, CONF-01).
- Split chunks, multiline data, CRLF, and unterminated final blocks reassemble correctly (per D-03, CONF-01).
- [DONE] before terminal event produces 502 premature-EOF protocol error (per D-07, CONF-01).
- Bytes arriving after a terminal event (completed, failed, incomplete) are discarded (per D-03, CONF-01).
- Safe header filtering strips cookies, authorization, and internal headers under test (per D-07, CONF-01).

## Prohibitions
- Do not buffer or collect a successful upstream SSE response.
- Do not forward client credentials, cookies, hop-by-hop headers, or internal Shunt headers upstream.
- Do not expose upstream cookies or arbitrary response headers in WS error frames.
- Do not add public config keys or alter documented provider selection semantics.
- Do not change credential-file writeback behavior.
- Do not add persistent history, a second pool, or a general response-repair framework.

## Artifacts this phase produces
- tests/fixtures/responses_ws.rs (conformance test fixtures)
- src/codex_endpoint/frame.rs (unit tests for framing and safe headers)
- tests/inbound_codex_websocket.rs (protocol edge tests)
</objective>

<execution_context>
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.12.0+gsd.dad16b6ef9a9.wrapper.2/gsd-core/workflows/execute-plan.md
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.12.0+gsd.dad16b6ef9a9.wrapper.2/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-inbound-responses-websocket/01-CONTEXT.md
@.planning/notes/opencodex-port-audit.md
@src/codex_endpoint/frame.rs
@src/codex_endpoint/websocket.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conformance fixtures for warmup, chunk splitting, and SSE delimiters</name>
  <files>tests/fixtures/responses_ws.rs, src/codex_endpoint/frame.rs</files>
  <read_first>src/codex_endpoint/frame.rs, .planning/notes/opencodex-port-audit.md, tests/inbound_codex_endpoint.rs</read_first>
  <action>
Create tests/fixtures/responses_ws.rs (and/or inline unit tests in src/codex_endpoint/frame.rs) containing conformance fixtures ported from OpenCodex (tests/ws-endpoint.test.ts):
1. Warmup fixture: verify build_warmup_completion_frames generates exactly two frames: response.created (sequence_number: 0, status: in_progress) and response.completed (sequence_number: 1, status: completed), both with id: "" and preserving the requested model name.
2. Split chunk fixture: feed multi-part SSE chunks split across arbitrary byte boundaries (including splitting  and 
, and splitting JSON fields) into BoundedSseFrameBuffer; verify extracted payloads match the expected complete JSON objects.
3. Multiline data fixture: feed SSE blocks containing multiple data: lines (e.g. data: line1
data: line2); verify they are joined with newlines into a single payload.
4. Delimiter variations fixture: verify LF LF (

), CRLF CRLF (

), LF CRLF (

), and CRLF LF (

) are all recognized as valid block delimiters.
5. Unterminated final block fixture: verify finish() returns any trailing block bytes that lack trailing delimiters.
6. Post-terminal discard fixture: feed a stream where response.completed is followed by another response.output_text.delta; verify parsing terminates at response.completed and later bytes are discarded.
  </action>
  <acceptance_criteria>
    - Warmup fixture confirms two frames with id "" and sequence numbers 0 and 1
    - Split chunks reassemble correctly regardless of where chunk boundaries fall
    - Multiline data lines are properly concatenated with newlines
    - All 4 newline delimiter variations are correctly detected
    - Unterminated trailing block is yielded on finish()
    - Frames following a terminal event are ignored
  </acceptance_criteria>
  <verify>
    <automated>cargo test codex_endpoint::frame::tests</automated>
    <fails_when>non-zero exit code or test failure</fails_when>
  </verify>
  <done>OpenCodex-derived framing and warmup conformance fixtures pass in Rust.</done>
</task>

<task type="auto">
  <name>Task 2: Protocol edge tests for error framing, safe headers, and memory bounds</name>
  <files>src/codex_endpoint/frame.rs, tests/inbound_codex_websocket.rs</files>
  <read_first>src/codex_endpoint/frame.rs, src/codex_endpoint/websocket.rs, tests/inbound_codex_websocket.rs</read_first>
  <action>
Implement edge tests in src/codex_endpoint/frame.rs and tests/inbound_codex_websocket.rs:
1. Premature EOF: verify that an SSE stream ending with [DONE] (or ending abruptly without any terminal event) produces a 502 websocket_protocol_error frame with message "Upstream stream ended before response terminal event" and incomplete status.
2. Malformed upstream JSON: verify that an SSE block containing non-JSON data produces a 502 websocket_protocol_error frame with message "Invalid JSON payload in upstream SSE frame".
3. Safe header projection test: construct HeaderMap with set-cookie, authorization, cookie, x-shunt-internal, retry-after, x-request-id, openai-request-id, x-codex-turn-state, openai-model, x-models-etag, x-reasoning-included, x-ratelimit-limit, x-codex-primary-used-percent. Pass through safe_response_headers and assert set-cookie, authorization, cookie, and internal headers are absent, while safe headers are preserved.
4. Max frame bounds test: feed an SSE chunk exceeding 4 MiB into BoundedSseFrameBuffer; assert it returns SseFrameTooLarge error. If a terminal event was encountered in the chunk prior to exceeding the limit, verify the terminal event is preserved rather than dropped.
  </action>
  <acceptance_criteria>
    - [DONE] without terminal event produces 502 protocol error
    - Malformed upstream JSON produces 502 protocol error
    - safe_response_headers excludes sensitive headers and preserves allowlisted headers
    - 4 MiB frame boundary enforces SseFrameTooLarge error while protecting earlier terminal frames
  </acceptance_criteria>
  <verify>
    <automated>cargo test --test inbound_codex_websocket</automated>
    <fails_when>non-zero exit code or test failure</fails_when>
  </verify>
  <done>Error envelopes, header allowlists, premature EOF, and memory boundary edge tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream SSE -> Shunt Framer | Potentially malformed, oversized, or premature-EOF upstream data streams |
| Shunt -> Client Error Frame | Error envelopes containing headers exposed to downstream clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Information Disclosure | tests/fixtures/responses_ws.rs | High | mitigate | Assert safe_response_headers completely strips set-cookie and authorization tokens under test fixtures (ASVS L1 3.4.1). |
| T-01-09 | Denial of Service | src/codex_endpoint/frame.rs | High | mitigate | Test that 4 MiB boundary enforcement prevents unbounded buffer allocation on malicious upstream SSE streams (ASVS L1 5.1.2). |
| T-01-10 | Repudiation | src/codex_endpoint/frame.rs | Medium | mitigate | Verify premature-EOF and malformed SSE events are reported with explicit websocket_protocol_error codes and terminal incomplete classification. |
</threat_model>

<verification>
Automated verification commands:
- cargo test codex_endpoint::frame::tests
- cargo test --test inbound_codex_websocket
</verification>

<success_criteria>
- Warmup and SSE delimiter conformance fixtures pass cleanly
- Premature EOF with [DONE] is rejected as protocol error
- Safe header projection allowlist is proven to strip sensitive headers
- Memory bounds protect against oversized frames
</success_criteria>

<output>
Create .planning/phases/01-inbound-responses-websocket/01-03-SUMMARY.md when done.
</output>

