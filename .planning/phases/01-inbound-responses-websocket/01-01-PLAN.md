---
phase: 01-inbound-responses-websocket
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/server.rs
  - src/codex_endpoint.rs
  - src/codex_endpoint/websocket.rs
  - src/codex_endpoint/frame.rs
autonomous: true
requirements:
  - WS-01
  - WS-02
  - WS-03
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - Inbound client authentication is enforced before WebSocket upgrade succeeds, rejecting missing or invalid tokens with HTTP 401 in the OpenAI Responses error shape (per D-07, WS-01).
    - Every registered Responses path (/backend-api/codex/responses, /responses, /v1/responses) accepts WebSocket GET upgrades when [server.codex_endpoint] is enabled, while disabled endpoint returns 404 (per D-01, WS-01).
    - A response.create frame with generate: false completes locally with deterministic empty-id response.created and response.completed frames without making any upstream call (per D-05, WS-02).
    - A normal streaming response.create frame forwards through the existing ChatGPT OAuth account pool machinery, while response.processed is handled as a harmless acknowledgement no-op (per D-02, D-03, WS-03).
  artifacts:
    - Cargo.toml with axum ws feature enabled
    - src/codex_endpoint/websocket.rs with handshake auth, upgrade handler, and session loop
    - src/codex_endpoint/frame.rs with inbound frame parsing, local warmup frame generator, and SSE block parsing
  key_links:
    - src/server.rs routes GET on codex_endpoint::PATHS to codex_endpoint::websocket::get alongside existing POST (per D-01).
    - src/codex_endpoint/websocket.rs calls existing forward_codex_inbound via codex_endpoint helpers without duplicating account-pool machinery (per D-02).
---

<objective>
Deliver the vertical tracer slice for inbound Responses WebSocket transport: enable Axum WebSocket support, register GET upgrades alongside existing POST routes on all registered Responses paths, enforce client authentication prior to upgrade, implement local generate: false warmup completions, and route streaming response.create turns through Shunt's existing ChatGPT OAuth account-pool machinery.

Purpose: Establishes the foundational WebSocket transport boundary for Codex clients without altering the existing HTTP/SSE passthrough or duplicating account pooling.
Output: Enabled Axum WebSocket feature, registered GET routes in src/server.rs, src/codex_endpoint/websocket.rs, src/codex_endpoint/frame.rs, and initial tracer tests.

## must_haves
- Client authentication is enforced before HTTP 101 Switching Protocols upgrade (per D-07, WS-01).
- Registered paths (/backend-api/codex/responses, /responses, /v1/responses) serve both GET WebSocket upgrades and existing HTTP POST (per D-01, WS-01).
- Local warmup response.create with generate: false emits response.created and response.completed locally with empty IDs and zero upstream calls (per D-05, WS-02).
- Normal response.create frames forward to upstream via existing account pool without duplicating auth/pool logic, and response.processed frames are ignored (per D-02, D-03, WS-03).

## Prohibitions
- Do not buffer or collect a successful upstream SSE response.
- Do not forward client credentials, cookies, hop-by-hop headers, or internal Shunt headers upstream.
- Do not expose upstream cookies or arbitrary response headers in WS error frames.
- Do not add public config keys or alter documented provider selection semantics.
- Do not change credential-file writeback behavior.
- Do not add persistent history, a second pool, or a general response-repair framework.

## Artifacts this phase produces
- Cargo.toml (with axum ws feature)
- src/server.rs (GET route registration on codex_endpoint::PATHS)
- src/codex_endpoint.rs (exposes websocket module and shared helpers)
- src/codex_endpoint/websocket.rs (Axum WebSocket upgrade and turn loop)
- src/codex_endpoint/frame.rs (inbound frame parser, warmup frames, SSE block parser)
</objective>

<execution_context>
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.12.0+gsd.dad16b6ef9a9.wrapper.2/gsd-core/workflows/execute-plan.md
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.12.0+gsd.dad16b6ef9a9.wrapper.2/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-inbound-responses-websocket/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@src/server.rs
@src/codex_endpoint.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Inbound Responses WebSocket tracer slice</name>
  <files>Cargo.toml, src/server.rs, src/codex_endpoint.rs, src/codex_endpoint/websocket.rs</files>
  <read_first>Cargo.toml, src/server.rs, src/codex_endpoint.rs, src/error.rs, src/adapters/responses/inbound.rs</read_first>
  <action>
Enable the "ws" feature for axum in Cargo.toml (axum = { version = "0.8", features = ["ws"] }). In src/codex_endpoint.rs, expose the websocket module (pub mod websocket;) and expose narrowly scoped helpers for inbound client auth resolution, pool sticky key calculation, and forward dispatch so code is not duplicated per D-02. In src/server.rs, update route registration for codex_endpoint::PATHS so that when [server.codex_endpoint] is configured, each path is registered with .route(path, get(codex_endpoint::websocket::get).post(codex_endpoint::post)) per D-01. Implement codex_endpoint::websocket::get to inspect inbound authentication using state.refreshed().inbound_auth BEFORE calling WebSocketUpgrade::on_upgrade. If auth fails, return an HTTP 401 with the OpenAI Responses error envelope per D-07. Once upgraded, run a minimal session loop that accepts a text frame, parses it, and for a normal response.create frame, dispatches through forward_codex_inbound and relays initial response stream frames to the client.
  </action>
  <acceptance_criteria>
    - Cargo.toml compiles with axum ws feature enabled
    - src/server.rs registers both GET and POST on every path in codex_endpoint::PATHS when codex_endpoint is configured
    - GET request to registered path without valid auth returns HTTP 401 with OpenAI error envelope before WebSocket handshake completes
    - GET request with valid auth upgrades to WebSocket with HTTP 101 Switching Protocols
    - A valid response.create frame sent over the WebSocket initiates upstream streaming via forward_codex_inbound
  </acceptance_criteria>
  <verify>
    <automated>cargo check --all-targets</automated>
    <fails_when>non-zero exit code or compilation error</fails_when>
  </verify>
  <done>The WebSocket upgrade endpoint is wired end-to-end on all registered Responses paths with pre-upgrade auth enforcement and compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Inbound control frame parsing and local warmup completion</name>
  <files>src/codex_endpoint/websocket.rs, src/codex_endpoint/frame.rs</files>
  <read_first>src/codex_endpoint/websocket.rs, src/codex_endpoint.rs, .planning/phases/01-inbound-responses-websocket/01-CONTEXT.md</read_first>
  <action>
Create src/codex_endpoint/frame.rs defining inbound frame models: ClientFrame enum (ResponseCreate, ResponseProcessed, IgnoredText, BinaryUnsupported). Parse incoming text frames as JSON. If type is response.create, inspect the generate field. When generate is false (per D-05, WS-02), synthesize local warmup completion frames: response.created (sequence_number: 0, status: in_progress, id: "", model: model, output: []) followed by response.completed (sequence_number: 1, status: completed, id: "", model: model, output: []). Both frames share the same empty id string. Send these frames directly to the WebSocket and do NOT invoke upstream forward. When type is response.processed, treat it as an acknowledgement no-op per D-03 and WS-03. For unknown text types or unparseable text JSON, log and ignore without closing the connection, matching OpenCodex behavior. For binary frames, close or return a protocol error without decoding as JSON.
  </action>
  <acceptance_criteria>
    - response.create with generate: false emits two frames: response.created then response.completed
    - Both warmup frames have id equal to empty string and matching sequence numbers (0 and 1)
    - Zero upstream requests occur when generate: false is processed
    - response.processed causes no upstream action and no error
    - Unparseable JSON text frames do not terminate the session
    - Binary frames are rejected as unsupported
  </acceptance_criteria>
  <verify>
    <automated>cargo test codex_endpoint::frame::tests</automated>
    <fails_when>non-zero exit code or test failure</fails_when>
  </verify>
  <done>Inbound control frames are parsed, warmup completions execute locally with exact OpenCodex-matching fields, and acknowledgements/unrecognized frames behave safely.</done>
</task>

<task type="auto">
  <name>Task 3: Bounded SSE block framer and parser</name>
  <files>src/codex_endpoint/frame.rs</files>
  <read_first>src/codex_endpoint/frame.rs, .planning/research/ARCHITECTURE.md</read_first>
  <action>
In src/codex_endpoint/frame.rs, implement BoundedSseFrameBuffer ported from the proven OpenCodex byte-scanning algorithm. The buffer must scan for SSE block delimiters (LF LF, LF CR LF, CR LF LF, CR LF CR LF) across arbitrary chunk boundaries. Extract all data: lines within each block, strip the leading optional space after data:, and join multiline data fields with newline characters into a single payload string. Enforce MAX_CLIENT_SSE_FRAME_BYTES (4 MiB) and maximum frames per feed (max_frames_per_feed) to prevent memory exhaustion and object amplification. Implement parse_payload_type to classify the payload's type field without modifying the payload text. Implement terminal classification for response.completed, response.failed, and response.incomplete. If [DONE] is encountered before a terminal event, do not treat it as completed.
  </action>
  <acceptance_criteria>
    - BoundedSseFrameBuffer reassembles SSE blocks across split chunks
    - Handles CRLF and mixed newline delimiter variations correctly
    - Joins multiline data: fields into unified payload with newlines
    - Exceeding 4 MiB frame size triggers SseFrameTooLarge error
    - Classifies response.completed, response.failed, and response.incomplete as terminal events
    - Ignores [DONE] as a completion event
  </acceptance_criteria>
  <verify>
    <automated>cargo test codex_endpoint::frame::tests::sse_framer</automated>
    <fails_when>non-zero exit code or test failure</fails_when>
  </verify>
  <done>SSE blocks are bounded, parsed, and classified correctly across all delimiter and chunking variations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Inbound WebSocket | Untrusted client HTTP handshake headers, query parameters, and WebSocket text frames |
| Shunt -> Upstream ChatGPT | Upstream SSE stream bytes and status responses across the network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | src/codex_endpoint/websocket.rs | High | mitigate | Enforce InboundAuth::authenticate_bearer before calling on_upgrade; reject unauthorized requests with HTTP 401 OpenAI error envelope before handshake completes (ASVS L1 2.1.1). |
| T-01-02 | Denial of Service | src/codex_endpoint/websocket.rs | High | mitigate | Reject binary frames and enforce strict 4 MiB limit on incoming text frames to prevent heap exhaustion (ASVS L1 5.1.1). |
| T-01-03 | Denial of Service | src/codex_endpoint/frame.rs | High | mitigate | Bound SSE frame buffer to MAX_CLIENT_SSE_FRAME_BYTES (4 MiB) and cap max frames per feed to prevent amplification attacks (ASVS L1 5.1.2). |
| T-01-04 | Tampering | Cargo.toml | High | mitigate | Use exact cargo package versions and verify builds compile with locked dependency features. |
</threat_model>

<verification>
Automated verification commands:
- cargo check --all-targets
- cargo test codex_endpoint::frame
</verification>

<success_criteria>
- Axum WebSocket support is active and compiled without errors
- GET route registration matches codex_endpoint::PATHS when enabled and 404s when disabled
- Unauthorized upgrade requests are rejected before 101 Switching Protocols
- Warmup response.create generate: false completes locally with zero upstream requests
- SSE block framer parses multiline, CRLF, split-chunk streams up to 4 MiB bounds
</success_criteria>

<output>
Create .planning/phases/01-inbound-responses-websocket/01-01-SUMMARY.md when done.
</output>

