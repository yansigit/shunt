---
phase: 01-inbound-responses-websocket
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - tests/inbound_codex_websocket.rs
  - docs/m11-inbound-codex-endpoint.md
  - README.md
  - README.ko.md
  - README.ja.md
  - README.zh-CN.md
  - site/src/content/docs/guides/inbound-codex-endpoint.md
  - site/src/content/docs/ja/guides/inbound-codex-endpoint.md
  - site/src/content/docs/ko/guides/inbound-codex-endpoint.md
  - site/src/content/docs/zh-cn/guides/inbound-codex-endpoint.md
  - site/src/content/docs/reference/endpoints.md
autonomous: true
requirements:
  - CONF-02
estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - End-to-end integration tests prove authenticated WebSocket upgrade across all 3 registered Responses paths (/backend-api/codex/responses, /responses, /v1/responses), disabled-endpoint 404 behavior, and pre-upgrade 401 rejection (per D-01, D-07, CONF-02).
    - Synchronized tests using Tokio signals prove that mid-stream turn replacement and socket disconnect abort the active upstream turn and leak zero stale frames (per D-04, D-09, CONF-02).
    - Documentation is synchronized across docs/m11-inbound-codex-endpoint.md, the root README and its 3 maintained translations (ko, ja, zh-CN), and Nimbus site guides/reference in all 4 locales (English, ko, ja, zh-cn), while wiki/ remains untouched (per D-10).
    - All repository quality gates pass: cargo fmt --all --check, cargo clippy --all-targets --all-features -- -D warnings, and cargo test --all-features --workspace.
  artifacts:
    - tests/inbound_codex_websocket.rs with full end-to-end test suite
    - docs/m11-inbound-codex-endpoint.md updated with WebSocket transport specification
    - README.md, README.ko.md, README.ja.md, README.zh-CN.md updated with WebSocket support
    - site/src/content/docs/ guides and reference updated in English, ko, ja, and zh-cn
  key_links:
    - tests/inbound_codex_websocket.rs exercises the real Axum server and MockServer upstream with synchronization barriers to prevent race conditions (per D-09).
    - Documentation changes cover both English and all maintained translations in the same PR without touching generated wiki/ files (per D-10, AGENTS.md).
---

<objective>
Complete end-to-end integration verification for inbound Responses WebSocket, update all English and translated documentation across READMEs and Nimbus site docs, and pass all repository quality gates (fmt, clippy, test).

Purpose: Delivers comprehensive regression coverage for the new transport boundary, maintains strict doc-code synchronization without drift, and validates repository release readiness.
Output: End-to-end integration test suite in tests/inbound_codex_websocket.rs, synchronized docs across 4 locales, and clean clippy/fmt/test run.

## must_haves
- All registered inbound paths upgrade over WebSocket with authentication, and disabled endpoint returns 404 (per D-01, CONF-02).
- Cancellation tests use deterministic synchronization primitives (Notify/barriers) rather than sleeps alone (per D-09, CONF-02).
- Documentation across READMEs (English, ko, ja, zh-CN), docs/m11, and Nimbus site docs (English, ko, ja, zh-cn) is updated in lockstep (per D-10).
- wiki/ is NOT modified (per D-10, AGENTS.md).
- cargo fmt, clippy (-D warnings), and cargo test pass across the full workspace.

## Prohibitions
- Do not buffer or collect a successful upstream SSE response.
- Do not forward client credentials, cookies, hop-by-hop headers, or internal Shunt headers upstream.
- Do not expose upstream cookies or arbitrary response headers in WS error frames.
- Do not add public config keys or alter documented provider selection semantics.
- Do not change credential-file writeback behavior.
- Do not add persistent history, a second pool, or a general response-repair framework.
- Do not hand-edit wiki/.

## Artifacts this phase produces
- tests/inbound_codex_websocket.rs (end-to-end integration test suite)
- docs/m11-inbound-codex-endpoint.md (WebSocket protocol addition)
- README.md, README.ko.md, README.ja.md, README.zh-CN.md (capability description)
- site/src/content/docs/guides/inbound-codex-endpoint.md and locales (ko, ja, zh-cn)
- site/src/content/docs/reference/endpoints.md (endpoint method updates)
</objective>

<execution_context>
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.12.0+gsd.dad16b6ef9a9.wrapper.2/gsd-core/workflows/execute-plan.md
@/Users/user/.codex/plugins/cache/gsd-codex-community/gsd-codex-plugin/1.12.0+gsd.dad16b6ef9a9.wrapper.2/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-inbound-responses-websocket/01-CONTEXT.md
@tests/inbound_codex_endpoint.rs
@docs/m11-inbound-codex-endpoint.md
@site/src/content/docs/guides/inbound-codex-endpoint.md
@site/src/content/docs/reference/endpoints.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end integration test suite with synchronized cancellation</name>
  <files>tests/inbound_codex_websocket.rs</files>
  <read_first>tests/inbound_codex_websocket.rs, tests/inbound_codex_endpoint.rs, src/codex_endpoint/websocket.rs</read_first>
  <action>
In tests/inbound_codex_websocket.rs, implement complete end-to-end tests against a live Axum server and MockServer upstream:
1. Multi-path upgrade test: verify GET upgrades succeed on /backend-api/codex/responses, /responses, and /v1/responses when [server.codex_endpoint] is enabled.
2. Disabled endpoint test: verify GET on all 3 paths returns 404 Not Found when [server.codex_endpoint] is absent.
3. Pre-upgrade auth rejection test: verify GET without token or with invalid token returns HTTP 401 with OpenAI Responses error envelope without upgrading to WebSocket.
4. Live streaming delivery test: send response.create frame, mock upstream streaming response, verify client receives ordered text frames through response.completed.
5. Synchronized replacement cancellation test (per D-09): use an Arc<tokio::sync::Notify> or channel inside a custom wiremock response stream. Start turn A, await notification that turn A is streaming, then send turn B. Assert turn A is aborted, turn A's body stream is dropped, and turn B completes without receiving turn A frames.
6. Synchronized disconnect cancellation test (per D-09): start turn, await notification that upstream is streaming, drop client WebSocket, verify upstream request/body is immediately cancelled and pool permits released.
7. Backpressure test: verify slow reading client does not cause runaway memory or broken delivery.
  </action>
  <acceptance_criteria>
    - All 3 paths upgrade when enabled; return 404 when disabled
    - Missing/invalid auth rejected with HTTP 401 before upgrade
    - Live turn stream delivers ordered WebSocket text frames
    - Replacement turn cancels active stream with zero stale frames leaked
    - Client disconnect aborts upstream request promptly
    - All tests use deterministic synchronization without flaky sleep dependencies
  </acceptance_criteria>
  <verify>
    <automated>cargo test --test inbound_codex_websocket</automated>
    <fails_when>non-zero exit code or test failure</fails_when>
  </verify>
  <done>Full end-to-end integration test suite passes reliably.</done>
</task>

<task type="auto">
  <name>Task 2: Engineering documentation and root README translations</name>
  <files>docs/m11-inbound-codex-endpoint.md, README.md, README.ko.md, README.ja.md, README.zh-CN.md</files>
  <read_first>docs/m11-inbound-codex-endpoint.md, README.md, README.ko.md, README.ja.md, README.zh-CN.md</read_first>
  <action>
Update engineering specifications and root README files:
1. In docs/m11-inbound-codex-endpoint.md, document the inbound WebSocket transport: GET upgrade on codex_endpoint::PATHS, handshake client auth enforcement, local generate: false warmup completions, single active turn per socket, replacement/disconnect cancellation, bounded SSE framer (4 MiB max), and safe error frame projection.
2. In README.md, update the [server.codex_endpoint] section and feature overview to note that the inbound Responses endpoint serves both HTTP/SSE and WebSocket v2 transports.
3. In README.ko.md, README.ja.md, and README.zh-CN.md, update the corresponding sections with accurate translations matching the English README changes (per D-10, AGENTS.md).
Do NOT edit wiki/ (wiki is generated).
  </action>
  <acceptance_criteria>
    - docs/m11-inbound-codex-endpoint.md accurately describes WebSocket transport mechanics and bounds
    - README.md notes WebSocket upgrade support for inbound Responses endpoint
    - README.ko.md, README.ja.md, and README.zh-CN.md contain synchronized translations of the capability
    - wiki/ directory is completely untouched
  </acceptance_criteria>
  <verify>
    <automated>git status --porcelain wiki/</automated>
    <fails_when>any output (wiki must not have any changes)</fails_when>
  </verify>
  <done>Engineering documentation and root README translations are fully synchronized.</done>
</task>

<task type="auto">
  <name>Task 3: Nimbus documentation site updates and workspace quality gates</name>
  <files>site/src/content/docs/guides/inbound-codex-endpoint.md, site/src/content/docs/ja/guides/inbound-codex-endpoint.md, site/src/content/docs/ko/guides/inbound-codex-endpoint.md, site/src/content/docs/zh-cn/guides/inbound-codex-endpoint.md, site/src/content/docs/reference/endpoints.md</files>
  <read_first>site/src/content/docs/guides/inbound-codex-endpoint.md, site/src/content/docs/reference/endpoints.md, AGENTS.md</read_first>
  <action>
Update Nimbus documentation site pages across all supported locales:
1. In site/src/content/docs/reference/endpoints.md (and locale copies if applicable), update the table rows for /backend-api/codex/responses, /responses, and /v1/responses to list methods as "GET, POST" with a note about WebSocket upgrade support.
2. In site/src/content/docs/guides/inbound-codex-endpoint.md and its locale counterparts (ja/, ko/, zh-cn/), document that Codex clients can connect via WebSocket (ws:// / wss://) or HTTP POST on the registered paths, explain local warmup, and describe streaming error envelopes.
3. Run workspace verification commands to enforce all repository boundaries:
   - cargo fmt --all --check
   - cargo clippy --all-targets --all-features -- -D warnings
   - cargo test --all-features --workspace
Fix any format, lint, or test regressions.
  </action>
  <acceptance_criteria>
    - site/src/content/docs/reference/endpoints.md lists GET, POST for Responses routes
    - Inbound Codex endpoint guide updated in English, ja, ko, and zh-cn
    - cargo fmt --all --check passes with zero diffs
    - cargo clippy --all-targets --all-features -- -D warnings passes with zero warnings
    - cargo test --all-features --workspace passes completely
  </acceptance_criteria>
  <verify>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <fails_when>non-zero exit code or clippy warnings</fails_when>
  </verify>
  <done>Nimbus documentation site is updated across all locales and all workspace quality gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Inbound WebSocket | Full end-to-end integration boundary over TCP/WebSocket |
| Documentation -> Client | Public operational documentation and endpoint specifications |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Denial of Service | tests/inbound_codex_websocket.rs | High | mitigate | Verify via integration test that rapid turn replacement and sudden socket drops release all account pool and concurrency permits without leakage (ASVS L1 5.2.1). |
| T-01-12 | Elevation of Privilege | tests/inbound_codex_websocket.rs | High | mitigate | Test that disabled endpoint configuration (codex_endpoint = None) strictly refuses connections with HTTP 404, preventing unauthorized access to unconfigured surfaces (ASVS L1 4.1.1). |
| T-01-13 | Information Disclosure | docs/ and site/ documentation | Medium | mitigate | Review documentation to ensure no internal URLs, secrets, or unallowlisted headers are documented as public interfaces. |
</threat_model>

<verification>
Automated verification commands:
- cargo test --test inbound_codex_websocket
- cargo fmt --all --check
- cargo clippy --all-targets --all-features -- -D warnings
- cargo test --all-features --workspace
</verification>

<success_criteria>
- End-to-end integration tests pass on all paths and lifecycle scenarios
- Synchronized cancellation proves zero stale frame leakage
- English and translated docs (ko, ja, zh-cn) are completely synchronized
- wiki/ is not modified
- fmt, clippy (-D warnings), and workspace test suite pass without issues
</success_criteria>

<output>
Create .planning/phases/01-inbound-responses-websocket/01-04-SUMMARY.md when done.
</output>

