---
phase: 02-native-responses-routing
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/codex_endpoint/websocket.rs
  - src/codex_endpoint.rs
  - tests/inbound_codex_websocket.rs
autonomous: true
requirements: [ROUTE-01, ROUTE-02]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "HTTP and WebSocket live turns call the same resolver and select the same provider for a model (D-08)."
    - "One refreshed config snapshot and one immutable route decision govern each live WebSocket turn; replacements and concurrent turns cannot mutate another decision, and later reload affects only later turns (D-01, D-08, D-10)."
    - "WebSocket framing remains Phase 1 bounded transport normalization and does not semantically translate the native payload (D-05, D-06)."
    - "No provider hop or replay occurs after a response event is observable (D-09)."
  artifacts:
    - path: src/codex_endpoint/websocket.rs
      provides: "Shared native resolver call, immutable per-turn route, and existing cancellation/backpressure lifecycle"
    - path: tests/inbound_codex_websocket.rs
      provides: "HTTP/WS parity, hot-reload concurrency, rejection, and no-hop fixtures"
  key_links:
    - from: src/codex_endpoint/websocket.rs
      to: src/routing.rs
      via: "run_turn resolves the model through the same native policy as HTTP"
    - from: tests/inbound_codex_websocket.rs
      to: src/codex_endpoint/websocket.rs
      via: "Notify-gated upstream proves event visibility and generation isolation"
  prohibitions:
    - statement: "Do not add a transport-specific resolver or mutate another live turn's decision during reload/replacement."
      status: unverified
      flagged: true
    - statement: "Do not replay or hop after the first WebSocket response event."
      status: unverified
      flagged: true
---

<objective>Extend the shared native routing contract to WebSocket live turns and prove concurrency, reload, parity, and stream-boundary invariants.</objective>
<context>@.planning/phases/02-native-responses-routing/02-CONTEXT.md
@.planning/phases/02-native-responses-routing/02-RESEARCH.md
@.planning/phases/02-native-responses-routing/02-PATTERNS.md
@src/codex_endpoint/websocket.rs
@tests/inbound_codex_websocket.rs</context>
<tasks>
<task type="auto">
  <name>Task 1: Use shared native resolution for WebSocket live turns</name>
  <files>src/codex_endpoint/websocket.rs, src/codex_endpoint.rs, tests/inbound_codex_websocket.rs</files>
  <read_first>src/codex_endpoint/websocket.rs, src/codex_endpoint.rs, src/routing.rs, tests/inbound_codex_websocket.rs</read_first>
  <action>Pass the normalized response.create model into the shared native resolver used by HTTP, capture the refreshed AppState and resulting decision once per turn, and pass its immutable Route into forwarding. Preserve Phase 1 removal of transport-only type/forcing stream, warmup, generation cancellation, bounded 4 MiB input, one-slot backpressure, and safe WS error frames. Add exact selected, pinned fallback, disabled-endpoint, boot-time opt-in, and incompatible/ambiguous pre-dispatch tests asserting HTTP/WS provider parity without claiming WS envelope byte equality (D-01, D-03, D-06, D-08, D-10, D-11).</action>
  <verify><automated>cargo test --test inbound_codex_websocket native_route && cargo test --test inbound_codex_websocket pinned_fallback && cargo test --test inbound_codex_websocket disabled_endpoint && cargo test --test inbound_codex_websocket boot_time_opt_in</automated><fails_when>WS selects a different provider than HTTP, disabled or boot-time opt-in behavior regresses, rejected routes contact a mock, lifecycle bounds regress, or any test fails</fails_when></verify>
  <acceptance_criteria>WebSocket exact and fallback decisions match HTTP; incompatible and ambiguous routes fail before dispatch; existing Phase 1 transport lifecycle tests remain green.</acceptance_criteria>
  <done>WebSocket live turns use the shared resolver and preserve Phase 1 lifecycle semantics.</done>
  <produced_artifacts>WS shared-resolver wiring and parity/rejection fixtures.</produced_artifacts>
</task>
<task type="auto">
  <name>Task 2: Prove immutable snapshots and no mid-stream hop under concurrency</name>
  <files>tests/inbound_codex_websocket.rs</files>
  <read_first>tests/inbound_codex_websocket.rs, src/codex_endpoint/websocket.rs, src/state.rs, 02-VALIDATION.md</read_first>
  <action>Build Notify-gated tests for two concurrent/replaced live turns and a hot reload between turns: each turn must retain its initial provider decision, a replacement must suppress stale frames without changing the newer decision, and a later turn must observe the refreshed config. Gate the upstream to deliver one response event before failure and assert no second-provider request after observability. Retain terminal-event, cancellation, bounded queue, and byte-faithful SSE payload assertions (D-08, D-09, D-10, D-11).</action>
  <verify><automated>cargo test --test inbound_codex_websocket hot_reload_snapshot && cargo test --test inbound_codex_websocket no_mid_stream_hop</automated><fails_when>concurrent turns share/mutate a route, reload changes an in-flight turn, stale frames leak, or a second provider is contacted</fails_when></verify>
  <acceptance_criteria>Automated probes establish the explicit invariant: each HTTP request and WebSocket live turn captures one refreshed config snapshot and one immutable route decision; concurrent/replaced turns are isolated and later reload affects only later turns.</acceptance_criteria>
  <done>Concurrency, reload, generation-isolation, and no-hop tests pass deterministically.</done>
  <produced_artifacts>Concurrency, hot-reload, generation-isolation, and no-mid-stream-hop WS tests.</produced_artifacts>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| WebSocket client -> live-turn supervisor | Concurrent untrusted frames and replacement signals |
| Upstream SSE -> WebSocket | First event establishes an observable replay boundary |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03-01 | Tampering | turn route state | high | mitigate | Capture snapshot/decision per turn and use generation checks; Notify tests exercise replacement and reload. |
| T-02-03-02 | Denial of service | WS relay | medium | mitigate | Preserve bounded frames, queue, cancellation, and awaited sends from Phase 1. |
</threat_model>
<verification><automated>cargo test --test inbound_codex_websocket</automated><fails_when>WebSocket integration suite fails</fails_when></verification>
