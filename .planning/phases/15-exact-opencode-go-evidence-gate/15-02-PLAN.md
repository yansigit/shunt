---
phase: phase-15-exact-opencode-go-evidence-gate
plan: 02
type: execute
wave: 2
depends_on: [15-01]
autonomous: true
files_modified:
  - .planning/phases/15-exact-opencode-go-evidence-gate/15-EVIDENCE.md
  - .planning/phases/15-exact-opencode-go-evidence-gate/15-EDGE-COVERAGE.json
  - tests/opencode_go_evidence.rs
requirements: [OGO-01, OGO-02]
must_haves:
  truths:
    - "D-04/D-05: all four named candidates (glm-5.3-flash, omen-alpha, muse-spark-1.3-contributor, deepseek-v4-flash) have dated, source/capture/live-separated records containing every required field with explicit unknowns; provenance is pinned to source revision 055c3ecf, inspection date 2026-09-08."
    - "D-06/OGO-02: the admitted tuple set is explicitly empty written into a machine-readable ledger frame; source-only, failed, unknown-field, wrong-wire, family-inferred, unsupported-effort, and wrong-terminal evidence is in a rejected register and cannot promote a tuple."
  artifacts:
    - path: ".planning/phases/15-exact-opencode-go-evidence-gate/15-EVIDENCE.md"
      provides: "machine-parsed four-candidate ledger, rejected-evidence register, and zero-admission disposition inside one fenced opencode-go-ledger JSON code block"
    - path: "tests/opencode_go_evidence.rs"
      provides: "deterministic ledger validation parsing the fenced JSON frame (not free prose)"
  key_links:
    - from: "15-EVIDENCE.md fenced JSON ledger frame"
      to: "tests/opencode_go_evidence.rs"
      via: "tests parse the block and assert candidate IDs, required fields, provenance classes, and admitted:[]"
  prohibitions:
    - statement: "Source-only records are never labeled captured or live; provenance classes stay source/capture/live-separated and unknowns stay unknown (D-05/D-11)."
      status: unresolved
      verification: judgment
    - statement: "OGO-02 and no edge row is silently resolved; the unclassified OGO-02 row stays unresolved and visibly flagged for manual review."
      status: unresolved
      verification: judgment
    - statement: "No tuple is admitted without both hermetic wire evidence and credential-safe capture evidence ledgered together; zero admission is a valid, asserted end state."
      status: unresolved
      verification: judgment
---

<objective>Produce the evidence ledger and explicit rejection accounting in a machine-readable frame before any tuple can be promoted.</objective>
<execution_context>
@.planning/phases/15-exact-opencode-go-evidence-gate/15-CONTEXT.md
@.planning/phases/15-exact-opencode-go-evidence-gate/15-RESEARCH.md
@.planning/phases/15-exact-opencode-go-evidence-gate/15-EDGE-COVERAGE.json
</execution_context>
<threat_model>
ASVS level 1; block high severity. Trust boundaries: external research capture (the source documents) and the planner-authored ledger itself. High spoofing threat: a fabricated quote or wrong revision hash could admit a tuple on false provenance; mitigate by pinning every record to source revision 055c3ecf with inspection date 2026-09-08 and having tests reject records whose provenance fields are missing or mismatched. High tampering threat: silently upgrading a source-only record to captured/live (or promoting a rejected tuple) would fake eligibility; mitigate by asserting the provenance class labels in the deterministic ledger test and failing on any nonzero admitted array without paired hermetic + credential-safe evidence. Medium information threat: no credential value, token, or live endpoint traffic is recorded in the ledger at any point. No runtime code changes in this plan.
</threat_model>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Four-candidate ledger, rejected-evidence register, and deterministic validation</name>
  <files>.planning/phases/15-exact-opencode-go-evidence-gate/15-EVIDENCE.md, .planning/phases/15-exact-opencode-go-evidence-gate/15-EDGE-COVERAGE.json, tests/opencode_go_evidence.rs</files>
  <read_first>.planning/phases/15-exact-opencode-go-evidence-gate/15-RESEARCH.md; .planning/phases/15-exact-opencode-go-evidence-gate/15-EDGE-COVERAGE.json</read_first>
  <behavior>D-04/D-05: glm-5.3-flash, omen-alpha, muse-spark-1.3-contributor, and deepseek-v4-flash each record exact model id, canonical https://opencode.ai/zen/go/v1 destination, wire (Chat Completions only), headers, context handling, modalities, effort semantics, tools behavior, filtering, terminal behavior, date/revision, and provenance class with explicit unknowns left unknown. Rejected register classifies every non-admissible tuple: failed evidence, unknown fields, wrong wire, family inference, unsupported effort, wrong terminal behavior, and unverified live assumptions. All eight edge rows carry explicit disposition, mapped criterion, verification owner, and evidence status; OGO-02 stays unclassified and flagged as a pending manual assumption — never silently resolved.</behavior>
  <action>Author the ledger inside one fenced JSON code block tagged opencode-go-ledger in 15-EVIDENCE.md (the machine-readable frame this plan defines): top-level object {candidates: [...], rejected: [...], admitted: []} with admitted exactly empty; rejected rows carry reason ∈ {failed-evidence, unknown-fields, wrong-wire, family-inference, unsupported-effort, wrong-terminal, unverified-live}. tests/opencode_go_evidence.rs parses that fenced block (not surrounding prose) and deterministically rejects: missing required fields on any candidate, a false captured/live label on source-only evidence, provenance not pinned to 055c3ecf / 2026-09-08, any nonzero admitted set lacking both hermetic and credential-safe evidence classes, and candidate set not exactly the four named ids. Update 15-EDGE-COVERAGE.json rows only where disposition text must match the frame (OGO-02 row stays unresolved/flagged).</action>
  <verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test opencode_go_evidence ledger -- --test-threads=1</automated><fails_when>The filter selects zero tests, a required field is omitted, a rejected row is silently promoted, OGO-02 is marked resolved, source-only evidence is labeled captured/live, or the fenced frame is absent so the test cannot parse it.</fails_when></verify>
  <acceptance_criteria>Ledger validation passes with four complete records, an explicit rejected register, admitted exactly [], and eight edge dispositions including the flagged OGO-02 assumption.</acceptance_criteria>
  <done>D-04/D-05/D-06 evidence is machine-checked, provenance-honest, and structurally parseable by the owning test.</done>
</task>
</tasks>
