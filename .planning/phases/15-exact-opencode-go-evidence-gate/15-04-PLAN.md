---
phase: phase-15-exact-opencode-go-evidence-gate
plan: 04
type: execute
wave: 5
depends_on: [15-01, 15-02, 15-03, 15-05]
autonomous: true
files_modified:
  - tests/check_cli.rs
  - .planning/phases/15-exact-opencode-go-evidence-gate/15-VALIDATION.md
requirements: [OGO-02, OGO-03, OGO-04]
must_haves:
  truths:
    - "D-09/D-11: an owned synthetic isolated CLI smoke starts the project binary through the isolation wrapper with a fresh temporary OPENCODEX_HOME and a non-10100 port, sends an actual POST /v1/messages routed at the synthetic opencode-go upstream, asserts the gateway-owned unsupported-selection error (Anthropic shape on the standard channel), verifies the fixture received zero requests (no egress), and removes its temporary state. A mere missing-credential startup exit is NOT the asserted path: a fake SHUNT_OPENCODE_GO_API_KEY is set so boot succeeds and rejection is exercised through real admission."
    - "D-08/OGO-02: warm-cache RUSTFLAGS=-Dwarnings formatting, Clippy, and workspace test gates pass with strict terminal and zero-admission tests; focused commands use one Cargo -- separator with a non-zero test selection."
  artifacts:
    - path: "tests/check_cli.rs"
      provides: "owned synthetic negative CLI smoke exercising real request rejection, fixture request counter == 0, and cleanup assertions (opencode_go_cli_negative)"
    - path: ".planning/phases/15-exact-opencode-go-evidence-gate/15-VALIDATION.md"
      provides: "task-level verification map including 15-05 rows and honest sign-off state"
  key_links:
    - from: "tests/check_cli.rs (process-level smoke)"
      to: "src/proxy/opencode_go_tests.rs (crate-local counter evidence)"
      via: "separate proofs: the CLI smoke needs no crate-private resolver seam; the counter proof stays crate-local (15-01)"
  prohibitions:
    - statement: "The general check_cli suite is never relabeled as the OpenCode Go smoke; the dedicated opencode_go_cli_negative test is the only smoke evidence."
      status: flagged
      verification: unverified
    - statement: "No production /Users/user/.opencodex, port 10100, or credential file is read; every stateful command runs under node /tmp/shunt-phase12-isolated-run.cjs with fingerprint audit preserved."
      status: flagged
      verification: unverified
    - statement: "HTML-encoded shell conjunctions (literal &amp;&amp;) are never used; release-gate commands run as separate wrapper invocations rather than one fragile chained call."
      status: flagged
      verification: unverified
---

<objective>Close the phase with a real interaction-level negative smoke (request sent, correctly rejected, zero egress, cleaned state) and honest, non-zero-selecting quality gates.</objective>
<execution_context>
@.planning/phases/15-exact-opencode-go-evidence-gate/15-CONTEXT.md
@.planning/phases/15-exact-opencode-go-evidence-gate/15-VALIDATION.md
@.planning/phases/15-exact-opencode-go-evidence-gate/15-EVIDENCE.md
</execution_context>
<threat_model>
ASVS level 1; block high severity. Trust boundaries: spawned gateway process and its temp home config; loopback fixture server. High spoofing threat: a process-level smoke that only proves a config parse exit would wallpaper over real rejection; mitigate by asserting an actual POST /v1/messages exchange that hits the pre-credential admission gate with counters proving zero upstream egress. High escape threat: a miswired test could contact production /Users/user/.opencodex or port 10100 or leave temp credentials on disk; mitigate with the isolation wrapper, explicit temp home creation/removal assertions, non-10100 fixture port, and fingerprint checks. Medium information threat: synthetic credentials never the real env. No runtime source changes in this plan.
</threat_model>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Real rejection CLI smoke — actual unsupported request, zero egress, cleaned temp state</name>
  <files>tests/check_cli.rs</files>
  <read_first>tests/check_cli.rs (existing run/check spawn pattern); src/main.rs; src/config.rs; .planning/phases/15-exact-opencode-go-evidence-gate/15-CONTEXT.md D-09/D-11; AGENTS.md gateway error-shape rule (issue #127)</read_first>
  <behavior>D-09/D-11 (test opencode_go_cli_negative): through the isolation wrapper the test (a) creates a fresh temporary OPENCODEX_HOME and writes a synthetic config selecting the opencode-go provider with base_url overridden to the test fixture origin and a fake SHUNT_OPENCODE_GO_API_KEY set in the process env so boot succeeds from credentials, not from production state; (b) spawns the owned release/debug binary on a free non-10100 port; (c) sends an actual POST /v1/messages with a real request body routed to the synthetic Go upstream; (d) asserts the HTTP error is the gateway-owned unsupported-selection error in the Anthropic error shape (standard inbound channel; the codex endpoint keeps its OpenAI Responses shape per issue #127); (e) asserts the fixture server received zero requests — no model list, no auth probe, no partial upstream attempt; (f) asserts the temporary home was removed or verifiably contained no credential file, and production fingerprints are unchanged.</behavior>
  <action>Extend tests/check_cli.rs with the dedicated test using visibly synthetic values (fake key string, 127.0.0.1 fixture, fresh mktemp-style home). The earlier missing-credential exit case is explicitly NOT evidence here; rejection must occur post-boot via the 15-01 admission gate on a real routed request. Keep the crate-local CredentialResolver counter proofs in 15-01 rather than re-implementing them in an integration binary.</action>
  <verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test check_cli opencode_go_cli_negative -- --test-threads=1</automated><fails_when>The filter selects zero tests, the error case is only a startup/missing-credential exit, a fixture request count is nonzero, temporary state persists, or production fingerprints change.</fails_when></verify>
  <acceptance_criteria>An owned synthetic smoke proves real request rejection with correct error shape, zero outbound requests, nonzero test selection, and state cleanup.</acceptance_criteria>
  <done>CLI evidence exercises the actual admission path end-to-end and is not misreported as generic check_cli coverage.</done>
</task>
<task type="auto">
  <name>Task 2: Run and record release gates (separate wrapper calls, warm cache, single -- per focused command)</name>
  <files>.planning/phases/15-exact-opencode-go-evidence-gate/15-VALIDATION.md</files>
  <read_first>.planning/phases/15-exact-opencode-go-evidence-gate/15-VALIDATION.md; .planning/phases/15-exact-opencode-go-evidence-gate/15-EDGE-COVERAGE.json; all five revised plan verify commands</read_first>
  <behavior>D-10/D-11: the verification map lists every plan/task ID (15-01..15-05 inclusive) with its exact executable command. Focused commands run with a nonzero selection and one -- separator; release gates run as separate wrapper invocations (fmt, clippy, workspace tests, site build) — never one fragile chained call, never HTML-encoded conjunctions — with warm-cache RUSTFLAGS=-D warnings spelling preserved for Clippy and the workspace suite.</behavior>
  <action>Run: cargo fmt --all --check; cargo clippy --all-targets --all-features -- -D warnings (warm cache, RUSTFLAGS=-D warnings); env RUSTFLAGS=-Dwarnings cargo test --all-features --workspace; npm --prefix site run build; plus every per-plan focused command from 15-01/15-02/15-03/15-05 and Task 1 above, each through the isolation wrapper. Update 15-VALIDATION.md: add 15-05 per-file locale assertion rows, fix all focused commands to the single -- form, and note the ledger's fenced machine-readable frame so validation names the parseable artifact rather than unspecified Markdown parsing. Keep rejected-ledger categories (failed evidence, unknown fields, wrong wire, family inference, unsupported effort, wrong terminal, unverified live) and leave OGO-02 flagged as a manual assumption; docs scope notes cover README locales, site locales, nav, and the engineering note per 15-03/15-05 ownership.</action>
  <verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check</automated><fails_when>Any gate fails, a focused command selects zero tests, a validation claim asserts live/captured support, the verification map omits 15-05 or the per-file assertion evidence, or production fingerprints change.</fails_when></verify>
  <acceptance_criteria>Validation records executable nonzero-selecting commands, warm-cache warnings-denied gates, the separate CLI smoke, 15-05 locale assertions, full surface ownership notes, and the honest pending manual OGO-02 status.</acceptance_criteria>
  <done>Phase validation is complete without claiming live support or probing production state.</done>
</task>
</tasks>
