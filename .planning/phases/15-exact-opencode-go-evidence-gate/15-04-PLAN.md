---
phase: phase-15-exact-opencode-go-evidence-gate
plan: 04
type: execute
wave: 4
depends_on: [15-01, 15-02, 15-03]
files_modified:
  - tests/check_cli.rs
  - .planning/phases/15-exact-opencode-go-evidence-gate/15-VALIDATION.md
requirements: [OGO-02, OGO-03, OGO-04]
must_haves:
  truths:
    - "D-10/D-11: an owned synthetic isolated CLI negative smoke proves status, zero outbound requests, and temporary-state cleanup without reading credentials or touching production state."
    - "D-08/OGO-02: warm-cache formatting, warnings-denied Clippy, and workspace tests pass with strict terminal and zero-admission tests."
  artifacts:
    - path: "tests/check_cli.rs"
      provides: "owned synthetic negative CLI smoke with request counter and cleanup assertions"
    - path: ".planning/phases/15-exact-opencode-go-evidence-gate/15-VALIDATION.md"
      provides: "task-level verification map and honest sign-off state"
  key_links:
    - from: "tests/check_cli.rs"
      to: "src/proxy/opencode_go_tests.rs"
      via: "separate CLI smoke versus crate-local counter evidence"
  prohibitions:
    - "Do not relabel the general check_cli suite as the OpenCode Go smoke."
    - "Do not HTML-encode shell conjunctions; use raw shell && or separate commands."
---

<objective>Close the phase with honest quality gates and a separately owned synthetic CLI negative smoke.</objective>
<execution_context>
@.planning/phases/15-exact-opencode-go-evidence-gate/15-CONTEXT.md
@.planning/phases/15-exact-opencode-go-evidence-gate/15-VALIDATION.md
</execution_context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add the isolated negative CLI smoke</name>
  <files>tests/check_cli.rs</files>
  <read_first>tests/check_cli.rs; src/main.rs; src/config.rs</read_first>
  <behavior>D-09/D-11: a synthetic opencode-go config with no real credential exits nonzero, emits no outbound request to the fixture server, and removes its fresh temporary OPENCODEX_HOME; no production home or port 10100 is contacted.</behavior>
  <action>Add a dedicated test/binary action with visibly synthetic values, a fresh temporary home, non-10100 fixture port, request counter, status assertion, and cleanup assertion. Keep this evidence distinct from crate-local resolver counters and document missing credentials as an expected negative result.</action>
  <verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --test check_cli -- opencode_go_cli_negative -- --test-threads=1</automated><fails_when>Filter selects zero tests, status is successful, fixture request count is nonzero, temporary state remains, or the test targets production state.</fails_when></verify>
  <acceptance_criteria>Owned synthetic CLI smoke independently proves nonzero status, zero outbound requests, and cleanup.</acceptance_criteria>
  <done>CLI evidence is present and not misreported as general check_cli coverage.</done>
</task>
<task type="auto">
  <name>Task 2: Run and record release gates</name>
  <files>.planning/phases/15-exact-opencode-go-evidence-gate/15-VALIDATION.md</files>
  <read_first>.planning/phases/15-exact-opencode-go-evidence-gate/15-VALIDATION.md; .planning/phases/15-exact-opencode-go-evidence-gate/15-EDGE-COVERAGE.json</read_first>
  <action>D-10: update the verification map with exact plan/task IDs and commands. Run each through node /tmp/shunt-phase12-isolated-run.cjs using warm-cache RUSTFLAGS=-D warnings for Clippy/workspace tests, separate real libtest filters, cargo fmt, and site build. Record rejected-ledger categories (failed evidence, unknown, wrong wire, family inference, effort mismatch, wrong terminal) and leave live success/manual OGO-02 assumptions explicitly flagged.</action>
  <verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo fmt --all --check && node /tmp/shunt-phase12-isolated-run.cjs cargo clippy --all-targets --all-features -- -D warnings && node /tmp/shunt-phase12-isolated-run.cjs env RUSTFLAGS=-Dwarnings cargo test --all-features --workspace && node /tmp/shunt-phase12-isolated-run.cjs npm --prefix site run build</automated><fails_when>Any gate fails, a filter selects zero tests, production-state fingerprints change, or the validation document claims live/captured support without evidence.</fails_when></verify>
  <acceptance_criteria>Validation records executable nonzero-selecting commands, warm-cache warnings-denied gates, separate CLI smoke, all edge dispositions, and honest pending manual OGO-02 status.</acceptance_criteria>
  <done>Phase validation is complete without claiming implementation or live support.</done>
</task>
</tasks>
