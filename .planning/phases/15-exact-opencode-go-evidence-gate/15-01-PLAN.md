---
phase: phase-15-exact-opencode-go-evidence-gate
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - src/config/presets.rs
  - src/config/upstreams.rs
  - src/config.rs
  - src/routing.rs
  - src/proxy/capability.rs
  - src/proxy/failover.rs
  - src/codex_endpoint.rs
  - src/proxy.rs
  - src/server.rs
  - src/proxy/opencode_go_tests.rs
requirements: [OGO-02, OGO-04]
must_haves:
  truths:
    - "D-01/D-02: identity is one approved additive enum variant, ProviderKind::OpenCodeGo serialized as kind opencode_go, plus one ordered opencode-go preset row (canonical base https://opencode.ai/zen/go/v1, AuthMode::ApiKey, env SHUNT_OPENCODE_GO_API_KEY); no extra ProviderConfig field exists and full-literal call sites are untouched."
    - "D-03/OGO-04: a shared crate-private admission gate rejects every Go selection (exact table is empty) before any CredentialResolver call or HTTP client invocation on primary, fallback, count-tokens, and inbound Codex paths; the gate never infers Go from URL, bearer, nickname, family, or host model."
    - "D-08: the gate adds no EOF recovery, aliases, dynamic catalogs, or session producers; OpenCodeGo maps to AdapterKind::OpenAiChat and every other exhaustive match stays arm-complete without a new arm."
  artifacts:
    - path: "src/config.rs"
      provides: "ProviderKind::OpenCodeGo additive variant with #[serde(rename = \"opencode_go\")]"
    - path: "src/config/presets.rs"
      provides: "table-driven opencode-go preset row at https://opencode.ai/zen/go/v1 with AuthMode::ApiKey and SHUNT_OPENCODE_GO_API_KEY"
    - path: "src/proxy/opencode_go_tests.rs"
      provides: "crate-local cfg(test) behavioral RED/GREEN tests with injected credential and HTTP counters"
  key_links:
    - from: "src/config/upstreams.rs:202 (presets::find -> ConfigError::UnknownProviderPreset)"
      to: "src/proxy/opencode_go_tests.rs"
      via: "config-acceptance RED asserts load_from_str accepts provider = opencode-go"
    - from: "src/proxy/capability.rs (new pub(super) fn enforce_opencode_go_admission)"
      to: "src/proxy/failover.rs; src/codex_endpoint.rs"
      via: "one shared reject function called by both forward paths before client/credential seams"
  prohibitions:
    - statement: "No public test seams: gate internals, counters, and resolver injection stay crate-private cfg(test); integration binaries never name pub(crate) CredentialResolver or builder helpers."
      status: unresolved
      verification: judgment
    - statement: "No Go inference from generic URL, bearer, nickname, family, or host-model signals; only the explicit provider = opencode-go preset (or equivalent kind opencode_go config) trips the gate."
      status: unresolved
      verification: judgment
    - statement: "No credential-file writeback and no generic-provider behavior change; existing config backups and sibling preset rows are preserved bit-for-bit."
      status: unresolved
      verification: judgment
---

<objective>Deliver the first vertical slice: an approved additive product identity, an empty exact admission gate wired to every routing boundary, and behavioral RED-first evidence that unsupported Go selections fail closed before secrets or sockets.</objective>

<execution_context>
@.planning/phases/15-exact-opencode-go-evidence-gate/15-CONTEXT.md
@.planning/phases/15-exact-opencode-go-evidence-gate/15-RESEARCH.md
@.planning/phases/15-exact-opencode-go-evidence-gate/15-PATTERNS.md
</execution_context>

<threat_model>
ASVS level 1; block high severity. Trust boundaries: operator config (approved additive preset/kind and env) and the upstream-facing resolution chain. High spoofing threat: a lookalike base URL or nickname could smuggle a Go product identity; mitigate by making the gate key solely on the named preset/kind and asserting the canonical URL in tests. High tampering threat: an unadmitted selection reaching the credential resolver could leak the SHUNT_OPENCODE_GO_API_KEY or a foreign bearer; mitigate by placing the shared gate upstream of check_inbound_auth and resolver/client access with zero-counter assertions. Medium information threat: oversized debug logging of rejected selections; mitigate by emitting one concise gateway-shaped error. No extra public endpoints or ProviderConfig fields are added; the approved ProviderKind/preset addition is the sole public config surface, and test seams remain crate-private.
</threat_model>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Behavioral RED — config acceptance of the opencode-go preset (compiles today)</name>
  <files>src/proxy.rs, src/proxy/opencode_go_tests.rs</files>
  <read_first>src/config/presets.rs; src/config/upstreams.rs:190-210; src/config.rs (ProviderKind, ConfigError::UnknownProviderPreset, Config::load_from_str); src/routing.rs:24-39; src/proxy.rs:160-170</read_first>
  <behavior>tests named opencode_go_config_acceptance (cfg(test), under src/proxy/opencode_go_tests.rs declared in src/proxy.rs as #[cfg(test)] mod opencode_go_tests;) build against CURRENT code and fail a behavioral assertion: Config::load_from_str on a synthetic TOML that selects provider = "opencode-go" (the same preset-selection shape the commandcode row uses today) must return Ok. Today upstreams.rs:202 returns ConfigError::UnknownProviderPreset, so the assertion genuinely fails while the test compiles; the new enum and serialized kind are asserted only by the later GREEN test.</behavior>
  <action>Own src/proxy.rs and add the single module declaration #[cfg(test)] mod opencode_go_tests; next to the existing pub(crate) mod capability declaration (precedent: src/codex_endpoint.rs cfg(test) mod tests sibling pattern). Author the config-acceptance test first with let result = Config::load_from_str(synthetic_go_toml); assert!(result.is_ok(), "{result:?}"); run it through the isolation wrapper to record genuine behavioral RED, never a compile failure and never a generic URL or nickname heuristic. No production change in this task.</action>
  <verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib opencode_go_config_acceptance -- --test-threads=1</automated><fails_when>The filter selects zero tests, the pre-implementation run passes, the failure is reported as a compile error instead of a behavioral assertion, or the test names a non-preset heuristic.</fails_when></verify>
  <acceptance_criteria>A crate-local test that compiles against unmodified code fails on the UnknownProviderPreset behavioral cliff before any production edit exists.</acceptance_criteria>
  <done>RED evidence is recorded for a future GREEN that only a real preset row plus variant can satisfy.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN — additive identity, preset row, and one shared admission gate</name>
  <files>src/config.rs, src/config/presets.rs, src/config/upstreams.rs, src/routing.rs, src/proxy/capability.rs, src/proxy/failover.rs, src/codex_endpoint.rs, src/proxy/opencode_go_tests.rs</files>
  <read_first>Task 1 artifacts; src/routing.rs From<ProviderKind> match (line ~24); src/proxy/capability.rs filter_fallbacks; src/proxy/failover.rs forward (resolve_request_chain_value -> filter_fallbacks -> check_inbound_auth); src/codex_endpoint.rs:407 resolve_native_inbound; src/proxy/capability.rs incompatibilities; src/proxy/failover.rs dispatch match and src/codex_endpoint.rs:510 wildcard</read_first>
  <behavior>D-02/D-03/OGO-04: opencode_go_config_acceptance turns GREEN — the synthetic config now resolves ProviderKind::OpenCodeGo (kind opencode_go) with the canonical base and env. No generic provider's routing, backups, or preset table rows change. New From<ProviderKind> arm maps OpenCodeGo -> AdapterKind::OpenAiChat; every other exhaustive match site already compiles because it matches on AdapterKind or has a wildcard, verified during this task: failover dispatch per-variant match, capability::incompatibilities (exhaustive, no wildcard), and codex_endpoint's _ => unreachable! wildcard — the plan adds NO arm there and NO dormant adapter implementation.</behavior>
  <action>Add the additive ProviderKind::OpenCodeGo variant (serde opencode_go) beside CommandCode; add the ordered opencode-go preset row (canonical https://opencode.ai/zen/go/v1, AuthMode::ApiKey, SHUNT_OPENCODE_GO_API_KEY) following the commandcode/command-code row precedent. Extend provider validation with canonical-destination and API-key/env fixtures: the exact preset is accepted, while wrong destination, auth mode, or env combinations for this kind are rejected without changing existing OpenAiChat guards. Implement one shared crate-visible reject helper in src/proxy/capability.rs with an empty exact-tuple table. Extend filter_fallbacks with configured-provider-kind context so later OpenCodeGo routes are removed while index 0 is preserved; a Go primary is rejected before credentials, while a generic primary remains valid and uses only its own resolver path. Call the helper from failover::forward after fallback filtering and before check_inbound_auth, count_tokens truncation, or dispatch. In codex_endpoint, preserve resolve_native_inbound's authoritative rejection for exact OpenAiChat-backed Go routes, explicitly reject pinned or unknown Go routes before client/resolver access, and use a crate-visible helper only where that path is reachable; do not claim the shared helper ran for the existing exact native rejection. Keep the admitted table empty; adding even one tuple later requires OGO-02 evidence.</action>
  <verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib opencode_go_config_acceptance -- --test-threads=1</automated><fails_when>The config still fails acceptance, an extra ProviderConfig field appears, any existing preset row or external literal changes, From<ProviderKind> does not name OpenCodeGo, a dormant adapter or wildcard arm is added, or the empty table contains a tuple.</fails_when></verify>
  <acceptance_criteria>Identity is additive and table-driven; one shared gate covers every Go selection path before resolver/client; the RED test is now GREEN.</acceptance_criteria>
  <done>Config marker survives preset expansion into a real ProviderKind without breaking anything else.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Prove zero-egress and generic-preservation at the router boundaries</name>
  <files>src/proxy/opencode_go_tests.rs, src/proxy/capability.rs, src/proxy/failover.rs, src/codex_endpoint.rs, src/server.rs</files>
  <read_first>Task 2 artifacts; src/proxy/failover.rs forward; src/codex_endpoint.rs forward</read_first>
  <behavior>D-03/D-08 (tests named opencode_go_router_boundaries): primary, fallback, count-tokens, and inbound Codex unsupported selections fail before any CredentialResolver call or HTTP client invocation — injected counter totals stay zero — and no session header or bearer is constructed. Generic Chat and native Codex positive controls still route through their unchanged paths (concurrency-safe shared gate).</behavior>
  <action>Strengthen the crate-local harness with injected counters and a test-only server wrapper owned in src/server.rs, for example #[cfg(test)] pub(crate) fn build_router_with_test_dependencies(config: Config, client: reqwest::Client, resolver: Arc<dyn CredentialResolver>) -> ...; this exposes no production API. Cover empty Go admission, wrong auth, off-origin config, generic-primary-plus-Go-fallback (generic lookups remain legitimate while Go lookups stay zero), Go primary, count_tokens, exact native Go rejection from resolve_native_inbound, and pinned/unknown native Go rejection; assert no selected Go case reaches resolver/client while the generic Chat control runs unchanged.</action>
  <verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib opencode_go_router_boundaries -- --test-threads=1</automated><fails_when>Any selected Go case reaches resolver/client code, a session or bearer is emitted, a generic positive control changes status, or the filter selects zero tests.</fails_when></verify>
  <acceptance_criteria>All required boundaries are covered by crate-local evidence that fails if the gate order ever changes.</acceptance_criteria>
  <done>D-03/OGO-04 are executable at the real router, with counters proving zero lookups and zero requests.</done>
</task>
</tasks>
