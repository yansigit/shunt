---
phase: 15-exact-opencode-go-evidence-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/presets.rs
  - src/config/upstreams.rs
  - src/config.rs
  - src/proxy/capability.rs
  - src/proxy/failover.rs
  - src/routing.rs
  - src/codex_endpoint.rs
  - tests/command_code_conformance.rs
  - tests/inbound_codex_endpoint.rs
  - tests/openai_chat_conformance.rs
autonomous: true
requirements: [OGO-02, OGO-04]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The opt-in OpenCode Go identity survives preset expansion as an explicit product marker; generic OpenAI Chat routes retain their existing behavior."
    - "Every candidate Go selection, including primary, fallback, count-tokens, and inbound Codex requests, is rejected before credential lookup or network dispatch when the admitted tuple table is empty."
    - "Existing generic Chat and native inbound positive controls continue to route successfully."
  artifacts:
    - path: "src/config/presets.rs"
      provides: "Additive opencode-go preset and stable ordering"
    - path: "src/config/upstreams.rs"
      provides: "Explicit product identity copied into normalized provider state"
    - path: "src/proxy/capability.rs"
      provides: "Exact Go admission filtering before fallback credential resolution"
    - path: "tests/openai_chat_conformance.rs"
      provides: "Real-router zero-credential/zero-dispatch negative tracer"
  key_links:
    - from: "src/config/upstreams.rs"
      to: "src/proxy/capability.rs"
      via: "normalized product marker consumed by exact tuple admission"
    - from: "src/proxy/failover.rs"
      to: "src/codex_endpoint.rs"
      via: "shared rejection before credential/client seams"
prohibitions:
  - "Do not infer Go identity from URL, nickname, bearer, model family, or host model lists."
  - "Do not add Responses/Anthropic Go dispatch, session-header production, or EOF recovery while no tuple has captured/live evidence."
  - "Do not change credential-file writeback or generic provider semantics."
---

<objective>
Build the tracer-first, fail-closed OpenCode Go admission path with an empty admitted-tuple set.

Purpose: satisfy D-02, D-03, D-06, D-08, and D-09 while proving the real gateway rejects unsupported Go selections before any secret or socket boundary.
Output: explicit opt-in config identity, crate-private empty exact-tuple gate, and executable negative router evidence.
</objective>

<execution_context>
@.planning/phases/15-exact-opencode-go-evidence-gate/15-CONTEXT.md
@.planning/phases/15-exact-opencode-go-evidence-gate/15-RESEARCH.md
@.planning/phases/15-exact-opencode-go-evidence-gate/15-PATTERNS.md
@.planning/phases/14-command-code-product-separation/14-01-PLAN.md
@.planning/phases/14-command-code-product-separation/14-02-PLAN.md
</execution_context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Add the real-router empty-admission tracer</name>
  <files>tests/openai_chat_conformance.rs, tests/command_code_conformance.rs, tests/inbound_codex_endpoint.rs, src/proxy/capability.rs, src/proxy/failover.rs</files>
  <read_first>tests/openai_chat_conformance.rs; tests/command_code_conformance.rs; tests/inbound_codex_endpoint.rs; src/proxy/failover.rs; src/proxy/capability.rs</read_first>
  <behavior>Unknown, family-inferred, out-of-ladder, and wrong-wire Go selections return a gateway error with zero injected credential lookups and zero mock-server requests; the same assertion holds for fallback, count-tokens, and inbound Codex paths, while a generic Chat positive fixture still dispatches.</behavior>
  <action>Write failing behavioral fixtures against the existing injectable credential and loopback-client seams, then add the smallest pre-credential exact-product admission call used by primary and fallback filtering and by the inbound decision. Keep candidate records empty so all four named candidates remain unsupported; preserve strict terminal handling and the existing generic Chat branch.</action>
  <verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features openai_chat_conformance command_code_conformance inbound_codex_endpoint -- --test-threads=1</automated><fails_when>Any selected Go case reaches a credential resolver or mock socket, or any generic positive control changes status.</fails_when></verify>
  <acceptance_criteria>Behavioral tests fail before implementation and pass after the gate is wired; no credential counter or socket counter increments for any unsupported Go route.</acceptance_criteria>
  <done>End-to-end negative tracer proves zero-admission rejection on every required boundary and preserves a generic positive route.</done>
  <reversibility rating="reversible">The gate and fixtures are additive and can be extended only when evidence is later promoted.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Preserve explicit Go product identity through config normalization</name>
  <files>src/config/presets.rs, src/config/upstreams.rs, src/config.rs, src/routing.rs, src/codex_endpoint.rs</files>
  <read_first>src/config/presets.rs; src/config/upstreams.rs; src/config.rs; src/routing.rs; src/codex_endpoint.rs</read_first>
  <action>Introduce one table-driven opt-in opencode-go preset using the canonical Go base and API-key mode, carry a private product marker through preset expansion and runtime snapshots, and route that marker into the exact empty admission gate. Update preset-order and normalization tests without changing existing defaults, inbound Responses/Anthropic selection, count-token applicability, or credential writeback.</action>
  <verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features opencode_go -- --test-threads=1</automated><fails_when>Normalized state loses the Go marker, generic providers acquire Go behavior, or an unsupported route reaches credential resolution.</fails_when></verify>
  <acceptance_criteria>The marker is explicit and immutable after expansion; no URL or model-family inference exists; all four candidates reject before credential/network activity.</acceptance_criteria>
  <done>Config and routing identity are wired to the tracer gate with generic behavior unchanged.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| config → route admission | User-selected provider/model/effort crosses into routing decisions |
| route admission → credentials/network | Rejected tuples could otherwise trigger secret lookup or outbound I/O |
| inbound Codex → provider route | External model selection crosses the native compatibility boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-15-01 | Elevation | exact Go admission | high | mitigate | Require explicit product marker and empty-table rejection before credentials. |
| T-15-02 | Information disclosure | credential/client seams | high | mitigate | Assert zero lookup and zero socket counters in real-router fixtures. |
| T-15-03 | Tampering | preset normalization | medium | mitigate | Preserve immutable marker and exact lowercase tuple identity. |
</threat_model>

<artifacts_this_phase_produces>
- Explicit opencode-go preset identity and empty exact-tuple admission gate.
- Real-router negative evidence for primary, fallback, count-tokens, and inbound Codex paths.
</artifacts_this_phase_produces>
