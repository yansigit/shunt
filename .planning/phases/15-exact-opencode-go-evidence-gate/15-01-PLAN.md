---
phase: phase-15-exact-opencode-go-evidence-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/presets.rs
  - src/config/upstreams.rs
  - src/config.rs
  - src/proxy/capability.rs
  - src/proxy/failover.rs
  - src/routing.rs
  - src/codex_endpoint.rs
  - src/proxy/opencode_go_tests.rs
requirements: [OGO-02, OGO-04]
must_haves:
  truths:
    - "D-01/D-02: an additive opencode-go marker survives preset expansion without changing generic providers or backed-up settings."
    - "D-03/OGO-04: an empty exact tuple table rejects unknown, family-inferred, wrong-wire, and out-of-ladder selections before credential lookup and network dispatch on primary, fallback, count-tokens, and inbound Codex paths."
    - "D-08: the gate does not add EOF recovery, aliases, dynamic catalogs, or session producers."
  artifacts:
    - path: "src/proxy/opencode_go_tests.rs"
      provides: "crate-local cfg(test) behavioral RED/GREEN tests with injected credential and HTTP counters"
    - path: "src/config/presets.rs"
      provides: "table-driven opencode-go preset at https://opencode.ai/zen/go/v1 with SHUNT_OPENCODE_GO_API_KEY"
  key_links:
    - from: "src/config/upstreams.rs"
      to: "src/proxy/capability.rs"
      via: "private product marker consumed before resolver"
    - from: "src/proxy/failover.rs"
      to: "src/codex_endpoint.rs"
      via: "shared admission gate before client and credential seams"
  prohibitions:
    - "No URL, nickname, bearer, family, or host-model inference."
    - "No public API widening; use crate-private cfg(test) seams."
    - "No credential-file writeback or generic-provider behavior change."
---

<objective>Deliver the first vertical slice: explicit config identity, an empty exact admission gate, and real-router behavioral proof that unsupported Go selections fail closed before secrets or sockets.</objective>

<execution_context>
@.planning/phases/15-exact-opencode-go-evidence-gate/15-CONTEXT.md
@.planning/phases/15-exact-opencode-go-evidence-gate/15-RESEARCH.md
@.planning/phases/15-exact-opencode-go-evidence-gate/15-PATTERNS.md
</execution_context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Add config identity, empty gate, and behavioral RED</name>
  <files>src/config/presets.rs, src/config/upstreams.rs, src/config.rs, src/proxy/capability.rs, src/proxy/failover.rs, src/routing.rs, src/codex_endpoint.rs, src/proxy/opencode_go_tests.rs</files>
  <read_first>src/config/presets.rs; src/config/upstreams.rs; src/proxy/capability.rs; src/proxy/failover.rs; src/routing.rs; src/codex_endpoint.rs; src/auth/mod.rs; src/server.rs</read_first>
  <behavior>D-02/D-03: selecting opencode-go with each exact candidate, an unknown/family name, a wrong wire, or an out-of-ladder effort returns a gateway error; injected credential lookup count and HTTP request count remain zero. The same holds through fallback, count-tokens, and inbound Codex, while a generic OpenAI Chat control still routes.</behavior>
  <action>First create crate-local cfg(test) tests and run them against the existing code to record a genuine behavioral RED (assertions must fail, not merely fail to compile). In the same vertical task add the table-driven opencode-go preset, private immutable product marker, and one empty exact-tuple admission function invoked by primary/fallback/codex/count-tokens decisions before resolver/client access. Use canonical https://opencode.ai/zen/go/v1 and SHUNT_OPENCODE_GO_API_KEY. Keep the admitted table empty; reject before credential/count applicability; preserve generic behavior and existing config backups.</action>
  <verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib opencode_go_empty_admission -- --test-threads=1</automated><fails_when>Filter selects zero tests, the pre-implementation run was not genuinely red, any unsupported case increments a credential or HTTP counter, or the generic control changes status.</fails_when></verify>
  <acceptance_criteria>D-01/D-02/D-03 are represented by explicit config identity and a crate-private pre-credential gate; RED evidence is recorded before implementation and the focused test passes after implementation with zero lookups and zero requests.</acceptance_criteria>
  <done>One vertical route exists from config marker through shared admission to all required boundaries, with no public seam widening.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove private zero-egress and generic-preservation seams</name>
  <files>src/proxy/opencode_go_tests.rs, src/proxy/capability.rs, src/proxy/failover.rs, src/codex_endpoint.rs</files>
  <read_first>src/proxy/opencode_go_tests.rs; src/proxy/capability.rs; src/proxy/failover.rs; src/codex_endpoint.rs</read_first>
  <behavior>D-03/D-08: primary, fallback, count-tokens, and inbound Codex unsupported selections fail before any CredentialResolver call or HTTP client invocation; generic Chat and native Codex positive controls remain admitted.</behavior>
  <action>Strengthen the crate-local harness with injected counters and test-only router/client construction using existing private or crate-private seams. Do not move these proofs into integration tests that cannot name pub(crate) CredentialResolver or builder helpers. Cover empty Go admission, wrong auth, off-origin config, fallback, count-tokens, and inbound Codex; assert no session header or bearer is constructed.</action>
  <verify><automated>node /tmp/shunt-phase12-isolated-run.cjs cargo test --all-features --lib opencode_go_router_boundaries -- --test-threads=1</automated><fails_when>Any selected Go case reaches resolver/client code, a session or bearer is emitted, or generic positive controls fail.</fails_when></verify>
  <acceptance_criteria>Private seam tests prove zero credential/network activity and preserved generic routes without changing public signatures or credential writeback.</acceptance_criteria>
  <done>All required admission boundaries are covered by executable crate-local evidence.</done>
</task>
</tasks>
