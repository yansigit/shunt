---
phase: 04-native-compaction
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - README.md
  - README.ko.md
  - README.ja.md
  - README.zh-CN.md
  - docs/m11-inbound-codex-endpoint.md
  - site/src/content/docs/guides/inbound-codex-endpoint.md
  - site/src/content/docs/ko/guides/inbound-codex-endpoint.md
  - site/src/content/docs/ja/guides/inbound-codex-endpoint.md
  - site/src/content/docs/zh-cn/guides/inbound-codex-endpoint.md
  - .planning/phases/04-native-compaction/04-VALIDATION.md
autonomous: true
requirements: [COMP-01]
estimate: {tasks: 2, confidence: high}
must_haves:
  truths:
    - "English and maintained locale documentation describes native compact support, capability rejection, and absence of local history state."
    - "Focused and repository-wide quality gates pass with generated wiki untouched."
---

<objective>Document the compaction contract across maintained surfaces and complete Phase 04 quality gates.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Document native compact behavior in English and maintained locales</name>
  <files>README.md, README.ko.md, README.ja.md, README.zh-CN.md, docs/m11-inbound-codex-endpoint.md, site/src/content/docs/guides/inbound-codex-endpoint.md, site/src/content/docs/ko/guides/inbound-codex-endpoint.md, site/src/content/docs/ja/guides/inbound-codex-endpoint.md, site/src/content/docs/zh-cn/guides/inbound-codex-endpoint.md</files>
  <action>Describe POST `/v1/responses/compact`, the shared inbound auth/limits/account routing, pinned ChatGPT and exact official OpenAI capability, pre-network rejection for unsupported targets, byte-faithful opaque continuation forwarding, and the explicit absence of local history or synthetic summaries. Keep translations synchronized and do not edit wiki.</action>
  <verify><automated>git diff --check && test -z "$(git status --short -- wiki/)"</automated><fails_when>observable behavior is undocumented, locales drift, wiki changes, or diff checks fail</fails_when></verify>
  <done>All maintained docs surfaces accurately describe COMP-01.</done>
</task>
<task type="auto">
  <name>Task 2: Finalize validation and run all quality gates</name>
  <files>.planning/phases/04-native-compaction/04-VALIDATION.md</files>
  <action>Run focused compact, ordinary ingress, routing, and multi-account suites, then format, strict Clippy, and full workspace tests. Mark all validation rows complete and the artifact validated only after success; preserve the empty wiki status.</action>
  <verify><automated>cargo test routing:: --lib && cargo test --test inbound_codex_endpoint && cargo test --test codex_multi_account && cargo fmt --all --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test --all-features --workspace && git diff --check && test -z "$(git status --short -- wiki/)"</automated><fails_when>any focused/full gate fails, validation is incomplete, or wiki changes</fails_when></verify>
  <done>Phase 04 implementation, docs, translations, validation, and repository gates are complete.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Severity | Mitigation |
|-----------|----------|----------|------------|
| T-04-02-01 | Tampering | medium | source-to-doc review and synchronized locales |
| T-04-02-02 | Repudiation | high | focused/full automated gates and validation record |
</threat_model>

<verification><automated>cargo fmt --all --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test --all-features --workspace && git diff --check && test -z "$(git status --short -- wiki/)"</automated></verification>
